---
phase: 11-results-fast-recalc
plan: 10
type: execute
wave: 5
depends_on: ["11-06"]
files_modified:
  - web/src/map/objectStyles.ts
  - web/src/map/hatchPatterns.ts
  - web/src/map/MapCanvas.tsx
  - web/src/theme.css
  - web/tests/e2e/objectStyling.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every scene-object kind renders with a differentiating color AND geometry-specific secondary channel: point = symbol/size/border, line = width/color, area = fill + border + separate hatch pattern (D-17)"
    - "Objects render at full styling ON TOP of the isophone fill and stay legible over it (D-18)"
    - "The categorical palette + hatch angles come from the dataviz-validated 11-UI-SPEC set (D-19)"
    - "Terra Draw draw-time styling/validation behavior does NOT regress — display layers and draw-time styling are separate systems"
  artifacts:
    - path: "web/src/map/objectStyles.ts"
      provides: "per-kind display styling (color/symbol/width/fill/border/hatch) for the MapLibre object layers (D-17)"
      contains: "objectStyles"
    - path: "web/src/map/hatchPatterns.ts"
      provides: "runtime canvas hatch generators → map.addImage → fill-pattern (D-17)"
      contains: "hatchPattern"
  key_links:
    - from: "web/src/map/objectStyles.ts"
      to: "web/src/draw/kinds.ts"
      via: "styles the 9 frozen kinds; display layers only, not the draw-time KIND_META"
      pattern: "Kind|kinds"
    - from: "web/src/map/MapCanvas.tsx"
      to: "web/src/map/isophoneLayer.ts"
      via: "object layers inserted ABOVE the isophone fill (D-18 draw order)"
      pattern: "isophone|layer order"
---

<objective>
Restyle the Phase-7 scene-object MapLibre display layers with the full NoizCalc per-object-kind formatting model (D-17, TI 386 §4.6.3): point = symbol/size/border, line = width/color, area = fill color + border + a SEPARATE hatch pattern (arcering). Objects render at full styling ON TOP of the isophone fill (D-18) and stay legible over it, using the dataviz-validated categorical palette + hatch angles pinned in 11-UI-SPEC (D-19). Hatch patterns (MapLibre has no native hatch) are generated at runtime on a `<canvas>` → `map.addImage` → `fill-pattern`.

Purpose: One of the two cross-cutting sweeps CONTEXT flagged for its own plan. The load-bearing constraint (Pitfall 8 / D-19): this restyles the DISPLAY layers in `web/src/map/` only — it must NOT change Terra Draw's DRAW-TIME styling/validation behavior; the acceptance includes a regression guard on the Phase-7 editor journeys.
Output: `map/objectStyles`, `map/hatchPatterns`, layer-order wiring, theme tokens, a draw-time regression guard + styling UAT.

This plan realizes user-addition decisions D-17 (per-kind color + hatch), D-18 (objects over isophone fill), and D-19 (validated accessible palette) — no new requirement ID.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-results-fast-recalc/11-CONTEXT.md
@.planning/phases/11-results-fast-recalc/11-RESEARCH.md
@.planning/phases/11-results-fast-recalc/11-UI-SPEC.md
@web/src/draw/kinds.ts
@web/src/map/MapCanvas.tsx
@web/src/map/useTerraDraw.ts
@web/src/map/basemap.ts
@web/src/theme.css
@.planning/phases/11-results-fast-recalc/11-06-SUMMARY.md
</context>

## Artifacts this phase produces

- `web/src/map/objectStyles.ts` — `objectStyles: Record<Kind, KindDisplayStyle>` (color, symbol/size/border for points; width/color for lines; fill%/border/hatchId for areas), from the UI-SPEC categorical table.
- `web/src/map/hatchPatterns.ts` — `hatchPattern(kind)` runtime canvas generators (45°/135° lines, dotted/stipple, cross-hatch) → `ImageData` registered via `map.addImage`.
- `web/src/map/MapCanvas.tsx` (+) — MapLibre `fill`/`line`/`symbol` object layers inserted above the isophone fill (draw order: isophone → areas → lines → points).
- `web/src/theme.css` (+) — dark-validated categorical + hatch tokens (extend metrao3; do not invent unrelated tokens).
- `web/tests/e2e/objectStyling.spec.ts` — styling UAT + Terra Draw draw-time regression guard.

<tasks>

<task type="auto">
  <name>Task 1: Per-kind object styles + runtime hatch patterns (D-17/D-19)</name>
  <read_first>
    - web/src/draw/kinds.ts (the 9 frozen kinds + the existing KIND_META draw-time metadata — DO NOT repurpose this for display; create a separate display-styles map)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§Data Visualization Palettes #3 — the 8-slot categorical hex, the per-kind table: source/receiver/elevation_point points, wall/elevation_line lines, building/forest/ground_zone/calc_area areas with fill% + hatch type + angle; validator evidence)
    - web/src/theme.css (metrao3 tokens to extend — dark-only; do not invent)
    - .planning/phases/11-results-fast-recalc/11-RESEARCH.md (§NoizCalc Object Styling — MapLibre hatch via runtime canvas + addImage)
  </read_first>
  <action>Create `web/src/map/objectStyles.ts` (a `Record<Kind, KindDisplayStyle>` — a SEPARATE display map, never a repurposing of `kinds.ts` `KIND_META` draw-time metadata) encoding the UI-SPEC §Palettes #3 table: point kinds get color + symbol + size + border, line kinds width + color, area kinds fill% + border + a hatch id (building 45° solid-diagonal, forest dotted/stipple, ground_zone cross-hatch, calc_area sparse 135°). Create `web/src/map/hatchPatterns.ts` generating each hatch at runtime on a `<canvas>` (angles from the dataviz texture rule 45°/135°) → `ImageData` for `map.addImage`. Extend `web/src/theme.css` with the dark-validated categorical + hatch tokens (do not invent unrelated tokens — D-11 rule). Colors/hatch come program-generated (no user string → image; T-11-10-02, no XSS surface).</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run test:unit -- objectStyles</automated>
  </verify>
  <acceptance_criteria>
    - A unit test asserts all 9 kinds have a display style with the geometry-appropriate secondary channel (points → symbol/border, lines → width, areas → fill%+hatchId), matching the UI-SPEC hex/angles.
    - `hatchPattern(kind)` returns a canvas `ImageData` for each area kind (asserted by size/non-empty pixels).
  </acceptance_criteria>
  <done>Per-kind display styles + runtime hatch patterns exist, matching the validated palette.</done>
</task>

<task type="auto">
  <name>Task 2: Wire object layers above the isophone fill (D-18) without touching draw-time behavior</name>
  <read_first>
    - web/src/map/MapCanvas.tsx (where object display layers + the isophone layer mount; the layer-insertion order)
    - web/src/map/isophoneLayer.ts (11-06 — the fill layer objects sit above)
    - web/src/map/useTerraDraw.ts (the Terra Draw draw-time styling/validation — MUST NOT be edited; display and draw-time are separate systems — Pitfall 8)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (draw order: isophone fill bottom → areas → lines → points top)
  </read_first>
  <action>In `web/src/map/MapCanvas.tsx`, register the object `fill`/`line`/`symbol` display layers from `objectStyles.ts`, registering the hatch images via `map.addImage`, inserted ABOVE the 11-06 isophone fill and in the D-18 draw order (isophone → areas → lines → points). Semi-transparent hatched fills (§4.6.5) keep buildings/zones/forests legible over the colored noise fill. DO NOT edit `useTerraDraw.ts` or any draw-time styling/validation — the restyle is display-only (D-19/Pitfall 8). If a shared style constant must be read by both, read it, do not move draw-time logic.</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` produces a fresh `web/dist`; object layers render above the isophone fill with hatch on area kinds.
    - `useTerraDraw.ts` is unchanged by this plan (`git diff --stat web/src/map/useTerraDraw.ts` empty) — draw-time behavior untouched.
  </acceptance_criteria>
  <done>Objects render at full styling above the isophone fill; draw-time styling is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Styling UAT + Terra Draw draw-time regression guard (D-18/Pitfall 8)</name>
  <read_first>
    - web/tests/e2e/ (existing Phase-7 editor journey specs — the draw-time behavior that must not regress)
    - web/tests/e2e/isophone.spec.ts (11-06 fixture pattern to reuse for the over-fill assertion)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§Palettes #3 draw order + hatch)
  </read_first>
  <action>Create `web/tests/e2e/objectStyling.spec.ts` on the real bundle, offline (COOP/COEP + `page.route` mocks, fixture scene + isophone grid). Assert: each object kind renders with its expected color/secondary channel; area kinds show a hatch `fill-pattern`; object layers sit ABOVE the isophone fill (query layer order); the objects stay legible over the noise fill. CRUCIAL regression guard: re-run (or reuse) a Phase-7 editor draw journey and assert draw-time styling/validation behavior is unchanged (drawing a ground zone, the partial-crossing reject, last-object inheritance — Pitfall 8). DevDependency-only; artifacts git-ignored.</action>
  <verify>
    <automated>cd web && npm run test:e2e -- objectStyling</automated>
  </verify>
  <acceptance_criteria>
    - `objectStyling.spec.ts` passes offline, asserting per-kind display styling + hatch, the above-fill draw order, and that a Phase-7 draw-time journey (draw/validate/inherit) behaves identically (no regression).
    - The existing Phase-7 editor E2E journeys still pass (`npm run test:e2e` full suite green).
  </acceptance_criteria>
  <done>An offline UAT proves the object styling over the fill and guards Phase-7 draw-time behavior from regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kind → display style/hatch | program-generated styling; no user string reaches image generation |
| display layers ↔ draw-time layers | separate systems; the restyle must not cross into draw-time validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10-01 | Integrity | draw-time regression | mitigate | display-only restyle; `useTerraDraw.ts` untouched; Phase-7 journey regression guard (Pitfall 8) |
| T-11-10-02 | Tampering | canvas hatch generation | accept | patterns are program-generated (no user string → image); no XSS surface |
| T-11-10-03 | Integrity | legibility over fill | mitigate | dataviz-validated palette + hatch (relief rule); contrast-checked against the dark surface (D-18/D-19) |
</threat_model>

<verification>
- `cd web && npm run typecheck && npm run build && npm run test:e2e` green (full suite — draw-time regression guard).
- `git diff --stat web/src/map/useTerraDraw.ts` empty.
- `web/dist` rebuilt and committed.
</verification>

<success_criteria>
Every scene-object kind renders with differentiating color + geometry-specific secondary channel (symbol/width/hatch) at full styling over the isophone fill (D-17/D-18), on the validated palette (D-19), with Terra Draw draw-time behavior proven not to regress.
</success_criteria>

<output>
Create `.planning/phases/11-results-fast-recalc/11-10-SUMMARY.md` when done.
</output>
