---
phase: 11-results-fast-recalc
plan: 06
type: execute
wave: 4
depends_on: ["11-02", "11-05"]
files_modified:
  - web/src/map/isophoneLayer.ts
  - web/src/panels/ColorScaleEditor.tsx
  - web/src/store/colorScale.ts
  - web/tests/e2e/isophone.spec.ts
autonomous: false
requirements: [WEB-06, GRID-04]
must_haves:
  truths:
    - "The noise map renders as MapLibre fill polygons (never a heatmap raster layer)"
    - "A single breaks[]/colors[] source of truth drives the contour tracer, the fill layer, and the legend — legend breaks ≡ contour breaks ≡ class colors"
    - "The default scale is EU END-style ~5 dB bands with the canonical palette; viridis/turbo presets are selectable"
    - "Editing break values re-contours the cached level grid without re-running propagation"
    - "The dB weighting label comes from result metadata"
  artifacts:
    - path: "web/src/map/isophoneLayer.ts"
      provides: "MapLibre fill layer from WASM iso-band polygons + docked legend (D-02/D-18)"
      contains: "isophoneLayer"
    - path: "web/src/panels/ColorScaleEditor.tsx"
      provides: "preset picker + editable breaks (NoizCalc §4.6.5 controls), re-contour no re-solve (D-04)"
      contains: "ColorScaleEditor"
    - path: "web/src/store/colorScale.ts"
      provides: "the single breaks[]/colors[] source of truth (D-04) + validated presets (D-03/D-19)"
      contains: "breaks"
  key_links:
    - from: "web/src/map/isophoneLayer.ts"
      to: "crates/envi-compute/src/isoband.rs"
      via: "renders the WASM-traced iso-band polygons reprojected to LonLat"
      pattern: "isoband|fill"
    - from: "web/src/panels/ColorScaleEditor.tsx"
      to: "web/src/store/colorScale.ts"
      via: "editing breaks re-runs the tracer over the cached grid (no re-solve)"
      pattern: "breaks|recontour"
---

<objective>
Render the isophone noise map as MapLibre FILL polygons (D-02, never a heatmap) from the 11-02 WASM iso-band tracer, and ship the editable color-scale editor (D-03/D-04): an EU END-style ~5 dB default preset plus perceptually-uniform viridis/turbo presets, with editable breaks following the NoizCalc §4.6.5 controls, where editing the scale re-contours the CACHED level grid without re-running propagation (SC3). A single `breaks[]`/`colors[]` array is the source of truth for the tracer, the fill layer, and the legend — enforcing legend ≡ contour ≡ class colors. The dB weighting label comes from result metadata.

Purpose: SC3 / WEB-06 / GRID-04. The palettes are dataviz-validated (D-19). The isophone fill sits BELOW the full-styled scene objects (D-18; the object restyle is 11-10).
Output: `map/isophoneLayer`, `ColorScaleEditor`, `store/colorScale`, offline Playwright UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-results-fast-recalc/11-CONTEXT.md
@.planning/phases/11-results-fast-recalc/11-RESEARCH.md
@.planning/phases/11-results-fast-recalc/11-UI-SPEC.md
@web/src/map/basemap.ts
@web/src/map/MapCanvas.tsx
@web/src/panels/ResultsPanel.tsx
@.planning/phases/11-results-fast-recalc/11-02-SUMMARY.md
@.planning/phases/11-results-fast-recalc/11-05-SUMMARY.md
</context>

## Artifacts this phase produces

- `web/src/store/colorScale.ts` — `type Preset = "end" | "viridis" | "turbo"`, `ScaleState { preset, breaks: number[], colors: string[] }`, the EU END default breaks `[55,60,65,70,75]` + hex from UI-SPEC (A4 assumption), viridis/turbo samplers, `validateBreaks` (monotonic/finite/≥2).
- `web/src/map/isophoneLayer.ts` — `addIsophoneLayer(map, bands, scale)` MapLibre fill layer + `MapLegend` docked bottom-left.
- `web/src/panels/ColorScaleEditor.tsx` — preset picker + editable break rows (smallest interval, interval magnitude, number of intervals, ascending, keep-color-sequence).
- `web/tests/e2e/isophone.spec.ts` — offline Playwright UAT.

<tasks>

<task type="auto">
  <name>Task 1: Color-scale store — single breaks[]/colors[] source of truth + presets (D-03/04/19)</name>
  <read_first>
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§Data Visualization Palettes #1 — the EU END default break edges + hex, viridis/turbo presets, the editable-breaks contract, V5 validation; A4 assumption note)
    - .planning/phases/11-results-fast-recalc/11-RESEARCH.md (§Color System — sequential/END/viridis/turbo; Open Q3 A4 default break edges)
    - web/src/store/results.ts (the results store the weighting metadata label comes from)
  </read_first>
  <action>Create `web/src/store/colorScale.ts` with the SINGLE `breaks[]`/`colors[]` source of truth (D-04) driving tracer + fill + legend — never three parallel definitions. Ship the EU END default preset (~5 dB bands, break edges `[55,60,65,70,75]` + the canonical green→yellow→orange→red→violet hex from UI-SPEC — carry A4 as an EXPLICIT assumption in a code comment: confirm the exact END/CNOSSOS Lden edges + official hex before ship) plus viridis/turbo perceptually-uniform presets (D-04, sampled at the break count). `validateBreaks` enforces strictly-monotonic-increasing/finite/≥2 (V5) surfaced as an inline `.form-error`. The dB weighting label is read from result metadata (D-04), never the toggle. This is display config only — NO acoustic math in TS.</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run test:unit -- colorScale</automated>
  </verify>
  <acceptance_criteria>
    - A unit test asserts the END default breaks/hex, viridis/turbo sampling at N breaks, and that `validateBreaks` rejects non-monotonic/<2/non-finite.
    - `breaks[]` and `colors[]` are a single exported state object consumed by both the layer and the legend (asserted by import structure) — no duplicate break arrays.
  </acceptance_criteria>
  <done>The color-scale store is the single breaks/colors truth with END + viridis + turbo presets and validation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm the EU-END default break edges + palette hex (A4) before the default ships</name>
  <action>Do NOT ship the isophone default scale until the human signs off on the A4 break edges + hex. Present the values below, then wait for the resume signal. On "approved" the executor keeps `[55, 60, 65, 70, 75]` + the listed hex as the shipped END default; on a supplied correction the executor substitutes the given break edges + hex in `web/src/store/colorScale.ts` before Task 2 renders the layer/legend. This gates the A4 assumption (RESEARCH Assumptions Log) — a familiar-default color scale acousticians will read, so wrong edges look non-standard.</action>
  <what-built>Task 1 hardcoded the EU-END default noise-map scale — break edges `[55, 60, 65, 70, 75]` dB and the green→yellow→orange→red→violet palette hex (`#7fbf7f`, `#bfdc6b`, `#f6e04b`, `#f4a637`, `#e34948`, `#8a2be2`) — carried as RESEARCH assumption A4 (the canonical-style default, not yet confirmed against the official END/CNOSSOS Lden Annex).</what-built>
  <how-to-verify>
    1. Review the six break edges + hex in `web/src/store/colorScale.ts` against the official EU END / CNOSSOS Lden noise-map band scheme (or the value you want ENVI to ship as the familiar default).
    2. Confirm the ~5 dB banding + the <55 / ≥75 end caps match the printed maps acousticians expect.
    3. Approve the values, OR provide the corrected break edges + hex to substitute before the isophone layer + legend ship.
  </how-to-verify>
  <resume-signal>Type "approved" to ship these EU-END defaults, or supply the corrected break edges + hex to substitute.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: MapLibre isophone fill layer + legend + ColorScaleEditor (D-02/04/18, re-contour no re-solve)</name>
  <read_first>
    - web/src/map/basemap.ts + web/src/map/MapCanvas.tsx (how layers are added/ordered; the `.map-readout` docked-legend idiom)
    - crates/envi-compute/src/isoband.rs + crates/envi-geo/ (the traced bands reprojected SceneXY→LonLat via the single CRS boundary)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§Color-Scale Editor inventory; NoizCalc §4.6.5 control set; §State Matrix isophone row)
  </read_first>
  <action>Create `web/src/map/isophoneLayer.ts`: a MapLibre FILL layer (fill polygons only, never a raster density overlay — D-02) rendering the WASM iso-band polygons (reprojected to LonLat via `envi-geo` in WASM — the map receives LonLat), inserted BELOW the scene-object layers (D-18 draw order) with a docked bottom-left legend showing break values + the metadata weighting label. Create `web/src/panels/ColorScaleEditor.tsx` in the ResultsPanel slot with the NoizCalc §4.6.5 controls (smallest interval, interval magnitude dB, number of intervals, ascending, keep-color-sequence) + preset picker. Editing breaks re-runs ONLY the WASM tracer over the CACHED level grid (Pattern 4 / Pitfall 5 — never re-solve) and updates layer + legend from the single source of truth. Validation errors inline (`.form-error`).</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; the map shows a fill layer (a `fill` paint type, asserted by layer config); a comment-stripped scan finds no raster-density layer type: `grep -v '^//' web/src/map/isophoneLayer.ts | grep -c heatmap` == 0.
    - Editing a break triggers a re-contour call over the cached grid, not a solve (asserted in the Playwright test).
    - Legend, contour, and fill all read the one `breaks[]`/`colors[]` (single source of truth).
  </acceptance_criteria>
  <done>The isophone fill map renders with an editable color scale that re-contours the cached grid without re-solving.</done>
</task>

<task type="auto">
  <name>Task 3: Offline Playwright isophone UAT (WEB-06/GRID-04)</name>
  <read_first>
    - web/tests/e2e/ (offline harness + COOP/COEP + page.route mocks — Pitfall 7)
    - web/tests/e2e/spectrum.spec.ts (11-05 fixture-tensor pattern to reuse)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§State Matrix isophone row)
  </read_first>
  <action>Create `web/tests/e2e/isophone.spec.ts` on the real bundle, offline (`page.route` mocks, COOP/COEP), with a fixture level grid. Assert: the noise map renders fill polygons (a `fill`-type layer, never a raster-density layer); switching preset (END → viridis) recolors without re-solve; editing a break edge re-contours (polygon count/shape changes) with NO new solve worker message (SC3); the legend break values match the contour breaks; the weighting label follows result metadata. DevDependency-only; artifacts git-ignored.</action>
  <verify>
    <automated>cd web && npm run test:e2e -- isophone</automated>
  </verify>
  <acceptance_criteria>
    - `isophone.spec.ts` passes offline, asserting fill-polygon rendering, preset switch, break-edit re-contour with no re-solve, and legend ≡ contour ≡ class colors.
    - No network egress; the assertion that a break edit issues zero solve messages proves SC3.
  </acceptance_criteria>
  <done>An offline Playwright UAT proves the isophone fill map + editable scale re-contours without re-solve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| break edits → tracer | user-editable breaks validated before contouring (also gated in WASM, 11-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Tampering | ColorScaleEditor breaks | mitigate | validate monotonic/finite/≥2 in TS (inline `.form-error`) AND in WASM (11-02); typed error |
| T-11-06-02 | Integrity | isophone rendering | mitigate | single breaks[]/colors[] source of truth; no heatmap layer (grep gate) — D-02/D-04 |
| T-11-06-03 | Availability | re-contour path | mitigate | re-contour cached grid only; never a re-solve (SC3) — asserted by zero-solve-message UAT |
</threat_model>

<verification>
- `cd web && npm run typecheck && npm run build && npm run test:e2e -- isophone` green.
- `grep -v '^//' web/src/map/isophoneLayer.ts | grep -c heatmap` == 0.
- `web/dist` rebuilt and committed.
</verification>

<success_criteria>
The noise map renders as fill polygons with an editable color scale (END default + viridis/turbo), legend ≡ contour ≡ class colors, and break edits re-contour the cached grid with no re-solve (SC3, WEB-06, GRID-04).
</success_criteria>

<output>
Create `.planning/phases/11-results-fast-recalc/11-06-SUMMARY.md` when done.
</output>
