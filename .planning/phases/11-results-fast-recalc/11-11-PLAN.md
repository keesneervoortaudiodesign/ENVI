---
phase: 11-results-fast-recalc
plan: 11
type: execute
wave: 6
depends_on: ["11-05", "11-06", "11-07", "11-08", "11-09", "11-10"]
files_modified:
  - web/src/help/catalog.ts
  - web/src/help/InfoButton.tsx
  - web/src/help/controlIds.ts
  - web/src/help/coverage.test.ts
  - web/src/icons.ts
  - web/src/panels/Palette.tsx
  - web/src/panels/Inspector.tsx
  - web/src/panels/ImportPanel.tsx
  - web/src/panels/WeatherPanel.tsx
  - web/src/panels/CalcPanel.tsx
  - web/src/panels/SpectrumPanel.tsx
  - web/src/panels/ColorScaleEditor.tsx
  - web/src/panels/ConditioningPanel.tsx
  - web/src/panels/ScenarioPanel.tsx
  - web/src/panels/ExportMenu.tsx
  - web/tests/e2e/infoButton.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "A reusable <InfoButton controlId=…/> renders exactly one 'i' affordance on EVERY interactive control across all panels (D-23)"
    - "Help content is structured data (a typed keyed catalog), not text scattered in JSX (D-25)"
    - "Help is extensive + standards-cited: Nord2000 (AV 1106/07 by report number) + TI 386, in our own words, never pasting copyrighted text, English-only (D-24)"
    - "A coverage check FAILS if any interactive control lacks a catalog entry (D-25)"
    - "The retrofit spans all Phase-7/8/9/10 panels AND the new Phase-11 controls"
  artifacts:
    - path: "web/src/help/InfoButton.tsx"
      provides: "reusable <InfoButton controlId> — popover (glance) + docked side-panel (depth) (D-23)"
      contains: "InfoButton"
    - path: "web/src/help/catalog.ts"
      provides: "typed Record<ControlId, HelpEntry{title, body[], citations[]}> (D-24/D-25)"
      contains: "catalog"
    - path: "web/src/help/coverage.test.ts"
      provides: "test asserting every ControlId has a help entry (D-25)"
      contains: "coverage"
  key_links:
    - from: "web/src/help/InfoButton.tsx"
      to: "web/src/help/catalog.ts"
      via: "looks up the HelpEntry by ControlId; a control cannot render an InfoButton for an id not in the union"
      pattern: "ControlId|catalog"
    - from: "web/src/help/coverage.test.ts"
      to: "web/src/help/controlIds.ts"
      via: "enumerates the ControlId union and asserts catalog coverage"
      pattern: "ControlId"
---

<objective>
Build the app-wide self-explanatory-UI system (D-23/24/25): a reusable `<InfoButton controlId=…/>` affordance placed on EVERY interactive control, backed by a structured typed help catalog (data, not JSX-scattered text), with extensive standards-cited English-only help (Nord2000 AV 1106/07 cited by report number + TI 386, explained in our own words — NEVER pasting copyrighted text), and a coverage check that FAILS if any interactive control lacks a help entry. The retrofit spans all Phase-7/8/9/10 panels (scene editor / palette / inspector, import, weather, calc) AND the new Phase-11 controls (spectrum, color-scale, conditioning, scenarios, export).

Purpose: The second cross-cutting sweep CONTEXT isolated into its own final wave (D-25) so it does not serialize the feature work and can cover every Phase-11 control once they all exist. Goal: a 100% self-explanatory UI — no control ships without help.
Output: `help/InfoButton`, `help/catalog`, `help/controlIds`, `help/coverage.test`, the `info` icon, and the retrofit across all panels.

This plan realizes user-addition decisions D-23 (info button on every control), D-24 (extensive standards-cited help), and D-25 (structured catalog + coverage check) — no new requirement ID.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-results-fast-recalc/11-CONTEXT.md
@.planning/phases/11-results-fast-recalc/11-RESEARCH.md
@.planning/phases/11-results-fast-recalc/11-UI-SPEC.md
@web/src/icons.ts
@web/src/panels/Palette.tsx
@web/src/panels/Inspector.tsx
@web/src/panels/CalcPanel.tsx
@web/src/panels/WeatherPanel.tsx
@web/src/panels/ImportPanel.tsx
@.planning/phases/11-results-fast-recalc/11-09-SUMMARY.md
@.planning/phases/11-results-fast-recalc/11-10-SUMMARY.md
</context>

## Artifacts this phase produces

- `web/src/help/controlIds.ts` — the `ControlId` string-literal union (every interactive control's id) — the single enumeration the coverage test derives from.
- `web/src/help/catalog.ts` — `catalog: Record<ControlId, HelpEntry>`, `HelpEntry = { title: string; body: string[]; citations: Citation[] }`.
- `web/src/help/InfoButton.tsx` — `<InfoButton controlId=…/>`: 16px "i" glyph → popover (glance) + "More" → docked right-rail help panel (depth).
- `web/src/help/coverage.test.ts` — asserts every `ControlId` has a catalog entry (a control without help FAILS).
- `web/src/icons.ts` (+) — the `info` glyph.
- retrofit edits across the Phase-7/8/9/10 + Phase-11 panels.
- `web/tests/e2e/infoButton.spec.ts` — offline Playwright UAT.

<tasks>

<task type="auto">
  <name>Task 1: InfoButton component + ControlId union + typed help catalog (D-23/25)</name>
  <read_first>
    - web/src/icons.ts (the inline-SVG icon idiom — add an `info` glyph, 16px, currentColor)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§Info-Button Help Contract — the component shape, popover + docked side-panel presentation, HelpEntry shape, placement contract, coverage rule)
    - .planning/phases/11-results-fast-recalc/11-RESEARCH.md (§Info-Button Help System — recommended shape + coverage-from-type derivation)
    - web/src/theme.css (`--color-text-dim`/`--color-text-muted`/focus ring tokens for the glyph states)
  </read_first>
  <action>Add an `info` glyph to `web/src/icons.ts`. Create `web/src/help/controlIds.ts` with the `ControlId` string-literal union (enumerate every interactive control across all panels — this is the coverage source of truth; a control cannot render an `<InfoButton>` for an id not in the union). Create `web/src/help/catalog.ts` with `catalog: Record<ControlId, HelpEntry>` where `HelpEntry = { title; body: string[]; citations: Citation[] }` — structured data, NOT text in JSX (D-25). Create `web/src/help/InfoButton.tsx`: a reusable `<InfoButton controlId=…/>` rendering the 16px "i" glyph (dim resting → muted hover → accent focus ring), a popover for glance + a "More" affordance opening the docked right-rail help panel for the long body (D-23 presentation). Because the catalog is a `Record<ControlId, …>`, a missing entry fails `tsc`.</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run test:unit -- help</automated>
  </verify>
  <acceptance_criteria>
    - `<InfoButton controlId>` compiles only for ids in the `ControlId` union; the catalog `Record<ControlId, HelpEntry>` requires all ids (a missing key fails `tsc`).
    - A unit test renders `<InfoButton>` and asserts the popover + "More" → docked panel behavior.
  </acceptance_criteria>
  <done>The InfoButton component + ControlId union + typed catalog exist, type-enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Extensive standards-cited help content — never paste AV text (D-24)</name>
  <read_first>
    - docs/references/dbaudio-ti386-1.6-en.md (TI 386 / NoizCalc — the control-level explanations to cite for the D-24 rationale)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (copy rules: English-only, sentence case, units always shown; band identity as band index + exact Hz; NEVER paste AV 1106/07)
    - .claude/CLAUDE.md (licensing / data hygiene — AV 1106/07 is copyrighted: explain in our own words, cite by report number)
  </read_first>
  <action>Author the `catalog.ts` `HelpEntry` bodies: multi-paragraph per control — what it does, every option/range/unit/default, and the Nord2000 / NoizCalc rationale where relevant. Cite AV 1106/07 BY REPORT NUMBER and TI 386 in the `citations` field. Write all rationale in our OWN WORDS — never paste AV 1106/07 text (copyright; threat T-11-11-02). English-only. Show units (dB, dB(A), dB(C), Hz, m, ms) and band identity as band index + exact Hz. Cover every `ControlId` (the coverage test enforces completeness).</action>
  <verify>
    <automated>cd web && npm run test:unit -- coverage</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/help/coverage.test.ts` passes: every `ControlId` has a catalog entry with a non-empty multi-paragraph `body` and at least the relevant citation (a control without help FAILS the test — D-25).
    - Citations reference AV 1106/07 by report number and/or TI 386; no verbatim AV standard text appears (reviewed; the catalog is our-own-words prose).
  </acceptance_criteria>
  <done>Every control has extensive, standards-cited, own-words English help; the coverage test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Retrofit the Phase-7/8/9/10 panels with InfoButton (D-23/25)</name>
  <read_first>
    - web/src/panels/ (Palette, Inspector, ImportPanel, WeatherPanel, CalcPanel, FacadePanel, ProjectBar + any `fields/` sub-field components — the prior-phase controls to retrofit)
    - web/src/help/InfoButton.tsx + controlIds.ts (Task 1 — the affordance + id union to wire)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§Info-Button placement contract — one InfoButton per control, after the label / trailing the panel header)
  </read_first>
  <action>Retrofit the Phase-7/8/9/10 panels only — Palette, Inspector, ImportPanel, WeatherPanel, CalcPanel, FacadePanel, ProjectBar, and any `fields/` sub-field components — so EVERY interactive control (input, toggle, segmented control, slider, button, preset picker) renders exactly one `<InfoButton controlId=…/>` placed after the control's label (UI-SPEC placement contract). Add each control's id to the `ControlId` union in `controlIds.ts` so the coverage test stays complete. Additive affordance only — do not change control behavior.</action>
  <verify>
    <automated>cd web && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every interactive control in the prior-phase panels renders exactly one `<InfoButton>`, its id present in the `ControlId` union (asserted by a sampled Playwright query in Task 4 + `tsc`).
    - No behavior change to the retrofitted controls (additive only) — the existing Phase-7/8/9/10 E2E journeys still pass.
  </acceptance_criteria>
  <done>All prior-phase panel controls carry an InfoButton wired to the ControlId union.</done>
</task>

<task type="auto">
  <name>Task 4: Retrofit the Phase-11 panels + coverage check + offline info-button UAT (D-23/25)</name>
  <read_first>
    - web/src/panels/ (SpectrumPanel, ColorScaleEditor, ConditioningPanel, ScenarioPanel, ExportMenu — the new Phase-11 controls to retrofit)
    - web/src/help/controlIds.ts + catalog.ts + coverage.test.ts (Tasks 1–2 — the union + catalog + coverage oracle)
    - web/tests/e2e/spectrum.spec.ts (the offline harness + COOP/COEP + page.route pattern to reuse)
  </read_first>
  <action>Retrofit the new Phase-11 panels — SpectrumPanel, ColorScaleEditor, ConditioningPanel, ScenarioPanel, ExportMenu — so every interactive control (segmented toggles, break row, receiver row, gain/delay/filter inputs, scenario met inputs, export menu items) renders exactly one `<InfoButton controlId=…/>`, adding each id to the `ControlId` union. Create `web/tests/e2e/infoButton.spec.ts` and run the single `coverage.test.ts` (covering ALL controls across all panels). Rebuild `web/dist`.</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run build && npm run test:unit -- coverage && npm run test:e2e -- infoButton</automated>
  </verify>
  <acceptance_criteria>
    - `web/tests/e2e/infoButton.spec.ts` (real bundle, offline, `page.route('**/api/*')` mocks + COOP/COEP): an InfoButton is present on a sampled control in each panel (scene editor/palette/inspector, import, weather, calc, spectrum, color-scale, conditioning, scenario, export); clicking opens the popover; "More" opens the docked help panel; content is non-empty and cites a standard.
    - `web/src/help/coverage.test.ts` passes (100% control coverage — the single D-25 guarantee covering all panels).
    - The full Playwright suite stays green (no retrofit regression); `web/dist` rebuilt and committed.
  </acceptance_criteria>
  <done>Every Phase-11 control has an InfoButton; the single coverage check proves 100% app-wide coverage, verified by an offline UAT.</done>
</task>

</tasks>

<scope_note>
**Accepted risk — 16 files_modified (exceeds the 15-file guideline).** This plan is the app-wide info-button sweep that CONTEXT D-25 explicitly mandates be isolated into its OWN single plan/wave ("isolate this sweep into its own plan/wave"); splitting it into multiple plans would violate D-25 and fragment the single coverage-check guarantee. The file count is driven by the retrofit touching one file per prior-phase + Phase-11 panel, and every edit is purely ADDITIVE (inserting an `<InfoButton>` after a control's label — no behavior change). The work is split into two tasks (Task 3 prior-phase panels, Task 4 Phase-11 panels) so each task stays within the per-task file guideline, and the single `coverage.test.ts` plus the offline `infoButton.spec.ts` regression-guard the additive-only nature.
</scope_note>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| help catalog content → UI | authored prose; must not contain copyrighted AV 1106/07 text |
| ControlId union → coverage | the enumeration is the completeness oracle for the help sweep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11-01 | Integrity | help coverage | mitigate | coverage test derives from the `ControlId` type; a control without help fails the test (D-25) |
| T-11-11-02 | Info disclosure / licensing | help content | mitigate | D-24 rule: cite AV 1106/07 by report number, own words, never paste copyrighted text; reviewed |
| T-11-11-03 | Integrity | retrofit regression | mitigate | additive affordance only; full Playwright suite stays green |
</threat_model>

<verification>
- `cd web && npm run typecheck && npm run build && npm run test:unit -- help coverage && npm run test:e2e` green.
- `coverage.test.ts` proves 100% control coverage.
- No verbatim AV 1106/07 text in the catalog (reviewed).
- `web/dist` rebuilt and committed.
</verification>

<success_criteria>
Every interactive control across the whole app carries an InfoButton with extensive, standards-cited, own-words help from a structured catalog, with a coverage check that fails on any gap — a 100% self-explanatory UI (D-23/24/25).
</success_criteria>

<output>
Create `.planning/phases/11-results-fast-recalc/11-11-SUMMARY.md` when done.
</output>
