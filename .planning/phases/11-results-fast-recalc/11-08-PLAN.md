---
phase: 11-results-fast-recalc
plan: 08
type: execute
wave: 5
depends_on: ["11-06"]
files_modified:
  - web/src/store/scenarios.ts
  - web/src/panels/ScenarioPanel.tsx
  - web/src/map/differenceLayer.ts
  - web/src/store/difference.ts
  - web/tests/e2e/scenarios.spec.ts
autonomous: true
requirements: [WEB-12, METX-03, METX-04]
must_haves:
  truths:
    - "A new scenario clones the current met settings; the user edits overrides, names it, and it computes its own hash-keyed cached tensor (clone-then-edit)"
    - "Friendly overrides (T/RH/p, Beaufort wind + direction, temp gradient, downwind worst-case) drive the Phase-9 envi_gis::weather derivation to per-azimuth A/B/C; raw per-azimuth A/B/C is an advanced override"
    - "Downwind worst-case assumes downward-refraction favourable conditions along each source→receiver bearing independently"
    - "Named scenarios switch instantly via their per-scenario cached tensors"
    - "A difference map renders the per-receiver dB(A) delta (A − B) on a diverging blue–gray–red scale"
  artifacts:
    - path: "web/src/store/scenarios.ts"
      provides: "scenario registry {name, met_overrides, tensor_hash} + clone-then-edit + full-solve recompute (D-13/METX-04)"
      contains: "scenarios"
    - path: "web/src/panels/ScenarioPanel.tsx"
      provides: "scenario list + friendly/advanced met inputs + compare + delete (D-14/METX-03)"
      contains: "ScenarioPanel"
    - path: "web/src/map/differenceLayer.ts"
      provides: "diverging difference-map fill layer (A − B dB(A) delta, D-16)"
      contains: "differenceLayer"
  key_links:
    - from: "web/src/store/scenarios.ts"
      to: "crates/envi-gis/src/weather.rs"
      via: "friendly met overrides → per-azimuth A/B/C via the Phase-9 derivation (D-14)"
      pattern: "weather|azimuth"
    - from: "web/src/store/scenarios.ts"
      to: "web/src/compute/worker.ts"
      via: "met change → new tensor identity → full Phase-10 solve into a per-scenario OPFS calc dir"
      pattern: "solve|recompute"
---

<objective>
Ship the weather what-if system (WEB-12, METX-03, METX-04): clone-then-edit named scenarios (D-13), each computing its own hash-keyed cached tensor via the full Phase-10 solve path (a met change is a recompute, not a MAC — it alters tensor identity), with friendly met overrides (T/RH/p, Beaufort wind class + direction, temperature gradient, downwind worst-case toggle) driving the Phase-9 `envi_gis::weather` derivation to per-azimuth A/B/C (D-14) — plus a raw per-azimuth A/B/C advanced override. Downwind worst-case assumes downward-refraction (favourable) conditions along EACH source→receiver bearing independently (D-15). Named scenarios switch instantly via their per-scenario cached tensors; a difference map renders the per-receiver dB(A) delta (A − B) on a diverging blue–gray–red scale (D-16).

Purpose: SC4. Reuses the Phase-9 weather derivation and the Phase-10 solve/OPFS store; the diverging palette is dataviz-validated (UI-SPEC #2).
Output: `store/scenarios`, `ScenarioPanel`, `map/differenceLayer`, `store/difference`, offline Playwright UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-results-fast-recalc/11-CONTEXT.md
@.planning/phases/11-results-fast-recalc/11-RESEARCH.md
@.planning/phases/11-results-fast-recalc/11-UI-SPEC.md
@crates/envi-gis/src/weather.rs
@web/src/store/weather.ts
@web/src/panels/WeatherPanel.tsx
@web/src/compute/worker.ts
@.planning/phases/11-results-fast-recalc/11-06-SUMMARY.md
@.planning/phases/11-results-fast-recalc/11-07-SUMMARY.md
</context>

## Artifacts this phase produces

- `web/src/store/scenarios.ts` — `Scenario { id, name, metOverrides, tensorHash }`, `cloneScenario()`, `computeScenario()` (full solve → per-scenario OPFS `calc/<hash>/`), `switchScenario()`.
- `web/src/panels/ScenarioPanel.tsx` — scenario list, "New scenario"/"Save scenario"/"Compare scenarios", friendly met inputs + advanced raw A/B/C disclosure, delete-scenario confirm dialog.
- `web/src/store/difference.ts` — per-receiver dB(A) delta (A − B) from two scenarios' cached readouts.
- `web/src/map/differenceLayer.ts` — diverging fill layer (blue↔red, gray midpoint, symmetric clamp).
- `web/tests/e2e/scenarios.spec.ts` — offline Playwright UAT.

<tasks>

<task type="auto">
  <name>Task 1: Scenario registry — clone-then-edit + friendly/advanced met + full-solve cached tensor (D-13/14/15, METX-03/04)</name>
  <read_first>
    - crates/envi-gis/src/weather.rs (per-azimuth A/B/C derivation — reuse for friendly overrides, D-14; do not fork the met math)
    - web/src/store/weather.ts + web/src/panels/WeatherPanel.tsx (the Phase-9 weather import/derivation UI + state to build on)
    - web/src/compute/worker.ts + web/src/store/calc.ts (the Phase-10 full solve path + OPFS calc-dir layout to key per-scenario tensors)
    - .planning/phases/11-RESEARCH.md (§Pattern 6 scenario = met-override → new hash-keyed tensor; D-15 per-azimuth favourable)
  </read_first>
  <action>Create `web/src/store/scenarios.ts`. A scenario is `{id, name, metOverrides, tensorHash}`. "New scenario" clones the current met (clone-then-edit, D-13). Friendly overrides (T/RH/p, Beaufort wind class + direction, temp gradient, downwind worst-case toggle) drive the Phase-9 `envi_gis::weather` derivation (in WASM/Rust, D-01 — never TS met math) to per-azimuth A/B/C; expose raw per-azimuth A/B/C as an advanced override (D-14). Downwind worst-case = per-azimuth favourable: assume downward-refraction along EACH source→receiver bearing independently (D-15) — the standard Nord2000 worst-case envelope. A met change alters tensor identity ⇒ `computeScenario()` runs the FULL Phase-10 solve into a per-scenario OPFS dir `calc/<scenario_tensor_hash>/` (METX-04). `switchScenario()` loads that tensor + readout (instant, no re-solve). The per-scenario OPFS layout/naming is Claude's discretion — reuse the Phase-10 hash-keyed convention.</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run test:unit -- scenarios</automated>
  </verify>
  <acceptance_criteria>
    - A unit test asserts clone-then-edit produces a distinct scenario with its own tensor hash; switching loads the cached tensor with no re-solve; friendly overrides route to the WASM weather derivation (no TS met math — `grep -c "Math.log\|Math.exp" web/src/store/scenarios.ts` == 0).
    - Downwind worst-case sets per-azimuth favourable along each bearing (asserted against the derivation output).
  </acceptance_criteria>
  <done>Scenarios clone-then-edit, derive per-azimuth A/B/C via Phase-9, and cache per-scenario tensors switchable instantly.</done>
</task>

<task type="auto">
  <name>Task 2: ScenarioPanel + difference map (diverging A − B, D-16)</name>
  <read_first>
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§Scenario Manager inventory; §Data Visualization Palettes #2 diverging blue↔gray↔red hex + symmetric clamp + ±0.5 dB dead-zone; copy for New/Save/Compare/Delete)
    - web/src/map/isophoneLayer.ts (11-06 fill-layer pattern to mirror for the difference layer)
    - web/src/panels/DeleteProjectDialog.tsx (the destructive-confirm dialog idiom to reuse for delete-scenario)
    - web/src/store/results.ts (per-receiver dB(A) readouts feeding the delta)
  </read_first>
  <action>Create `web/src/panels/ScenarioPanel.tsx` in the ResultsPanel slot: named scenario list (base + user), "New scenario" (clone-then-edit), "Save scenario", friendly met inputs + an advanced raw per-azimuth A/B/C disclosure (D-14), "Compare scenarios" (pick A and B), and a destructive delete-scenario confirm (reuse the dialog idiom + UI-SPEC copy). Create `web/src/store/difference.ts` that computes the per-receiver dB(A) delta (A − B) in the WASM readout path (D-01): the delta is produced by the compute worker from the two scenarios' cached readouts and returned to TS, which only marshals the request and renders the returned numbers. TS performs zero acoustic arithmetic — no dB subtraction or weighting math in `difference.ts`. Create `web/src/map/differenceLayer.ts`: a diverging fill layer (blue `#2a78d6` ↔ gray `#383835` ↔ red `#d03b3b`, UI-SPEC #2), symmetric clamp at ±max(|Δ|) rounded to 1 dB, ±0.5 dB dead-zone rendered neutral; legend shows signed dB.</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; the difference layer renders a diverging fill with gray at Δ≈0 (never a hue at the midpoint) and a signed-dB legend.
    - The brand accent `#4ea8ff` is NOT used as a difference pole (asserted by grep of the layer colors).
    - The A−B delta is computed in WASM, not TS: `grep -c "Math.log10\|Math.pow\|Math.exp" web/src/store/difference.ts` == 0 (D-01).
    - Delete-scenario uses the destructive confirm dialog with the UI-SPEC copy.
  </acceptance_criteria>
  <done>Scenario management + a diverging difference map render A − B per-receiver deltas.</done>
</task>

<task type="auto">
  <name>Task 3: Offline Playwright scenarios UAT (WEB-12/METX-03/04)</name>
  <read_first>
    - web/tests/e2e/spectrum.spec.ts + web/tests/e2e/isophone.spec.ts (offline harness + fixture-tensor patterns to reuse)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§State Matrix difference-map row; scenario empty state "One scenario")
  </read_first>
  <action>Create `web/tests/e2e/scenarios.spec.ts` on the real bundle, offline (COOP/COEP + `page.route` mocks, fixture tensors for two scenarios). Assert: "New scenario" clones + edits met + computes (a solve is dispatched for the met change); switching between named scenarios is instant (loads a cached tensor, no re-solve); "Compare scenarios" renders the diverging difference map with a signed-dB legend; deleting a scenario prompts the destructive confirm and removes its cached result; the "One scenario" empty state shows with only the base. DevDependency-only; artifacts git-ignored.</action>
  <verify>
    <automated>cd web && npm run test:e2e -- scenarios</automated>
  </verify>
  <acceptance_criteria>
    - `scenarios.spec.ts` passes offline, asserting clone-then-edit compute, instant switch (cached, no re-solve), the diverging difference map, and delete-with-confirm.
    - No network egress.
  </acceptance_criteria>
  <done>An offline Playwright UAT proves the weather what-if scenarios + difference map (SC4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| friendly met overrides → derivation | T/RH/p/wind ranges validated before the Phase-9 derivation |
| per-scenario OPFS dir | scenario tensor hashes form OPFS paths; guarded like the Phase-10 store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08-01 | Tampering | met override inputs | mitigate | validate T/RH/p/Beaufort/gradient in physical ranges (V5) in the WASM derivation; typed error |
| T-11-08-02 | Integrity | scenario tensor identity | mitigate | each scenario keyed by its own tensor_hash; switching loads the matching cached tensor (no cross-serve) |
| T-11-08-03 | Info disclosure | per-scenario tensors | accept | cached locally in OPFS (Phase-10 D-09 keeps everything); quota/eviction deferred (CONTEXT deferred) |
</threat_model>

<verification>
- `cd web && npm run typecheck && npm run build && npm run test:e2e -- scenarios` green.
- No TS met/acoustic math (grep gate).
- `web/dist` rebuilt and committed.
</verification>

<success_criteria>
Named weather scenarios clone-then-edit with friendly + advanced met overrides, cache per-scenario tensors, switch instantly, and render a diverging A − B difference map (SC4, WEB-12, METX-03/04).
</success_criteria>

<output>
Create `.planning/phases/11-results-fast-recalc/11-08-SUMMARY.md` when done.
</output>
