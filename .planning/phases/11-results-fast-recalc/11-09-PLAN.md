---
phase: 11-results-fast-recalc
plan: 09
type: execute
wave: 4
depends_on: ["11-04", "11-05"]
files_modified:
  - web/src/panels/ExportMenu.tsx
  - web/src/store/exportUi.ts
  - web/src/compute/worker.ts
  - web/tests/e2e/export.spec.ts
autonomous: true
requirements: [GRID-05]
must_haves:
  truths:
    - "The results panel offers an Export menu with GeoTIFF (raster level grid), GeoJSON (isophone polygons), and CSV (spectra: band index + exact Hz)"
    - "Each export invokes the 11-04 WASM encoder over the OPFS tensor/grid and downloads bytes via a Blob — nothing leaves the device"
    - "Every downloaded file carries the full attribution/metadata footer (CRS, dB weighting, engine/scene identity, data attribution)"
    - "Export is disabled until a result exists and while stale"
  artifacts:
    - path: "web/src/panels/ExportMenu.tsx"
      provides: "the Export… menu (GeoTIFF/GeoJSON/CSV) + Blob download (D-20/D-21)"
      contains: "ExportMenu"
    - path: "web/src/store/exportUi.ts"
      provides: "export dispatch to the WASM encoder + Blob/objectURL download + metadata assembly (D-22)"
      contains: "downloadExport"
  key_links:
    - from: "web/src/store/exportUi.ts"
      to: "crates/envi-compute-wasm/src/export.rs"
      via: "invokes the WASM export boundary and downloads the returned bytes"
      pattern: "export"
    - from: "web/src/panels/ExportMenu.tsx"
      to: "web/src/store/results.ts"
      via: "reads the current result/weighting to enable/label the export"
      pattern: "results|weighting"
---

<objective>
Ship the results Export menu (GRID-05, D-20/21/22): a `.menu` with GeoTIFF (the continuous dB(A)/dB(C) level grid), GeoJSON (the isophone fill polygons), and CSV (receiver spectra with band index + exact Hz). Each item invokes the 11-04 WASM encoder over the OPFS tensor/grid in the compute worker and downloads the returned bytes via a `Blob`/`URL.createObjectURL` — nothing leaves the device (D-20). Every file carries the attribution footer (CRS + dB weighting label + engine/scene `tensor_hash` + OSM/Overture/ESA WorldCover/Copernicus attribution, D-22).

Purpose: SC5 user-facing surface. The encoders (bytes + metadata) are 11-04; this plan is the UI + download + worker dispatch. Fills the `ExportMenu` stub created by 11-05.
Output: `ExportMenu`, `store/exportUi`, worker export dispatch, offline Playwright UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-results-fast-recalc/11-CONTEXT.md
@.planning/phases/11-results-fast-recalc/11-RESEARCH.md
@.planning/phases/11-results-fast-recalc/11-UI-SPEC.md
@web/src/compute/worker.ts
@web/src/compute/client.ts
@web/src/store/results.ts
@.planning/phases/11-results-fast-recalc/11-04-SUMMARY.md
@.planning/phases/11-results-fast-recalc/11-05-SUMMARY.md
</context>

## Artifacts this phase produces

- `web/src/panels/ExportMenu.tsx` — the "Export…" `.menu` (GeoTIFF / GeoJSON / CSV), disabled-until-result + disabled-when-stale states.
- `web/src/store/exportUi.ts` — `downloadExport(format)` dispatching the WASM encoder in the worker, assembling `ExportMeta` (attribution footer), and triggering the Blob download.
- `web/src/compute/worker.ts` (+) — an export message that calls `envi-compute-wasm::export` and returns bytes.
- `web/tests/e2e/export.spec.ts` — offline Playwright UAT.

<tasks>

<task type="auto">
  <name>Task 1: Worker export dispatch + exportUi store + Blob download (D-20/D-22)</name>
  <read_first>
    - crates/envi-compute-wasm/src/export.rs (the 11-04 `export(req)` boundary: ExportFormat + returned Vec<u8>; the ExportReq DTO shape)
    - web/src/compute/worker.ts + web/src/compute/client.ts (the worker message protocol + how the main thread awaits a worker result)
    - web/src/store/results.ts (current result, selected weighting, tensor_hash for metadata)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (export attribution footer copy; §State Matrix export row)
  </read_first>
  <action>Add an export message to `web/src/compute/worker.ts` that calls the 11-04 `envi-compute-wasm::export` over the OPFS tensor/grid and returns the encoded `Vec<u8>`. Create `web/src/store/exportUi.ts` with `downloadExport(format)`: assemble the `ExportMeta` (CRS EPSG from the project, dB weighting label from result metadata, engine version + scene `tensor_hash`, and the Phase-8 `Provenance` attribution strings — D-22), dispatch the worker export, wrap the returned bytes in a `Blob`, and trigger a browser download via an object URL + a program-derived sanitized filename (V12, T-11-09-02). Bytes are produced in WASM (D-01/D-20) — TS marshals the request and triggers the download only; no acoustic math in TS.</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run test:unit -- exportUi</automated>
  </verify>
  <acceptance_criteria>
    - A unit test asserts `downloadExport('csv'|'geojson'|'geotiff')` dispatches the worker export with the correct `ExportFormat` and a metadata footer containing the CRS, weighting label, engine/scene identity, and the data-attribution string.
    - The download uses a Blob/objectURL (no server request) and a sanitized filename — `grep -c "createObjectURL" web/src/store/exportUi.ts` ≥ 1; `grep -c "Math.log10\|Math.pow" web/src/store/exportUi.ts` == 0.
  </acceptance_criteria>
  <done>The worker export dispatch + Blob download produce attributed files entirely client-side.</done>
</task>

<task type="auto">
  <name>Task 2: ExportMenu UI (GeoTIFF/GeoJSON/CSV) + honest states (GRID-05)</name>
  <read_first>
    - web/src/panels/ExportMenu.tsx (the 11-05 stub to fill)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§Export menu inventory + copy "Export…"; §State Matrix — disabled until a result exists, disabled if stale, encode-error toast)
    - web/src/store/results.ts + web/src/store/stale.ts (result-exists + stale state gating the menu)
  </read_first>
  <action>Fill `web/src/panels/ExportMenu.tsx` (the 11-05 stub) with a `.menu` primary CTA "Export…" opening GeoTIFF (raster level grid), GeoJSON (isophone polygons), CSV (spectra: band index + exact Hz). Each item calls `downloadExport(format)`. Honest states (UI-SPEC): disabled until a result exists; disabled while stale; an encode-error toast on failure. English-only, sentence case, units shown.</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` produces a fresh `web/dist`; the menu shows the three formats and is disabled with no result / when stale.
    - The menu labels match the UI-SPEC copy (GeoTIFF / GeoJSON / CSV).
  </acceptance_criteria>
  <done>The Export menu offers all three formats with correct enable/stale gating.</done>
</task>

<task type="auto">
  <name>Task 3: Offline Playwright export UAT (GRID-05)</name>
  <read_first>
    - web/tests/e2e/spectrum.spec.ts (offline harness + fixture tensor + COOP/COEP + page.route mocks to reuse)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (export attribution footer — the metadata to assert)
  </read_first>
  <action>Create `web/tests/e2e/export.spec.ts` on the real bundle, offline (COOP/COEP + `page.route('**/api/*')` mocks, fixture tensor/grid). Assert via Playwright's download API: each of GeoTIFF/GeoJSON/CSV triggers a Blob download (no network request); the CSV bytes contain band-index + exact-Hz columns and the attribution footer; the GeoJSON parses as valid FeatureCollection with the attribution foreign member; the menu is disabled with no result and when stale. DevDependency-only; artifacts git-ignored.</action>
  <verify>
    <automated>cd web && npm run test:e2e -- export</automated>
  </verify>
  <acceptance_criteria>
    - `export.spec.ts` passes offline, asserting all three downloads occur via Blob (zero network egress), the CSV/GeoJSON content + attribution, and the disabled/stale gating.
  </acceptance_criteria>
  <done>An offline Playwright UAT proves client-side export of all three formats with attribution (SC5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export request → WASM encoder | format selection UI-controlled; encoder is 11-04 (bounded) |
| generated bytes → browser download | Blob only; sanitized filename; no server write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09-01 | Info disclosure | export path | mitigate | D-20: bytes in WASM, Blob download only; nothing leaves the device |
| T-11-09-02 | Tampering | download filename | mitigate | program-derived sanitized filename (V12); objectURL, no filesystem path |
| T-11-09-03 | Availability | encode failure | mitigate | encode-error toast honest state; no silent partial file |
</threat_model>

<verification>
- `cd web && npm run typecheck && npm run build && npm run test:e2e -- export` green.
- grep gate: zero TS acoustic math in exportUi.
- `web/dist` rebuilt and committed.
</verification>

<success_criteria>
The Export menu downloads GeoTIFF/GeoJSON/CSV entirely client-side with full attribution, proven by an offline Playwright UAT (SC5, GRID-05).
</success_criteria>

<output>
Create `.planning/phases/11-results-fast-recalc/11-09-SUMMARY.md` when done.
</output>
