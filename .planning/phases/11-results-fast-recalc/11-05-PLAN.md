---
phase: 11-results-fast-recalc
plan: 05
type: execute
wave: 3
depends_on: ["11-01"]
files_modified:
  - web/src/compute/opfs.ts
  - web/src/compute/worker.ts
  - web/src/store/results.ts
  - web/src/panels/ResultsPanel.tsx
  - web/src/panels/SpectrumPanel.tsx
  - web/src/panels/ColorScaleEditor.tsx
  - web/src/panels/ConditioningPanel.tsx
  - web/src/panels/ScenarioPanel.tsx
  - web/src/panels/ExportMenu.tsx
  - web/src/App.tsx
  - web/tests/e2e/spectrum.spec.ts
autonomous: true
requirements: [WEB-11]
must_haves:
  truths:
    - "A receiver's spectrum panel shows per-band levels from WASM-produced values — the frontend performs zero acoustic arithmetic (D-01)"
    - "The 1/3-oct display (default) toggles to the full 105-point 1/12-oct expert view, aggregated by band index"
    - "The dB(A)⇄dB(C) toggle switches totals instantly with no recompute (both weightings precomputed)"
    - "The coherent/incoherent split shows in totals always, with an on-demand per-band overlay"
    - "A receiver is selectable via map click AND a synced receiver list"
  artifacts:
    - path: "web/src/panels/SpectrumPanel.tsx"
      provides: "chart-primary + expandable exact-numbers table receiver spectrum (D-05/06/07/08)"
      contains: "SpectrumPanel"
    - path: "web/src/compute/opfs.ts"
      provides: "worker-side OPFS tensor read glue (pre-open read handles) feeding the WASM reader"
      contains: "readChunk"
    - path: "web/src/store/results.ts"
      provides: "results state: selected receiver, weighting/display toggles, cached readouts"
      contains: "results"
  key_links:
    - from: "web/src/panels/SpectrumPanel.tsx"
      to: "web/src/store/results.ts"
      via: "renders WASM-produced band levels + totals; no dB math in TS"
      pattern: "results|readout"
    - from: "web/src/compute/opfs.ts"
      to: "crates/envi-compute-wasm/src/opfs_reader.rs"
      via: "reads OPFS chunk bytes in the worker and hands them to read_chunk"
      pattern: "SyncAccessHandle|readChunk"
---

<objective>
Ship the receiver spectrum panel (WEB-11) and the results shell it lives in: a chart-primary + expandable exact-numbers table (D-05) showing per-band levels with a 1/3-oct default ⇄ 1/12-oct expert toggle (D-06), an instant dB(A)⇄dB(C) toggle (D-09, no recompute), the coherent/incoherent split (D-08), and receiver selection via map click AND a synced list (D-07). The frontend performs ZERO acoustic arithmetic — it renders WASM-produced values from the 11-01 readout core over the OPFS tensor read glue added here.

Purpose: SC1. This plan also establishes the reusable `ResultsPanel` shell + App mount that the isophone map (11-06), conditioning (11-07), scenarios (11-08), and export (11-09) slot into without editing App.tsx.
Output: `ResultsPanel`, `SpectrumPanel`, `store/results`, OPFS read glue, offline Playwright spectrum UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-results-fast-recalc/11-CONTEXT.md
@.planning/phases/11-results-fast-recalc/11-RESEARCH.md
@.planning/phases/11-results-fast-recalc/11-UI-SPEC.md
@web/src/App.tsx
@web/src/spectrum/SpectrumEditor.tsx
@web/src/compute/opfs.ts
@web/src/compute/worker.ts
@web/src/store/calc.ts
@.planning/phases/11-results-fast-recalc/11-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `web/src/panels/ResultsPanel.tsx` — the right-rail results shell that imports and renders five slot components (`SpectrumPanel` real; `ColorScaleEditor`/`ConditioningPanel`/`ScenarioPanel`/`ExportMenu` created here as stubs, each OWNED and filled in by a later plan so `ResultsPanel.tsx` itself is never re-edited).
- `web/src/panels/ColorScaleEditor.tsx`, `ConditioningPanel.tsx`, `ScenarioPanel.tsx`, `ExportMenu.tsx` — placeholder stubs (render nothing) created here; filled by 11-06/11-07/11-08/11-09 respectively.
- `web/src/panels/SpectrumPanel.tsx` — inline-SVG chart + `.spectrum-table` (band index · exact Hz · dB), display/weighting/split toggles.
- `web/src/store/results.ts` — `selectedReceiverId`, `displayMode` (third|twelfth), `weighting` (A|C), `showSplit`, cached per-receiver `ReceiverReadout`.
- `web/src/compute/opfs.ts` (+) — `readChunk(...)` worker-side pre-open + sync-read glue.
- `web/tests/e2e/spectrum.spec.ts` — offline Playwright UAT.

<tasks>

<task type="auto">
  <name>Task 1: OPFS tensor read glue + results store + readout wiring</name>
  <read_first>
    - web/src/compute/opfs.ts (the existing write-path hoist: async createSyncAccessHandle then sync I/O, worker-only — Pitfall 4; safeSeg/assertHex path guards, V12)
    - web/src/compute/worker.ts (the compute worker message protocol; where to add a read+readout message)
    - web/src/store/calc.ts (tier-complete plumbing; the results feed)
    - crates/envi-compute-wasm/src/opfs_reader.rs (11-01 read_chunk boundary)
  </read_first>
  <action>Add `readChunk` glue to `web/src/compute/opfs.ts` mirroring the write-path handle hoist (pre-open async, read sync, WORKER-ONLY — Pitfall 4), reusing the existing `safeSeg`/`assertHex` OPFS path guards (V12, T-11-05-02). Add a worker message that, for a selected receiver's `global_index`, reads only the covering chunk(s) via the manifest `chunk_receivers`, calls the 11-01 WASM readout, and returns the `ReceiverReadout` (band levels + both totals + split). Create `web/src/store/results.ts` holding `selectedReceiverId`, `displayMode`, `weighting`, `showSplit`, and the cached readouts. ALL acoustic values come from WASM — no dB/weighting math in TS (D-01; RESEARCH anti-pattern: no `10*Math.log10` in Phase-11 TS).</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run test:unit -- results</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/store/results.ts` compiles under `tsc` strict; a unit test asserts the store toggles (display/weighting/split) mutate state without invoking any acoustic computation in TS.
    - `grep -c "Math.log10\|Math.pow" web/src/store/results.ts web/src/panels/SpectrumPanel.tsx` == 0 (zero client acoustic math, D-01).
  </acceptance_criteria>
  <done>OPFS read glue + results store feed WASM-produced readouts to the panel with zero TS acoustic math.</done>
</task>

<task type="auto">
  <name>Task 2: SpectrumPanel + ResultsPanel shell + App mount (D-05/06/07/08)</name>
  <read_first>
    - web/src/spectrum/SpectrumEditor.tsx (the inline-SVG chart idiom + `.curve-axis-label` + `.spectrum-table` to match, zero new JS dep)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§Spectrum Panel component inventory; §Data Visualization Palettes #4 series colors; §Typography tabular-nums; state matrix)
    - web/src/App.tsx (the four-region shell + how CalcPanel mounts; add the results shell without breaking existing panels)
  </read_first>
  <action>Create `web/src/panels/ResultsPanel.tsx` (right-rail shell) importing/rendering five slot components, and create four render-nothing STUB files (`ColorScaleEditor.tsx`, `ConditioningPanel.tsx`, `ScenarioPanel.tsx`, `ExportMenu.tsx`) so later plans fill their OWN file and never re-edit `ResultsPanel.tsx` (avoids same-wave file conflict). Then build `web/src/panels/SpectrumPanel.tsx`: inline-SVG bar/line chart (series colors from UI-SPEC #4: total `#3987e5`, coherent `#199e70`, incoherent `#d95926`) + an expandable `.spectrum-table` (columns band index · exact Hz · dB, `--font-mono` tabular-nums). Segmented toggles: 1/3-oct (default) ⇄ 1/12-oct expert (aggregate by BAND INDEX — every 4th grid point, never nominal Hz, D-06); dB(A) (default) ⇄ dB(C) instant (D-09, re-apply the precomputed WASM table, no recompute); coherent/incoherent split always in totals + on-demand per-band overlay (D-08). Receiver selection: map-click AND a synced receiver list, selected row/marker highlighted with the accent ring (D-07). Honest states: "Select a receiver" empty, `.loading` 0.55 opacity, stale badge slot. Mount `ResultsPanel` in App.tsx. If the toggle weighting ≠ result metadata weighting, show the mismatch note (UI-SPEC copy).</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` produces a fresh `web/dist`; the panel renders the chart + table and both toggles.
    - Weighting/display toggles re-render from cached WASM values (no worker round-trip on toggle — asserted in the Playwright test below).
  </acceptance_criteria>
  <done>The spectrum panel renders WASM band levels + totals with the display/weighting/split toggles and dual receiver selection.</done>
</task>

<task type="auto">
  <name>Task 3: Offline Playwright spectrum UAT (WEB-11)</name>
  <read_first>
    - web/playwright.config.ts + web/tests/e2e/ (the offline harness: local devserver on the real web/dist, per-test `page.route('**/api/*')` mocks, COOP/COEP headers — Pitfall 7)
    - web/tests/e2e (an existing spec to mirror the mock/route + fixture pattern)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (state matrix — the observable behaviors to assert)
  </read_first>
  <action>Create `web/tests/e2e/spectrum.spec.ts` driving the REAL built bundle on a local devserver with COOP/COEP (reuse the Phase-10 header setup — Pitfall 7), backend `/api/*` mocked per-test via `page.route(...)` (offline, no credentials), with a fixture tensor in OPFS. Assert: selecting a receiver (map click AND list) shows its spectrum; the 1/3⇄1/12 toggle changes the band count; the dB(A)⇄dB(C) toggle changes totals with NO recompute (no new worker solve message); the coherent/incoherent overlay appears on demand; the "Select a receiver" empty state shows with none selected. Keep Playwright a devDependency; artifacts git-ignored.</action>
  <verify>
    <automated>cd web && npm run test:e2e -- spectrum</automated>
  </verify>
  <acceptance_criteria>
    - `spectrum.spec.ts` passes offline against `web/dist`, asserting each WEB-11 observable (receiver select both ways, display toggle, instant weighting toggle with no recompute, split overlay, empty state).
    - The test uses `page.route` mocks only — no network egress; artifacts under `test-results/`/`playwright-report/` remain git-ignored.
  </acceptance_criteria>
  <done>An offline Playwright UAT proves the spectrum panel's WEB-11 behavior on the real bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OPFS path segments → sync handle | receiver/calc ids form OPFS paths; must be guarded (traversal) |
| WASM readout → panel render | JS renders numbers only; no acoustic math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Integrity | SpectrumPanel | mitigate | D-01: zero TS acoustic math; grep gate asserts no `Math.log10`/`Math.pow`; all values from WASM |
| T-11-05-02 | Tampering | opfs.ts readChunk | mitigate | reuse `safeSeg`/`assertHex` path guards (V12); worker-only sync handles (Pitfall 4) |
| T-11-05-03 | Availability | corrupt/missing chunk | mitigate | typed ChunkDecodeError (11-01) surfaces the "result data unavailable" honest state, no crash |
</threat_model>

<verification>
- `cd web && npm run typecheck && npm run build && npm run test:e2e -- spectrum` green.
- `grep` gate: zero acoustic math in the new TS.
- `web/dist` rebuilt and committed.
</verification>

<success_criteria>
A receiver spectrum reads per-band levels + dB(A)/dB(C) + coherent/incoherent split entirely from WASM, with instant toggles and dual receiver selection, proven by an offline Playwright UAT (SC1).
</success_criteria>

<output>
Create `.planning/phases/11-results-fast-recalc/11-05-SUMMARY.md` when done.
</output>
