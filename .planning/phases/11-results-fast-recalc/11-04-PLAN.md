---
phase: 11-results-fast-recalc
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - crates/envi-compute/src/export/mod.rs
  - crates/envi-compute/src/export/geotiff.rs
  - crates/envi-compute/src/export/geojson.rs
  - crates/envi-compute/src/export/csv.rs
  - crates/envi-compute/src/lib.rs
  - crates/envi-compute-wasm/src/export.rs
  - crates/envi-compute-wasm/src/dto.rs
  - crates/envi-compute-wasm/src/lib.rs
  - web/src/generated/wire.ts
autonomous: true
requirements: [GRID-05]
must_haves:
  truths:
    - "The continuous dB(A)/dB(C) level grid encodes to a valid single-strip Float32 GeoTIFF with GeoKeyDirectory + ModelPixelScale + ModelTiepoint (readable by a GIS tool)"
    - "The isophone fill polygons encode to RFC-7946 GeoJSON via the in-tree geojson crate"
    - "Spectra encode to CSV carrying band index AND exact Hz columns"
    - "Every export carries the full attribution/metadata footer (CRS, dB weighting, engine/scene identity, data attribution)"
  artifacts:
    - path: "crates/envi-compute/src/export/geotiff.rs"
      provides: "hand-rolled minimal Float32 GeoTIFF encoder — zero new dep (D-20/D-21)"
      contains: "pub fn encode_geotiff"
    - path: "crates/envi-compute/src/export/csv.rs"
      provides: "spectra CSV with band_index + exact_hz + level columns (D-21)"
      contains: "pub fn encode_spectra_csv"
    - path: "crates/envi-compute-wasm/src/export.rs"
      provides: "WASM export boundary → bytes for browser download (D-20)"
      contains: "pub fn export"
  key_links:
    - from: "crates/envi-compute/src/export/geojson.rs"
      to: "crates/envi-gis/src/landcover.rs"
      via: "reuses the in-tree geojson crate already vetted in envi-gis"
      pattern: "geojson"
    - from: "crates/envi-compute/src/export/csv.rs"
      to: "crates/envi-engine/src/freq.rs"
      via: "exact Hz columns from FreqAxis::centres (nominal labels display-only)"
      pattern: "centres|FreqAxis"
---

<objective>
Build the three WASM export encoders (GRID-05, D-20/21/22): GeoTIFF (the continuous dB(A)/dB(C) level grid, raster), GeoJSON (the isophone fill polygons, vector), and CSV (receiver spectra, band index + exact Hz). All bytes generated in WASM from the OPFS tensor/grid; the browser downloads them directly (nothing leaves the device). Every export carries CRS + dB weighting label + engine/scene identity + OSM/Overture/ESA WorldCover/Copernicus attribution.

Purpose: SC5. Per RESEARCH the GeoTIFF is hand-rolled as a minimal single-strip Float32 image (zero new crates, matching the project's hand-roll ethos and avoiding a package-legitimacy gate); GeoJSON reuses the in-tree `geojson` crate; CSV is hand-rolled.
Output: `envi-compute::export::{geotiff, geojson, csv}`, `envi-compute-wasm::export` boundary + ExportReq DTO.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-results-fast-recalc/11-CONTEXT.md
@.planning/phases/11-results-fast-recalc/11-RESEARCH.md
@crates/envi-engine/src/freq.rs
@crates/envi-gis/src/landcover.rs
@crates/envi-compute/src/grid.rs
@crates/envi-compute/src/isoband.rs
@.planning/phases/11-results-fast-recalc/11-01-SUMMARY.md
@.planning/phases/11-results-fast-recalc/11-02-SUMMARY.md
</context>

## Artifacts this phase produces

- `crates/envi-compute/src/export/mod.rs` — `struct ExportMeta { epsg, weighting_label, engine_version, tensor_hash, attribution: String }`.
- `crates/envi-compute/src/export/geotiff.rs` — `fn encode_geotiff(grid: &LevelGrid, epsg: u32, meta: &ExportMeta) -> Vec<u8>`.
- `crates/envi-compute/src/export/geojson.rs` — `fn encode_isophone_geojson(bands: &[IsoBandLonLat], meta: &ExportMeta) -> String`.
- `crates/envi-compute/src/export/csv.rs` — `fn encode_spectra_csv(receivers: &[ReceiverReadout], axis: &FreqAxis, meta: &ExportMeta) -> String`.
- `crates/envi-compute-wasm/src/export.rs` — `#[wasm_bindgen] pub fn export(req: JsValue) -> Result<Vec<u8>, JsValue>`; `enum ExportFormat { GeoTiff, GeoJson, Csv }` DTO.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-rolled minimal Float32 GeoTIFF + CSV spectra encoders (D-21/D-22)</name>
  <read_first>
    - crates/envi-engine/src/freq.rs (FreqAxis::centres — exact Hz for the CSV column)
    - crates/envi-compute/src/grid.rs (LevelGrid dims/origin/spacing for the GeoTIFF geotransform)
    - .planning/phases/11-results-fast-recalc/11-RESEARCH.md (§Export — the minimal single-strip Float32 GeoTIFF recipe: GeoKeyDirectoryTag + ModelPixelScaleTag + ModelTiepointTag; the CSV columns)
  </read_first>
  <behavior>
    - GeoTIFF: a single-strip Float32 image whose pixel values are the level grid; geotransform from grid origin+spacing; CRS EPSG in the GeoKeyDirectory; metadata in ImageDescription/GDAL_METADATA.
    - CSV: header comment carries the attribution footer; columns `band_index, exact_hz, level_db` plus a dB(A)/dB(C) totals row; exact Hz from FreqAxis::centres (nominal labels never used as the identity — RESEARCH Pitfall 3).
  </behavior>
  <action>Create `crates/envi-compute/src/export/mod.rs`, `geotiff.rs`, `csv.rs`. Hand-roll the minimal single-strip Float32 GeoTIFF (~200 LOC, ZERO new dep — do not add `tiff`/`geotiff-writer`/`tiff-writer`; the SUS/new packages are removed per RESEARCH Package Legitimacy, and the mature `tiff` crate is intentionally skipped to keep zero-dep). Encode `GeoKeyDirectoryTag`, `ModelPixelScaleTag`, `ModelTiepointTag`, and the EPSG. Hand-roll the CSV with band index + exact Hz columns. Both embed `ExportMeta` (CRS, dB weighting label, engine version + `tensor_hash`, data attribution — D-22). Register `pub mod export;`.</action>
  <verify>
    <automated>cargo test -p envi-compute export</automated>
  </verify>
  <acceptance_criteria>
    - A GeoTIFF round-trip test decodes the produced bytes (via a dev-only reader or byte-offset assertions) and confirms Float32 pixel values, strip layout, and the geo-keys/EPSG present.
    - A CSV test asserts the band_index + exact_hz columns and the attribution header line are present; exact Hz equal `FreqAxis::centres`.
    - No new runtime dependency added (`cargo tree -p envi-compute` shows no `tiff`/`geotiff-writer`/`tiff-writer`).
  </acceptance_criteria>
  <done>GeoTIFF and CSV encoders produce valid bytes with full metadata, zero new deps.</done>
</task>

<task type="auto">
  <name>Task 2: GeoJSON isophone encoder + WASM export boundary → browser bytes (D-20)</name>
  <read_first>
    - crates/envi-gis/src/landcover.rs (the in-tree `geojson` crate usage + RFC-7946 winding to reuse)
    - crates/envi-compute/src/isoband.rs (IsoBand rings; reprojected to LonLat by the map layer — the export takes LonLat polygons)
    - crates/envi-compute-wasm/src/dto.rs (ts-rs `TS` no-drift pattern for the ExportReq DTO)
    - crates/envi-geo/ (SceneXY→LonLat reprojection boundary — the single CRS boundary, GEOX-04)
  </read_first>
  <action>Create `crates/envi-compute/src/export/geojson.rs` reusing the in-tree `geojson` crate (RFC-7946 winding, geometry types — do not hand-write JSON) to encode the isophone fill polygons, each carrying its band range + fill color + the `ExportMeta` as GeoJSON foreign members/properties. Create `crates/envi-compute-wasm/src/export.rs`: a `#[wasm_bindgen] export(req)` that dispatches an `ExportFormat` (GeoTiff/GeoJson/Csv), reprojects grid/polys through `envi-geo` (never inline proj — GEOX-04), and returns `Vec<u8>` for the browser to `Blob`-download (D-20, nothing leaves the device). Add the `ExportReq`/`ExportFormat` DTO (ts-rs `TS`) and regenerate wire.ts. Sanitize any export filename (V12 path-traversal, threat T-11-04-02) — the filename is program-derived, the bytes are a Blob.</action>
  <verify>
    <automated>cargo test -p envi-compute-wasm export && cargo test -p envi-service wire</automated>
  </verify>
  <acceptance_criteria>
    - A GeoJSON test parses the output as valid RFC-7946 with the expected polygon count + band-range properties + attribution foreign member.
    - The wire.ts no-drift test passes with `ExportReq`/`ExportFormat` present (`grep -c "ExportFormat" web/src/generated/wire.ts` ≥ 1).
    - The WASM `export` entry returns bytes for all three formats in a boundary test; reprojection goes through `envi-geo` (`grep -c "envi_geo\|LonLat" crates/envi-compute-wasm/src/export.rs` ≥ 1).
    - clippy/fmt clean; engine byte-identical.
  </acceptance_criteria>
  <done>All three export encoders produce browser-downloadable bytes through the WASM boundary with full attribution, reprojected via the single CRS boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export request → WASM encoder | format + target selection UI-controlled; bytes never leave the device |
| generated filename → browser download | must be sanitized (no path traversal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Info disclosure | export bytes | mitigate | D-20: bytes generated in WASM, Blob download only; no server write, nothing leaves device |
| T-11-04-02 | Tampering | export filename | mitigate | program-derived sanitized filename (V12); Blob URL, no filesystem path |
| T-11-04-03 | Tampering | GeoTIFF byte encoding | mitigate | bounded single-strip encode; dims from the level grid, not unbounded input |
</threat_model>

<verification>
- `cargo test -p envi-compute && cargo test -p envi-compute-wasm` green; wire no-drift green.
- `cargo tree -p envi-compute` shows zero new deps (hand-rolled GeoTIFF); `cargo tree -p envi-engine` unchanged.
- clippy/fmt clean.
</verification>

<success_criteria>
Level grid → GeoTIFF, isophone polys → GeoJSON, spectra → CSV, all in WASM with full attribution and browser-download bytes (SC5 backend), zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/11-results-fast-recalc/11-04-SUMMARY.md` when done.
</output>
