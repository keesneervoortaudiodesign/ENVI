---
phase: 11-results-fast-recalc
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - crates/envi-compute/src/isoband.rs
  - crates/envi-compute/src/grid.rs
  - crates/envi-compute/src/lib.rs
autonomous: true
requirements: [GRID-04, WEB-06]
must_haves:
  truths:
    - "The fine-tier receiver lattice reconstructs into a dense [rows × cols] level grid keyed by lattice index (never nominal position)"
    - "A continuous scalar level grid + break edges produces nested iso-band fill polygons with linearly-interpolated crossings"
    - "Iso-bands never self-cross or leak at saddle cells"
    - "Editing break values re-runs only the tracer over the cached grid — no re-solve"
  artifacts:
    - path: "crates/envi-compute/src/grid.rs"
      provides: "fine-tier lattice → 2D level grid reconstruction (verifies RESEARCH A3)"
      contains: "pub fn reconstruct_level_grid"
    - path: "crates/envi-compute/src/isoband.rs"
      provides: "hand-rolled interpolated marching-squares iso-band tracer → SceneXY fill polygons (D-02)"
      contains: "pub fn trace_isobands"
  key_links:
    - from: "crates/envi-compute/src/isoband.rs"
      to: "crates/envi-gis/src/landcover.rs"
      via: "reuses ring-classification / DP-simplify / non-crossing machinery (adapted for interpolated iso-bands)"
      pattern: "Simplify|ring"
    - from: "crates/envi-compute/src/grid.rs"
      to: "crates/envi-compute/src/tiers.rs"
      via: "reconstructs the lattice from TierReceiver global_index + position + spacing"
      pattern: "TierReceiver|spacing"
---

<objective>
Build the pure-Rust isophone contouring core (D-02, GRID-04): reconstruct the fine-tier receiver lattice into a dense 2D level grid, then trace nested iso-band fill polygons with an interpolated marching-squares tracer adapted from the Phase-8 `landcover.rs` machinery. Isophones are FILL POLYGONS, never a heatmap layer.

Purpose: SC3 requires isophone fill polygons with an editable color scale where editing the scale re-contours the cached level grid WITHOUT re-running propagation. This plan owns the WASM-side geometry; the MapLibre fill layer + color-scale editor (11-06) consume it. The two `contour*` crates are SUS (declined in Phase 8) and gdal is not in the WASM path — hand-roll per D-02.
Output: `envi-compute::grid`, `envi-compute::isoband`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-results-fast-recalc/11-CONTEXT.md
@.planning/phases/11-results-fast-recalc/11-RESEARCH.md
@crates/envi-compute/src/tiers.rs
@crates/envi-gis/src/landcover.rs
@.planning/phases/11-results-fast-recalc/11-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `crates/envi-compute/src/grid.rs` — `struct LevelGrid { rows, cols, origin: [f64;2], spacing_m: f64, values: Vec<f64> }`, `fn reconstruct_level_grid(fine_tier: &Tier, dba: &[f64]) -> LevelGrid`.
- `crates/envi-compute/src/isoband.rs` — `struct BreakScale { breaks: Vec<f64> }`, `struct IsoBand { lower: f64, upper: f64, rings: Vec<Vec<[f64;2]>> }`, `fn trace_isobands(grid: &LevelGrid, breaks: &[f64]) -> Vec<IsoBand>`.

<tasks>

<task type="auto">
  <name>Task 1: Fine-tier lattice → 2D level grid reconstruction (verify RESEARCH A3)</name>
  <read_first>
    - crates/envi-compute/src/tiers.rs (TierReceiver.global_index + position, Tier.spacing_m, partition row-major i-outer/j-inner enumeration — the lattice origin/spacing that makes a reconstructable grid)
    - crates/envi-compute/src/lib.rs (module list)
    - .planning/phases/11-results-fast-recalc/11-RESEARCH.md (§Isophone Contouring "Grid reconstruction"; Assumption A3)
  </read_first>
  <action>Create `crates/envi-compute/src/grid.rs`. Verify RESEARCH A3 against `tiers.rs`: the fine-tier receivers are a regular lattice enumerated row-major (`i` outer over x, `j` inner over y) at `spacing_m` from `lattice_origin`. `reconstruct_level_grid` maps each fine-tier receiver's `position` back to integer `(col, row)` lattice indices (`round((pos−origin)/spacing)`) and scatters its dB(A) value into a dense `[rows × cols]` `Vec<f64>`, using a sentinel/`Option` hole for any lattice node the fine tier did not introduce (coarse-tier gaps are filled by the coarse readout, or left as holes the tracer treats as no-data). Key strictly by lattice index, never nominal position. If A3 proves false (irregular tiers) surface it: the tier plan's spacing/origin must be passed explicitly — document in the SUMMARY. Register `pub mod grid;`.</action>
  <verify>
    <automated>cargo test -p envi-compute grid</automated>
  </verify>
  <acceptance_criteria>
    - A test partitions a small calc area via `tiers::partition`, assigns synthetic dB values by `global_index`, reconstructs the grid, and asserts every fine-tier receiver lands at the expected `(row, col)` with its value.
    - A degenerate/empty lattice returns an empty grid without panic.
  </acceptance_criteria>
  <done>The fine-tier lattice reconstructs into a dense index-keyed 2D level grid.</done>
</task>

<task type="auto">
  <name>Task 2: Interpolated marching-squares iso-band tracer (D-02)</name>
  <read_first>
    - crates/envi-gis/src/landcover.rs (ring exterior/hole classification via signed area, sharpest-right-turn saddle stitch, geo::Simplify Douglas–Peucker, non-crossing guarantee — the DNA to adapt)
    - crates/envi-compute/src/grid.rs (Task 1 LevelGrid)
    - .planning/phases/11-results-fast-recalc/11-RESEARCH.md (§Isophone Contouring; Pitfall 6 saddle leaking; performance note)
  </read_first>
  <action>Create `crates/envi-compute/src/isoband.rs`. Hand-roll an interpolated marching-squares iso-band tracer producing nested fill polygons for each band `[breaks[k], breaks[k+1])`. CRUCIAL difference from `landcover.rs` (which traces a binary pixel partition with no interpolation): linearly interpolate the crossing point on each cell edge where the level crosses a break value so contours are smooth. Resolve ambiguous (saddle) cells with the mean-value (cell-average) rule to prevent self-crossing/leaking (Pitfall 6). Reuse `landcover.rs`'s exterior/hole assignment + `geo::Simplify` + the non-crossing property test. Output `IsoBand` rings as SceneXY `[x,y]` polygons (reprojection to LonLat is the map layer's job, 11-06). Emit a single source-of-truth `breaks[]` so 11-06 can enforce legend ≡ contour ≡ class colors (D-04). Validate breaks: strictly monotonic increasing, finite, ≥2 (V5) — typed error, never panic. Register `pub mod isoband;`.</action>
  <verify>
    <automated>cargo test -p envi-compute isoband</automated>
  </verify>
  <acceptance_criteria>
    - A known ramp grid + fixed breaks produces the expected band count and interpolated crossing coordinates within tolerance.
    - A saddle-cell fixture produces non-crossing rings (asserted via `geo::Relate` `is_overlaps() == false` across sibling bands — the landcover.rs non-crossing property).
    - Non-monotonic / <2 breaks return a typed error (asserted), no panic.
    - A 316×316 (~100k-cell) grid × 10 breaks traces in well under 100 ms (a timed test or documented benchmark; the roadmap-flagged perf check — the gdal escape hatch stays unused, D-02).
  </acceptance_criteria>
  <done>Iso-band fill polygons trace from a cached level grid, non-crossing, re-runnable on break edits with no re-solve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| break-scale input → tracer | break edges are user-editable; must be validated before contouring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | `isoband::trace_isobands` | mitigate | validate breaks monotonic/finite/≥2 (V5); typed error, never panic |
| T-11-02-02 | DoS | `grid`/`isoband` | mitigate | bounded work = O(cells × breaks); grid dims come from the tier plan, not unbounded user input |
| T-11-02-03 | Integrity | saddle cells | mitigate | mean-value saddle rule + non-crossing property test (Pitfall 6) |
</threat_model>

<verification>
- `cargo test -p envi-compute` green (grid + isoband).
- `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` clean.
- No new crate deps (hand-rolled; `contour*` not added — RESEARCH Package Legitimacy).
</verification>

<success_criteria>
A cached level grid contours into non-crossing nested iso-band fill polygons via a hand-rolled interpolated tracer; editing breaks re-contours with no re-solve (SC3 backend).
</success_criteria>

<output>
Create `.planning/phases/11-results-fast-recalc/11-02-SUMMARY.md` when done.
</output>
