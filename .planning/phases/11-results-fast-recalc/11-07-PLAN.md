---
phase: 11-results-fast-recalc
plan: 07
type: execute
wave: 4
depends_on: ["11-03", "11-05"]
files_modified:
  - web/src/store/conditioning.ts
  - web/src/panels/ConditioningPanel.tsx
  - web/src/store/stale.ts
  - web/tests/e2e/conditioning.spec.ts
autonomous: true
requirements: [WEB-05, SVC-06]
must_haves:
  truths:
    - "Changing a source's gain/filter/delay recomputes results interactively via the streaming tensor MAC (debounced ~150 ms), spectra + map update live with no re-propagation"
    - "The filter control is the reused Phase-7 SpectrumEditor (1/1·1/3·1/12 entry → 105-pt interpolation)"
    - "A results-stale badge appears the moment the re-minted blake3 identity diverges from the cached tensor hash; conditioning edits never stale"
    - "A MAC request against a mismatched tensor hash is refused with an honest reject banner, never silently served (SVC-06)"
  artifacts:
    - path: "web/src/panels/ConditioningPanel.tsx"
      provides: "per-source gain/delay + reused SpectrumEditor filter, live debounced recalc (D-10/11)"
      contains: "ConditioningPanel"
    - path: "web/src/store/conditioning.ts"
      provides: "conditioning state + debounced recondition MAC dispatch (WEB-05)"
      contains: "conditioning"
    - path: "web/src/store/stale.ts"
      provides: "re-mint blake3 identity on scene/terrain/ground/met edit → stale badge (D-12)"
      contains: "stale"
  key_links:
    - from: "web/src/store/conditioning.ts"
      to: "crates/envi-compute-wasm/src/recondition.rs"
      via: "debounced recondition MAC over the OPFS tensor; no re-propagation"
      pattern: "recondition"
    - from: "web/src/store/stale.ts"
      to: "crates/envi-compute-wasm/src/identity.rs"
      via: "re-mint tensor_hash vs cached manifest hash → stale/refuse"
      pattern: "tensor_hash|identity"
---

<objective>
Ship the flagship interactive conditioning fast-recalc (WEB-05, SVC-06): per-source Gain (dB) + Delay (ms) + a Filter control that REUSES the Phase-7 `SpectrumEditor` verbatim (D-11), driving a live debounced (~150 ms, D-10) recondition MAC (11-03) over the cached tensor — spectra + map update live with NO re-propagation. Plus the two honest-state guarantees: a results-stale badge that appears the moment the re-minted blake3 identity diverges from the cached tensor hash (D-12; conditioning edits never stale), and the SVC-06 409 refusal — a MAC against a mismatched hash is refused with an honest reject banner, never silently served (Open Q1 client-side realization).

Purpose: SC2. This is the concrete payoff of the complex-tensor architecture: adjust a source and watch the whole map + spectra update via the tensor MAC with no propagation re-run.
Output: `ConditioningPanel`, `store/conditioning`, `store/stale`, offline Playwright UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-results-fast-recalc/11-CONTEXT.md
@.planning/phases/11-results-fast-recalc/11-RESEARCH.md
@.planning/phases/11-results-fast-recalc/11-UI-SPEC.md
@web/src/spectrum/SpectrumEditor.tsx
@web/src/panels/RejectBanner.tsx
@web/src/store/sceneStore.ts
@.planning/phases/11-results-fast-recalc/11-03-SUMMARY.md
@.planning/phases/11-results-fast-recalc/11-05-SUMMARY.md
</context>

## Artifacts this phase produces

- `web/src/store/conditioning.ts` — per-source `ConditioningDto` state, `debouncedRecondition()` (~150 ms) dispatching the WASM MAC, live spectra+map update.
- `web/src/panels/ConditioningPanel.tsx` — per-source Gain/Delay numeric inputs + the reused `SpectrumEditor` filter, no button (live).
- `web/src/store/stale.ts` — `remintIdentity()` on scene/terrain/ground/met edit → `isStale` badge; conditioning edits excluded.
- `web/tests/e2e/conditioning.spec.ts` — offline Playwright UAT.

<tasks>

<task type="auto">
  <name>Task 1: Conditioning store + debounced recondition MAC dispatch (D-10/11, WEB-05)</name>
  <read_first>
    - web/src/spectrum/SpectrumEditor.tsx (the reused filter control: 1/1·1/3·1/12 entry → 105-pt interpolation → dense filter, D-11 — reuse verbatim, do not fork)
    - crates/envi-store/src/dto.rs (ConditioningDto{gain_db, delay_ms, filter_band_db} — the exact readout param)
    - crates/envi-compute-wasm/src/recondition.rs (the 11-03 hash-gated MAC boundary + HashMismatch)
    - web/src/store/results.ts (where reconditioned spectra land for the panel + map)
  </read_first>
  <action>Create `web/src/store/conditioning.ts` holding per-source `ConditioningDto` and a `debouncedRecondition()` (~150 ms, D-10) that dispatches the 11-03 WASM recondition MAC over the cached OPFS tensor and pushes the reconditioned spectra into the results store (spectra + isophone map update live, no re-propagation). The MAC runs in WASM (D-01) — TS only marshals the `ConditioningDto` (gain/delay/filter) and renders returned numbers. The filter value comes from the reused `SpectrumEditor` (D-11). Handle the returned `HashMismatch` → set the refuse state (Task 2).</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run test:unit -- conditioning</automated>
  </verify>
  <acceptance_criteria>
    - A unit test asserts a gain/filter/delay change debounces to exactly one recondition dispatch (~150 ms) and that no acoustic math runs in TS (`grep -c "Math.log10\|Math.pow" web/src/store/conditioning.ts` == 0).
    - The dispatched request reuses `ConditioningDto` (no forked shape).
  </acceptance_criteria>
  <done>Conditioning edits debounce into a single WASM MAC dispatch that updates spectra + map live.</done>
</task>

<task type="auto">
  <name>Task 2: Stale badge (re-mint identity) + 409 refusal banner (D-12, SVC-06)</name>
  <read_first>
    - web/src/store/sceneStore.ts (scene/terrain/ground/met edit events that must trigger a re-mint; conditioning edits must NOT)
    - crates/envi-compute-wasm/src/identity.rs (tensor_hash re-mint; conditioning excluded — Phase-6 D-07)
    - web/src/panels/RejectBanner.tsx (the existing honest-reject banner idiom to reuse)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (copy: "Out of date" badge, the 409 refuse copy; state matrix conditioning row)
  </read_first>
  <action>Create `web/src/store/stale.ts`: on any scene/terrain/ground/met edit, re-mint the blake3 tensor identity (via the WASM identity fn) and compare to the cached tensor's manifest hash — differ ⇒ set `isStale` (the `.chip.warn` "Out of date" badge, UI-SPEC copy). Conditioning edits change nothing in the identity ⇒ never stale (D-12). Wire the `HashMismatch` returned by the MAC (Task 1) to the reject banner with the UI-SPEC 409 copy ("Can't recondition — this result no longer matches the current scene. Recompute first.") — never silently serve (SVC-06). Create `ConditioningPanel.tsx` in the ResultsPanel slot: per-source Gain (dB numeric) + Delay (ms numeric) + the reused `SpectrumEditor` filter; live, no button; badge + reject banner surfaced; the badge never blocks edits (state matrix).</action>
  <verify>
    <automated>cd web && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - A scene/ground/met edit sets `isStale` (badge shown); a conditioning-only edit leaves `isStale` false (asserted by unit test + Playwright).
    - A mismatched-hash MAC surfaces the reject banner and yields no updated spectra (never silently served).
    - `npm run build` produces a fresh `web/dist`.
  </acceptance_criteria>
  <done>The stale badge appears on identity divergence, conditioning never stales, and a mismatched-hash MAC is honestly refused.</done>
</task>

<task type="auto">
  <name>Task 3: Offline Playwright conditioning UAT (WEB-05/SVC-06)</name>
  <read_first>
    - web/tests/e2e/spectrum.spec.ts (11-05 fixture-tensor + offline harness to reuse)
    - .planning/phases/11-results-fast-recalc/11-UI-SPEC.md (§State Matrix conditioning row — the observable behaviors)
  </read_first>
  <action>Create `web/tests/e2e/conditioning.spec.ts` on the real bundle, offline (COOP/COEP + `page.route` mocks, fixture tensor). Assert: adjusting a source's gain/filter/delay updates the spectrum + isophone map live via the MAC with NO propagation re-run (no solve worker message); a conditioning edit shows NO stale badge; a simulated scene edit (changing tensor identity) shows the "Out of date" badge; a MAC against a stale/mismatched hash surfaces the reject banner and does not update results. DevDependency-only; artifacts git-ignored.</action>
  <verify>
    <automated>cd web && npm run test:e2e -- conditioning</automated>
  </verify>
  <acceptance_criteria>
    - `conditioning.spec.ts` passes offline, asserting live MAC recalc with zero solve messages, conditioning-never-stale, stale-badge-on-scene-edit, and the honest 409 refusal.
    - No network egress.
  </acceptance_criteria>
  <done>An offline Playwright UAT proves the interactive MAC, stale badge, and 409 refusal (SC2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conditioning edit → WASM MAC | gain/filter/delay UI-controlled; validated in WASM (11-03) |
| scene identity → served result | a diverged/mismatched identity must surface stale/refuse, never serve silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07-01 | Spoofing | stale/409 handling | mitigate | re-mint identity on edit; badge on divergence; refuse mismatched MAC (SVC-06) — honest state (D-12) |
| T-11-07-02 | Integrity | conditioning recalc | mitigate | MAC runs in WASM (D-01); zero TS acoustic math (grep gate) |
| T-11-07-03 | Tampering | gain/delay/filter input | mitigate | dense `[105]` filter + finite gain/delay validated in the 11-03 WASM boundary (V5) |
</threat_model>

<verification>
- `cd web && npm run typecheck && npm run build && npm run test:e2e -- conditioning` green.
- grep gate: zero TS acoustic math in the new stores.
- `web/dist` rebuilt and committed.
</verification>

<success_criteria>
Adjusting a source's gain/filter/delay updates spectra + map live via the tensor MAC with no re-propagation; the stale badge appears on scene divergence (conditioning never stales); a mismatched-hash MAC is honestly refused (SC2, WEB-05, SVC-06).
</success_criteria>

<output>
Create `.planning/phases/11-results-fast-recalc/11-07-SUMMARY.md` when done.
</output>
