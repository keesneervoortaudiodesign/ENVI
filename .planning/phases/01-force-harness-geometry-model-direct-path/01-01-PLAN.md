---
phase: 01-force-harness-geometry-model-direct-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - .gitignore
  - refs/fetch.sh
  - cases/freefield_100m.toml
  - crates/envi-engine/Cargo.toml
  - crates/envi-engine/src/lib.rs
  - crates/envi-engine/src/freq.rs
  - crates/envi-harness/Cargo.toml
  - crates/envi-harness/src/lib.rs
  - crates/envi-harness/src/main.rs
  - crates/envi-harness/src/cases/mod.rs
  - crates/envi-harness/src/cases/toml.rs
  - crates/envi-harness/src/cases/xls.rs
  - crates/envi-harness/src/compare.rs
  - crates/envi-harness/src/capability.rs
  - crates/envi-harness/tests/force.rs
autonomous: true
requirements: [VAL-01]

must_haves:
  truths:
    - "Running `cargo test -p envi-harness --test force` discovers synthetic TOML cases and (when refs/ contains the workbooks) all FORCE .xls worksheets, and reports one outcome line per case"
    - "Every FORCE road case reports as ignored/Skipped with a named list of missing capabilities — the harness fails meaningfully before any propagation code exists"
    - "When refs/ is absent, .xls-backed cases degrade to Skipped(missing reference data) and the suite still exits green"
    - "The comparator produces per-band signed deviations against a 27-band reference spectrum with the FORCE 1 dB tolerance and the dip-shift allowance encoded"
  artifacts:
    - path: "Cargo.toml"
      provides: "cargo workspace with members crates/*"
      contains: "[workspace]"
    - path: "crates/envi-engine/src/freq.rs"
      provides: "105-point 1/12-octave FreqAxis, BandIdx, third_octave_pick"
      min_lines: 40
    - path: "crates/envi-harness/src/cases/xls.rs"
      provides: "calamine FORCE .xls loader, label-anchored"
      min_lines: 60
    - path: "crates/envi-harness/src/compare.rs"
      provides: "per-band deviation comparator + A-weighting"
      min_lines: 40
    - path: "crates/envi-harness/tests/force.rs"
      provides: "libtest-mimic dynamic test runner (harness = false)"
      min_lines: 20
    - path: "cases/freefield_100m.toml"
      provides: "synthetic free-field case fixture"
    - path: "refs/fetch.sh"
      provides: "download script for the 4 FORCE workbooks + AV reports with SHA-256 pinning"
  key_links:
    - from: "crates/envi-harness/tests/force.rs"
      to: "crates/envi-harness/src/cases/mod.rs"
      via: "envi_harness::cases::discover() feeding libtest_mimic::Trial per case"
      pattern: "cases::discover"
    - from: "crates/envi-harness/src/compare.rs"
      to: "crates/envi-engine/src/freq.rs"
      via: "third_octave_pick band mapping for 27-band comparison"
      pattern: "third_octave_pick"
    - from: "crates/envi-harness/src/cases/xls.rs"
      to: "crates/envi-harness/src/cases/mod.rs"
      via: "xls loader emits the shared CaseDefinition"
      pattern: "CaseDefinition"
---

<objective>
Stand up the Walking Skeleton's spine: a two-crate cargo workspace (`envi-engine` pure math, `envi-harness` all I/O) whose FORCE-driven test harness loads real test-case files and reports per-case outcomes — before any propagation code exists (VAL-01, ROADMAP success criterion 1).

Purpose: The FORCE suite is the acceptance gate for the whole engine. Building the harness first makes "validated against FORCE" an architectural property: every later physics plan flips a capability flag and watches cases go green. This plan ends with `cargo test` green, all 62 straight-road cases visibly ignored with named missing capabilities, and the free-field synthetic case Skipped pending plan 01-03.

Output: cargo workspace, `envi-engine::freq` (the 105-point 1/12-octave vocabulary), `envi-harness` case loaders (.xls via calamine + TOML via serde), comparator with FORCE tolerances, capability gating, libtest-mimic runner, CLI report bin, refs fetch script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-force-harness-geometry-model-direct-path/01-RESEARCH.md
</context>

## Phase Goal

**As a** Nord2000 engine developer, **I want to** run `cargo test` and see the FORCE test cases load, execute, and report per-band pass/fail/skip against reference values, **so that** every piece of propagation physics I add is validated the moment it lands.

(Derived from the ROADMAP Phase 1 Goal; the "user" of a computation engine is the developer driving `cargo test`.)

## Artifacts this plan produces

| Artifact | Kind | Public surface |
|----------|------|----------------|
| workspace `Cargo.toml` | build | members = `crates/*`, resolver 3 |
| crate `envi-engine` | lib | `freq::{FreqAxis, BandIdx, N_BANDS, G, NOMINAL_THIRD_OCT}`, `FreqAxis::new()`, `FreqAxis::third_octave_pick(usize) -> f64` |
| crate `envi-harness` | lib + bin | `cases::{discover, CaseDefinition, CaseKind, ReferenceVersion}`, `cases::toml::load_toml_case`, `cases::xls::load_straight_road`, `compare::{compare_spectrum, BandDeviation, a_weighting_db}`, `capability::{Capability, required_capabilities}`, `run_case(&CaseDefinition) -> Outcome`, `Outcome::{Pass, Fail, Skipped}` |
| `cases/freefield_100m.toml` | fixture | synthetic free-field case (committed — our own content) |
| `refs/fetch.sh` | script | curls the 4 FORCE workbooks + AV reports into git-ignored `refs/`, verifies SHA-256 |
| `crates/envi-harness/tests/force.rs` | test target | `harness = false`, one libtest-mimic Trial per discovered case |
| bin `envi-harness` | CLI | `cargo run -p envi-harness -- report` prints per-case outcome table |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold workspace + 1/12-octave frequency axis + failing end-to-end harness test</name>
  <files>Cargo.toml, .gitignore, refs/fetch.sh, cases/freefield_100m.toml, crates/envi-engine/Cargo.toml, crates/envi-engine/src/lib.rs, crates/envi-engine/src/freq.rs, crates/envi-harness/Cargo.toml, crates/envi-harness/src/lib.rs, crates/envi-harness/src/cases/mod.rs, crates/envi-harness/tests/force.rs</files>
  <read_first>
    - .planning/phases/01-force-harness-geometry-model-direct-path/01-RESEARCH.md — "Standard Stack" (exact crate versions + install commands), "Pattern 1: Fixed frequency axis", "Recommended Project Structure", "FORCE Test Suite" table of URLs, "Recommended harness input schema" (TOML shape)
  </read_first>
  <behavior>
    - Test 1 (engine unit): FreqAxis::new() yields 105 centres; centres[0] ≈ 25.118864 Hz (rel 1e-6), centres[64] == 1000.0 (rel 1e-12), centres[104] ≈ 10000.0 (rel 1e-12)
    - Test 2 (engine unit): third_octave_pick(16) == 1000.0 and third_octave_pick(i) == centres[i*4] for all i in 0..27
    - Test 3 (e2e RED): `cargo test -p envi-harness --test force` FAILS with a message containing "no cases discovered" — the harness runs end-to-end but discovery is a stub returning an empty Vec
  </behavior>
  <action>
    Create the cargo workspace exactly per RESEARCH "Recommended Project Structure": root Cargo.toml with [workspace] members = ["crates/*"], resolver = "3". Create lib crate envi-engine and lib+bin crate envi-harness (main.rs may be a stub printing "envi-harness report: no cases" for now).

    Dependencies (pin per RESEARCH "Installation", versions verified on crates.io 2026-07-07): envi-engine gets ndarray 0.17, num-complex 0.4, thiserror 2, dev-dep approx 0.5. envi-harness gets calamine 0.36, serde 1 (derive), toml, anyhow, libtest-mimic 0.8, dev-dep approx 0.5, and a path dependency on envi-engine. Boundary rule (RESEARCH "Architectural Responsibility Map"): envi-engine carries zero I/O dependencies — all file parsing lives in envi-harness. This is what makes "harness before propagation code" architectural.

    envi-engine/src/freq.rs per RESEARCH Pattern 1: pub const N_BANDS: usize = 105; pub const G: f64 = 1.9952623149688795 (10^0.3); struct FreqAxis { centres: [f64; N_BANDS] } built as 1000.0 * G.powf(x/12.0) for x = -64..=40; newtype BandIdx(pub usize); fn third_octave_pick(&self, third_idx: usize) -> f64 returning centres[third_idx * 4] with debug_assert third_idx < 27; a NOMINAL_THIRD_OCT: [f64; 27] label table (25, 31.5, 40, ... 10000) for display only; expose a std::sync::LazyLock<FreqAxis> singleton. Doc-comment the deliberate deviation from IEC 61260-1 even-b midbands (the x/12 rule keeps all 27 exact 1/3-octave centres on the grid — this is an evaluation grid, not a filter bank) and the pitfall: never compare nominal frequencies as floats; index by BandIdx.

    envi-harness/src/cases/mod.rs: define the shared internal representation now so Task 2 fills it in — pub struct CaseDefinition with fields: id: String, name: String, kind: CaseKind (enum: FreeField, Geometry, ForceStraightRoad — extensible), reference_version: ReferenceVersion (enum: Analytic, Force2009, Force2010 — Pitfall 6: provenance travels with every case), description: String, propagation params struct (hr_m, t0_c, z0_m, zu_m, u_ms, phi_deg, su_ms, dtdz, sdtdz, cv2, ct2 — all Option<f64>; rh_percent defaulting to 70.0 per the report text), raw terrain profile rows Vec<(x, z, flow_resistivity, roughness)>, reference_spectrum: Option of 27-band table (freq label, leq, le, dl) plus overall (laeq24h, lae, lamax), and synthetic expected block (tolerance_db, expected-kind string). pub fn discover(refs_dir: &Path, cases_dir: &Path) -> Vec<CaseDefinition> — STUB returning an empty Vec in this task (Task 2 implements it).

    envi-harness/tests/force.rs per RESEARCH "Dynamic FORCE tests": set [[test]] name = "force", harness = false in envi-harness/Cargo.toml; main() uses libtest_mimic::Arguments, calls cases::discover("refs/", "cases/"), and if the result is empty pushes a single failing Trial named "harness::discovery" whose error message contains "no cases discovered". This is the phase's failing end-to-end test (MVP RED): the binary compiles, runs, and fails for the right reason.

    cases/freefield_100m.toml: copy the schema from RESEARCH "Recommended harness input schema" verbatim (source at [0,0,0.5], receiver at [100,0,1.5], 15 °C / 70 % / 101.325 kPa, kind = "free-field", reference = "analytic", tolerance_db = 1e-9, bands = "analytic:divergence+iso9613").

    refs/fetch.sh + .gitignore: .gitignore must contain refs/ (plus target/) from this first commit — Pitfall 7, the FORCE .xls and AV PDFs are copyrighted and must never land in git. fetch.sh curls the four workbooks from the mst.dk URLs in the RESEARCH "FORCE Test Suite" table (TestStraightRoad.xls, TestCurvedRoad.xls, TestCityStreet.xls, TestYearlyAverage.xls) and the AV 1106/07 PDF via its Wayback URL, then records/verifies SHA-256 sums in a committed refs.sha256 manifest (script computes sums on first fetch and verifies on refetch). The script must be idempotent and fail soft (warn, exit 0) when offline — the harness degrades to Skipped without refs.

    Run refs/fetch.sh once during this task (network verified available in RESEARCH "Environment Availability"); if download fails, proceed — nothing downstream hard-requires the files.
  </action>
  <verify>
    <automated>cargo test -p envi-engine && cargo build --workspace && (cargo test -p envi-harness --test force; test $? -ne 0)</automated>
  </verify>
  <done>Workspace builds; envi-engine freq unit tests pass (105 centres, exact 1000 Hz at index 64, exact 10 kHz at index 104, third_octave_pick identity); the force test target compiles and exits NONZERO with "no cases discovered" — the failing end-to-end test exists before any loader or physics.</done>
  <acceptance_criteria>
    - `cargo build --workspace` succeeds on stable Rust (toolchain 1.96)
    - `cargo test -p envi-engine` passes, including: centres[64] == 1000.0 exactly (assert_relative_eq epsilon 1e-12), centres[0] ≈ 25.118864 (max_relative 1e-6), centres[104] ≈ 10000.0 (max_relative 1e-12)
    - `cargo test -p envi-harness --test force` exits nonzero and its output contains the string "no cases discovered"
    - `cargo tree -p envi-engine -e normal --depth 1` lists only ndarray, num-complex and thiserror as direct dependencies (I/O quarantine holds)
    - `.gitignore` contains a `refs/` entry; `git status` shows no .xls or .pdf files staged
    - `refs/fetch.sh` exists, is idempotent, and references all four workbook URLs plus SHA-256 verification
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Case loaders — TOML synthetic cases + FORCE .xls via calamine</name>
  <files>crates/envi-harness/src/cases/mod.rs, crates/envi-harness/src/cases/toml.rs, crates/envi-harness/src/cases/xls.rs, crates/envi-harness/src/lib.rs</files>
  <read_first>
    - .planning/phases/01-force-harness-geometry-model-direct-path/01-RESEARCH.md — "Per-worksheet cell layout" (the verified FORCE grid), "FORCE .xls loading" code example (label-anchored parsing note), Pitfalls 1, 5, 6, "Security Domain" V5 row
  </read_first>
  <behavior>
    - Test 1: load_toml_case("cases/freefield_100m.toml") yields CaseDefinition with kind FreeField, reference_version Analytic, source position [0,0,0.5], receiver [100,0,1.5], t_air 15.0, rh 70.0, tolerance 1e-9
    - Test 2 (ignored when refs/ absent): load_straight_road("refs/TestStraightRoad.xls") yields exactly 62 cases; case "1" description contains "Flat terrain"; its LAeq,24h reference equals 39.39836757521 within 1e-9 abs (full-precision cell, NOT the rounded report value — Pitfall 1); its terrain profile's first X is 3.25 and rows are strictly ascending in X
    - Test 3: loader rejects (typed error, no panic) a synthetic worksheet/TOML with NaN cell values, non-ascending profile X, or > 10_000 profile rows
  </behavior>
  <action>
    Implement cases::toml with serde derive structs mirroring the RESEARCH TOML schema; map into CaseDefinition. Unknown `kind` strings must produce a typed error listing accepted kinds.

    Implement cases::xls with calamine 0.36 current API (Xls, open_workbook, worksheet_range returning Result, Data enum — RESEARCH "State of the Art" warns pre-0.30 examples do not compile). Parse every sheet ("1".."62") of TestStraightRoad.xls using LABEL-ANCHORED parsing as the primary key: scan column A for the label cells ("hr", "t0", "z0", "zu", "u", and the Greek phi label), column F for result labels ("LAeq", "LAE", "LAmax", "Freq."), and read the value from the adjacent column; the fixed row positions from RESEARCH are the fallback with a warning. This survives the 2010-revision layout drift. Read the description string from row 2; read the 27-row spectrum table (freq, Leq,24h, LE, dL) below the "Freq." label; read terrain profile columns A–D (X, Z, flow resistivity kNs·m⁻⁴, roughness m) from below the profile header until a zero-padded/empty row terminates it. Tag every loaded case reference_version = Force2009.

    Input-validation posture (ASVS V5, RESEARCH Security Domain): the .xls is untrusted input. No unwrap/expect on cell data — every extraction returns Result with thiserror-style variants in the harness (anyhow context at the bin boundary only). Reject NaN/Inf numerics where a finite value is required, enforce strictly ascending profile X, cap profile rows at 10_000 and sheets at 200, and bound the spectrum table to exactly 27 rows. Malformed sheets yield a per-case load error that discovery converts into a Failed-to-load outcome rather than aborting the whole run.

    Implement cases::discover(refs_dir, cases_dir) for real: glob cases_dir for *.toml (sorted, deterministic order) and, if refs_dir/TestStraightRoad.xls exists, load its 62 cases; if the file is missing, emit NO xls cases and record a discovery note (the runner in Task 3 surfaces "reference workbooks not fetched — run refs/fetch.sh") — the suite must stay green without refs (downstream consumers may not have the copyrighted files). Discovery must only read inside the two given directories — canonicalize and verify each path is under the expected root before opening (path-traversal guard T-01-02). Curved/CityStreet/YearlyAverage workbooks: discover and count them if present, but loading their layouts is out of scope until Phases 3–4 — represent each as a single Skipped placeholder case per workbook.

    For Test 3's malformed-input coverage, generate the bad TOML inline in tests; for the xls reject-path, unit-test the row-validation functions directly (constructing a bad workbook binary is not required).
  </action>
  <verify>
    <automated>cargo test -p envi-harness cases</automated>
  </verify>
  <done>TOML and .xls loaders produce CaseDefinition; case 1's full-precision LAeq anchor and profile semantics verified when refs present (test auto-ignored when absent); malformed input yields typed errors, never panics; discovery is deterministic and confined to refs/ + cases/.</done>
  <acceptance_criteria>
    - `cargo test -p envi-harness cases` passes
    - With refs fetched: test asserting 62 sheets and LAeq,24h == 39.39836757521 (abs 1e-9) for case "1" passes; without refs it reports as ignored, not failed
    - Validation unit tests prove: NaN cell → Err, non-ascending X → Err, row cap enforced → Err (no panic paths; `grep -rn "unwrap()" crates/envi-harness/src/cases/` shows no unwrap on cell/file data — test-only unwraps allowed in #[cfg(test)] blocks)
    - Every CaseDefinition carries a reference_version; xls-loaded cases are Force2009
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Comparator, capability gating, and the meaningful-failure test runner (GREEN)</name>
  <files>crates/envi-harness/src/compare.rs, crates/envi-harness/src/capability.rs, crates/envi-harness/src/lib.rs, crates/envi-harness/src/main.rs, crates/envi-harness/tests/force.rs</files>
  <read_first>
    - .planning/phases/01-force-harness-geometry-model-direct-path/01-RESEARCH.md — "Acceptance tolerances" + "Harness design consequence", "Per-band tolerance comparison" and "Dynamic FORCE tests" code examples, "Verified Physics" §5 (A-weighting formula + anchors), Pitfall 2
  </read_first>
  <behavior>
    - Test 1: compare_spectrum against itself → all deviations 0.0, pass = true; against a copy with one band shifted +1.5 dB → pass = false and that band's dev_db == 1.5
    - Test 2: a_weighting_db anchors — |A(1000.0)| < 0.001, A(100.0) == −19.145 ± 0.01, A(8000.0) == −1.147 ± 0.01
    - Test 3: required_capabilities for a ForceStraightRoad case includes at least Capability::EmissionModel and Capability::GroundEffect; for the free-field TOML case it is exactly {Capability::FreeField}
    - Test 4 (e2e GREEN): `cargo test -p envi-harness --test force` now exits 0; output lists the free-field case and (when refs present) 62 straight-road cases, all as ignored with their requires-list visible
  </behavior>
  <action>
    compare.rs: implement BandDeviation {band, nominal_hz, got_db, want_db, dev_db} and compare_spectrum(got, want, tol_db) -> (Vec<BandDeviation>, bool) per the RESEARCH example, extended with: max |deviation| and overall-level deviation in a ComparisonReport struct, the FORCE tolerance constants (1.0 dB overall, 1.0 dB per 1/3-octave band, per Env. Project 1335 §6), and the ground-dip allowance as an explicit optional rule (a band failure is downgraded to a warning if shifting the got-spectrum by ±1 band index brings that band within tolerance — encode it, document it, default ON for ForceStraightRoad kinds only). ComparisonReport::render_table() returns a human-readable per-band table used by both the test error path and the CLI. Implement a_weighting_db(f) from the IEC 61672-1 formula in RESEARCH §5 (R_A ratio then 20·log10 + 2.00) — needed for LAeq-style totals from Phase 4 on; 10 lines now, anchored by tests. Band mapping between the engine's 105-point grid and the 27-band FORCE spectrum goes through FreqAxis::third_octave_pick — by INDEX, never by float equality on nominal frequencies (Pitfall 3).

    capability.rs: enum Capability {FreeField, Geometry, EmissionModel, GroundEffect, Diffraction, Refraction, ...}; fn required_capabilities(&CaseDefinition) -> BTreeSet<Capability> — FORCE straight-road cases require EmissionModel + GroundEffect at minimum (screen groups also Diffraction; wind/temperature-gradient variants also Refraction — derive from the case description string and propagation params: nonzero u or dt/dz implies Refraction; description mentioning screens implies Diffraction). fn implemented_capabilities() -> BTreeSet<Capability> returns the currently-built set: EMPTY in this plan. Plans 01-02/01-03 and Phases 2–3 extend it — no harness rewrite (Pitfall 2: FORCE road cases are NOT a Phase 1 numeric gate).

    lib.rs run_case(&CaseDefinition) -> Outcome where Outcome is Pass | Fail(ComparisonReport) | Skipped(String): if required − implemented is non-empty → Skipped("requires: emission-model, ground-effect, ..."); otherwise dispatch by kind (every dispatch arm in this plan returns Skipped since implemented is empty — plan 01-02 adds Geometry, plan 01-03 adds FreeField).

    tests/force.rs: replace the RED stub — for each discovered case build a libtest_mimic Trial named "{workbook-or-toml}::{id}"; Skipped cases get .with_ignored_flag(true) and the requires-list appended to the trial kind/name so `cargo test` output shows WHY each case is ignored; Fail returns Err(report.render_table()); Pass returns Ok. Keep a real (non-ignored) meta-test Trial "harness::discovery" asserting at least one case was discovered — the Task 1 RED test flips GREEN here.

    main.rs: `cargo run -p envi-harness -- report` runs discovery + run_case over everything and prints a fixed-width table: case id | kind | reference_version | outcome | detail (requires-list or max deviation). This is the walking skeleton's human-readable "write" end.
  </action>
  <verify>
    <automated>cargo test --workspace && cargo run -p envi-harness -- report</automated>
  </verify>
  <done>`cargo test --workspace` is green; every FORCE case is visibly ignored with named missing capabilities; free-field case shows Skipped(requires: free-field); the CLI report renders the full outcome table. Success criterion 1 of the phase is satisfied: the harness runs — and fails meaningfully — before propagation code exists.</done>
  <acceptance_criteria>
    - `cargo test --workspace` exits 0
    - `cargo test -p envi-harness --test force` output contains "harness::discovery" as passed and (refs present) 62 ignored straight-road trials whose displayed name/kind includes a requires-list such as "requires: emission-model, ground-effect"
    - `cargo run -p envi-harness -- report | grep -c "Skipped"` ≥ 1 (free-field case present even without refs)
    - A-weighting anchors pass: |A(1000)| < 0.001 dB, A(100) = −19.145 ± 0.01, A(8000) = −1.147 ± 0.01
    - compare_spectrum tolerance constants are 1.0 dB (overall) and 1.0 dB (per band), sourced in a doc comment to Env. Project 1335 §6
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → cases::xls / cases::toml | Untrusted FORCE .xls (legacy BIFF) and TOML case files are parsed; a corrupt or malicious file crosses here |
| network → refs/fetch.sh | Reference artifacts downloaded from mst.dk / web.archive.org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering/DoS | cases::xls, cases::toml parsers | mitigate | calamine (pure Rust, memory-safe) + label-anchored parsing; Result on every cell read, no unwrap on data; caps: ≤200 sheets, ≤10,000 profile rows, exactly 27 spectrum rows; reject NaN/Inf/non-ascending X (Task 2 tests) |
| T-01-02 | Information Disclosure (path traversal) | cases::discover | mitigate | discovery canonicalizes paths and refuses anything outside refs/ and cases/ roots; no user-controlled path joins |
| T-01-03 | Tampering (reference integrity) | refs/fetch.sh downloads | mitigate | SHA-256 manifest (refs.sha256) committed; fetch verifies on every run; reference_version provenance on every case |
| T-01-04 | Repudiation/compliance | git history vs copyrighted refs | mitigate | refs/ git-ignored from first commit; acceptance criterion checks no .xls/.pdf staged |
| T-01-SC | Tampering | cargo installs (9 crates) | accept | RESEARCH Package Legitimacy Audit ran on all 9 crates — all Approved, zero [ASSUMED]/[SUS]/[SLOP]; crates.io has no postinstall mechanism; optional cargo-deny in CI later |
</threat_model>

<verification>
- `cargo build --workspace` and `cargo test --workspace` green on stable Rust 1.96
- `cargo test -p envi-harness --test force` lists one trial per case; FORCE cases ignored with requires-list; discovery meta-test passes
- `cargo tree -p envi-engine -e normal --depth 1` → only ndarray, num-complex, thiserror (I/O quarantine)
- No copyrighted artifact tracked by git (`git ls-files | grep -iE '\.(xls|pdf)$'` returns nothing)
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion 1 holds: the test suite loads FORCE road-traffic case files, executes what exists, and reports per-case outcomes with named missing capabilities — before propagation code exists
- VAL-01 satisfied: harness architecture (loader → CaseDefinition → capability gate → comparator → report) is complete and extensible by flipping capability flags
- Workspace, frequency axis, and comparator are in place for plans 01-02/01-03 to build on without structural change
</success_criteria>

<output>
Create `.planning/phases/01-force-harness-geometry-model-direct-path/01-01-SUMMARY.md` when done. Record: exact public API of cases::CaseDefinition, capability::Capability and run_case/Outcome (plan 01-02/01-03 wire into these), whether refs were successfully fetched, and any deviation from the verified .xls layout encountered.
</output>
