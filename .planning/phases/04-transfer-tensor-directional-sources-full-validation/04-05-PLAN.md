---
phase: 04-transfer-tensor-directional-sources-full-validation
plan: 05
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - tools/noisemodelling_oracle/README.md
  - tools/noisemodelling_oracle/scenes/free_field.geojson
  - tools/noisemodelling_oracle/scenes/thin_barrier.geojson
  - tools/noisemodelling_oracle/scenes/soft_ground.geojson
  - crates/envi-harness/tests/fixtures/oracle/noisemodelling.toml
  - crates/envi-harness/tests/oracle_noisemodelling.rs
autonomous: true
requirements: [VAL-03]
must_haves:
  truths:
    - "ENVI's geometrical divergence equals NoiseModelling's CNOSSOS Adiv (20·lg d + 11 ≡ −10·lg(4πR²)) within ≤0.1 dB, compared BY BAND INDEX at the 8 octave centres (grid indices 16,28,…,100)"
    - "ENVI's ISO 9613-1 air absorption equals NoiseModelling's within ≤0.2 dB per octave point at 15 °C / 70 % RH; a larger 8 kHz delta is DOCUMENTED (band-averaging vs centre value), not forced"
    - "Barrier insertion loss and flat-ground excess attenuation are REPORT-ONLY expected-delta tables (Hadden-Pierce wedge vs CNOSSOS empirical; complex Q̂ dips vs empirical G-model) — equality would indicate a comparison bug, not success"
    - "The comparison runs fully offline from committed TOML fixtures — no Java, no network, no credentials at test time (mirrors the scipy-oracle pattern)"
    - "The fixture carries provenance: NoiseModelling version (v6.0.0), scene hash, generation date, and a documented human-run recipe"
  artifacts:
    - path: crates/envi-harness/tests/oracle_noisemodelling.rs
      provides: "band-index equality gates (divergence, air absorption) + report-only delta tables (barrier, ground)"
      contains: "band_index"
      min_lines: 80
    - path: crates/envi-harness/tests/fixtures/oracle/noisemodelling.toml
      provides: "committed NoiseModelling CNOSSOS outputs with provenance meta"
    - path: tools/noisemodelling_oracle/README.md
      provides: "the human-run recipe (one-time Java) + scene definitions + fixture-generation procedure"
  key_links:
    - from: crates/envi-harness/tests/oracle_noisemodelling.rs
      to: crates/envi-engine/src/propagation/mod.rs
      via: "compares direct_path divergence + ISO 9613-1 air absorption against the committed CNOSSOS fixture at octave band indices"
      pattern: "direct_path|band_index"
  prohibitions:
    - "NEVER force-close / kill / restart the JVM, Gradle, Maven, VS Code, or any language-server process — NoiseModelling fixture generation runs a JVM and this has crashed the editor twice; let Java processes finish on their own"
    - "NO porting or translation of NoiseModelling (GPLv3) source — run the binary and commit its numeric OUTPUTS only; ideas, not code"
    - "NO equality gate on barrier insertion loss or ground excess attenuation — those are documented-expected-delta reports (different models)"
    - "NO comparison by nominal frequency — CNOSSOS octaves map to fixed 1/12-octave grid indices 16+12k; compare by index"
    - "NO Java / network / credential dependency at test time — fixtures are committed; regeneration is operator-driven and one-time"
---

<objective>
Cross-validate ENVI's shared sub-effects against NoiseModelling's CNOSSOS output (VAL-03) via the project's committed-fixture oracle pattern — the same posture as the scipy oracles (no Java at test time).

**Phase-4 slice (MVP framing):** after this plan an independent external implementation (NoiseModelling v6, CNOSSOS-EU) confirms that the sub-effects ENVI shares with the wider acoustics world — geometrical divergence and ISO 9613-1 air absorption — agree to the decimal, while the genuinely-different models (Hadden-Pierce wedge, complex-impedance ground) have their differences documented rather than papered over.

Purpose: VAL-03 is a milestone-acceptance cross-check. It is parallel-safe with 04-02/04-03/04-04 because it touches only fixtures + a comparison test and reuses the already-shipped Phase-1 divergence + air absorption. It never needs the emission model or the hard propagation.
Output: `tools/noisemodelling_oracle/` (3 minimal scenes + a documented human-run recipe), a committed `noisemodelling.toml` fixture with provenance, and `tests/oracle_noisemodelling.rs` with equality gates for divergence/air-absorption and report-only delta tables for barrier/ground.

**Environment rule (CRITICAL — repeated because it has crashed the editor twice):** fixture generation runs a JVM. NEVER force-close/kill/restart Java, Gradle, Maven, the language server, or VS Code. Java is not installed on this machine — the recipe documents a one-time Temurin JRE install (or generation on another machine); either way the committed fixtures mean tests never need Java.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-RESEARCH.md
@.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-PATTERNS.md
@.planning/research/OPEN-GIS-LANDSCAPE.md
@crates/envi-engine/src/propagation/mod.rs
@crates/envi-engine/src/freq.rs
@crates/envi-harness/tests/oracle_refraction.rs
@tools/nord2000_oracle/gen_ground_fixtures.py
@tools/nord2000_oracle/common.py
</context>

<artifacts_this_phase_produces>
- `tools/noisemodelling_oracle/README.md` + `scenes/{free_field,thin_barrier,soft_ground}.geojson` — the human-run recipe + minimal scenes
- `crates/envi-harness/tests/fixtures/oracle/noisemodelling.toml` — committed CNOSSOS outputs + provenance
- `crates/envi-harness/tests/oracle_noisemodelling.rs` — equality gates + expected-delta report
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: NoiseModelling oracle scenes + human-run recipe + committed fixture</name>
  <files>tools/noisemodelling_oracle/README.md, tools/noisemodelling_oracle/scenes/free_field.geojson, tools/noisemodelling_oracle/scenes/thin_barrier.geojson, tools/noisemodelling_oracle/scenes/soft_ground.geojson, crates/envi-harness/tests/fixtures/oracle/noisemodelling.toml</files>
  <read_first>
    - .planning/research/OPEN-GIS-LANDSCAPE.md (NoiseModelling v6.0.0 provenance, GPL posture, offline WPS/standalone run modes)
    - tools/nord2000_oracle/common.py 8-12 (licensing note — cite equations by report/number, no document text) + gen_ground_fixtures.py 1-10 / 60-83 (docstring: regeneration operator-driven, TOML committed, Python NOT a build dep; [meta] block with oracle/provenance sha256/justified tolerances)
    - 04-RESEARCH.md "NoiseModelling Cross-Validation" section (scene defs, comparable-quantities table, expected-delta posture, offline/no-credential)
    - 04-PATTERNS.md "oracle_noisemodelling.rs + tools/noisemodelling_oracle/" section
  </read_first>
  <action>
    Create `tools/noisemodelling_oracle/` with three minimal scenes as GeoJSON data: (a) free field — point source, flat HARD ground (G=0), receiver at 100 m, no barrier; (b) same + a thin barrier (documented height/position); (c) soft ground (G=1) flat case for the report-only ground comparison. Standard atmosphere 15 °C / 70 % RH to match FORCE parameters.
    Write `README.md` as the documented human-run recipe: one-time Temurin JRE install (winget) OR generation on another machine; the NoiseModelling v6.0.0 WPS/standalone invocation; how to export per-octave-band receiver levels and the separately-reported CNOSSOS attenuation terms (Adiv, Aatm, Abar, Aground); how to record NM version + scene hash + date. State prominently and first: NEVER force-close the JVM/Gradle/Maven/language-server/VS Code processes — let Java finish on its own.
    Run the recipe once (JRE permitting) and commit `crates/envi-harness/tests/fixtures/oracle/noisemodelling.toml` with a `[meta]` block (oracle = "NoiseModelling v6.0.0 CNOSSOS-EU", provenance sha256 + scene hash + date + generator = human-run recipe) and per-band rows carrying `band_index`. If Java cannot be installed this session, commit the fixture with clearly-marked placeholder rows + a TODO in the recipe, and make the test Skip on placeholder rows (fail-soft, never a false Pass) — the operator regenerates when Java is available.
    License posture: run the GPL binary, commit its numeric outputs; never port its source.
  </action>
  <verify>
    <automated>test -f tools/noisemodelling_oracle/README.md && test -f crates/envi-harness/tests/fixtures/oracle/noisemodelling.toml && grep -q 'band_index' crates/envi-harness/tests/fixtures/oracle/noisemodelling.toml && grep -qi 'never force-close\|do not kill\|never kill' tools/noisemodelling_oracle/README.md && echo ORACLE_ASSETS_OK</automated>
  </verify>
  <done>Three minimal scenes, a JVM-safe human-run recipe, and a committed provenance-tagged CNOSSOS fixture exist (real values, or clearly-marked placeholders that force a Skip).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Band-index equality gates + expected-delta report against the committed fixture</name>
  <files>crates/envi-harness/tests/oracle_noisemodelling.rs</files>
  <read_first>
    - crates/envi-harness/tests/oracle_refraction.rs (copy whole structure: Deserialize fixture structs + Meta 17-79; load() via concat! 81-88; band-index comparison 144-160; coverage assertions 131-134/192-196; abs-vs-rel tolerance near zero 90-92/108-113)
    - crates/envi-engine/src/propagation/mod.rs (direct_path — divergence + ISO 9613-1 air absorption already shipped in Phase 1)
    - crates/envi-engine/src/freq.rs (FREQ_AXIS centres; CNOSSOS octaves = grid indices 16+12k from 63.096 Hz)
    - 04-RESEARCH.md "Comparable quantities and the delta posture" table
  </read_first>
  <behavior>
    - Test (divergence equality): ENVI's divergence at a receiver equals the fixture's Adiv within ≤0.1 dB at each of the 8 octave band indices (16,28,40,52,64,76,88,100); coverage assertion ensures all 8 are checked (a truncated fixture cannot silently pass).
    - Test (air-absorption equality): ENVI's ISO 9613-1 band attenuation equals the fixture's Aatm within ≤0.2 dB per octave point; a larger 8 kHz delta is asserted-and-DOCUMENTED (band-averaging vs centre value), not a hard failure.
    - Test (report-only): barrier insertion loss and ground excess attenuation deltas are printed as an expected-delta table (magnitude + trend), with NO equality assertion (different models — equality would be a bug).
    - Test (offline): the test reads only the committed TOML; no Java, no network. Placeholder fixture rows Skip cleanly (fail-soft), never a false Pass.
  </behavior>
  <action>
    Create `crates/envi-harness/tests/oracle_noisemodelling.rs` copying `oracle_refraction.rs` whole: `#[derive(Deserialize)]` fixture structs incl. `Meta` with named tolerances; `load()` via `concat!(env!("CARGO_MANIFEST_DIR"), "/tests/fixtures/oracle/noisemodelling.toml")`; band-index comparison evaluating ENVI at `axis.centres[row.band_index]`; coverage assertions; abs-vs-rel tolerance switch near zero.
    Equality gates ONLY for divergence (≤0.1 dB) and air absorption (≤0.2 dB/octave). Barrier and ground rows are report-only expected-delta tables (eprintln the deltas + trend; assert nothing). Compare strictly by band index for the 8 CNOSSOS octave centres (indices 16,28,40,52,64,76,88,100) — never by nominal Hz. If fixture rows are placeholders (from Task 1), Skip with an eprintln pointer to the regeneration recipe.
  </action>
  <acceptance_criteria>
    - Divergence equality ≤0.1 dB and air-absorption equality ≤0.2 dB pass at all 8 octave indices (or Skip on placeholder rows).
    - Coverage assertions present (no silent truncated-fixture pass).
    - Barrier/ground are report-only (no equality assertion).
    - Test runs offline (no Java/network); `cargo test --workspace` green; clippy `-D warnings` + fmt clean.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-harness --test oracle_noisemodelling && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>Divergence and air-absorption equality gates pass by band index against the committed CNOSSOS fixture (offline), and barrier/ground deltas are documented, not gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NoiseModelling fixture TOML → test | committed external-tool output; a corrupt/truncated fixture must Skip or fail loudly, never silently pass |
| GeoJSON scene files → NoiseModelling (offline, generation-time) | operator-run only; not a test-time input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | truncated / corrupt fixture silently passing | mitigate | coverage assertions (all 8 octave indices checked); provenance sha256 + scene hash in [meta]; placeholder rows force a Skip, never a Pass |
| T-04-05-02 | Denial of Service | killing the JVM crashes the editor | mitigate | recipe states first-and-prominently: NEVER force-close JVM/Gradle/Maven/LSP/VS Code; a verify grep asserts the warning is present in the README |
| T-04-05-03 | Repudiation | equality forced on incomparable models | mitigate | barrier/ground are report-only expected-delta tables; only genuinely-identical physics (divergence, ISO 9613-1) is gated |
| T-04-05-04 | Tampering | GPL source translated into the tree | mitigate | run the binary + commit outputs only; licensing note cites by report/number; no NoiseModelling code enters the repo |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new packages; NoiseModelling is an external one-time operator tool, not a workspace dependency; no Java/network at test time |
</threat_model>

<verification>
- `cargo test -p envi-harness --test oracle_noisemodelling` green (equality gates pass, or Skip on placeholder rows — never a false Pass).
- Fixture carries provenance (NM v6.0.0, scene hash, date); comparison is offline (no Java/network at test time).
- README recipe prominently warns never to force-close JVM/Gradle/VS Code processes.
- `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` clean.
</verification>

<success_criteria>
Maps to ROADMAP Phase-4 success criterion 5 (shared sub-effects agree with NoiseModelling CNOSSOS within documented expected deltas). VAL-03 demonstrated: divergence + ISO 9613-1 air absorption equality-gated by band index against a committed offline fixture; barrier + ground documented as expected-delta reports.
</success_criteria>

<output>
Create `.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-05-SUMMARY.md` when done.
</output>
