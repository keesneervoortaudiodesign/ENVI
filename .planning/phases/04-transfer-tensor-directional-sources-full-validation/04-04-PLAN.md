---
phase: 04-transfer-tensor-directional-sources-full-validation
plan: 04
type: tdd
wave: 4
depends_on: ["04-03"]
files_modified:
  - crates/envi-engine/src/propagation/terrain_effect/submodel11.rs
  - crates/envi-engine/src/propagation/terrain_effect/submodel10.rs
  - crates/envi-engine/src/propagation/mod.rs
  - crates/envi-harness/src/cases/xls.rs
  - crates/envi-harness/src/cases/mod.rs
  - crates/envi-harness/src/emission/mod.rs
  - crates/envi-harness/src/emission/passby.rs
  - crates/envi-harness/src/weather/route1.rs
  - crates/envi-harness/src/capability.rs
  - crates/envi-harness/src/lib.rs
  - crates/envi-harness/tests/oracle_submodel11.rs
  - tools/nord2000_oracle/gen_submodel11_fixtures.py
  - crates/envi-harness/tests/fixtures/oracle/submodel11.toml
autonomous: true
requirements: [VAL-02]
must_haves:
  truths:
    - "The curved-road and city-street 'Coordinates' sheets parse into per-source-point cut-plane profiles via the existing calamine label-anchored idiom, replacing the placeholder loaders"
    - "Sub-model 11 (§5.20 reflection effect) + harness-side image-source façade path construction compute 1st- AND 2nd-order façade reflections with ρE = 0.7 and 1.0 for the city-street cases"
    - "Multi-lane / multi-category emission composes (90% cat-1 @ 90 + 10% cat-2/3 @ 70 for curved; cat-1 @ 50 for city) with correct source spacing and the 20×-shortest-distance cutoff"
    - "Yearly-average runs per weather class and combines via L_den with DANISH day/evening/night hours 12/3/9 (NOT the EU 12/4/8 default — Pitfall 4)"
    - "The Open-Q3 forest decision is executed: either Sub-model 10 (§5.19) is pulled forward so cases 121-124 Pass, OR they stay Skipped(requires: forest-scattering) with an explicit accepted-gap note — never a false Pass"
    - "All in-scope curved/city/yearly FORCE cases report numeric Pass at Ch.6 tolerance; the milestone acceptance gate is met with an honest, shrinking Skip list"
  artifacts:
    - path: crates/envi-engine/src/propagation/terrain_effect/submodel11.rs
      provides: "§5.20 reflection-effect kernel (complex, screen.rs shape)"
      contains: "submodel11"
      min_lines: 90
    - path: crates/envi-harness/src/cases/xls.rs
      provides: "Coordinates-sheet + multi-class sheet parsers for curved/city; contour→profile builder"
    - path: crates/envi-harness/tests/oracle_submodel11.rs
      provides: "band-index oracle test against committed submodel11.toml"
  key_links:
    - from: crates/envi-harness/src/cases/xls.rs
      to: crates/envi-harness/src/lib.rs
      via: "curved/city/yearly loaders feed the FORCE run_case arm (real load_* replacing the placeholder block)"
      pattern: "load_curved_road|load_city_street|load_yearly"
    - from: crates/envi-harness/src/lib.rs
      to: crates/envi-engine/src/propagation/terrain_effect/submodel11.rs
      via: "city-street façade reflections built from image-source paths feed the SM11 kernel"
      pattern: "submodel11|reflect_over_segment"
  prohibitions:
    - "NO false FORCE numeric Pass — the yearly-average class→(A,B) mapping stays [ASSUMED]-quarantined (03-03 posture); forest cases stay Skipped unless SM10 is genuinely implemented and oracle-pinned"
    - "NO EU-default 12/4/8 L_den hours — Denmark is 12/3/9 (Pitfall 4)"
    - "NO conjugation under propagation/ — the single boundary stays in transfer.rs; grep gate = 0"
    - "NO coherent summation of road sub-sources or source points (incoherent Annex-A readout only)"
    - "NO new fork of the .xls parser — extend the calamine label-anchored idiom; enforce MAX_SHEETS/MAX_PROFILE_ROWS DoS caps + confine() path guard"
    - "NO GPL source translation; NO new engine dependency"
  user_setup:
    - service: FORCE curved/city/yearly workbooks
      why: "curved/city/yearly numeric Pass needs the TestCurvedRoad/TestCityStreet/TestYearlyAverage .xls in refs/ (git-ignored, sha256-pinned); tests fail-soft Skip when absent"
---

<objective>
Complete VAL-02 (part 2): the curved-road, city-street, and yearly-average FORCE workbooks — the 3D groups the roadmap's original plan understated. Land Sub-model 11 (reflection effect) + image-source façade paths, the Coordinates-sheet loaders + contour→profile builder, multi-lane/multi-category emission, and the Danish-hours yearly-average L_den, then turn the in-scope cases numeric-green. This is the Milestone-1 acceptance gate.

**Phase-4 slice (MVP framing):** after this plan the engine validates against the *whole* in-scope FORCE suite — not just the straight road — with `cargo run -p envi-harness -- report` showing Pass for curved, city, and yearly cases and an honest, shrinking Skip list for any deliberately-deferred physics.

Purpose: closes VAL-02 and Milestone 1. The Coordinates-sheet → per-source-point profile construction plus SM11 image-source paths is the largest single harness work item; it depends on the straight-road machinery from 04-03 (comparator, emission, tensor readout).
Output: `submodel11.rs` (+ SM10 iff Open-Q3 pulls forest forward), the curved/city/yearly loaders + contour builder, multi-class emission, Danish-hours L_den, SM11 scipy oracle, and the extended FORCE `run_case` arms + capability wiring.

**Open-Q3 forest decision (execute here, no human stop):** VAL-02 says "full FORCE suite"; ENG-09 (the same forest physics) is scoped to Milestone-2 Phase 5. Decide per 04-RESEARCH Open Q3: (a) pull the self-contained SM10 kernel (Fs Eqs. 288-290 + ΔLs) forward so cases 121-124 Pass, or (b) leave them Skipped(requires: forest-scattering) with an explicit accepted-gap note defining the Milestone-1 gate as the remaining in-scope cases. Prefer (a) if effort is bounded; otherwise (b). Either way: honest Skips, never a false Pass, and record the decision in the SUMMARY.

**Honest-green directive:** the yearly-average class→(A,B) mapping remains the 03-03 [ASSUMED] quarantine (validated by structure/direction property tests + the same-transcription oracle only). NEVER present a quarantined value as a verified numeric Pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-RESEARCH.md
@.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-PATTERNS.md
@crates/envi-engine/src/propagation/screen.rs
@crates/envi-engine/src/propagation/terrain_effect/submodel7.rs
@crates/envi-engine/src/geometry.rs
@crates/envi-harness/src/cases/xls.rs
@crates/envi-harness/src/cases/mod.rs
@crates/envi-harness/src/weather/route1.rs
@crates/envi-harness/src/emission/mod.rs
@crates/envi-harness/src/emission/passby.rs
@crates/envi-harness/src/capability.rs
@crates/envi-harness/src/lib.rs
@crates/envi-harness/tests/oracle_refraction.rs
@tools/nord2000_oracle/gen_ground_fixtures.py
</context>

<artifacts_this_phase_produces>
- `crates/envi-engine/src/propagation/terrain_effect/submodel11.rs` (+ `submodel10.rs` iff Open-Q3=(a))
- curved/city/yearly loaders in `cases/xls.rs` + `cases/mod.rs`; contour→profile builder
- multi-lane/multi-category emission in `emission/mod.rs`; Danish-hours L_den in `weather/route1.rs` + `passby.rs`
- SM11 scipy oracle (`gen_submodel11_fixtures.py` + `submodel11.toml` + `tests/oracle_submodel11.rs`)
- extended FORCE `run_case` arms + `capability.rs` wiring (forest-scattering variant per Open-Q3)
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Coordinates-sheet loaders + contour→profile builder + multi-class emission (curved/city/yearly)</name>
  <files>crates/envi-harness/src/cases/xls.rs, crates/envi-harness/src/cases/mod.rs, crates/envi-harness/src/emission/mod.rs, crates/envi-harness/src/weather/route1.rs, crates/envi-harness/src/emission/passby.rs</files>
  <read_first>
    - crates/envi-harness/src/cases/xls.rs 111-147 (find_label_row / labelled_num idioms), 150-307 (parse_sheet full shape), 262-285 (terrain-table zero-padded-terminator), 47-77 (validate_profile), 319-351 (load_straight_road workbook entry), 381-425 (refs-gated test skip)
    - crates/envi-harness/src/cases/mod.rs 49-170 (CaseLoadError variants + caps), 284-327 (TerrainRow/SpectrumRow schema), 445-464 (confine path guard), 508-523 (straight-road discovery arm), 527-569 (the three-workbook placeholder loop to replace)
    - crates/envi-harness/src/weather/route1.rs (l_den + load_met_probabilities 230 — yearly composes with this; period hours must parameterize to Danish 12/3/9)
    - 04-RESEARCH.md FORCE Gap Inventory (curved/city/yearly rows) + Pitfall 4 + Assumptions A9
    - 04-PATTERNS.md "cases/xls.rs — curved/city/yearly parsers" section
  </read_first>
  <behavior>
    - Test: the Coordinates sheet (road centre line, barriers, calc points, terrain contour lines) parses into typed rows; a contour→profile builder produces a per-source-point cut-plane TerrainProfile; validate_profile rejects non-finite / non-ascending-X and enforces MAX_PROFILE_ROWS.
    - Test: multi-class emission composes 90% cat-1 @ 90 km/h + 10% cat-2/3 @ 70 km/h (curved) and cat-1 @ 50 km/h (city) with the given source spacing; the 20×-shortest-distance source cutoff (city) is applied.
    - Test: yearly-average L_den uses Danish hours 12/3/9 (a regression asserting the weight differs from the EU 12/4/8 default), composing per-class runs via load_met_probabilities; class→(A,B) values stay [ASSUMED]-tagged.
    - Test (refs-gated): loader unit tests skip cleanly with a refs/fetch.sh pointer when the workbooks are absent; when present, full-precision cell anchors at ε=1e-9 with the correct provenance tag.
  </behavior>
  <action>
    Extend `cases/xls.rs` (do NOT fork the parser) with Coordinates-sheet and per-vehicle-class sheet parsers using the label-anchored `find_label_row`/`labelled_num` idiom; add a contour→profile builder that interpolates the terrain contour lines into per-source-point cut-plane profiles. Add new sheet-schema structs next to TerrainRow/SpectrumRow and new `CaseLoadError` variants carrying sheet/path context + the MAX_SHEETS/MAX_PROFILE_ROWS caps. Replace the three-workbook placeholder loop (`cases/mod.rs:527-569`) with real `load_curved_road`/`load_city_street`/`load_yearly` calls following the straight-road arm (present-file check → `confine()` → loader → append; missing refs degrade to a note, never an error).
    In `emission/mod.rs`, add multi-lane/multi-category composition (per-class RoadSource weighted by traffic share, per source spacing, with the 20× cutoff). In `weather/route1.rs`, parameterize the L_den period hours and use Danish 12/3/9 for FORCE (Pitfall 4); keep class→(A,B) values [ASSUMED]-quarantined (Assumption A9). Wire yearly to compose per-class propagation runs via `load_met_probabilities`.
  </action>
  <acceptance_criteria>
    - Coordinates sheets parse; contour→profile builder yields valid per-source-point profiles; DoS caps + confine() enforced.
    - Multi-class emission + source spacing + city cutoff correct.
    - Danish 12/3/9 L_den regression passes (differs from EU default).
    - Refs-gated loader tests skip cleanly / anchor at 1e-9 when present.
    - `cargo tree -p envi-engine` unchanged; clippy/fmt clean.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-harness cases emission route1 && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>Curved/city/yearly workbooks load into per-source-point profiles with multi-class emission and Danish-hours L_den; loaders fail-soft when refs are absent.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Sub-model 11 reflection effect + image-source façade paths (+ SM10 forest per Open-Q3)</name>
  <files>crates/envi-engine/src/propagation/terrain_effect/submodel11.rs, crates/envi-engine/src/propagation/terrain_effect/submodel10.rs, crates/envi-engine/src/propagation/mod.rs, crates/envi-harness/src/lib.rs, tools/nord2000_oracle/gen_submodel11_fixtures.py, crates/envi-harness/tests/oracle_submodel11.rs, crates/envi-harness/tests/fixtures/oracle/submodel11.toml</files>
  <read_first>
    - crates/envi-engine/src/propagation/screen.rs 77 (ScreenConfig) + 613 (submodel4 signature) — the complex-kernel shape to copy for SM11
    - crates/envi-engine/src/propagation/terrain_effect/submodel7.rs (energy-only f64 shape — the template for SM10 if pulled forward; cannot touch the phase channel)
    - crates/envi-engine/src/geometry.rs reflect_over_segment (used in lib.rs:132-165 run_geometry_case) — the image-source primitive
    - AV 1106/07 §5.20 (SM11, p. 129) + §5.19 (SM10, pp. 124-128, Fs Eqs. 288-290, ΔLs, kf/ka Table 8) — verify transcription against PDF page images
    - 04-PATTERNS.md "submodel8/10/11" + "SM11 image-source façade path builder" sections
  </read_first>
  <behavior>
    - Test (SM11 oracle): the §5.20 reflection-effect kernel matches its committed scipy fixture at each band_index within tolerance; coverage assertions present.
    - Test (image-source paths): 1st- and 2nd-order façade reflections built by composing `reflect_over_segment` per reflecting face produce the expected r1/r2 path lengths + on-segment `valid` flags; the ρE energy factor (0.7 / 1.0) applies at the SM11 kernel, not in geometry.
    - Test (façade free-field exclusion): a city-street façade receiver excludes its OWN-façade reflection (the "free-field" SPL convention).
    - Test (Open-Q3): IF forest pulled forward, SM10 Fs coherence reduction follows the CoherenceInputs.f_delta_nu injection pattern (multiplied, never overwritten) and matches its oracle; ELSE cases 121-124 Skip with `requires: forest-scattering` and an accepted-gap note.
  </behavior>
  <action>
    Create `submodel11.rs` copying the `screen.rs` complex-kernel shape (`Config<'a>` struct + `pub fn submodel11(f_hz, cfg) -> Result<GroundResult, PropagationError>`); implement §5.20 (reflection effect). Any needed sign flip is `Complex::new(re, -im)` — the single convention boundary stays in transfer.rs.
    In `crates/envi-harness/src/lib.rs`, build the city-street 1st/2nd-order façade reflection PATHS by composing `reflect_over_segment` per reflecting face (path construction is harness/case logic; the kernel is engine math). Apply the ρE factor at the SM11 kernel. Exclude the own-façade reflection for façade receivers.
    Execute the Open-Q3 forest decision: if (a), create `submodel10.rs` copying `submodel7.rs` (energy-only f64, cannot corrupt the phase channel; Fs Eqs. 288-290 + ΔLs; combine like sm7 energy; Fs via the f_delta_nu injection pattern) with a scipy oracle, and wire cases 121-124 to Pass; if (b), add/keep a `forest-scattering` Capability so 121-124 Skip honestly and record the accepted-gap note.
    Create `gen_submodel11_fixtures.py` + committed `submodel11.toml` + `tests/oracle_submodel11.rs` following the established generator/test template (provenance sha256, band_index rows, coverage assertions, no Python at test time).
  </action>
  <acceptance_criteria>
    - SM11 matches its scipy oracle; coverage assertions present.
    - Image-source 1st/2nd-order paths correct; ρE at the kernel; own-façade excluded.
    - Open-Q3 executed: SM10 implemented+oracle-pinned (Pass) OR 121-124 honestly Skipped with a recorded note.
    - conj grep over propagation/ = 0; `cargo tree -p envi-engine` unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-engine submodel11 submodel10 && cargo test -p envi-harness --test oracle_submodel11 && grep -rl --include='*.rs' 'conj()' crates/envi-engine/src/propagation/ | grep -c . | grep -qx 0 && echo CONJ_QUARANTINE_OK</automated>
  </verify>
  <done>Sub-model 11 + image-source façade reflections compute the city-street cases, the forest decision is executed honestly, and SM11 is oracle-pinned.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Curved/city/yearly run_case arms + capability wiring + full in-scope FORCE Pass (milestone gate)</name>
  <files>crates/envi-harness/src/lib.rs, crates/envi-harness/src/capability.rs, crates/envi-harness/src/cases/mod.rs</files>
  <read_first>
    - crates/envi-harness/src/lib.rs 287-324 (run_terrain_case dispatch shape) + the straight-road arm added in 04-03
    - crates/envi-harness/src/capability.rs 120-128 (implemented_capabilities) + 230-262 (shrink assertions)
    - crates/envi-harness/src/compare.rs 156-222 (compare_27_band dip-shift) — reuse for all groups
    - 04-RESEARCH.md "acceptance tolerance" + Pitfalls 2/3/10 + the recommended plan-split milestone-acceptance gate
  </read_first>
  <behavior>
    - Test: curved-road, city-street, and yearly-average `run_case` arms dispatch through the loaders + SM11/emission and compare via `compare_27_band` (dip-shift) with the overall LAeq/L_den gate strict ≤1 dB; every fallible step → FailDetail (never panic); unimplemented capability → Skipped mid-run.
    - Test (Annex-A): all groups use the INCOHERENT tensor readout (Pitfall 3).
    - Test (capability + shrink): flipping any newly-implemented capability comes with a shrink assertion; the total FORCE Skip list is a documented shrinking set (only genuinely-deferred physics remains, e.g. forest if Open-Q3=(b)).
    - Test (milestone gate): `cargo run -p envi-harness -- report` shows numeric Pass for every in-scope curved/city/yearly case at Ch.6 tolerance, with an honest Skip list and no false Pass.
  </behavior>
  <action>
    Add curved-road, city-street, and yearly-average `run_case` arms in `lib.rs` copying the `run_terrain_case`/straight-road dispatch shape: FailDetail naming the quantity on any fallible step, `Skipped(requires: …)` mid-run for unimplemented capabilities, `Fail(ComparisonReport)` on spectrum mismatch. Yearly composes per-weather-class runs then combines via the Danish-hours L_den. Use the incoherent readout throughout (Pitfall 3). Record implemented road extent per group + ReferenceVersion provenance (Pitfalls 2b, 10).
    In `capability.rs`, add any capability variants the new groups require (e.g. `reflection-effect`, and `forest-scattering` per Open-Q3) with plan-numbered doc comments, flip the implemented ones, and add the mandatory shrink assertions. Ensure the total FORCE Skip list is honest and shrinking.
    Update the REQUIREMENTS traceability note (in the SUMMARY) that VAL-02 is met for all in-scope cases; document any accepted gap (forest) explicitly.
  </action>
  <acceptance_criteria>
    - Curved/city/yearly in-scope cases report numeric Pass at Ch.6 tolerance.
    - Shrink assertions pass; Skip list documented + honest (no false Pass).
    - `cargo run -p envi-harness -- report` green for all in-scope groups; milestone-acceptance gate met.
    - `cargo test --workspace` green; clippy `-D warnings` + fmt clean; `cargo tree -p envi-engine` unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --workspace && cargo run -p envi-harness -- report && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>All in-scope curved/city/yearly FORCE cases pass numerically, remaining gaps Skip honestly, and VAL-02 (the Milestone-1 acceptance gate) is met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curved/city/yearly `.xls` (Coordinates, multi-class, Met. statistics) → loaders | untrusted operator input: contour lines, barrier geometry, calc points, class probabilities |
| image-source path construction → SM11 | reflecting-face geometry can be degenerate (zero-length façade, off-segment reflection) |
| weather-class table → yearly L_den | [ASSUMED] class→(A,B) mapping; wrong lineage biases L_den |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Denial of Service | oversized/ malformed Coordinates sheet | mitigate | MAX_SHEETS / MAX_PROFILE_ROWS caps + validate_profile (finite, ascending-X) + confine() path guard — typed CaseLoadError, never panic |
| T-04-04-02 | Tampering | SM11/SM10 transcription error from the PDF | mitigate | verify §5.20/§5.19 coefficients vs PDF page images; scipy oracle + coverage assertions |
| T-04-04-03 | Repudiation | quarantined class→(A,B) presented as verified | mitigate | [ASSUMED] tags + structure/direction property tests only; L_den composition never a fixed-value oracle (03-03 posture) |
| T-04-04-04 | Tampering | degenerate image-source path (off-segment reflection) | mitigate | `reflect_over_segment` `valid` flag checked; own-façade reflection excluded; typed errors on degenerate geometry |
| T-04-04-05 | Repudiation | false Pass hiding the forest gap | mitigate | Open-Q3 decision recorded; forest either genuinely oracle-pinned Pass or honest `requires: forest-scattering` Skip |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new packages; `cargo tree -p envi-engine` gate; Python oracle generators operator-run only; refs .xls sha256-pinned + git-ignored |
</threat_model>

<verification>
- `cargo test -p envi-engine submodel11 submodel10` + `cargo test -p envi-harness cases emission route1 --test oracle_submodel11` green.
- `cargo test --workspace` green; `cargo run -p envi-harness -- report` shows Pass for all in-scope curved/city/yearly cases + an honest shrinking Skip list.
- grep `conj()` over `crates/envi-engine/src/propagation/` = 0; `cargo tree -p envi-engine` unchanged.
- `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` clean.
</verification>

<success_criteria>
Completes ROADMAP Phase-4 success criterion 4 (the full FORCE road-traffic suite passes — the Milestone-1 acceptance gate). VAL-02 fully demonstrated across straight (04-03) + curved + city + yearly (this plan), with any deferred physics (forest, per Open-Q3) honestly Skipped and recorded as an accepted gap.
</success_criteria>

<output>
Create `.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-04-SUMMARY.md` when done.
</output>
