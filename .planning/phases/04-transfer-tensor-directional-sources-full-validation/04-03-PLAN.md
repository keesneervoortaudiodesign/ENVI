---
phase: 04-transfer-tensor-directional-sources-full-validation
plan: 03
type: tdd
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - crates/envi-engine/src/propagation/terrain_effect/submodel3.rs
  - crates/envi-engine/src/propagation/terrain_effect/mod.rs
  - crates/envi-engine/src/propagation/screen.rs
  - crates/envi-engine/src/propagation/mod.rs
  - crates/envi-harness/src/compare.rs
  - crates/envi-harness/src/capability.rs
  - crates/envi-harness/src/lib.rs
  - crates/envi-harness/src/cases/mod.rs
  - crates/envi-harness/tests/oracle_submodel3.rs
  - tools/nord2000_oracle/gen_submodel3_fixtures.py
  - crates/envi-harness/tests/fixtures/oracle/submodel3.toml
autonomous: true
requirements: [VAL-02]
must_haves:
  truths:
    - "Sub-model 3 (§5.12 non-flat terrain) computes a two-channel GroundResult behind the seam that currently raises NonFlatTerrainNotImplemented, and reproduces Sub-model 1 bit-for-bit on an actually-flat profile"
    - "Segmented-ground refraction is wired: the SegmentedRefractionNotImplemented seam is replaced by the already-implemented, oracle-tested calc_eq_ssp_ground per-band collapse inside eval"
    - "The screen channel either consumes the SoundSpeedProfile OR raises a typed weather+screen error (Pitfall 9 guard) — a wind screen case is never silently computed with an unrefracted screen"
    - "Sub-model 8 necessity is decided by evaluating Eq. 279 over all FORCE downwind geometries; SM8 is implemented iff any case demands N>1, otherwise the skip is an explicit accepted-gap note (Assumption A6)"
    - "The Ch.6 comparator (overall ≤1 dB, per-band ≤1 dB, one-third-octave dip-shift) is wired for straight-road cases via the existing compare_27_band + LAE/LAeq,24h/LAmax conversions"
    - "Capability::EmissionModel is flipped and the in-scope straight-road FORCE cases report numeric Pass at Ch.6 tolerance; any genuinely non-computable case stays Skipped(requires: …) with a shrinking list — NEVER a false Pass"
  artifacts:
    - path: crates/envi-engine/src/propagation/terrain_effect/submodel3.rs
      provides: "§5.12 segment classification + per-segment terrain effect + Fresnel-weight summation → two-channel GroundResult"
      contains: "submodel3"
      min_lines: 120
    - path: crates/envi-harness/src/compare.rs
      provides: "LAE/LAeq,24h/LAmax conversions + Ch.6 straight-road comparator wiring"
      contains: "l_aeq_24h"
    - path: tools/nord2000_oracle/gen_submodel3_fixtures.py
      provides: "scipy oracle generator for SM3 (committed TOML, no Python at test time)"
    - path: crates/envi-harness/tests/oracle_submodel3.rs
      provides: "band-index oracle test against committed submodel3.toml"
  key_links:
    - from: crates/envi-engine/src/propagation/terrain_effect/mod.rs
      to: crates/envi-engine/src/propagation/terrain_effect/submodel3.rs
      via: "the r_flat<1 blend branch dispatches to submodel3 instead of raising NonFlatTerrainNotImplemented"
      pattern: "submodel3"
    - from: crates/envi-harness/src/lib.rs
      to: crates/envi-harness/src/compare.rs
      via: "FORCE straight-road run_case arm compares via compare_27_band with dip-shift + overall LAeq,24h gate"
      pattern: "compare_27_band"
  prohibitions:
    - "NO conjugation introduced anywhere under propagation/ — the single convention boundary stays in transfer.rs; write any needed sign flip as Complex::new(re, -im); the grep gate over propagation/ stays 0"
    - "NO false FORCE numeric Pass — a case that cannot be numerically validated (missing coefficients / unimplemented capability) stays Skipped(requires: …), never Pass"
    - "NO silent computation of a wind screen case with an unrefracted screen — raise a typed weather+screen error until wired (Pitfall 9)"
    - "NO rebuilding of the Ch.6 dip-shift comparator or a_weighting_db — they already exist in compare.rs; extend and wire only"
    - "NO new engine dependency; NO band comparison by nominal Hz (105→27 by index via pick_third_octave)"
    - "NO perturbation of frozen Phase 1-3 validation — every new branch carries a bit-identical neutral-config regression guard"
---

<objective>
Land the straight-road FORCE pass (VAL-02, part 1): wire the propagation capabilities the suite needs behind their existing typed-error seams, wire the Ch.6 comparator, flip the emission capability, and turn the in-scope straight-road cases numeric-green.

**Phase-4 slice (MVP framing):** after this plan `cargo run -p envi-harness -- report` shows numeric Pass — not Skip — for the flat and non-flat straight-road FORCE cases, at the standard's Ch.6 tolerance. This is the first time the whole engine chain (emission → tensor → propagation → comparator) validates end-to-end against the reference.

Purpose: VAL-02 is the Milestone-1 acceptance gate. This plan closes the straight-road half by filling Sub-model 3, wiring segmented-ground refraction (the building block already exists and is oracle-tested), guarding the screen-refraction gap, and deciding Sub-model 8 by evaluating Eq. 279.
Output: `submodel3.rs`, the segmented-refraction + screen-refraction-guard edits, the SM3 scipy oracle, the Ch.6 comparator wiring + LAE/LAeq/LAmax conversions, the `Capability::EmissionModel` flip + shrink assertions, and the FORCE straight-road `run_case` arm.

**Honest-green directive:** NEVER fake a numeric Pass. Any case needing missing coefficients or an unimplemented capability stays capability-gated with an honest `Skipped(requires: …)` reason (a shrinking list). Plan success = build/test/clippy/fmt green + the in-scope straight-road cases Pass at Ch.6 tolerance + the Skip list honestly reflecting what is left.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-RESEARCH.md
@.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-PATTERNS.md
@crates/envi-engine/src/propagation/terrain_effect/mod.rs
@crates/envi-engine/src/propagation/terrain_effect/submodel2.rs
@crates/envi-engine/src/propagation/terrain_effect/submodel7.rs
@crates/envi-engine/src/propagation/screen.rs
@crates/envi-engine/src/propagation/mod.rs
@crates/envi-engine/src/propagation/refraction/eqssp.rs
@crates/envi-harness/src/compare.rs
@crates/envi-harness/src/capability.rs
@crates/envi-harness/src/lib.rs
@crates/envi-harness/src/cases/mod.rs
@crates/envi-harness/tests/oracle_refraction.rs
@tools/nord2000_oracle/gen_ground_fixtures.py
</context>

<artifacts_this_phase_produces>
- `crates/envi-engine/src/propagation/terrain_effect/submodel3.rs` — §5.12 non-flat terrain
- edits: `terrain_effect/mod.rs` (SM3 dispatch + segmented-refraction wiring), `screen.rs` + `propagation/mod.rs` (screen-refraction guard / wiring)
- `tools/nord2000_oracle/gen_submodel3_fixtures.py` + committed `submodel3.toml` + `tests/oracle_submodel3.rs`
- `compare.rs` LAE/LAeq,24h/LAmax + Ch.6 wiring; `capability.rs` EmissionModel flip + shrink tests
- `lib.rs` FORCE straight-road `run_case` arm; `cases/mod.rs` any needed capability/case wiring
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Sub-model 3 (§5.12 non-flat terrain) + scipy oracle + bit-identical flat regression</name>
  <files>crates/envi-engine/src/propagation/terrain_effect/submodel3.rs, crates/envi-engine/src/propagation/terrain_effect/mod.rs, tools/nord2000_oracle/gen_submodel3_fixtures.py, crates/envi-harness/tests/oracle_submodel3.rs, crates/envi-harness/tests/fixtures/oracle/submodel3.toml</files>
  <read_first>
    - crates/envi-engine/src/propagation/terrain_effect/submodel2.rs 49-88 (the geometry-struct + strip-slice-in + GroundResult-out module shape to copy)
    - crates/envi-engine/src/propagation/terrain_effect/mod.rs 69-91 (GroundResult::from_channels), 174-182 (the NonFlatTerrainNotImplemented seam to replace), 191-202 (the r-weight blend), 499-757 and 784-805 (flat/homogeneous bit-identical regression idioms)
    - AV 1106/07 §5.12 Eqs. 134-156 (refs/AV1106-07-rev4.pdf pp. 59-67) — verify transcription against the PDF page images (house rule)
    - tools/nord2000_oracle/gen_ground_fixtures.py + common.py (generator template, provenance sha256, no-Python-at-test-time docstring)
    - 04-PATTERNS.md "submodel3.rs" section
  </read_first>
  <behavior>
    - Test (flat regression): SM3 evaluated on an actually-flat profile reproduces Sub-model 1 bit-for-bit (assert_eq!, mirroring refraction_homogeneous_profile_is_bit_identical at mod.rs:784-805) — new physics must not perturb frozen validation.
    - Test (oracle): SM3 two-channel output matches the committed scipy fixture at each band_index within the fixture's stated tolerance (concave, convex, and transition segment configs); coverage assertions prevent a truncated fixture from passing silently.
    - Test (typed-error-not-silent): before wiring, a non-flat profile raised NonFlatTerrainNotImplemented; after wiring, the same profile returns a finite two-channel GroundResult with no NaN across all 105 bands.
  </behavior>
  <action>
    Create `crates/envi-engine/src/propagation/terrain_effect/submodel3.rs` copying the `submodel2.rs` module shape: a plain-data geometry struct + strip slice in, two-channel `GroundResult` out, per frequency. Implement §5.12 (segment classification concave/convex/transition + per-segment terrain effect + Fresnel-zone-weight summation, Eqs. 134-156). Transcribe every coefficient from the PDF page images (house rule; note any transcription risk in a doc comment). Refraction-corrected heights come through the existing `FresnelZoneW`/refraction state.
    In `terrain_effect/mod.rs`, replace the block at 174-182 that raises `NonFlatTerrainNotImplemented` when `r_flat < 1` carries weight with an SM3 branch of the Eq. 332 blend (channels blend linearly, delta_l_db blends in dB — keep BOTH readings per the r-weight blend at 191-202). Keep the flat path (r_flat = 1) reaching SM1 unchanged.
    Create `tools/nord2000_oracle/gen_submodel3_fixtures.py` following the gen_ground_fixtures.py template (module docstring: regeneration is operator-driven, TOML committed, Python NOT a build dependency; `# generated by … DO NOT EDIT` header; [meta] block with oracle, provenance sha256, per-quantity tolerance + justification; equations cited by report+number only). Commit `submodel3.toml`. Create `tests/oracle_submodel3.rs` mirroring `oracle_refraction.rs` (band_index rows, coverage assertions, abs-vs-rel tolerance near zero).
    Add a `#[cfg(test)]` flat-regression block in submodel3.rs.
  </action>
  <acceptance_criteria>
    - SM3-on-flat == SM1 bit-for-bit (assert_eq!).
    - Oracle test green across concave/convex/transition; coverage assertions present.
    - No NaN across 105 bands on the previously-erroring non-flat profile.
    - grep for a conjugation call under propagation/ still counts 0.
    - `cargo tree -p envi-engine -e normal --depth 1` unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-engine submodel3 && cargo test -p envi-harness --test oracle_submodel3 && grep -rl --include='*.rs' 'conj()' crates/envi-engine/src/propagation/ | grep -c . | grep -qx 0 && echo CONJ_QUARANTINE_OK</automated>
  </verify>
  <done>Sub-model 3 computes non-flat terrain effect, matches its scipy oracle, and leaves flat cases bit-identical.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Segmented-ground refraction wiring + screen-refraction guard + Sub-model 8 Eq. 279 decision</name>
  <files>crates/envi-engine/src/propagation/terrain_effect/mod.rs, crates/envi-engine/src/propagation/screen.rs, crates/envi-engine/src/propagation/mod.rs</files>
  <read_first>
    - crates/envi-engine/src/propagation/terrain_effect/mod.rs 302-327 (RefractionState precompute-once dispatch to replicate for SM2), 344-372 (per-band eval + shadow-zone branch), 393-405 (the SegmentedRefractionNotImplemented seam to replace), 421-497 (screen_channel), 844-875 + 974-998 (typed-error-not-silent + finiteness-sweep regression idioms)
    - crates/envi-engine/src/propagation/refraction/eqssp.rs (calc_eq_ssp_ground — already implemented + oracle-tested; per-band signature visible in tests/oracle_refraction.rs:148-160)
    - crates/envi-engine/src/propagation/screen.rs line ~37 (the Phase-3-deferred ξ<0 shadow-branch gap) and the ScreenConfig / submodel4 shape
    - crates/envi-engine/src/propagation/mod.rs 130-147 (SegmentedRefractionNotImplemented doc-comment style for the new weather+screen variant)
    - AV 1106/07 §5.17 Eq. 279 (SM8 ray count) — refs/AV1106-07-rev4.pdf p. 120
    - 04-PATTERNS.md "Segmented-ground + screen refraction wiring" section
  </read_first>
  <behavior>
    - Test (homogeneous bit-identical): segmented-ground with a null/near-clamp weather profile reproduces the Phase-2 straight-ray SM2 result bit-for-bit (assert_eq!) — the honest-green invariant across the wiring change.
    - Test (segmented refraction direction): a downward-refraction profile over mixed impedance changes the SM2 result in the physically-expected direction versus homogeneous (property test, not a fixed oracle — calc_eq_ssp_ground is already oracle-pinned).
    - Test (screen guard): a screen case WITH a SoundSpeedProfile either returns a refracted result OR raises the new typed weather+screen error — asserting it is NEVER silently computed with an unrefracted screen (Pitfall 9).
    - Test (SM8 decision): a helper evaluating Eq. 279 over the FORCE downwind geometries (d ≲ 200 m, u = 3 m/s) returns the max ray count N; a test records whether any case demands N>1 (drives the implement-or-accept-gap decision, Assumption A6).
  </behavior>
  <action>
    In `terrain_effect/mod.rs`, replace the `SegmentedRefractionNotImplemented` seam at 393-405 with a frequency-dependent `calc_eq_ssp_ground` collapse inside `eval` (it is per-band, so it lives in the band loop, not in `from_profile`), mirroring the Sub-model-1 refraction dispatch at 302-327/344-372. Precompute the eq-linear/ground state once per pair; consume it per band. Any needed sign flip is written as `Complex::new(re, -im)` — the single convention boundary remains solely in transfer.rs.
    Screen channel: thread `Option<&SoundSpeedProfile>` through `screen_channel` the way `FlatChannel` carries it. If the ξ<0 screen shadow branches (Eqs. 184-186) are implemented this plan, wire them; otherwise add a NEW typed `PropagationError` variant for weather+screen (doc-comment the honest-green rationale in the variant, mirroring `SegmentedRefractionNotImplemented`) and raise it so wind screen cases Skip rather than compute an unrefracted screen (Pitfall 9). Choose based on effort; the guard is mandatory either way.
    Add an Eq. 279 evaluation helper (SM8 ray count) and a test that reports N over the FORCE downwind geometries. If any case demands N>1, implement Sub-model 8 (§5.17) by copying the energy-only `submodel7.rs` shape (f64 dB, cannot touch the phase channel; combine like sm7 energy). If all cases give N≤1, record an explicit accepted-gap note in the module doc + SUMMARY (Assumption A6) and leave SM8 unimplemented.
    Clone the regression idioms: homogeneous-bit-identical (mod.rs:784-805 style), typed-error-not-silent (844-875), finiteness sweep over all bands × profiles (974-998).
  </action>
  <acceptance_criteria>
    - Segmented-ground homogeneous limit == Phase-2 SM2 bit-for-bit.
    - Segmented refraction moves the result in the expected direction.
    - Screen+weather never silently unrefracted (refracted result OR typed error, asserted).
    - Eq. 279 SM8 decision recorded (implemented iff N>1, else accepted-gap note).
    - Finiteness sweep: no NaN across all bands × profiles; conj grep over propagation/ = 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-engine terrain_effect screen && grep -rl --include='*.rs' 'conj()' crates/envi-engine/src/propagation/ | grep -c . | grep -qx 0 && echo CONJ_QUARANTINE_OK</automated>
  </verify>
  <done>Segmented-ground refraction is wired to the existing collapse, the screen-refraction gap is guarded (never silent), and the SM8 decision is made from Eq. 279 evidence.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Ch.6 comparator wiring + EmissionModel capability flip + straight-road numeric Pass</name>
  <files>crates/envi-harness/src/compare.rs, crates/envi-harness/src/capability.rs, crates/envi-harness/src/lib.rs, crates/envi-harness/src/cases/mod.rs</files>
  <read_first>
    - crates/envi-harness/src/compare.rs 18-28 (tolerances + investigation flag), 156-222 (compare_27_band with the dip-shift rule — already exists), 320-322 (pick_third_octave), 340-347 (a_weighting_db — already exists)
    - crates/envi-harness/src/capability.rs 17-46 (Capability + as_str), 120-128 (implemented_capabilities), 230-262 (the requires-shrink assertion pattern)
    - crates/envi-harness/src/lib.rs 47-56 (gate before dispatch), 76-82 (the FORCE dispatch stub to replace), 287-324 (run_terrain_case — the dispatch shape to copy: fallible step → FailDetail, unimplemented-capability typed error → Skipped mid-run, mismatch → Fail(report))
    - crates/envi-harness/src/cases/mod.rs 302-327 (SpectrumRow le_db/dl_db)
    - 04-RESEARCH.md "acceptance tolerance" + "dL validation asset" + Pitfalls 2/3/5/10 + Ch.6 conversions code example
  </read_first>
  <behavior>
    - Test (conversions): l_aeq_24h(l_ae, N) = l_ae + 10·lg(N) − 10·lg(86400); anchored so case-1 LAeq,24h = 39.398… reproduces from its LAE (full precision, ε=1e-9).
    - Test (Annex-A readout): the FORCE straight-road arm uses the INCOHERENT tensor readout (Pitfall 3) — a two-identical-subsource sanity gives +3 dB, never +6.
    - Test (dip-shift): a spectrum with a ground dip one third-octave off the reference passes via the existing dip-shift rule but the overall LAeq,24h gate stays strict ≤1 dB (Pitfall 5).
    - Test (capability flip + shrink): after adding Capability::EmissionModel to implemented_capabilities(), representative flat/non-flat straight-road cases report an empty (or only-still-gated) requires-set; wind screen cases still Skip with the guarded reason (Pitfall 9) — never a false Pass.
    - Test (report): `cargo run -p envi-harness -- report` shows numeric Pass for the in-scope straight-road cases at Ch.6 tolerance.
  </behavior>
  <action>
    In `compare.rs`, add `l_ae`, `l_aeq_24h`, and `l_amax` conversions (reusing the existing `a_weighting_db` at exact FREQ_AXIS centres and `pick_third_octave` for 105→27). Wire the straight-road comparison to `compare_27_band(got27, want27, Some((got_overall, want_overall)), dip_shift = true)`. Do NOT rebuild the dip-shift rule — it already exists at 156-222.
    In `capability.rs`, add `Capability::EmissionModel` to `implemented_capabilities()` with a plan-numbered doc comment (mirror the Phase-3 entry). Add the mandatory companion shrink test (230-262 pattern) asserting the missing-set shrinks for representative FORCE cases. If Open-Q3 resolves to defer forest cases 121-124, add a `forest-scattering` Capability variant + as_str label so those cases Skip with an honest reason (that decision is finalized in 04-04; here just leave the straight-road cases correct).
    In `lib.rs`, replace the FORCE dispatch stub (76-82) with a real straight-road `run_case` arm copying `run_terrain_case` (287-324): every fallible step → `Outcome::FailDetail(msg)` naming the quantity (never a panic); an unimplemented-capability TYPED error → `Outcome::Skipped(requires: …)` mid-run (honest-green even after the gate passes); spectrum mismatch → `Outcome::Fail(ComparisonReport)`. Read the reference via `SpectrumRow.le_db`/`dl_db`. Use the INCOHERENT tensor readout for the road pass-by (Pitfall 3). Record the implemented road extent per group (Pitfall 10) and per-case ReferenceVersion provenance (Pitfall 2b).
  </action>
  <acceptance_criteria>
    - LAeq,24h conversion reproduces the case-1 full-precision anchor.
    - Straight-road FORCE cases report numeric Pass at Ch.6 tolerance; wind screen cases Skip with the guarded reason.
    - Shrink assertion passes; no false Pass anywhere in the report.
    - `cargo run -p envi-harness -- report` prints Pass for the in-scope straight-road group.
    - `cargo test --workspace` green; clippy `-D warnings` + fmt clean.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-harness compare capability && cargo test --workspace && cargo run -p envi-harness -- report && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>The Ch.6 comparator is wired, EmissionModel is flipped with a shrink assertion, and the in-scope straight-road FORCE cases pass numerically at the standard's tolerance while remaining gaps Skip honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FORCE `.xls` → run_case | reference spectra, terrain rows, traffic parameters are operator-controlled untrusted input |
| non-flat profile → SM3 | segment geometry from case files can be degenerate (zero-length segments, non-monotone X) |
| weather profile → segmented/screen channels | SoundSpeedProfile can push ξ into singular / shadow regions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | SM3 transcription error from the PDF | mitigate | verify every §5.12 coefficient against the PDF page images (house rule); scipy oracle + flat-bit-identical regression catch drift |
| T-04-03-02 | Tampering | wind screen case silently unrefracted | mitigate | typed weather+screen error until wired (Pitfall 9); test asserts never-silent |
| T-04-03-03 | Denial of Service | degenerate segment geometry / ξ singularity | mitigate | existing ξ clamps + finiteness sweep across all bands × profiles; typed errors, never panic |
| T-04-03-04 | Repudiation | a false FORCE Pass masking a gap | mitigate | honest-green invariant: unimplemented-capability typed errors map to Skipped mid-run; shrink assertion; no [ASSUMED] value on a Pass path |
| T-04-03-05 | Tampering | stale reference-version drift (2009 vs 2010, rev4/2018) | mitigate | ReferenceVersion provenance per case; verify pinned `.xls` values vs EP 1335 appendix; do NOT implement 2018 amendments (Pitfall 2) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new packages; `cargo tree -p envi-engine` gate; Python oracle generator is operator-run, not a build/test dependency |
</threat_model>

<verification>
- `cargo test -p envi-engine submodel3 terrain_effect screen` green (SM3 + refraction wiring + regressions).
- `cargo test -p envi-harness --test oracle_submodel3` + `compare` + `capability` green.
- `cargo test --workspace` green; `cargo run -p envi-harness -- report` shows Pass for in-scope straight-road cases, honest Skips otherwise.
- grep `conj()` over `crates/envi-engine/src/propagation/` = 0; `cargo tree -p envi-engine` unchanged.
- `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` clean.
</verification>

<success_criteria>
Advances ROADMAP Phase-4 success criterion 4 (the full FORCE road-traffic suite passes) for the straight-road groups. VAL-02 (core straight-road) demonstrated: SM3 + segmented refraction + screen guard + Ch.6 comparator + emission flip turn the in-scope straight-road cases numeric-green, with all remaining gaps honestly Skipped. Curved/city/yearly + forest complete VAL-02 in 04-04.
</success_criteria>

<output>
Create `.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-03-SUMMARY.md` when done.
</output>
