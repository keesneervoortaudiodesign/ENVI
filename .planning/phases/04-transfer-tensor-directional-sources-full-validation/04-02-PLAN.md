---
phase: 04-transfer-tensor-directional-sources-full-validation
plan: 02
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - crates/envi-engine/src/lib.rs
  - crates/envi-engine/src/directivity.rs
  - crates/envi-harness/src/emission/mod.rs
  - crates/envi-harness/src/emission/coefficients.rs
  - crates/envi-harness/src/emission/passby.rs
  - crates/envi-harness/src/lib.rs
  - crates/envi-harness/src/scene_build.rs
  - crates/envi-harness/tests/emission_anchor.rs
  - refs/fetch.sh
  - refs/refs.sha256
autonomous: true
requirements: [SRC-02, SRC-03, SRC-04]
must_haves:
  truths:
    - "A DirectivityBalloon stores ΔL(θ,φ,f) as a per-band spherical grid on the 105-point FREQ_AXIS and evaluates a direction via bilinear interpolation into a per-band dB slice"
    - "A hand-rolled 3×3 rotation applied to the src→rcv unit vector before frame lookup rotates the balloon; rotating a lobe away from a fixed receiver lowers that receiver's level (direction + magnitude, by band index)"
    - "A complex source composed of multiple directional sub-sources evaluates each sub-source independently into the tensor (SolveJob per sub-source), and the road source is its first concrete instance"
    - "The Nord2000 road source expands to sub-sources at 0.01 / 0.30 / 0.75 m, 1 m from the vehicle centre line toward the receiver (⇒ FORCE straight-road source line x = 3.5 m, superseding the Phase-1 2.5 m placeholder, Pitfall 1)"
    - "Rolling (tyre/road) and propulsion sound power are computed separately then energy-split 80/20 (rolling→low, propulsion→high), summed INCOHERENTLY across sub-sources per Annex A"
    - "The LE − dL free-field anchor (emission + directivity + pass-by integration + divergence only) is green on the flat cat-1 straight-road case family, gating the emission pipeline BEFORE any hard propagation lands"
  artifacts:
    - path: crates/envi-engine/src/directivity.rs
      provides: "DirectivityBalloon (private Array3<f64> [az,pol,band]), validating ctor, bilinear eval, 3×3 rotation"
      contains: "pub struct DirectivityBalloon"
      min_lines: 100
    - path: crates/envi-harness/src/emission/mod.rs
      provides: "RoadSource → Vec<SubSource> + balloons + per-sub-source G_s(f) weights"
      contains: "RoadSource"
      min_lines: 80
    - path: crates/envi-harness/src/emission/coefficients.rs
      provides: "Jonasson rolling/propulsion coefficient tables, provenance-commented, [ASSUMED]-quarantined where SP 2006:12 is unavailable"
    - path: crates/envi-harness/src/emission/passby.rs
      provides: "179-point 1° discretization, LE(f)/LAeq,24h/LAmax integration, oblique-profile 1/cosθ stretch"
    - path: crates/envi-harness/tests/emission_anchor.rs
      provides: "LE − dL free-field anchor test on flat cat-1 cases (refs-gated, fail-soft)"
  key_links:
    - from: crates/envi-harness/src/emission/mod.rs
      to: crates/envi-engine/src/directivity.rs
      via: "road analytic directivities (horn / vertical / heavy-horizontal) sampled onto balloons at construction"
      pattern: "DirectivityBalloon"
    - from: crates/envi-harness/src/emission/mod.rs
      to: crates/envi-engine/src/solver.rs
      via: "each (source-point × height) sub-source becomes a SolveJob with a pre-evaluated directivity_gain_db"
      pattern: "SolveJob|directivity_gain_db"
  prohibitions:
    - "NO new dependencies in envi-engine (directivity.rs is pure ndarray math; rotation is hand-rolled 3×3, no linalg crate — D-08 precedent)"
    - "NO coefficient value presented as verified unless transcribed from SP 2006:12 and checked against the PDF page images; every [ASSUMED] value carries a provenance comment and is validated by structure/anchor/property tests only — NEVER a false FORCE numeric Pass"
    - "NO coherent summation of road sub-sources — Annex A mandates incoherent addition (two co-located subs ⇒ +3 dB)"
    - "NO use of the Swedish KCB 2015-adjusted tables or Harmonoise/CNOSSOS tables as if DK-reference (wrong lineage — Pitfall 7)"
    - "NO reintroduction of the x = 2.5 m / height-0 Phase-1 placeholder for emission (superseded to x = 3.5 m and 0.01/0.30/0.75 m — Pitfall 1)"
    - "NO nominal-Hz keying of directivity or A-weighting — balloons live on FREQ_AXIS band indices"
---

<objective>
Deliver the directional multi-sub-source model (SRC-02/03/04) and its first concrete instance — the Nord2000 road source — feeding the 04-01 tensor.

**Phase-4 slice (MVP framing):** after this plan the engine can evaluate a *directional* source: a caller attaches a directivity balloon to a sub-source, composes several sub-sources into one source, and the road emission model produces the exact free-field pass-by spectrum a FORCE case expects — provable against the `.xls` `LE − dL` anchor without any hard propagation in the loop.

Purpose: the road source is the first real directional multi-sub-source instance; landing it here (paired with the balloons) lets emission go green on the `LE − dL` anchor before SM3/refraction wiring, so 04-03/04-04 debug propagation ONLY, never emission and propagation at once (04-RESEARCH placement rationale).
Output: `directivity.rs` (engine, pure math), `emission/{mod,coefficients,passby}.rs` (harness), the User's Guide added to `refs/fetch.sh`, and the `LE − dL` anchor integration test.

**Honest-green directive (no human stop):** the authoritative coefficient report SP Rapport 2006:12 is NOT freely available. Do NOT create a blocking-human checkpoint. Proceed **quarantined**: anchor the emission pipeline on the `.xls` `dL` column (`LE − dL` = free-field pass-by spectrum) plus the freely-available User's Guide structure; mark any coefficient that genuinely needs SP 2006:12 as `[ASSUMED]` in doc-comments (validated by structure/anchor/property tests, never presented as verified). This mirrors Phase-3's approved `[ASSUMED]` weather-constant posture (03-03 option (c)).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-RESEARCH.md
@.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-PATTERNS.md
@crates/envi-engine/src/scene.rs
@crates/envi-engine/src/freq.rs
@crates/envi-engine/src/solver.rs
@crates/envi-engine/src/tensor.rs
@crates/envi-harness/src/scene_build.rs
@crates/envi-harness/src/compare.rs
@crates/envi-harness/src/weather/route1.rs
@crates/envi-harness/src/weather/route3.rs
@crates/envi-harness/src/cases/mod.rs
@refs/fetch.sh
</context>

<artifacts_this_phase_produces>
- `crates/envi-engine/src/directivity.rs` — DirectivityBalloon + rotation + bilinear eval
- `crates/envi-harness/src/emission/mod.rs` — RoadSource → SubSources + balloons + G_s
- `crates/envi-harness/src/emission/coefficients.rs` — Jonasson tables (provenance-commented; [ASSUMED] where SP 2006:12 absent)
- `crates/envi-harness/src/emission/passby.rs` — LE/LAeq,24h/LAmax integration + oblique-profile stretch
- `crates/envi-harness/tests/emission_anchor.rs` — LE − dL free-field anchor
- `refs/fetch.sh` + `refs/refs.sha256` — User's Guide AV 1171/06 added
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: DirectivityBalloon (engine) — per-band spherical grid, bilinear eval, 3×3 rotation</name>
  <files>crates/envi-engine/src/directivity.rs, crates/envi-engine/src/lib.rs</files>
  <read_first>
    - crates/envi-engine/src/scene.rs BandSpectrum 97-123 (fixed-length discipline) + TerrainProfile::new 209-265 (validating ctor: finiteness, monotonicity, dims, never panic) + coordinate-convention module doc 1-25
    - crates/envi-engine/src/freq.rs (FREQ_AXIS ~106; band-index discipline 26-31)
    - crates/envi-harness/src/weather/route3.rs (hand-rolled Cramer 3×3, "no linalg crate (D-08)" module doc ~11)
    - 04-PATTERNS.md "directivity.rs — DirectivityBalloon" section
    - 04-RESEARCH.md Pattern 6 (balloon sketch) + Pitfall 8 (grazing-angle sampling error)
  </read_first>
  <behavior>
    - Test: a validating constructor rejects a grid with a non-N_BANDS band axis, non-finite ΔL, or non-monotonic angle axes with a typed error — never a panic.
    - Test: evaluating the balloon at a stored grid node returns that node's ΔL slice exactly; evaluating between nodes returns the bilinear interpolant.
    - Test (sampling error): a smooth analytic road directivity (e.g. a cos-power vertical lobe) sampled onto a 5° grid and read back at the 179 evaluation azimuths of the pass-by integration deviates < 0.05 dB from the analytic form (Pitfall 8).
    - Test (rotation): a balloon with a known front lobe evaluated toward a fixed receiver gives a high ΔL; rotating the lobe 90° away (3×3 rotation applied to the src→rcv unit vector before frame lookup) lowers that receiver's ΔL (direction + magnitude, asserted by band index).
  </behavior>
  <action>
    Create `crates/envi-engine/src/directivity.rs`. Define `pub struct DirectivityBalloon` with a PRIVATE `Array3<f64>` grid indexed `[azimuth, polar, band]` (band axis locked to N_BANDS), equal-angle resolutions (default 5°), poles stored as single duplicated rows. Document the angular convention up top exactly like scene.rs documents its coordinate convention.
    Give it a validating constructor returning a typed error (finiteness, grid monotonicity, band-axis length) modeled on `TerrainProfile::new`; never panic on data. Add a `DirectivityError` (thiserror) next to it or reuse a scene-style error.
    Implement `eval(&self, dir_local: [f64;3]) -> [f64; N_BANDS]`: convert the unit direction to (az, pol), bilinear-interpolate the grid, return the per-band ΔL dB slice. This slice is what a SolveJob's `directivity_gain_db` carries.
    Implement a hand-rolled `Rotation3` (3×3 matrix, engine-local, no linalg crate — D-08 precedent) with `apply(unit_vec)`; the caller rotates the src→rcv vector into the balloon frame before `eval`.
    Register `pub mod directivity;` in lib.rs. Add zero dependencies.
  </action>
  <acceptance_criteria>
    - Balloon ctor rejects malformed grids with typed errors (no panic).
    - Node/bilinear eval correctness tests pass.
    - Sampling-error test < 0.05 dB across the 179 pass-by azimuths.
    - Rotation direction test passes (lobe-away lowers the level).
    - `cargo tree -p envi-engine -e normal --depth 1` unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-engine directivity && cargo tree -p envi-engine -e normal --depth 1 | grep -Eiv '^envi-engine|ndarray|num-complex|thiserror' | grep -c . | grep -qx 0 && echo DEP_QUARANTINE_OK</automated>
  </verify>
  <done>DirectivityBalloon evaluates and rotates on the 105-band grid within the 0.05 dB sampling budget, adding zero engine deps.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Road emission model — coefficients (quarantined), sub-source expansion, balloons, G_s weights</name>
  <files>crates/envi-harness/src/emission/mod.rs, crates/envi-harness/src/emission/coefficients.rs, crates/envi-harness/src/scene_build.rs, crates/envi-harness/src/lib.rs, refs/fetch.sh, refs/refs.sha256</files>
  <read_first>
    - crates/envi-harness/src/scene_build.rs (whole — the CaseDefinition→engine trust boundary; the placeholders this task supersedes at 45-59 and 121-135; hSv/hRv via TerrainProfile::endpoints 113-119; refs-gated test style 182-233)
    - crates/envi-engine/src/scene.rs impedance_class 317-341 (the house transcribed-constant-table + provenance-comment pattern) and SubSource type 130-136
    - crates/envi-harness/src/cases/mod.rs 233-242 (NORD2000_DEFAULT_CV2/CT2 — the [ASSUMED]-quarantine precedent)
    - refs/fetch.sh ARTIFACTS list (~29-37) + refs/refs.sha256 format
    - 04-RESEARCH.md "Emission Model" section (heights/splits/directivities/corrections + acquisition ladder) + Pitfalls 1, 7 + Assumptions A1/A4/A7/A8
  </read_first>
  <behavior>
    - Test: RoadSource for cat-1 @ 80 km/h expands to sub-sources at 0.01 m and 0.30 m, 1 m toward the receiver (x = 3.5 m for the FORCE straight road); cat-2/3 expands to 0.01 m + 0.75 m.
    - Test: rolling energy splits 80% to the low source / 20% to the high; propulsion 80% high / 20% low; summing the two sub-source powers back reproduces the total per-band emission (energy conservation).
    - Test: two identical co-located road sub-sources combine +3 dB incoherently (Annex A), never +6 dB.
    - Test (provenance guard): every coefficient constant in coefficients.rs carries either `[CITED: SP 2006:12 …]` or `[ASSUMED: …]` in a doc comment; a test asserts the module compiles and that [ASSUMED] values are exercised only by structure/property tests (documented), not by any FORCE numeric-Pass path.
    - Test: refs/fetch.sh gains the User's Guide row and refs.sha256 pins its hash 73f465e2… .
  </behavior>
  <action>
    Create `crates/envi-harness/src/emission/mod.rs` and `coefficients.rs`. `RoadSource { category, speed_kmh, surface, temperature_c }` → `expand(receiver_dir) -> Vec<SubSource>` + per-sub-source `DirectivityBalloon` + per-sub-source `G_s(f)` (the emission L_W per 1/3-oct mapped onto the 105-grid by band index).
    In `coefficients.rs`, transcribe the Jonasson rolling (a_R,b_R) and propulsion (a_P,b_P) per-category coefficient tables IF SP 2006:12 is present in refs/ (verify each value against the PDF page images per the house rule). If SP 2006:12 is absent, populate the tables with `[ASSUMED]` placeholder structure and derive the flat-cat-1/DAC-12/80 km/h combined effective L_W(f) by fitting to the `LE − dL` anchor (Fallback B) — mark every fitted value `[ASSUMED: LE−dL fit]` with the fit provenance. Speed law `L_W = a + b·lg(v/v_ref)` [ASSUMED A1], v_ref per the User's Guide. Apply the DAC-12 surface correction [ASSUMED A8, transcribe from AV 1171/06 §3.1.7 figure] and the 15 °C temperature correction. Do NOT use KCB-2015 or Harmonoise/CNOSSOS tables as DK-reference (Pitfall 7).
    Attach directivities as balloons sampled at construction: frequency-dependent vertical (car-body screening) on every sub-source, frequency-dependent horizontal horn effect on the 0.01 m source, frequency-independent horizontal on the heavy 0.75 m propulsion source [ASSUMED A7 until SP 2006:12 confirms].
    In `scene_build.rs`, supersede `FORCE_LANE_X_M = 2.5` / `FORCE_PLACEHOLDER_SOURCE_H_M = 0.0` explicitly (leave a doc comment recording the Phase-1 decision this replaces) and route the single-placeholder SubSource construction through the emission expansion.
    Append the User's Guide to `refs/fetch.sh` ARTIFACTS and pin its sha256 in `refs/refs.sha256`. SP 2006:12, if the operator drops it into refs/, gets a manual-drop note + pinned hash (no URL row).
    Register `pub mod emission;` in the harness lib.rs.
  </action>
  <acceptance_criteria>
    - Sub-source heights/offset match cat-1 (0.01/0.30 @ x=3.5) and cat-2/3 (0.01/0.75).
    - 80/20 rolling/propulsion energy split conserves total per-band power.
    - +3 dB incoherent co-location test passes.
    - Every coefficient carries a provenance comment; no [ASSUMED] value reaches a FORCE numeric-Pass path.
    - User's Guide row + pinned hash present; `refs/fetch.sh` still runs.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-harness emission && grep -q 'Users_Guide_Nord2000_Road' refs/fetch.sh && grep -q '73f465e2' refs/refs.sha256 && echo REFS_OK</automated>
  </verify>
  <done>The road source expands to correctly-placed, directivity-carrying sub-sources with quarantined coefficients, and the User's Guide is pinned in refs/.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Pass-by integration + the LE − dL free-field anchor</name>
  <files>crates/envi-harness/src/emission/passby.rs, crates/envi-harness/tests/emission_anchor.rs, crates/envi-harness/src/lib.rs</files>
  <read_first>
    - crates/envi-harness/src/weather/route1.rs l_den 173-... (energy-weighted dB combination; typed CaseLoadError on non-finite) — the accumulate-energy-then-log shape to reuse; note its period hours must become parameterizable (Pitfall 4, exercised in 04-04)
    - crates/envi-harness/src/compare.rs a_weighting_db 340-347 (already exists; call at exact FREQ_AXIS centres) + pick_third_octave 320-322 (105→27 by index)
    - crates/envi-harness/src/cases/mod.rs 302-327 (SpectrumRow.le_db / dl_db fields the anchor reads)
    - 04-RESEARCH.md "Pass-by integration" + Pattern 7 (oblique-profile 1/cosθ stretch) + "The dL validation asset" + Pitfall 10 + Assumptions A2/A5
  </read_first>
  <behavior>
    - Test: the 179-point 1° discretization spans θ ∈ [−89°, +89°] with segment lengths ℓ_i = d⊥·[tan(θ_i+0.5°) − tan(θ_i−0.5°)] and time weights t_i = ℓ_i/v; a symmetry test confirms ±θ segments have equal weight for a centred receiver.
    - Test: LE(f) = 10·lg Σ_i (t_i/t₀)·10^{L_i(f)/10} reproduces a hand-computed two-segment case exactly.
    - Test: LAeq,24h = LAE + 10·lg(N) − 10·lg(86400) with N = 10000 gives LAE − 9.37 dB; a cross-check against the sheet's separate LAE row is within the loader's precision.
    - Test (the anchor): for the flat cat-1 straight-road case family, LE − dL (free-field: no ground/screen/air absorption) computed from the emission model + directivity + pass-by + divergence-only matches the `.xls`-derived free-field spectrum within ~0.3 dB per 1/3-oct band (refs-gated, fail-soft Skip when refs absent).
  </behavior>
  <action>
    Create `crates/envi-harness/src/emission/passby.rs`. Implement the 179-point 1° immission-angle discretization, the oblique cut-plane profile via the perpendicular-profile × 1/cosθ stretch (04-RESEARCH Pattern 7 — exact under lateral invariance [ASSUMED A5]), and the energy integrals: `LE(f)`, `LAE`, `LAeq,24h`, and `LAmax` = max over source points of the A-weighted instantaneous level (all sub-sources energy-summed) [ASSUMED A2, no Fast-time smearing]. Reuse the accumulate-energy-then-log shape from route1::l_den; A-weight via `compare::a_weighting_db` at exact FREQ_AXIS centres (never nominal Hz). Guard non-finite inputs with typed errors.
    The tensor is the natural home for this: sub_source axis = (source point × height); LE = the 04-01 incoherent readout with weights `t_i·10^{L_W/10}`; LAmax = a per-source-point partial readout. Wire the integration to run over the tensor's incoherent readout so 04-01 is load-bearing here, not parallel plumbing.
    Create `crates/envi-harness/tests/emission_anchor.rs`: refs-gated (skip with an eprintln pointer to refs/fetch.sh when the `.xls` is absent). Read `SpectrumRow.le_db - dl_db` per band, compute the free-field pass-by spectrum through the emission+directivity+pass-by+divergence-only path, and assert agreement by 27-band index within ~0.3 dB. Document the extent assumption per group (Pitfall 10).
  </action>
  <acceptance_criteria>
    - Discretization symmetry + LE integral + LAeq,24h conversion tests pass.
    - The LE − dL anchor is green on the flat cat-1 case family (or Skips cleanly when refs are absent) — NEVER a false Pass.
    - `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` clean; `cargo test --workspace` green.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-harness --test emission_anchor && cargo test -p envi-harness passby && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>Pass-by integration produces LE/LAeq,24h/LAmax over the tensor, and the LE − dL anchor validates the emission pipeline before any hard propagation is wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.xls` workbook → emission/loader | LE/dL cells, traffic parameters are operator-controlled untrusted input |
| coefficient data files → coefficients.rs | transcribed constants; wrong-lineage data corrupts every FORCE case by design |
| balloon grid data → DirectivityBalloon ctor | angle axes / ΔL values caller-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | wrong-lineage coefficients (KCB-2015 / Harmonoise) | mitigate | provenance comment on every constant; [ASSUMED] values validated by structure/anchor/property tests only; the `LE − dL` anchor gates the pipeline (Pitfall 7) |
| T-04-02-02 | Spoofing | corrupt User's Guide / SP 2006:12 dropped into refs/ | mitigate | sha256 pin in refs/refs.sha256 (73f465e2…); manual-drop note + pinned hash for SP 2006:12 |
| T-04-02-03 | Tampering | malformed balloon grid causing panic | mitigate | validating ctor with typed DirectivityError (finiteness / monotonic angles / band length); never panic on data |
| T-04-02-04 | Denial of Service | huge directivity grid / degenerate 1/cosθ at θ→±90° | mitigate | θ capped at ±89° (never ±90°, tan blow-up); grid dims validated; balloon size bounded by fixed resolution |
| T-04-02-05 | Repudiation | an [ASSUMED] value silently presented as verified | mitigate | doc-comment discipline + a test asserting no [ASSUMED] value feeds a FORCE numeric Pass (honest-green invariant) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new packages; `cargo tree -p envi-engine` gate re-run; refs artifacts sha256-pinned, git-ignored, never committed |
</threat_model>

<verification>
- `cargo test -p envi-engine directivity` green (balloon eval + rotation + sampling error).
- `cargo test -p envi-harness emission passby --test emission_anchor` green (or honest Skip when refs absent).
- `cargo tree -p envi-engine -e normal --depth 1` unchanged.
- User's Guide row in refs/fetch.sh + pinned hash in refs/refs.sha256.
- Every emission coefficient carries a provenance comment; no [ASSUMED] value on a Pass path.
- `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` clean.
</verification>

<success_criteria>
Maps to ROADMAP Phase-4 success criterion 3 (complex source of multiple directional sub-sources; rotating a balloon changes receiver levels in the expected direction). SRC-02 (directivity ΔL(θ,φ,f)), SRC-03 (independent multi-sub-source composition into the tensor), SRC-04 (per-band spherical balloons) demonstrated. The road source + LE − dL anchor is the emission foundation VAL-02 (04-03/04-04) builds on — emission goes green here, propagation is debugged there.
</success_criteria>

<output>
Create `.planning/phases/04-transfer-tensor-directional-sources-full-validation/04-02-SUMMARY.md` when done.
</output>
