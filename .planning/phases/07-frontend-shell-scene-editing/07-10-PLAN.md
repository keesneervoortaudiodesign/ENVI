---
phase: 07-frontend-shell-scene-editing
plan: 10
type: execute
wave: 7
depends_on: [07-07, 07-08, 07-09]
files_modified:
  - web/tests/e2e/sc1-author-every-kind.spec.ts
  - web/tests/e2e/sc4-persistence.spec.ts
  - web/tests/e2e/_mocks.ts
  - web/dist/index.html
  - crates/README.md
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-04, WEB-08, WEB-09, WEB-10, SCN-01, SCN-02, SCN-03, SCN-04]
must_haves:
  truths:
    - "An end-to-end journey authors every scene kind with last-object inheritance, edits an isolation spectrum, and persists via the mocked API (SC1 + SC3 integration)"
    - "The drawn scene survives a basemap switch (style.load rehydrate), a page reload (GET returns the same scene), and a project close/reopen (reopen-last restores) (SC4)"
    - "The final committed web/dist bundle contains the full app, serves through envi-service, and keeps the static-bundle contract test green"
    - "The crate README documents the new envi-dgm boundary + web/ frontend + the two new endpoints"
  artifacts:
    - path: "web/tests/e2e/sc4-persistence.spec.ts"
      provides: "SC4 end-to-end: basemap switch + reload + close/reopen persistence"
      contains: "reopen"
    - path: "web/dist"
      provides: "Final committed production bundle (real app)"
      contains: "root"
  key_links:
    - from: "web/tests/e2e/sc4-persistence.spec.ts"
      to: "web/src/store/sceneStore.ts"
      via: "store-canonical scene re-hydrates on style.load and reload; reopen-last restores"
      pattern: "style.load"
---

<objective>
Prove the four Success Criteria end-to-end and finalize the shipped artifact: full SC1 (author every kind + inheritance) and SC3 (spectrum) authoring journeys, and the SC4 persistence journey (basemap switch → reload → close/reopen), all fully offline; rebuild and commit the final `web/dist`; and update `crates/README.md` to document the new `envi-dgm` boundary, the `web/` frontend, and the two new endpoints.

Purpose: the per-feature plans each carried targeted tests; this plan validates the SC1–SC4 journeys as integrated user flows and locks the final bundle. It is the phase's goal-backward proof that the whole authoring surface holds together.
Output: two SC-level E2E specs, an updated `_mocks.ts` (reopen-last/reload fixtures), the final committed bundle, and README documentation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-shell-scene-editing/07-CONTEXT.md
@.planning/phases/07-frontend-shell-scene-editing/07-RESEARCH.md
@.planning/phases/07-frontend-shell-scene-editing/07-UI-SPEC.md
@.planning/phases/07-frontend-shell-scene-editing/07-07-SUMMARY.md
@.planning/phases/07-frontend-shell-scene-editing/07-08-SUMMARY.md
@.planning/phases/07-frontend-shell-scene-editing/07-09-SUMMARY.md
@crates/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC1 + SC3 end-to-end authoring journey</name>
  <files>web/tests/e2e/sc1-author-every-kind.spec.ts, web/tests/e2e/_mocks.ts</files>
  <read_first>
    - 07-RESEARCH.md Validation Architecture SC1 + SC3 rows (draw each kind, assert store + mocked PUT; interpolate octave/third anchors on exact indices; live preview matches server)
    - .planning/ROADMAP.md Phase 7 SC1/SC3 wording
    - web/tests/e2e/_mocks.ts (07-06/07-07/07-09 installApiMocks/installBasemapMocks + interpolate fixture) + the draw/spectrum specs from 07-07/07-08 (reuse helpers, do not duplicate)
  </read_first>
  <action>
    Create `web/tests/e2e/sc1-author-every-kind.spec.ts` (offline mocks): a single integrated journey — place a directional source (with a sound-power spectrum), a receiver, the calc_area, a building, a wall, a ground_zone (impedance A–H + roughness N/S/M/L via the enum selects), a forest (density/stem-radius/height), and elevation points/lines; assert last-object inheritance fires on the second object of a kind; open the spectrum editor on the wall, enter octave values, and assert the mocked interpolate preview places anchors on band indices 4,16,…,100 and the live preview matches the server response (SC3); assert the whole authored scene is in the coalesced PUT payload. Extend `_mocks.ts` with the interpolate-spectrum fixture returning a deterministic 105-grid so the preview assertion is exact. Keep it a black-box drive of the real built bundle.
  </action>
  <verify>
    <automated>cd web && npx playwright test sc1-author-every-kind</automated>
  </verify>
  <acceptance_criteria>
    - The journey places all 9 kinds and asserts each is in the store + mocked PUT payload with a valid `properties.kind`.
    - Ground_zone is authored via enum selects (A–H / N/S/M/L); forest via numeric fields; last-object inheritance verified on a repeated kind.
    - The spectrum preview places authored octave anchors on indices 4,16,…,100 and matches the mocked server 105-grid (SC3).
    - The spec runs fully offline; green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SC4 persistence journey (basemap switch + reload + reopen)</name>
  <files>web/tests/e2e/sc4-persistence.spec.ts, web/tests/e2e/_mocks.ts</files>
  <read_first>
    - 07-RESEARCH.md Validation Architecture SC4 row (draw → toggle basemap [style.load rehydrate] → reload [GET returns same] → close/reopen [reopen-last restores])
    - 07-CONTEXT.md D-03 (store canonical; Terra Draw controlled view; style.load rehydrate) + Phase-6 reopen-last endpoint (GET /projects/last)
    - web/tests/e2e/lifecycle.spec.ts (07-06 — basemap-switch rehydrate already proven; extend to full reload + reopen) + web/tests/e2e/_mocks.ts
  </read_first>
  <action>
    Create `web/tests/e2e/sc4-persistence.spec.ts` (offline mocks): draw a representative scene; (1) toggle the basemap (setStyle) and assert every feature is still rendered after `style.load` re-hydration; (2) reload the page and assert the mocked `GET /projects/{id}/scene` returns the same scene and the store re-hydrates identically; (3) simulate project close then reopen-last (`GET /projects/last`) and assert the scene is restored. Extend `_mocks.ts` so the mocked PUT captures the scene and the subsequent GET/reopen returns exactly that captured payload (round-trip fidelity — "the persisted scene is what comes back"). Fully offline.
  </action>
  <verify>
    <automated>cd web && npx playwright test sc4-persistence</automated>
  </verify>
  <acceptance_criteria>
    - Features survive a basemap switch (style.load rehydrate), a page reload (GET returns the captured scene), and a close/reopen (reopen-last restores) — all asserted.
    - The mocked GET/reopen returns exactly the scene captured by the preceding PUT (round-trip fidelity).
    - The spec runs fully offline; green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Final bundle rebuild + README documentation</name>
  <files>web/dist/index.html, crates/README.md</files>
  <read_first>
    - crates/README.md (crate boundaries + quarantine gates — add envi-dgm + the web/ frontend + the two new endpoints; keep the documentation contract)
    - 07-PATTERNS.md call-out (c) (web/dist stays committed + load-bearing; contract test marker) + Shared Patterns "Quarantine gates"
    - .claude/CLAUDE.md ("Codebase as built" conventions + Module I/O headers + doc-consistency gate)
  </read_first>
  <action>
    Run `npm run build` to produce the FINAL `web/dist` bundle (full app: shell + map + draw + panels + spectrum + validation) and commit it (web/dist stays tracked). Update `crates/README.md`: add the `envi-dgm` crate to the boundary map (spade lives here, never in envi-engine; does not depend on envi-engine), document the `web/` frontend package (React+Vite+TSX, generated wire types, devDependency-only tooling), and record the two new endpoints (`POST /meta/interpolate-spectrum`, `POST /dgm/triangulate`) and the isolation/forest DTOs. Ensure the quarantine-gate commands section still lists the three engine gates plus the new `cargo tree -p envi-dgm` check (spade present, envi-engine absent). Confirm the static-bundle contract test still passes against the final bundle marker.
  </action>
  <verify>
    <automated>cd web && npm run build && cd .. && cargo test -p envi-service static_bundle_served_with_spa_fallback && cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `web/dist` is rebuilt with the full app and committed; `web/dist/index.html` references no external asset (offline gate green).
    - `cargo test -p envi-service static_bundle_served_with_spa_fallback` passes against the final bundle; full `cargo test` green.
    - `crates/README.md` documents envi-dgm, the web/ frontend, and the two new endpoints; the quarantine-gate section includes the `cargo tree -p envi-dgm` check.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **E2E** `web/tests/e2e/sc1-author-every-kind.spec.ts` (SC1 + SC3 integrated journey)
- **E2E** `web/tests/e2e/sc4-persistence.spec.ts` (SC4 basemap switch + reload + reopen)
- **Mock extensions** in `web/tests/e2e/_mocks.ts` (interpolate fixture + PUT-capture round-trip)
- **Final bundle** `web/dist/*` (committed production build of the full app)
- **Docs** `crates/README.md` updated (envi-dgm boundary, web/ frontend, two new endpoints)
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| static file server → browser | final bundle served to the client |
| test harness → network | E2E must stay offline |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10-01 | Spoofing | E2E silently hitting the live network | mitigate | All /api/* + basemap style/tiles/glyphs page.route-intercepted; un-mocked requests fail the test (offline invariant, D-13a) |
| T-07-10-02 | Tampering | external assets sneaking into the final bundle | mitigate | Final web/dist/index.html grep-gated for zero external assets; Phase-6 offline gate stays green |
| T-07-10-03 | Information Disclosure | shipped bundle contents | accept | web/dist is the intended public artifact; no secrets (light/no-auth localhost, PROJECT.md) |

*Auth/crypto: N/A — localhost single-user (PROJECT.md), accepted. No new package install.*
</threat_model>

<verification>
- `web/`: `npx playwright test` (full suite incl. sc1 + sc4) + `npm run build` pass, fully offline.
- `cargo test` green (static-bundle contract test against the final bundle).
- `cargo clippy --all-targets -- -D warnings`; `cargo fmt --check`.
- `cargo tree -p envi-engine -e normal --depth 1` exactly ndarray+num-complex+thiserror; `git diff --quiet crates/envi-engine/`.
- `cargo tree -p envi-dgm` shows spade, NOT envi-engine; `cargo tree | grep -ci 'proj-sys\|gdal'` returns 0.
- `grep -rh '\.conj()' crates/envi-engine/src/propagation/ | grep -vc '^\s*//'` returns 0.
- `grep -Ec 'https?://' web/dist/index.html` returns 0.
</verification>

<success_criteria>
All four Success Criteria are proven by integrated offline E2E journeys, the final full-app bundle is committed and serves through envi-service with the contract test green, and the README documents the phase's new boundaries and endpoints.
</success_criteria>

<output>
Create `.planning/phases/07-frontend-shell-scene-editing/07-10-SUMMARY.md` when done.
</output>
