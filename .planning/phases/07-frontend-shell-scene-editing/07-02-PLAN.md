---
phase: 07-frontend-shell-scene-editing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/envi-dgm/Cargo.toml
  - crates/envi-dgm/src/lib.rs
  - crates/envi-dgm/src/tin.rs
autonomous: true
requirements: [WEB-04]
must_haves:
  truths:
    - "A constrained-Delaunay TIN builds from ≥3 non-collinear elevation points plus non-crossing breaklines and interpolates a plausible Z at an interior query point"
    - "Interior-intersecting breaklines are REJECTED with a typed 4xx-mappable error, never a spade panic / thread abort"
    - "Degenerate input (0 points, 1 point, all-collinear, non-finite coordinate) is rejected with a typed error, never a 500 or panic"
    - "spade lives ONLY in envi-dgm; envi-dgm does not depend on envi-engine, so spade can never reach the engine's 3-dep quarantine"
  artifacts:
    - path: "crates/envi-dgm/Cargo.toml"
      provides: "New pure-Rust TIN boundary crate (D-08); spade + thiserror only"
      contains: "spade"
    - path: "crates/envi-dgm/src/tin.rs"
      provides: "Constrained-Delaunay TIN build + barycentric Z interpolation with pre-checked constraints"
      contains: "ConstrainedDelaunayTriangulation"
    - path: "crates/envi-dgm/src/lib.rs"
      provides: "Crate root + DgmError (Archetype A pure-domain error)"
      contains: "pub enum DgmError"
  key_links:
    - from: "crates/envi-dgm/src/tin.rs"
      to: "crates/envi-dgm/src/lib.rs"
      via: "TIN build returns Result<_, DgmError>; intersecting/degenerate input maps to typed variants"
      pattern: "DgmError::"
---

<objective>
Create the new `envi-dgm` crate (D-08): a pure-Rust, server-side constrained-Delaunay TIN built with `spade` 2.15 from user-drawn `elevation_point` vertices and `elevation_line` breaklines, with barycentric Z interpolation. It must pre-check constraints so `spade`'s interior-intersection PANIC (07-RESEARCH Pitfall 3) becomes a typed 4xx-mappable rejection, and reject degenerate input. `spade` lives HERE and never in `envi-engine`.

Purpose: SC1 requires elevation points/lines that "re-triangulate the DGM." This plan builds the TIN math inside the phase boundary — no terrain import, no C `gdal` (deferred to Phase 8). Phase 8 later extends the SAME seam by feeding imported samples as additional vertices.
Output: the crate scaffold (copied from `envi-geo`), `DgmError`, and the `tin.rs` TIN builder + interpolator with unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-shell-scene-editing/07-CONTEXT.md
@.planning/phases/07-frontend-shell-scene-editing/07-RESEARCH.md
@.planning/phases/07-frontend-shell-scene-editing/07-PATTERNS.md
@crates/README.md
@crates/envi-geo/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold envi-dgm crate + DgmError (copy envi-geo shape)</name>
  <files>crates/envi-dgm/Cargo.toml, crates/envi-dgm/src/lib.rs</files>
  <read_first>
    - crates/envi-geo/Cargo.toml (exact scaffold: workspace inheritance on version/edition/rust-version; one-line boundary description; boundary-comment block above [dependencies]; no [lints] table)
    - crates/envi-geo/src/lib.rs (module-doc + `#![deny(unsafe_code)]` + GeoError shape at line 64-119 — the Archetype A pure-domain error to mirror)
    - Cargo.toml (root; `members = ["crates/*"]` at line 3 — envi-dgm joins automatically, NO root edit)
    - 07-PATTERNS.md "envi-dgm/Cargo.toml" and "DgmError (Archetype A)" sections (exact variant list)
    - 07-RESEARCH.md Pitfall 3 (the failure modes DgmError variants must cover)
  </read_first>
  <action>
    Create `crates/envi-dgm/Cargo.toml`: package `envi-dgm`, one-line boundary `description` naming D-08 and "spade lives HERE, never in envi-engine"; `version.workspace/edition.workspace/rust-version.workspace = true`; a boundary-rule comment block directly above `[dependencies]` stating pure-Rust CDT, zero C toolchain, zero I/O, NO serde in the runtime graph, NO dependency on envi-engine; `[dependencies] spade = "2.15"`, `thiserror = "2"`; `[dev-dependencies]` only if an oracle/property test needs approx. Create `crates/envi-dgm/src/lib.rs` with a `//!` module-doc (what/why + Boundary statement mirroring envi-geo's GEOX-04 block + House rules: f64 throughout, typed errors never panics-on-data), `#![deny(unsafe_code)]`, `pub mod tin;`, and `pub enum DgmError` deriving `#[derive(Debug, Error, PartialEq)]` with struct variants carrying the offending values: `TooFewPoints { got: usize }`, `IntersectingConstraint { a: [f64;2], b: [f64;2] }`, `NonFinite { what: String }`, `Triangulation { message: String }` (spade errors captured as a message string to keep `PartialEq`, mirroring `GeoError::Proj`). Cite D-08.
  </action>
  <verify>
    <automated>cargo build -p envi-dgm && cargo tree -p envi-dgm -e normal --depth 1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p envi-dgm` succeeds; `cargo tree -p envi-dgm -e normal --depth 1` lists `spade` and `thiserror` and does NOT list `envi-engine`.
    - `crates/envi-dgm/src/lib.rs` contains `#![deny(unsafe_code)]` and `pub enum DgmError` with the four struct variants; `DgmError` derives `PartialEq`.
    - `cargo tree | grep -ci 'proj-sys\|gdal'` returns 0 (spade is pure Rust — zero-C-crate gate stays green).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Constrained-Delaunay TIN + barycentric Z (tin.rs) with panic-proof pre-checks</name>
  <files>crates/envi-dgm/src/tin.rs</files>
  <read_first>
    - crates/envi-dgm/src/lib.rs (created Task 1 — DgmError variants)
    - crates/envi-geo/src/transform.rs (role+flow analog: a pure transform module with typed errors + Module I/O header)
    - 07-RESEARCH.md "Gate 3: spade constrained-Delaunay TIN with Z interpolation" code example (method signatures: `insert`, `add_constraint_edges`, `can_add_constraint`, `intersects_constraint`, `barycentric().interpolate`) + Pitfall 3
  </read_first>
  <behavior>
    - build_tin(points ≥3 non-collinear, no breaklines) → Ok; interpolate_z at an interior point returns Some(z) between the surrounding vertex Zs
    - build_tin(2 points OR all-collinear points) → Err(DgmError::TooFewPoints { .. })
    - build_tin with two breaklines that cross in their interior → Err(DgmError::IntersectingConstraint { .. }) — NO panic
    - build_tin with a non-finite coordinate → Err(DgmError::NonFinite { .. })
    - interpolate_z outside the convex hull → None (documented, not an error)
  </behavior>
  <action>
    Add a `//!` Module I/O header (inputs: elevation points {x,y,z} + breakline polylines; output: a queryable TIN + Z interpolation; invariant: never panics on data). Define an `ElevVertex { pos: Point2<f64>, z: f64 }` implementing `HasPosition`. Provide `pub fn build_tin(points: &[[f64;3]], breaklines: &[Vec<[f64;2]>]) -> Result<Tin, DgmError>` and a `Tin` wrapper with `pub fn interpolate_z(&self, x: f64, y: f64) -> Option<f64>` (via `barycentric().interpolate`). BEFORE inserting: validate every coordinate finite (`NonFinite`) and reject if fewer than 3 distinct non-collinear points (`TooFewPoints`). For each breakline segment, PRE-CHECK with `can_add_constraint`/`intersects_constraint` (or `try_add_constraint`) against already-added constraints and return `IntersectingConstraint { a, b }` on interior crossing — NEVER call the panicking `add_constraint_edges` path without the guard (Pitfall 3). Cap point count and breakline count with a documented bound (DoS defense, Security Domain) and return a typed error above the cap. Wrap any residual spade `InsertionError` as `DgmError::Triangulation { message }`. Add `#[cfg(test)] mod tests` covering every behavior above. No `unwrap`/`panic!` on data paths.
  </action>
  <verify>
    <automated>cargo test -p envi-dgm && cargo clippy -p envi-dgm --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - A test with a known square of 4 points at Z=0,0,10,10 asserts `interpolate_z` at the centre ≈ 5.0 (within 1e-9).
    - A test with two interior-crossing breaklines asserts `matches!(build_tin(..), Err(DgmError::IntersectingConstraint { .. }))` and the test process does NOT abort (no panic).
    - A test with 2 points and a separate all-collinear test each assert `Err(DgmError::TooFewPoints { .. })`.
    - A test with `f64::NAN` in a coordinate asserts `Err(DgmError::NonFinite { .. })`.
    - `cargo clippy -p envi-dgm --all-targets -- -D warnings` and `cargo fmt --check` pass; no `unwrap()`/`panic!` in non-test code (grep-confirmable).
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Crate** `envi-dgm` (new workspace member via `crates/*` glob — no root Cargo edit)
- **Package dep** `spade = "2.15"` (quarantined to this crate)
- **Enum** `envi_dgm::DgmError { TooFewPoints, IntersectingConstraint, NonFinite, Triangulation }`
- **Module** `crates/envi-dgm/src/tin.rs` (`pub mod tin`)
- **Struct** `envi_dgm::tin::ElevVertex`, `envi_dgm::tin::Tin`
- **Function** `envi_dgm::tin::build_tin(&[[f64;3]], &[Vec<[f64;2]>]) -> Result<Tin, DgmError>`
- **Method** `Tin::interpolate_z(f64, f64) -> Option<f64>`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user-drawn elevation → TIN builder | Untrusted point/breakline sets cross into the geometry engine |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | DoS | `add_constraint_edges` on interior-crossing breaklines | mitigate | Pre-check `intersects_constraint`/`can_add_constraint` (or `try_add_constraint`); return `IntersectingConstraint` 4xx — never panic the thread (Pitfall 3) |
| T-07-02-02 | DoS | huge point/breakline set | mitigate | Cap point + breakline count per build; reject above the documented bound before O(n log n) triangulation |
| T-07-02-03 | Tampering | non-finite / degenerate coordinates | mitigate | Reject non-finite (`NonFinite`) and <3 non-collinear points (`TooFewPoints`) before insert |
| T-07-02-SC | Tampering | `cargo add spade` | accept | RESEARCH Package Legitimacy Audit: spade 2.15.1 = OK/Approved (crates.io, high downloads, public repo, pure Rust, no C deps). Not [ASSUMED]/[SUS] → no blocking-human checkpoint required |
| T-07-02-04 | Elevation of Privilege | spade reaching engine quarantine | mitigate | envi-dgm does NOT depend on envi-engine; `cargo tree -p envi-dgm` confirms spade cannot reach the engine graph |

*Access control / auth / crypto: N/A — localhost single-user tool, accepted (PROJECT.md).*
</threat_model>

<verification>
- `cargo test -p envi-dgm` green.
- `cargo clippy --all-targets -- -D warnings`; `cargo fmt --check`.
- `cargo tree -p envi-dgm -e normal --depth 1` shows spade+thiserror, NOT envi-engine.
- `cargo tree -p envi-engine -e normal --depth 1` still exactly ndarray+num-complex+thiserror; `git diff --quiet crates/envi-engine/`.
- `cargo tree | grep -ci 'proj-sys\|gdal'` returns 0.
</verification>

<success_criteria>
`envi-dgm` builds a constrained-Delaunay TIN with barycentric Z interpolation, rejects interior-crossing and degenerate input as typed errors (never a panic), and keeps spade fully quarantined from the engine.
</success_criteria>

<output>
Create `.planning/phases/07-frontend-shell-scene-editing/07-02-SUMMARY.md` when done.
</output>
