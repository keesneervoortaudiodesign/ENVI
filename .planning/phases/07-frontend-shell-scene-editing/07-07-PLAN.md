---
phase: 07-frontend-shell-scene-editing
plan: 07
type: execute
wave: 4
depends_on: [07-06, 07-04, 07-03]
files_modified:
  - web/src/draw/kinds.ts
  - web/src/draw/modes.ts
  - web/src/panels/Palette.tsx
  - web/src/panels/Inspector.tsx
  - web/src/panels/fields/GroundZoneFields.tsx
  - web/src/panels/fields/ForestFields.tsx
  - web/src/panels/fields/SourceFields.tsx
  - web/src/store/sceneStore.ts
  - web/src/store/inheritance.ts
  - web/src/store/dgm.ts
  - web/src/dgm/dgmTrigger.ts
  - web/src/map/DgmOverlay.tsx
  - web/src/map/MapCanvas.tsx
  - web/src/api/client.ts
  - web/tests/e2e/draw-kinds.spec.ts
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-04, SCN-04]
must_haves:
  truths:
    - "The object palette lists select/pan + all 9 locked kinds; selecting a tool activates the matching Terra Draw mode and drawing a shape adds a feature (with properties.kind) to the canonical store"
    - "Each drawn kind's non-geometric properties are editable in the inspector: ground_zone impedance A–H and roughness N/S/M/L are closed-enum SELECTS (no free numeric entry), forest density/stem-radius/height are numeric, source sub-source position + spectrum slot render"
    - "A new object of kind K inherits its non-geometric properties from the last-committed object of kind K this session, with a visible 'inherited from last {kind}' chip that clears on edit"
    - "A committed change to the elevation_point/elevation_line set triggers a debounced POST /dgm/triangulate whose success renders an observable TIN overlay on the map (SC1 re-triangulation), and whose 4xx reject state is stored for the validation panel (07-09)"
    - "All values reach the DOM as text only (no dangerouslySetInnerHTML); the fetch client imports types from generated wire.ts, never re-declaring them"
  artifacts:
    - path: "web/src/draw/kinds.ts"
      provides: "The 9-kind discriminated union + per-kind TD-mode/palette metadata (never-exhaustive)"
      contains: "ground_zone"
    - path: "web/src/panels/Inspector.tsx"
      provides: "Property inspector dispatching to per-kind field components (dense rows)"
      contains: "Inspector"
    - path: "web/src/store/inheritance.ts"
      provides: "Per-kind session-scoped last-object property inheritance (WEB-04)"
      contains: "lastOf"
    - path: "web/src/dgm/dgmTrigger.ts"
      provides: "Debounced elevation-set → POST /dgm/triangulate producer (SC1 re-triangulation)"
      contains: "triangulate"
  key_links:
    - from: "web/src/draw/modes.ts"
      to: "web/src/store/sceneStore.ts"
      via: "TD finish/change adds features with properties.kind into the canonical store"
      pattern: "kind"
    - from: "web/src/api/client.ts"
      to: "web/src/generated/wire.ts"
      via: "fetch client imports wire types (D-10), never hand-declares them"
      pattern: "generated/wire"
---

<objective>
Author every scene object kind (SC1): the object palette + per-kind Terra Draw modes for all 9 locked kinds, a property inspector with per-kind field components (ground_zone impedance A–H + roughness N/S/M/L as closed-enum selects; forest density/stem-radius/height; source sub-source position + spectrum slot; receiver/building/wall/elevation/calc_area basics), last-object property inheritance (WEB-04), and a typed fetch client that imports the generated wire types (D-10). Drawing writes into the canonical store (D-03).

Purpose: this is the core "place and edit every kind with last-object inheritance" of SC1. The isolation-spectrum editor + per-façade + SPL calibration land in 07-08; draw-time validation + autosave land in 07-09.
Output: `draw/kinds.ts`, `draw/modes.ts`, `panels/Palette.tsx`, `panels/Inspector.tsx` + per-kind field components, `store/inheritance.ts`, `api/client.ts`, and a draw-each-kind E2E.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-shell-scene-editing/07-CONTEXT.md
@.planning/phases/07-frontend-shell-scene-editing/07-RESEARCH.md
@.planning/phases/07-frontend-shell-scene-editing/07-UI-SPEC.md
@.planning/phases/07-frontend-shell-scene-editing/07-06-SUMMARY.md
@.planning/phases/07-frontend-shell-scene-editing/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 9-kind palette + per-kind Terra Draw modes + typed fetch client</name>
  <files>web/src/draw/kinds.ts, web/src/draw/modes.ts, web/src/panels/Palette.tsx, web/src/api/client.ts, web/src/store/sceneStore.ts</files>
  <read_first>
    - 07-UI-SPEC.md "Object Palette — All 9 Locked Kinds" (the KINDS table: per-kind TD mode point/linestring/polygon, map render tokens, icon glyph, palette design principle hue×geometry×symbol) + "The App Shell" palette region (~200px, --row-h-lg rows, .dot in kind hue, active tool = --color-primary tint)
    - crates/envi-store/src/geojson.rs KINDS (line 38 — the frozen 9-kind vocabulary; properties.kind must be one of these)
    - 07-PATTERNS.md "web/src/api/*.ts (fetch client) — DIVERGENCE FLAG" (copy the fetch/SSE plumbing shape from metrao3 api.ts but import types from ../generated/wire, never re-declare)
    - web/src/store/sceneStore.ts (07-06) + web/src/map/useTerraDraw.ts (07-06 — TD modes list to extend) + web/src/generated/wire.ts (07-04 types)
  </read_first>
  <action>
    Create `web/src/draw/kinds.ts`: the 9-kind discriminated union (`kind: "source" | "receiver" | "wall" | "building" | "forest" | "ground_zone" | "elevation_point" | "elevation_line" | "calc_area"`) with per-kind metadata (TD mode name, palette icon name, kind hue token — from the UI-SPEC palette table, NO invented tokens) and a `never`-exhaustive helper so a missing case is a compile error (D-09). Create `web/src/draw/modes.ts`: map each kind to its Terra Draw mode (point/linestring/polygon/select) and the active-tool → mode switch. Create `web/src/panels/Palette.tsx`: the vertical tool list (select/pan + 9 kinds), each row `--row-h-lg`, `svgIcon` + label + `.dot` in the kind hue, active tool = `--color-primary` tint + primary left-border. On finishing a shape, the new feature is written to the store with `properties.kind` set (via `applyTerraDrawChange`/`finish`). Create `web/src/api/client.ts`: typed fetch wrappers (GET/PUT project scene, GET freq-axis, POST interpolate-spectrum, POST dgm/triangulate) importing request/response types from `../generated/wire` (D-10) — NEVER re-declaring them. Module I/O headers on all files.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `draw/kinds.ts` enumerates exactly the 9 frozen KINDS and has a `never`-exhaustive switch (a removed case fails `tsc`).
    - `Palette.tsx` renders 9 kind rows + select/pan; selecting a tool activates the corresponding TD mode (grep: kind→mode mapping used).
    - `api/client.ts` imports from `../generated/wire` and declares no local interface duplicating a wire type (`grep -c "interface .*Dto\|type .*Dto" web/src/api/client.ts` returns 0).
    - `npx tsc --noEmit` + `npm run build` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Property inspector + per-kind field components (enums + numerics)</name>
  <files>web/src/panels/Inspector.tsx, web/src/panels/fields/GroundZoneFields.tsx, web/src/panels/fields/ForestFields.tsx, web/src/panels/fields/SourceFields.tsx</files>
  <read_first>
    - 07-UI-SPEC.md "Ground-Zone Inspector Fields" (impedance A–H select with σ display-only suffix, class B = 31.5; roughness N/S/M/L select — CLOSED ENUMS, never free numeric) + "Forest Inspector Fields" (density/stem-radius/height numeric, zero density = warn) + "Source Calibration" (sub-source position [x,y,z] three .mono inputs; L_W spectrum "Edit spectrum" slot) + "State Matrix" (inspector empty-state, dense --row-h rows)
    - 07-UI-SPEC.md "Accessibility + Safety" Never-innerHTML invariant (all values via textContent/React children)
    - web/src/generated/wire.ts (07-04 — GroundSegmentDto/ForestParamsDto/SourceDto field shapes) + web/src/store/sceneStore.ts
  </read_first>
  <action>
    Create `web/src/panels/Inspector.tsx`: the right-rail property inspector that dispatches on the selected feature's `kind` to a per-kind field component; empty-state "Select an object to edit its properties." when nothing selected; `.dense` rows (`--row-h` 28px). Create `GroundZoneFields.tsx`: impedance class as a `<select>` with options A–H (class letter + σ as a `--color-text-muted` `.mono` display-only suffix, class B = 31.5) and roughness as a `<select>` N/S/M/L — closed enums, structurally rejecting out-of-vocabulary values (no text input). Create `ForestFields.tsx`: density (trees/m²), stem radius (m), height (m) as `.mono` `.dense` numeric inputs; zero density flagged (the warn surfaces in the validation panel in 07-09). Create `SourceFields.tsx`: sub-source position `[x,y,z]` as three `.mono` inputs + a "Edit spectrum" button slot (the editor itself is 07-08). Building/wall/receiver/elevation/calc_area basic property rows can be minimal inline in the Inspector dispatch. Edits write to the store. Every value reaches the DOM as a React text child — no `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Ground-zone impedance and roughness render as `<select>` elements (grep for `<select`), NOT text inputs; impedance options are exactly A–H and roughness exactly N/S/M/L; class B shows σ = 31.5.
    - Forest fields render three numeric inputs (density/stem-radius/height).
    - Source fields render three position inputs + an "Edit spectrum" trigger.
    - `grep -rc 'dangerouslySetInnerHTML' web/src/` returns 0.
    - `npx tsc --noEmit` + `npm run build` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Last-object property inheritance + draw-each-kind E2E</name>
  <files>web/src/store/inheritance.ts, web/src/store/sceneStore.ts, web/src/panels/fields/GroundZoneFields.tsx, web/src/panels/fields/ForestFields.tsx, web/tests/e2e/draw-kinds.spec.ts</files>
  <read_first>
    - 07-UI-SPEC.md "Last-Object Property Inheritance" (per-kind session-scoped; new object of kind K seeded from last-committed kind K; `.chip.info` "inherited from last {kind}" until edited, chip clears on edit; first-of-kind uses documented defaults e.g. ground default class 'D')
    - 07-RESEARCH.md Validation Architecture SC1 row (E2E: draw each of 9 kinds, assert feature in store and in the mocked PUT payload)
    - web/src/store/sceneStore.ts + web/tests/e2e/_mocks.ts (07-06 installApiMocks/installBasemapMocks)
  </read_first>
  <action>
    Create `web/src/store/inheritance.ts`: a per-kind, session-scoped record of the last-committed non-geometric properties (`lastOf(kind)`), and a seeding function applied when a new object of kind K is finished — copying the previous kind-K properties, marking each seeded field "inherited" so the inspector can show the `.chip.info` "inherited from last {kind}" chip (chip clears when the user edits the field). First-of-kind objects fall back to documented defaults (ground class 'D', etc.). Wire the seeding into the store's finish/commit path and thread the "inherited" markers into `GroundZoneFields`/`ForestFields`. Create `web/tests/e2e/draw-kinds.spec.ts`: with `installApiMocks`+`installBasemapMocks`, drive drawing (or programmatic store commits) of each of the 9 kinds; assert each feature lands in the store with the right `properties.kind`; assert that drawing a second ground_zone inherits the first's impedance/roughness and shows the "inherited" chip until edited; assert the outbound (mocked) PUT payload contains all placed features. Fully offline.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx playwright test draw-kinds</automated>
  </verify>
  <acceptance_criteria>
    - Drawing a second forest/ground_zone seeds its properties from the first and shows the "inherited from last {kind}" chip; the chip clears once the field is edited (asserted in the E2E).
    - The draw-kinds E2E places all 9 kinds and asserts each appears in the store and in the mocked PUT payload with a valid `properties.kind`.
    - The E2E runs fully offline (no un-mocked network); `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: DGM re-triangulation producer — trigger + TIN overlay (SC1)</name>
  <files>web/src/dgm/dgmTrigger.ts, web/src/map/DgmOverlay.tsx, web/src/store/dgm.ts, web/src/store/sceneStore.ts</files>
  <read_first>
    - .planning/ROADMAP.md Phase 7 SC1 ("elevation points/lines that re-triangulate the DGM" — the observable requirement this task delivers) + 07-CONTEXT.md D-08 (server-side spade TIN from elevation_point vertices + elevation_line breaklines)
    - 07-RESEARCH.md Pattern 4 (debounced committed-edit trigger — reuse the finish-not-change debounce; do NOT re-trigger on every drag frame) + Open Question 3 RESOLVED (stateless POST /dgm/triangulate; TIN not persisted — the source elevation objects are the persisted truth)
    - 07-03-SUMMARY.md (POST /api/v1/dgm/triangulate request/response shape: points + breaklines → triangles; 4xx on interior-cross/degenerate) + web/src/api/client.ts (Task 1 — the dgm/triangulate wrapper) + web/src/generated/wire.ts (DgmReq/DgmResp)
    - web/src/store/sceneStore.ts (elevation_point/elevation_line features) + web/src/map/MapCanvas.tsx (07-06 — where an overlay layer attaches)
  </read_first>
  <action>
    Create `web/src/dgm/dgmTrigger.ts`: subscribe to committed changes of the elevation_point / elevation_line set and, DEBOUNCED (~750 ms on committed edits only, reusing the D-04 finish-not-change discipline — never per drag frame), assemble a `DgmReq` from the store's elevation features and call the `POST /api/v1/dgm/triangulate` client wrapper (07-07 Task 1). On success, write the returned triangulation into a `web/src/store/dgm.ts` slice; on a 4xx, write the reject (status + `detail`) into that slice so 07-09's ValidationPanel can surface it as the `crit` row (the error surface itself is 07-09 — this task only PRODUCES the request and stores the outcome). Create `web/src/store/dgm.ts`: the DGM state slice (`triangulation`, `rejectReason`) with setters. Create `web/src/map/DgmOverlay.tsx`: a MapLibre overlay layer rendering the TIN edges/faces from the store slice (recessive `--color-info`/`--color-border` tokens, no invented tokens) mounted into the map canvas; the overlay updates when the slice changes. Skip triangulation (clear the overlay) for <3 non-collinear points rather than firing a doomed request. Every subscription torn down in its effect cleanup. Module I/O headers; all values via text/React children.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Committing a valid elevation set (≥3 non-collinear points) fires exactly one debounced `POST /dgm/triangulate` (not one per drag frame) and renders a TIN overlay from the response (asserted via the mocked E2E in 07-10).
    - A 4xx from the endpoint writes `rejectReason` into the `dgm` store slice (the source the 07-09 ValidationPanel crit row reads) — grep confirms `dgmTrigger.ts` sets the reject state, not a silent swallow.
    - The trigger reuses the committed-edit debounce (grep: no triangulate call inside the raw TD `change`/drag path).
    - `web/src/store/dgm.ts` exposes `triangulation` + `rejectReason`; `DgmOverlay.tsx` reads the slice and mounts on the map.
    - `npx tsc --noEmit` + `npm run build` pass.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Module** `web/src/draw/kinds.ts` (9-kind discriminated union + per-kind metadata, never-exhaustive)
- **Module** `web/src/draw/modes.ts` (kind → Terra Draw mode mapping)
- **Component** `web/src/panels/Palette.tsx` (object palette, 9 kinds + select/pan)
- **Component** `web/src/panels/Inspector.tsx` (property inspector dispatch)
- **Components** `web/src/panels/fields/{GroundZoneFields,ForestFields,SourceFields}.tsx`
- **Module** `web/src/store/inheritance.ts` (`lastOf`, per-kind session seeding)
- **Module** `web/src/api/client.ts` (typed fetch client importing wire.ts)
- **Module** `web/src/dgm/dgmTrigger.ts` (debounced elevation-set → POST /dgm/triangulate producer, SC1)
- **Store slice** `web/src/store/dgm.ts` (`triangulation`, `rejectReason`)
- **Component** `web/src/map/DgmOverlay.tsx` (TIN overlay on the map)
- **Store slice extension** in `sceneStore.ts` (commit/finish → store + inheritance)
- **E2E** `web/tests/e2e/draw-kinds.spec.ts`

> **Scope decision (checker warning 6):** 07-07 is kept as one cohesive "author-every-kind" plan (now 4 tasks / 14 files) rather than split. All four tasks share `sceneStore.ts`; splitting palette/modes+client from inspector/fields would add a wave-chain hop against the same serialized store without reducing that serialization. The DGM producer is co-located here because SC1 re-triangulation is exactly what the elevation kinds *do* when drawn. If an executor finds context strained, the natural cut is Task 1 (palette/modes/client) as a separate wave-4 sibling.
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server strings → DOM | scene/object names, class labels reach the inspector |
| client → API | fetch client sends scene payloads |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering (XSS) | inspector field values, object ids | mitigate | All values via React text children / textContent; no dangerouslySetInnerHTML (grep-gated 0); inline SVG icons only |
| T-07-07-02 | Tampering | out-of-vocabulary impedance/roughness | mitigate | Closed-enum `<select>` for A–H and N/S/M/L — an invalid class is structurally impossible client-side; server `TryFrom` remains the backstop |
| T-07-07-03 | Tampering | wire type drift | mitigate | Fetch client imports generated wire.ts (D-10); no hand-declared DTO to drift |

*Auth/crypto: N/A — localhost single-user (PROJECT.md), accepted. No new package install.*
</threat_model>

<verification>
- `web/`: `npx tsc --noEmit`, `npm run build`, `npx playwright test draw-kinds` pass.
- `grep -rc 'dangerouslySetInnerHTML' web/src/` returns 0.
- Rust unchanged: `cargo test` green; `git diff --quiet crates/envi-engine/`.
</verification>

<success_criteria>
Every one of the 9 kinds can be placed and edited, ground/roughness are closed-enum selects, forest/source fields render, last-object inheritance works with a visible clearing chip, and the fetch client is typed from wire.ts — proven by an offline draw-each-kind E2E.
</success_criteria>

<output>
Create `.planning/phases/07-frontend-shell-scene-editing/07-07-SUMMARY.md` when done.
</output>
