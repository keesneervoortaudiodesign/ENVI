---
phase: 07-frontend-shell-scene-editing
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/vite.config.ts
  - web/tsconfig.json
  - web/playwright.config.ts
  - web/index.html
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/theme.css
  - web/src/app.css
  - web/src/icons.ts
  - web/dist/index.html
  - crates/envi-service/tests/contract_meta_static.rs
  - .gitignore
autonomous: true
requirements: [WEB-01]
user_setup:
  - service: nodejs
    why: "The repo has no JS toolchain yet; the frontend build (Vite) requires Node.js + npm"
    env_vars: []
must_haves:
  truths:
    - "npm install + npm run build produces a self-contained web/dist bundle with zero external assets in index.html"
    - "The dark ops-console theme tokens are present verbatim (metrao3 system-font variant); no token is invented"
    - "envi-service serves the real bundle and the static-bundle contract test passes against a stable marker in the new index.html"
    - "web/dist stays git-tracked; web/node_modules, web/test-results, web/playwright-report are git-ignored"
  artifacts:
    - path: "web/package.json"
      provides: "The repo's first npm package; runtime deps + devDependency-only tooling (D-09)"
      contains: "react-map-gl"
    - path: "web/src/theme.css"
      provides: "metrao3 dark ops-console tokens copied verbatim (D-11, system-font variant)"
      contains: "--color-primary"
    - path: "web/src/App.tsx"
      provides: "App shell: four persistent regions (project bar, palette, map slot, right rail) on the token grid"
      contains: "App"
  key_links:
    - from: "crates/envi-service/src/api/mod.rs"
      to: "web/dist/index.html"
      via: "ServeDir(web/dist).fallback(index.html) serves the real bundle (SVC-03)"
      pattern: "index.html"
---

<objective>
Bootstrap the `web/` React + Vite + TypeScript frontend (D-09), adopt the metrao3 dark ops-console theme verbatim (D-11/D-12, system-font variant), build a minimal app shell (the four layout regions from the UI-SPEC), produce a real committed `web/dist` bundle, update the Phase-6 static-bundle contract test to a stable marker, and add the frontend `.gitignore` entries. This is the toolchain bootstrap the whole frontend depends on.

Purpose: the repo has no JS toolchain; every later frontend plan needs `web/` scaffolded, the theme tokens present, and the build pipeline serving through `envi-service`. The basemap and drawing land in the next plan (07-06).
Output: `web/` package + config + theme + icons + shell + a committed real bundle; the contract test updated so `cargo test` stays green; `.gitignore` updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-shell-scene-editing/07-CONTEXT.md
@.planning/phases/07-frontend-shell-scene-editing/07-RESEARCH.md
@.planning/phases/07-frontend-shell-scene-editing/07-PATTERNS.md
@.planning/phases/07-frontend-shell-scene-editing/07-UI-SPEC.md
@crates/envi-service/src/api/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: web/ toolchain scaffold + theme + icons (metrao3 conventions)</name>
  <files>web/package.json, web/vite.config.ts, web/tsconfig.json, web/playwright.config.ts, web/index.html, web/src/main.tsx, web/src/theme.css, web/src/icons.ts, .gitignore</files>
  <read_first>
    - 07-PATTERNS.md sections "web/package.json", "web/vite.config.ts", "web/tsconfig.json", "web/playwright.config.ts", "web/src/theme.css", "web/src/icons.ts" (exact conventions + the DIVERGENCE flags: ENVI has real runtime deps, adds @vitejs/plugin-react, jsx:react-jsx)
    - 07-RESEARCH.md Standard Stack (exact pinned versions) + "Recommended Project Structure" + Installation block
    - D:\====CLAUDE\metrao3\crates\metrao3-web\ui\src\theme.css (READ-ONLY — copy VERBATIM into web/src/theme.css; system-font variant, NOT the portal variant)
    - D:\====CLAUDE\metrao3\crates\metrao3-web\ui\src\icons.ts (READ-ONLY — the inline-SVG DOM-construction pattern; 0..16 viewBox, stroke currentColor, never innerHTML)
    - .claude/CLAUDE.md ("Frontend testing" — Playwright devDependency only, offline, artifacts git-ignored) + current .gitignore
  </read_first>
  <action>
    Create `web/package.json`: `"type":"module"`, `"private":true`, `"engines":{"node":">=20"}`; scripts `dev`/`build` (= `tsc --noEmit && vite build`)/`preview`/`test:e2e`/`test:e2e:install`; `dependencies`: react 19, react-dom 19, maplibre-gl 5, react-map-gl 8, terra-draw 1, terra-draw-maplibre-gl-adapter 1, zustand 5, @turf/turf 7 (pins per 07-RESEARCH); `devDependencies` ONLY: vite 8, typescript, @vitejs/plugin-react, @playwright/test 1, @types/react, @types/react-dom. Create `vite.config.ts` (base `"./"`, target es2022, cssCodeSplit false, emptyOutDir true, `outDir: "dist"`, React plugin, Module I/O header). Create `tsconfig.json` copying metrao3's strictness verbatim (`strict`, `noUnusedLocals`, `noUnusedParameters`, `noFallthroughCasesInSwitch`, `verbatimModuleSyntax`, `isolatedModules`, `moduleResolution:"bundler"`) plus `"jsx":"react-jsx"` and `"lib":["ES2022","DOM","DOM.Iterable"]`. Create `playwright.config.ts` (webServer drives the real bundle, `reuseExistingServer:!CI`). Create `web/index.html` as the Vite root — ZERO external assets (no CDN, no web font, no external stylesheet/script), a stable `<title>ENVI</title>` and a `<div id="root">` mount + a stable marker comment the contract test can assert. Copy `metrao3 ui/src/theme.css` VERBATIM to `web/src/theme.css` (system-font variant; DO NOT invent tokens). Port `icons.ts` (DOM-constructed inline SVG, never innerHTML). Create `web/src/main.tsx` mounting `<App/>` into `#root` importing theme.css + app.css. Add `.gitignore` entries `web/node_modules/`, `web/test-results/`, `web/playwright-report/` (keep `web/dist/` TRACKED). Run `npm install` in `web/`.
  </action>
  <verify>
    <automated>cd web && npm install && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `web/package.json` lists react-map-gl, maplibre-gl, terra-draw, zustand, @turf/turf under `dependencies` and vite/typescript/@playwright/test under `devDependencies` only.
    - `npx tsc --noEmit` in `web/` passes with the strict config.
    - `web/src/theme.css` contains `--color-primary: #4ea8ff` and the `ok`/`warn`/`crit`/`off` severity tokens (verbatim from metrao3); it references no web-font name (`grep -c "Inter\|JetBrains" web/src/theme.css` returns 0).
    - `web/index.html` has zero external asset references (`grep -Ec 'https?://|cdn|fonts.googleapis' web/index.html` returns 0).
    - `.gitignore` contains `web/node_modules/`, `web/test-results/`, `web/playwright-report/` and does NOT ignore `web/dist/`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: App shell (four regions) + real web/dist bundle + contract-test update</name>
  <files>web/src/App.tsx, web/src/app.css, web/dist/index.html, crates/envi-service/tests/contract_meta_static.rs</files>
  <read_first>
    - 07-UI-SPEC.md "The App Shell — Layout Regions" (the four persistent regions + overlay; region primitives .topbar/.panel; --row-h/--row-h-lg control heights; 44px retained only for primary/destructive)
    - 07-PATTERNS.md "web/index.html" + call-out (c) (web/dist stays committed and load-bearing; the contract-test assertion `html.contains("frontend arrives in Phase 7")` must be UPDATED to a stable real-bundle marker)
    - crates/envi-service/tests/contract_meta_static.rs (the `static_bundle_served_with_spa_fallback` test + its placeholder-text assertion around lines 86-118)
    - D:\====CLAUDE\metrao3\crates\metrao3-web\ui\src\styles.css (READ-ONLY — .panel/.topbar/.btn usage reference; do not copy wholesale, layer ENVI CSS on tokens)
  </read_first>
  <action>
    Create `web/src/App.tsx`: the app shell with the four persistent regions from the UI-SPEC — project bar (`.topbar`, sticky), object palette rail (`.panel`, ~200px, the 9-kind tool list placeholder rows), a map-canvas slot (empty div now; MapLibre lands in 07-06), and the right rail (`.panel` stack: property inspector empty-state + validation panel empty-state). Control heights from `--row-h-lg`/`--row-h`; 44px min only on the primary Save + destructive actions (D-12). No new tokens. Create `web/src/app.css` layering ENVI component CSS on the theme tokens (grid layout for the shell). Every new `.tsx`/`.ts` file carries the `// # Module I/O` header. Run `npm run build` to produce `web/dist/` (index.html + hashed assets) — the real bundle REPLACES the Phase-6 placeholder. Update `contract_meta_static.rs`: change the placeholder-text assertion (`"frontend arrives in Phase 7"`) to assert a STABLE marker present in the real built `index.html` (e.g. the `<title>ENVI` string or the `#root` div id) so the `static_bundle_served_with_spa_fallback` test verifies the real bundle without coupling to hashed asset names.
  </action>
  <verify>
    <automated>cd web && npm run build && cd .. && cargo test -p envi-service static_bundle_served_with_spa_fallback</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` writes `web/dist/index.html` + hashed JS/CSS assets; `web/dist/index.html` still references no external asset (offline gate green).
    - `App.tsx` renders the four regions (project bar, palette, map slot, right rail) — verifiable by grep for the region class names / testids in the built markup or source.
    - `cargo test -p envi-service static_bundle_served_with_spa_fallback` passes against the updated stable marker (not the removed placeholder string).
    - `git check-ignore web/dist` exits non-zero (web/dist tracked); `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` pass (Rust side unchanged except the test assertion).
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **npm package** `web/package.json` (first JS toolchain in the repo) — runtime deps + dev-only tooling
- **Config** `web/vite.config.ts`, `web/tsconfig.json`, `web/playwright.config.ts`
- **Entry** `web/index.html` (Vite root, zero external assets), `web/src/main.tsx`
- **Component** `web/src/App.tsx` (app shell, four regions)
- **CSS** `web/src/theme.css` (verbatim metrao3 tokens), `web/src/app.css` (ENVI layout)
- **Util** `web/src/icons.ts` (inline SVG DOM pattern)
- **Bundle** `web/dist/*` (real Vite build, committed, replaces placeholder)
- **Updated test** `contract_meta_static.rs::static_bundle_served_with_spa_fallback` (stable marker)
- **npm scripts:** `dev`, `build`, `preview`, `test:e2e`, `test:e2e:install`
- **.gitignore entries:** `web/node_modules/`, `web/test-results/`, `web/playwright-report/`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build tooling → shipped bundle | npm dependency tree is new attack surface; only runtime deps may ship |
| static file server → browser | envi-service serves web/dist to the client |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-SC | Tampering | first `npm install` (react/maplibre/terra-draw/zustand/turf + dev tooling) | accept | RESEARCH Package Legitimacy Audit: all packages OK/Approved (npm, very-high downloads, public repos, official orgs). None [ASSUMED]/[SUS] → no blocking-human checkpoint. Build tooling is devDependencies ONLY — nothing tooling ships in the bundle |
| T-07-05-01 | Tampering | external assets in index.html | mitigate | Zero external assets in index.html (grep-gated); web fonts forbidden; system-font stack only (offline gate stays green) |
| T-07-05-02 | Information Disclosure | served static bundle | accept | web/dist is the intended public artifact; no secrets in the bundle (light/no-auth localhost, PROJECT.md) |

*Auth/session/crypto: N/A — localhost single-user (PROJECT.md), accepted.*
</threat_model>

<verification>
- `npm install` + `npx tsc --noEmit` + `npm run build` succeed in `web/`.
- `cargo test -p envi-service` green (updated static-bundle contract test).
- `cargo clippy --all-targets -- -D warnings`; `cargo fmt --check`.
- `git diff --quiet crates/envi-engine/`; `cargo tree -p envi-engine -e normal --depth 1` unchanged.
- `grep -Ec 'https?://' web/dist/index.html` returns 0.
</verification>

<success_criteria>
The `web/` toolchain exists (D-09), the dark theme tokens are present verbatim (D-11/D-12), the app shell renders the four layout regions, the real bundle serves through envi-service with the contract test green, and web/dist stays tracked while node_modules/test artifacts are ignored.
</success_criteria>

<output>
Create `.planning/phases/07-frontend-shell-scene-editing/07-05-SUMMARY.md` when done.
</output>
