---
phase: 07-frontend-shell-scene-editing
plan: 04
type: execute
wave: 3
depends_on: [07-01, 07-03, 07-05]
files_modified:
  - crates/envi-store/Cargo.toml
  - crates/envi-store/src/dto.rs
  - crates/envi-service/Cargo.toml
  - crates/envi-service/src/api/meta.rs
  - crates/envi-service/src/api/dgm.rs
  - crates/envi-service/src/jobs.rs
  - crates/envi-service/tests/wire_no_drift.rs
  - web/src/generated/wire.ts
autonomous: true
requirements: [WEB-02, WEB-08, WEB-09, WEB-10]
must_haves:
  truths:
    - "Every wire DTO (envi-store + envi-service, including the new isolation/forest/interpolate/dgm types and JobStatus) derives ts-rs and exports into a single committed web/src/generated/wire.ts"
    - "JobStatus's internally-tagged enum (#[serde(tag='state')]) renders as a real TS discriminated union in wire.ts (no zod fallback needed)"
    - "A no-drift test regenerates the types and asserts byte-equality with the committed wire.ts — a renamed Rust field fails cargo test, not the browser"
    - "envi-engine remains byte-identical; ts-rs is added ONLY to envi-store/envi-service, never to the engine"
  artifacts:
    - path: "web/src/generated/wire.ts"
      provides: "Committed generated TS mirror of the ~27 Rust wire types (D-10)"
      contains: "JobStatus"
    - path: "crates/envi-service/tests/wire_no_drift.rs"
      provides: "Regenerate-and-compare no-drift guard (oracle-fixture pattern)"
      contains: "wire"
  key_links:
    - from: "crates/envi-store/src/dto.rs"
      to: "web/src/generated/wire.ts"
      via: "#[ts(export, export_to=...)] emits each DTO into the committed wire.ts"
      pattern: "ts\\(export"
    - from: "crates/envi-service/tests/wire_no_drift.rs"
      to: "web/src/generated/wire.ts"
      via: "test regenerates to TempDir and asserts byte-equality with the committed file"
      pattern: "wire.ts"
---

<objective>
Generate the TypeScript wire contract from the Rust serde DTOs (D-10): add `ts-rs` 12 derives + `#[ts(export, export_to=…)]` to the ~27 wire types across `envi-store` (13 DTOs, incl. the new isolation/forest DTOs) and `envi-service` (interpolate/dgm req+resp, JobStatus, project/calc types), export a single committed `web/src/generated/wire.ts`, and add a no-drift test that regenerates and asserts byte-equality. `envi-engine` stays byte-identical (ts-rs never enters it).

Purpose: `res.json()` is `any`; a hand-written TS mirror is a second source of truth with nothing enforcing agreement — a renamed Rust field would compile clean and fail in the browser. Generation makes drift structurally impossible, mirroring the oracle-fixture "commit-the-artifact + test-asserts-no-drift" pattern.
Output: ts-rs derives, `web/src/generated/wire.ts`, `wire_no_drift.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-shell-scene-editing/07-CONTEXT.md
@.planning/phases/07-frontend-shell-scene-editing/07-RESEARCH.md
@.planning/phases/07-frontend-shell-scene-editing/07-PATTERNS.md
@crates/README.md
@.planning/phases/07-frontend-shell-scene-editing/07-01-SUMMARY.md
@.planning/phases/07-frontend-shell-scene-editing/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ts-rs derives on envi-store DTOs (13 + new isolation/forest)</name>
  <files>crates/envi-store/Cargo.toml, crates/envi-store/src/dto.rs</files>
  <read_first>
    - crates/envi-store/src/dto.rs (all 13 DTOs: BandSpectrumDto, SubSourceDto, SourceDto, ReceiverDto, BarrierDto, BuildingDto, GroundSegmentDto, TerrainProfileDto, CrsDto, MetDto, SettingsDto, ProjectMetaDto, ConditioningDto + the new IsolationSpectrumDto/AuthoredSpectrumDto/ForestParamsDto from 07-01)
    - 07-RESEARCH.md Pattern 7 (ts-rs derive + export_to="../../web/src/generated/wire.ts") + Pitfall 5 (`[f64;105] → number[]` length erased — accept, enforced server-side; `Uuid → string` needs the ts-rs uuid feature) + Assumptions A2
    - 07-PATTERNS.md "ts-rs generated wire types + no-drift test" (provenance-header convention; export path)
  </read_first>
  <action>
    Add `ts-rs = "12"` to `crates/envi-store/Cargo.toml` `[dependencies]` (enable the `serde-compat` default + the uuid feature flag — verify the exact ts-rs 12 feature name for `Uuid → string`, A2/Pitfall 5, at wiring time; consult ts-rs 12 Cargo features via Context7/docs). On every wire DTO add `#[derive(..., ts_rs::TS)]` and `#[ts(export, export_to = "../../web/src/generated/wire.ts")]` so all types emit into ONE file. Accept `[f64;105] → number[]` (length erased — enforced server-side by the existing `BadBandCount` check; document it in a comment). Ensure the generated file gets a `// GENERATED by cargo test (ts-rs) from envi-store/envi-service DTOs — DO NOT EDIT` banner (ts-rs header or a wrapper). Do NOT add ts-rs to envi-engine.
  </action>
  <verify>
    <automated>cargo test -p envi-store export_bindings 2>/dev/null; cargo test -p envi-store</automated>
  </verify>
  <acceptance_criteria>
    - `ts-rs = "12"` appears in `envi-store/Cargo.toml`; running the export (via `cargo test`) writes the store DTOs into `web/src/generated/wire.ts`.
    - The isolation/forest DTOs from 07-01 are present in the generated file.
    - `git diff --quiet crates/envi-engine/`; `cargo tree -p envi-engine -e normal --depth 1` unchanged (ts-rs not in engine).
    - `cargo clippy --all-targets -- -D warnings`; `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ts-rs derives on envi-service types + JobStatus union + no-drift test</name>
  <files>crates/envi-service/Cargo.toml, crates/envi-service/src/api/meta.rs, crates/envi-service/src/api/dgm.rs, crates/envi-service/src/jobs.rs, crates/envi-service/tests/wire_no_drift.rs, web/src/generated/wire.ts</files>
  <read_first>
    - crates/envi-service/src/jobs.rs line 50 (`JobStatus` `#[serde(tag="state")]`, snake_case tags queued/running/done/failed/cancelled — "the contract Phase-7 binds its EventSource handling to")
    - crates/envi-service/src/api/meta.rs (InterpolateReq/Resp from 07-03) + api/dgm.rs (DgmReq/Resp from 07-03) + api/projects.rs + api/calc.rs (the 14 service wire types)
    - 07-RESEARCH.md "Gate 2: JobStatus as a real TS discriminated union" (ts-rs 12 serde-compat honors tag/rename_all → real union; no zod fallback) 
    - 07-PATTERNS.md call-out (a) + "wire.ts no-drift test" (put the single no-drift #[test] in crates/envi-service/tests/wire_no_drift.rs — it sees both DTO sets; regenerate to TempDir + assert byte-equality OR git diff --exit-code)
    - crates/envi-harness/tests/oracle_ground.rs (the commit-the-artifact / test-asserts-no-drift mechanism to mirror)
  </read_first>
  <action>
    Add `ts-rs = "12"` to `crates/envi-service/Cargo.toml`. Add `#[derive(ts_rs::TS)]` + `#[ts(export, export_to = "../../web/src/generated/wire.ts")]` to the service wire types: `JobStatus` (verify serde-compat emits the discriminated union — `{state:"running"; progress; message}` etc., per Gate 2), `InterpolateReq/Resp`, `DgmReq/Resp`, `SubmitResponse`, `ReconditionRequest/Response`, `RecomputeRequest/Response/Reason`, `OriginDto`, `CreateProjectRequest`, `UpdateProjectRequest`, `FreqAxisDto`, `JobId`. Run the export so `web/src/generated/wire.ts` contains the full union of ~27 types; commit it. Create `crates/envi-service/tests/wire_no_drift.rs`: a `#[test]` that re-runs the ts-rs export to a `TempDir` (or the crate's out dir) and asserts BYTE-EQUALITY against the committed `web/src/generated/wire.ts` (via `common::repo_web_dist()`/manifest-relative path), failing on any diff — the "generate-at-dev-time, commit-the-artifact, test-asserts-no-drift" contract. Assert specifically that the emitted `JobStatus` is a discriminated union with a `state` discriminant.
  </action>
  <verify>
    <automated>cargo test -p envi-service wire_no_drift && git diff --quiet crates/envi-engine/ && cargo tree -p envi-engine -e normal --depth 1</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/generated/wire.ts` exists, carries the DO-NOT-EDIT provenance banner, and contains `JobStatus` rendered as a union with variants keyed on `state` (`"queued"|"running"|...`) including the payload-carrying `running` variant (`progress`, `message`).
    - `crates/envi-service/tests/wire_no_drift.rs` passes; if a DTO field is renamed without regenerating, the test FAILS (drift caught in Rust, not the browser) — verifiable by a temporary rename smoke-check during development.
    - `cd web && npx tsc --noEmit` still passes with `wire.ts` present (valid TS).
    - `git diff --quiet crates/envi-engine/`; `cargo tree -p envi-engine` unchanged.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Dep** `ts-rs = "12"` on `envi-store` and `envi-service` (never on envi-engine)
- **Derives** `ts_rs::TS` + `#[ts(export, export_to=…)]` on the ~27 wire DTOs
- **Generated committed file** `web/src/generated/wire.ts` (single-file union of all wire types, DO-NOT-EDIT banner)
- **Test** `crates/envi-service/tests/wire_no_drift.rs` (regenerate + byte-equality no-drift guard)
- **TS type** `JobStatus` as a real discriminated union in wire.ts
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Rust serde source ↔ TS wire types | Generated artifact must stay in sync or the browser sees `any` |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | wire.ts drifting from the Rust source | mitigate | Generated + committed; no-drift test fails cargo test on any drift (drift structurally impossible, not merely detected) |
| T-07-04-SC | Tampering | `cargo add ts-rs` | accept | RESEARCH Package Legitimacy Audit: ts-rs 12.0.1 = OK/Approved (crates.io, high downloads, Aleph-Alpha public repo). Not [ASSUMED]/[SUS] → no blocking-human checkpoint |
| T-07-04-02 | Elevation of Privilege | ts-rs reaching engine quarantine | mitigate | ts-rs added only to envi-store/envi-service; `cargo tree -p envi-engine` stays 3-dep; `git diff crates/envi-engine/` empty |
| T-07-04-03 | Tampering | `[f64;105]` length erased in TS | accept | Documented; runtime length enforced server-side by `BadBandCount` — the browser cannot forge a valid 105-length via the type alone |

*Auth/crypto: N/A — localhost single-user (PROJECT.md), accepted.*
</threat_model>

<verification>
- `cargo test -p envi-service wire_no_drift` + full `cargo test` green.
- `cd web && npx tsc --noEmit` passes with wire.ts.
- `cargo tree -p envi-engine -e normal --depth 1` exactly ndarray+num-complex+thiserror; `git diff --quiet crates/envi-engine/`.
- `cargo clippy --all-targets -- -D warnings`; `cargo fmt --check`.
</verification>

<success_criteria>
All wire types are generated from the serde source into a committed `wire.ts` (D-10), JobStatus is a real discriminated union, and the no-drift test makes divergence fail the Rust build — with the engine untouched.
</success_criteria>

<output>
Create `.planning/phases/07-frontend-shell-scene-editing/07-04-SUMMARY.md` when done.
</output>
