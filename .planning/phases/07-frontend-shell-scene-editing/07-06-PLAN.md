---
phase: 07-frontend-shell-scene-editing
plan: 06
type: execute
wave: 2
depends_on: [07-05]
files_modified:
  - web/src/map/MapCanvas.tsx
  - web/src/map/useTerraDraw.ts
  - web/src/map/basemap.ts
  - web/src/store/sceneStore.ts
  - web/src/App.tsx
  - web/tests/e2e/_mocks.ts
  - web/tests/e2e/lifecycle.spec.ts
autonomous: true
requirements: [WEB-01]
must_haves:
  truths:
    - "A MapLibre dark-vector basemap renders with OSM attribution, and a Terra Draw instance is created exactly once (StrictMode double-mount produces no duplicate instance)"
    - "The Zustand store is the single source of truth: user draws write into the store; the store re-adds features into Terra Draw; TD echoes of the store's own addFeatures (origin === 'api') are ignored (no feedback loop)"
    - "After map.setStyle() (basemap switch), features re-hydrate from the store on the single style.load hook and remain visible"
    - "Every imperative map/draw/protocol subscription created in an effect is torn down in that effect's cleanup; the E2E suite runs fully offline (basemap style/tiles/glyphs page.route-intercepted)"
  artifacts:
    - path: "web/src/map/useTerraDraw.ts"
      provides: "Instance-in-ref Terra Draw lifecycle hook with StrictMode guard + change/finish wiring (Gate-1 core)"
      contains: "drawRef"
    - path: "web/src/store/sceneStore.ts"
      provides: "Canonical Zustand scene store: features by id, applyTerraDrawChange, terraDrawFeatures (D-03)"
      contains: "applyTerraDrawChange"
    - path: "web/src/map/basemap.ts"
      provides: "Dark basemap style config (OpenFreeMap styles/dark) + OSM AttributionControl (D-13a)"
      contains: "attribution"
  key_links:
    - from: "web/src/map/useTerraDraw.ts"
      to: "web/src/store/sceneStore.ts"
      via: "TD change handler calls store.applyTerraDrawChange; rehydrate reads store.terraDrawFeatures"
      pattern: "applyTerraDrawChange"
    - from: "web/src/map/MapCanvas.tsx"
      to: "web/src/map/basemap.ts"
      via: "MapLibre <Map> uses the dark basemap style + AttributionControl"
      pattern: "basemap"
---

<objective>
Land the flagged Gate-1 Terra Draw ⇄ react-map-gl lifecycle spike (research's mandated standalone first UI plan): a MapLibre dark-vector basemap under a Terra Draw controlled view held in a ref, with the Zustand store as the single source of truth (D-03), `style.load` re-hydration after `setStyle()` (SC4 core), a StrictMode double-mount guard, and an offline Playwright test proving the store-canonical loop and basemap interception. No per-kind drawing yet — that is 07-07.

Purpose: `map.setStyle()` tears down every source and layer and StrictMode double-mounts in dev; getting the lifecycle wrong silently deletes the scene or double-binds handlers. This plan de-risks all later drawing/editor plans by proving the lifecycle once.
Output: `MapCanvas.tsx`, `useTerraDraw.ts`, `basemap.ts`, a skeletal `sceneStore.ts`, the basemap page.route mocks, and a lifecycle E2E spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-shell-scene-editing/07-CONTEXT.md
@.planning/phases/07-frontend-shell-scene-editing/07-RESEARCH.md
@.planning/phases/07-frontend-shell-scene-editing/07-UI-SPEC.md
@.planning/phases/07-frontend-shell-scene-editing/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonical Zustand store skeleton + basemap config (D-03/D-13a)</name>
  <files>web/src/store/sceneStore.ts, web/src/map/basemap.ts</files>
  <read_first>
    - 07-RESEARCH.md Pattern 1 (app-store-canonical: change handler ignores context.origin === "api"; rehydrate via draw.clear()+addFeatures) + Pattern 6/System Architecture diagram (store shape: features by id, selection, dirty)
    - 07-RESEARCH.md "Gate 4: dark basemap wiring" (Option B OpenFreeMap `https://tiles.openfreemap.org/styles/dark`, no API key; AttributionControl; D-13a "no API key ≠ no network")
    - 07-UI-SPEC.md "Map canvas" region + Color section (dark basemap ≈ --color-bg luminance; OSM attribution bottom-right)
    - web/src/App.tsx (07-05 shell — the map-canvas slot to fill)
  </read_first>
  <action>
    Create `web/src/store/sceneStore.ts`: a Zustand store holding the canonical scene — `features: Map<id, Feature>` (or record), `selection`, `dirty` flag, and the methods `applyTerraDrawChange(ids, type, snapshot)` (write user-driven geometry into the store), `terraDrawFeatures()` (return the FeatureCollection to re-add into TD), plus `select(id)`/`markDirty()` stubs. A 105-band spectrum is scene data — it lives HERE keyed by feature/edge id, NEVER in TD feature properties (D-03/anti-pattern). Create `web/src/map/basemap.ts`: export the dark basemap style config (OpenFreeMap `styles/dark`, D-13a) and a helper to attach `new maplibregl.AttributionControl({ compact: true })` showing OSM (CLAUDE.md data-hygiene). Both files carry the `// # Module I/O` header. All values reach the DOM as text only (never innerHTML).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `sceneStore.ts` exports a Zustand store with `applyTerraDrawChange`, `terraDrawFeatures`, `selection`, and `dirty`; no spectrum/acoustic array is stored inside TD feature properties (grep: spectrum data referenced only in the store module).
    - `basemap.ts` references the OpenFreeMap dark style and attaches an AttributionControl mentioning OSM.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Instance-in-ref Terra Draw lifecycle + MapCanvas (Gate-1 core)</name>
  <files>web/src/map/useTerraDraw.ts, web/src/map/MapCanvas.tsx, web/src/App.tsx</files>
  <read_first>
    - 07-RESEARCH.md Pattern 3 (instance-in-ref + StrictMode guard: drawRef, build after map.isStyleLoaded()/load, cleanup calls draw.stop()) + Pattern 2 (setStyle re-hydration on style.load, NOT styledata) + Pattern 1 (change/finish wiring)
    - 07-PATTERNS.md call-out (b) (React useEffect cleanup subsumes metrao3 lifecycle.ts; but map.on/draw.on/addProtocol/EventSource in an effect MUST each be torn down in the return)
    - 07-RESEARCH.md Standard Stack (react-map-gl 8 `import { Map } from 'react-map-gl/maplibre'`, useMap(); terra-draw-maplibre-gl-adapter TerraDrawMapLibreGLAdapter)
    - web/src/store/sceneStore.ts (Task 1) + web/src/map/basemap.ts (Task 1)
  </read_first>
  <action>
    Create `web/src/map/useTerraDraw.ts`: a hook that creates the Terra Draw instance ONCE in a `useEffect`, guarded by `drawRef.current` against StrictMode double-mount; build after `map.isStyleLoaded()` else `map.once("load", build)`; construct `new TerraDraw({ adapter: new TerraDrawMapLibreGLAdapter({ map, lib: maplibregl }), modes: [select, point, linestring, polygon] })`, `draw.start()`. Wire `draw.on("change", (ids, type, ctx) => { if (ctx?.origin === "api") return; store.applyTerraDrawChange(ids, type, draw.getSnapshot()); })` (ignore our own addFeatures echoes) and a `draw.on("finish", …)` stub (autosave lands in 07-09). Provide a `rehydrate()` that calls `draw.clear(); draw.addFeatures(store.terraDrawFeatures())`. The effect cleanup returns `() => { drawRef.current?.stop(); drawRef.current = null; map.off(...) }` — EVERY subscription paired (call-out b). Create `web/src/map/MapCanvas.tsx`: the react-map-gl `<Map>` with the dark basemap style, AttributionControl, `useMap()`, and a `map.once("style.load", () => rehydrate())` after `setStyle()` (SC4 — use style.load, never styledata). Add a temporary "switch basemap" control to exercise setStyle in the E2E. Mount `<MapCanvas/>` into the App shell's map slot. Module I/O headers on both files.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `useTerraDraw.ts` guards creation with `drawRef.current` and returns a cleanup that calls `draw.stop()` and unsubscribes; the change handler contains the `ctx?.origin === "api"` early-return (grep-confirmable).
    - Re-hydration is hooked on `style.load` (grep for `"style.load"`); `styledata` is NOT used as the re-hydration hook (grep for `styledata` returns 0 in map/).
    - `npm run build` succeeds and `web/dist` rebuilds with the map bundle.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Offline lifecycle E2E (basemap intercept + store-canonical proof)</name>
  <files>web/tests/e2e/_mocks.ts, web/tests/e2e/lifecycle.spec.ts</files>
  <read_first>
    - 07-PATTERNS.md "web/playwright.config.ts + web/tests/e2e/_mocks.ts" (installApiMocks pattern; ENVI-CRITICAL ADDITION: page.route-intercept basemap style/tiles/glyphs, not just /api/*)
    - 07-RESEARCH.md D-13a (Playwright must run fully offline — a test that hits the network is a failed test) + Validation Architecture SC4 row
    - web/playwright.config.ts (07-05 — webServer drives the real bundle)
  </read_first>
  <action>
    Create `web/tests/e2e/_mocks.ts`: an `installApiMocks(page)` that `page.route`-fulfills every `/api/*` fixture AND a `installBasemapMocks(page)` that intercepts the OpenFreeMap style JSON, `{z}/{x}/{y}` vector tiles, and `{fontstack}/{range}.pbf` glyph requests with local stub payloads — so no test touches the network (D-13a). Create `lifecycle.spec.ts`: load the app with both mock installers; assert the map renders and OSM attribution is visible; simulate a draw (or programmatically seed the store) then toggle the basemap (setStyle) and assert the feature is STILL present after `style.load` re-hydration (SC4); assert no duplicate Terra Draw instance / duplicate change events under StrictMode-like double-mount. Fail the test if any un-mocked network request occurs.
  </action>
  <verify>
    <automated>cd web && npx playwright install --with-deps chromium && npx playwright test lifecycle</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test lifecycle` passes with the webServer driving the real bundle.
    - The spec asserts OSM attribution is present and that a feature survives a basemap switch (style.load re-hydration).
    - The test configuration rejects/aborts any request not matched by `installApiMocks`/`installBasemapMocks` (proving offline); no request reaches `tiles.openfreemap.org`.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Component** `web/src/map/MapCanvas.tsx` (react-map-gl `<Map>` + AttributionControl + setStyle/style.load rehydrate)
- **Hook** `web/src/map/useTerraDraw.ts` (instance-in-ref, StrictMode guard, change/finish wiring, rehydrate)
- **Module** `web/src/map/basemap.ts` (dark style config + OSM attribution)
- **Store** `web/src/store/sceneStore.ts` (Zustand canonical scene: features, selection, dirty, applyTerraDrawChange, terraDrawFeatures)
- **Test helpers** `web/tests/e2e/_mocks.ts` (`installApiMocks`, `installBasemapMocks`)
- **E2E** `web/tests/e2e/lifecycle.spec.ts`
- **App wiring:** `<MapCanvas/>` mounted into the shell map slot
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| runtime basemap XHR → third-party (OpenFreeMap) | Network tile/style/glyph fetch (accepted, D-13a) |
| server strings → DOM | Any server-derived string reaches the DOM |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Information Disclosure | OpenFreeMap tile XHR | accept | Third-party network basemap accepted (D-13a, PROJECT.md pulls public-API data). A runtime XHR is NOT an index.html asset → Phase-6 "zero external assets in index.html" gate stays green |
| T-07-06-02 | Tampering (XSS) | attribution/label strings to DOM | mitigate | All strings via React text children / textContent; no dangerouslySetInnerHTML; inline SVG only (D-11) |
| T-07-06-03 | DoS | leaked map/draw/SSE subscriptions | mitigate | Every effect subscription (map.on/draw.on/addProtocol) torn down in the effect cleanup (call-out b) — no leak on unmount |
| T-07-06-04 | Spoofing | test silently hitting the live network | mitigate | Playwright page.route intercepts basemap style/tiles/glyphs + /api/*; un-mocked requests fail the test (offline invariant) |

*Auth/crypto: N/A — localhost single-user (PROJECT.md), accepted. No new package install (deps landed in 07-05).*
</threat_model>

<verification>
- `web/`: `npx tsc --noEmit`, `npm run build`, `npx playwright test lifecycle` all pass.
- `grep -rc 'styledata' web/src/map/` returns 0 (style.load is the re-hydration hook).
- No network request to `tiles.openfreemap.org` during the E2E (offline).
- Rust side unchanged: `cargo test` green; `git diff --quiet crates/envi-engine/`.
</verification>

<success_criteria>
The store-canonical Terra Draw lifecycle works (single instance under StrictMode, no feedback loop), the dark basemap renders with OSM attribution, features survive a basemap switch via style.load re-hydration (SC4 core), and the E2E proves it fully offline.
</success_criteria>

<output>
Create `.planning/phases/07-frontend-shell-scene-editing/07-06-SUMMARY.md` when done.
</output>
