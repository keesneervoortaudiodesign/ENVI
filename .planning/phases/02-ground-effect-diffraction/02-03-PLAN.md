---
phase: 02-ground-effect-diffraction
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - crates/envi-engine/src/propagation/diffraction.rs
  - tools/nord2000_oracle/gen_wedge_fixtures.py
  - crates/envi-harness/tests/fixtures/oracle/wedge_il.toml
  - crates/envi-harness/tests/oracle_wedge.rs
autonomous: true
requirements: [ENG-03]

must_haves:
  truths:
    - "The hard thin screen (β≈2π, S=(0,1), T=(50,6), R=(100,1)) reproduces the research IL table: 12.01/14.35/17.02/19.90/22.87/25.87 dB at 125/250/500/1000/2000/4000 Hz within ±0.05 dB"
    - "At the shadow boundary, |p̂_diffr|·ℓ → 0.500 ± 0.01 as |θ_S−θ_R−π| → 0.01° from BOTH sides — the analytically-required half-field limit, continuous through the θₙ=π singularity guard"
    - "pwedge returns finite complex values on the literal FORCE case-71 thin-screen profile coordinates (Δx = 0.01 m near-vertical segments, β = 2π−ε) at all 105 frequency points — no 0/0 from sin|A|/|A|"
    - "p̂_diffr is a genuine Complex<f64> carrying the e^{+jωτ} over-the-top phase (Nord2000-native) — the diffracted contribution enters ENG-07's coherent channel as complex pressure, never as a level"
    - "p2edge (thick screen) carries the 0.5 factor and a hard (Ẑ=∞) top face; p2wedge chains two Dwedge calls with the document's printed argument lists (Assumption A6 verified on page images)"
  artifacts:
    - path: "crates/envi-engine/src/propagation/diffraction.rs"
      provides: "pwedge/Dwedge (Eqs. 78-94), lit-zone additions (88-90), angle-modification scheme (p. 43), p2wedge (95-99), p2edge (100-104), pwedge0/Dwedge0 (105-107)"
      min_lines: 250
    - path: "crates/envi-harness/tests/fixtures/oracle/wedge_il.toml"
      provides: "oracle-pinned wedge IL + shadow-boundary + soft-face fixtures"
      contains: "oracle"
  key_links:
    - from: "crates/envi-engine/src/propagation/diffraction.rs"
      to: "crates/envi-engine/src/propagation/special.rs"
      via: "Â_D(B) = Sign(B)·(f(|B|) − j·g(|B|)) via fresnel_f/fresnel_g"
      pattern: "fresnel_f"
    - from: "crates/envi-engine/src/propagation/diffraction.rs"
      to: "crates/envi-engine/src/propagation/ground.rs"
      via: "Q₂/Q₃ spherical-wave coefficients on wedge faces (Eq. 80: τ₂ = τ_S+τ_R, grazing min(β−θ_S, π/2) / min(θ_R, π/2))"
      pattern: "spherical_q"
---

<objective>
Implement the Hadden-Pierce finite-impedance wedge solution and its composites — `pwedge`/`Dwedge` (Eqs. 78-94) with the four-term image sum, lit-zone ray additions, and the angle-modification scheme; `p2wedge` for two separate wedges (Eqs. 95-99); `p2edge` for thick screens (Eqs. 100-104); and the non-reflecting `pwedge0` (Eqs. 105-107). This is ENG-03's diffraction kernel family: everything the screen⇄ground sub-models (plan 02-04) call per ray.

Purpose: diffraction is a complex, phase-preserving operator per the user-locked phase-coherence contract (PROJECT.md Key Decisions, 2026-07-07) — p̂_diffr carries the over-the-top travel-time phase e^{+jωτ} so the diffracted contribution can be summed as complex pressure into H_coh (plan 02-05). The kernels live in the Nord2000-native (e^{−jωt}) quarantined module family; no convention conversion here.

Output: `propagation::diffraction` complete, validated against the research IL anchors, the shadow-boundary half-field limit, and a committed oracle fixture set including soft-face wedges.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ground-effect-diffraction/02-RESEARCH.md
@.planning/phases/02-ground-effect-diffraction/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts this plan CREATES. Plan 02-04's screen engine calls these as its DiffractionKernel implementations. -->

```rust
// diffraction.rs — Nord2000-native (e^{−jωt}); all pressures carry e^{+jωτ} outgoing phase
pub struct WedgeGeometry {           // §5.7.1 inputs
    pub tau_s: f64, pub tau_r: f64, pub tau: f64,     // travel times (τ = τ_S+τ_R normally)
    pub r_s: f64, pub r_r: f64, pub l: f64,           // distances (ℓ = R_S+R_R normally)
    pub theta_s: f64, pub theta_r: f64,               // angles from the RECEIVER wedge face
    pub beta: f64,                                     // wedge angle, validity β > π
}
pub fn pwedge(f_hz: f64, geo: &WedgeGeometry, z_s: Complex<f64>, z_r: Complex<f64>)
    -> Result<Complex<f64>, PropagationError>;         // Eq. 91 procedure (Eqs. 78-90)
pub fn dwedge(f_hz: f64, geo: &WedgeGeometry, z_s: Complex<f64>, z_r: Complex<f64>)
    -> Result<Complex<f64>, PropagationError>;         // D̂ = pwedge·ℓ·e^{−jωτ} (Eqs. 92-94)
pub fn pwedge0(f_hz: f64, geo: &WedgeGeometry) -> Result<Complex<f64>, PropagationError>; // n=1 term only
pub fn p2wedge(f_hz: f64, w1: &WedgeGeometry, w2: &WedgeGeometry, /* per Eqs. 95-99 printed args */
               z: &TwoWedgeImpedances) -> Result<Complex<f64>, PropagationError>;
pub fn p2edge(f_hz: f64, /* thick-screen two-edge args per Eqs. 100-104 */ ...)
    -> Result<Complex<f64>, PropagationError>;         // top face FORCED hard, factor 0.5
```

(Exact p2wedge/p2edge argument lists are fixed by Eqs. 95-104 — transcribe and freeze them in this plan; 02-04 consumes whatever is committed here, so document them in the SUMMARY.)

Consumes from 02-01: `special::{fresnel_f, fresnel_g}`, `ground::spherical_q` (wedge faces, Eq. 80), `propagation::sound_speed_ms`.
</interfaces>

## Artifacts this plan produces

| Artifact | Kind | Public surface |
|----------|------|----------------|
| `envi-engine::propagation::diffraction` | module | `WedgeGeometry`, `pwedge`, `dwedge`, `pwedge0`, `p2wedge`, `p2edge` — the ENG-03 kernel family |
| oracle fixtures | TOML | `wedge_il.toml`: hard-screen IL grid, shadow-boundary approach series, finite-impedance-face wedge values |
| `tools/nord2000_oracle/gen_wedge_fixtures.py` | script | independent Python Hadden-Pierce implementation (imports common.py for Q̂) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pwedge core — four-term Hadden-Pierce sum (Eqs. 78-87)</name>
  <files>crates/envi-engine/src/propagation/diffraction.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — §10 IN FULL (Eqs. 78-86 image-verified: θₙ table, Q̂ₙ per Eq. 80 with the prescriptive min-grazing/τ_S+τ_R arguments, Ê_ν Eq. 81, A(θₙ) Eq. 82 with Heaviside, B Eq. 83, Â_D Eq. 84, f/g asymptote switch at x=5, ε=1e-8 singularity guard verbatim from p. 41), Pitfall 6 (sinc 0/0 guard), Anti-Patterns (never re-derive the wedge-face grazing angles)
  </read_first>
  <behavior>
    - Test 1 (deep-shadow magnitude): hard faces (|Ẑ|→∞ ⇒ Q̂ₙ→1 limit; pass a very large real Ẑ), β=2π·0.9999, S=(0,1), T=(50,6), R=(100,1), c₀=340.348: −20·lg(|pwedge|·ℓ·R_direct/1... ) — i.e. IL vs the free-field direct path — equals 12.01/14.35/17.02/19.90/22.87/25.87 dB at 125/250/500/1000/2000/4000 Hz, ±0.05 dB
    - Test 2 (θₙ=π guard): geometries constructed to land |θₙ−π| < 1e-8 return finite values (the p. 41 ε-subtraction engages); a sweep of θ_S−θ_R through π shows no Inf/NaN
    - Test 3 (sinc guard): a geometry driving |A(θₙ)| < 1e-6 returns finite Ê_ν (Taylor limit sin|A|/|A| → 1)
    - Test 4 (Â_D consistency): for x ≥ 5, Â_D(B) uses the asymptotes and is continuous with the polynomial branch at the switch within 1e-3 relative
    - Test 5 (phase liveness): pwedge output has non-zero imaginary part in a generic shadow geometry, and its phase advances with f consistent with e^{+jωτ} (arg increases with f modulo 2π for fixed geometry) — the kernel is complex, not a magnitude
  </behavior>
  <action>
    Implement Eqs. 78-87 exactly as transcribed in RESEARCH §10 (image-verified, definitive): p̂_diffr = −(1/π)·Σₙ₌₁⁴ Q̂ₙ·A-term·Ê_ν(A(θₙ))·e^{+jωτ}/ℓ with the θₙ set (Eq. 79), ν = π/β, A(θₙ) per Eq. 82 (Heaviside per Eq. 354: H(x)=1 for x>0 else 0), B per Eq. 83, Â_D(B) = Sign(B)·(fresnel_f(|B|) − j·fresnel_g(|B|)) (Eq. 84). Q̂ₙ per Eq. 80 PRESCRIPTIVELY: Q₁=1; Q₂ = spherical_q(f, τ_S+τ_R, min(β−θ_S, π/2), Ẑ_S); Q₃ = spherical_q(f, τ_S+τ_R, min(θ_R, π/2), Ẑ_R); Q₄ = Q₂·Q₃ — transcribe, do not re-derive (research anti-pattern; the τ₂ = total travel time choice is deliberate).

    Guards (all three mandatory): (1) |θₙ−π| < ε → θₙ −= ε with ε = 1e-8 (p. 41 verbatim — cite it); (2) |A(θₙ)| < 1e-6 → Taylor sinc; (3) validate the §5.7.1 domain (β > π, 0 ≤ θ_R ≤ θ_S ≤ β ≤ 2π) with typed errors — the angle-modification scheme for out-of-domain image points arrives in Task 2.

    Note on IL in Test 1: insertion loss = level of the free-field direct path S→R minus level of |pwedge|; compute the direct path R_SR from the same coordinates — write a small test helper, keep pwedge itself pure.

    Rendered-page check: eyeball Eqs. 78-86 on PDF pp. 39-41 (pdfplumber to_image) before implementing — research already validated the transcription numerically, so this is a confirm-and-cite step, not re-derivation.
  </action>
  <verify>
    <automated>cargo test -p envi-engine diffraction::tests::pwedge</automated>
  </verify>
  <done>The four-term sum reproduces the hard-screen IL anchor row-for-row, survives the θₙ=π and A→0 singular geometries, and outputs live complex pressure.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine diffraction` passes behavior tests 1-5
    - IL(1000 Hz) == 19.90 dB ± 0.05 on the anchor geometry
    - `grep -n "1e-8" crates/envi-engine/src/propagation/diffraction.rs` finds the p. 41 ε guard with citation
    - `grep -n "min(β−θ\|beta - theta_s" crates/envi-engine/src/propagation/diffraction.rs` shows the prescriptive Eq. 80 grazing-angle forms
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lit-zone additions, angle-modification scheme, Dwedge, pwedge0 + shadow-boundary anchor</name>
  <files>crates/envi-engine/src/propagation/diffraction.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — §10 "Lit-zone additions" (Eqs. 88-90 conditions θ₁/θ₃/θ₂ < π, R₁ from the cos θ₁ law, the p. 43 four-case angle-modification table for image sources/receivers inside the wedge), Eqs. 92-94 (D̂ definition), Eqs. 105-107 (pwedge0/Dwedge0 keep only n=1), "Numeric Anchors" (shadow-boundary 0.500 ± 0.01 at |θ_S−θ_R−π| = 0.01°)
  </read_first>
  <behavior>
    - Test 1 (shadow-boundary continuity): |pwedge|·ℓ == 0.500 ± 0.01 at |θ_S−θ_R−π| = 0.01° approached from BOTH sides (lit side includes the direct-ray addition; shadow side does not) — the half-field limit is the analytic requirement that the Eq. 88 lit-zone logic and the four-term sum agree at the boundary
    - Test 2 (deep lit zone): with the edge far below the S→R line (θ₁ ≪ π), |pwedge| → the free-field direct value within 1 % — diffraction vanishes where there is no screening
    - Test 3 (Dwedge normalization): dwedge == pwedge·ℓ·e^{−jωτ}, so |dwedge| is dimensionless and → 0.5 at the shadow boundary
    - Test 4 (pwedge0): equals the n=1 term of pwedge exactly (no face reflections) — verify by comparing against pwedge with both face impedances set so Q₂=Q₃=0 is NOT the same thing (document why: pwedge0 drops terms 2-4 entirely, per Eqs. 105-107)
    - Test 5 (angle-modification): a geometry that places the image source inside the wedge triggers the p. 43 scheme and returns finite, boundary-continuous values (no domain-validation error escapes for physically valid exterior S/R)
  </behavior>
  <action>
    Lit-zone additions per Eqs. 88-90: θ₁ < π → add the direct ray e^{+jωτ·(R₁/ℓ)}/R₁ with R₁ = √(R_S²+R_R²−2·R_S·R_R·cos θ₁) (Eq. 88); θ₃ < π → additionally add the source-face-reflected ray with Q-weighting per Eq. 89 (R₂ from the cos θ₂ law; grazing angle in the |arcsin| form — transcribe from PDF p. 42); θ₂ < π → the receiver-face reflection per Eq. 90 (symmetric). Transcribe the exact conditions and weights from the page images; the research gives the structure but flags p. 42 forms for transcription.

    Angle-modification scheme (p. 43): when the image method puts "source"/"receiver" inside the wedge, apply the four-case table verbatim (θ′_R, θ′_S, β′) before the four-term sum. This is what plan 02-04's image rays (p̂₂, p̂₃, p̂₄ of Eq. 157) rely on — without it, ground-reflected diffraction paths crash the domain validation.

    `dwedge` per Eqs. 92-94 (D̂ = pwedge·ℓ·e^{−jωτ} — the phase-stripped diffraction coefficient used in chaining), `pwedge0`/`dwedge0` per Eqs. 105-107 (n=1 term only; used by Phase 3 shadow-zone shielding and v2 finite screens — implement now, cheap, keeps Eq. numbering complete).
  </action>
  <verify>
    <automated>cargo test -p envi-engine diffraction</automated>
  </verify>
  <done>Shadow-boundary limit 0.500 holds from both sides, lit-zone rays restore free field away from screening, the angle-modification scheme admits image-ray geometries, and Dwedge/pwedge0 are normalized per the document.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine diffraction` passes behavior tests 1-5
    - |p̂_diffr|·ℓ == 0.500 ± 0.01 at ±0.01° from the shadow boundary (both sides)
    - Free-field recovery within 1 % in the deep lit zone
    - The p. 43 four-case table is implemented with a citation comment
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: p2wedge + p2edge composites, case-71 robustness, oracle wedge fixtures</name>
  <files>crates/envi-engine/src/propagation/diffraction.rs, tools/nord2000_oracle/gen_wedge_fixtures.py, crates/envi-harness/tests/fixtures/oracle/wedge_il.toml, crates/envi-harness/tests/oracle_wedge.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — §11 (p2wedge Eqs. 95-99: most-important-wedge chaining, the printed (R,τ,θ,Z) argument lists encode effective source/receiver positions — Assumption A6: VERIFY Eqs. 97-98 and 102-103 on page images; p2edge Eqs. 100-104: shared top segment, top face FORCED hard, factor 0.5, wedge angles via the Max(arctan…) forms of Eqs. 194-195), §14 last paragraph (FORCE screen encodings: thin = 0.01 m x-steps at 15 m, thick = flat-top trapezoid 15-30 m, double = spike + trapezoid 75-85 m), Pitfall 6 (near-vertical segments)
  </read_first>
  <behavior>
    - Test 1 (case-71 robustness): pwedge on the literal FORCE thin-screen wedge geometry (segment x-coordinates 14.99→15.00→15.01, screen height 2 m over flat ground, S and R at FORCE-like positions) returns finite complex values at all 105 grid points — the β≈2π−ε near-vertical arctan path is exercised
    - Test 2 (p2edge factor): for a thick screen whose two edges coincide in the thin-screen limit, |p2edge| approaches |pwedge| of the equivalent thin screen within a documented tolerance band, and the 0.5 factor + hard top face are verifiably present (unit-test the composition against a hand-assembled D̂₁·D̂₂ product)
    - Test 3 (p2wedge monotonicity): two separated wedges attenuate ≥ the single more-important wedge alone at every frequency (adding a second screen never adds energy in deep shadow)
    - Test 4 (oracle fixtures): engine pwedge/p2wedge/p2edge match wedge_il.toml — hard-screen IL grid (the anchor geometry × 6 frequencies), a shadow-boundary approach series, a finite-impedance-face wedge (faces σ=200) — each within 0.05 dB / 1e-3 relative on complex values
  </behavior>
  <action>
    `p2wedge` (Eqs. 95-99): τ = τ_S+τ_M+τ_R, ℓ = R_S+R_M+R_R; the composition p̂ = D̂₁·D̂₂·e^{+jωτ}/ℓ² where each D̂ is a dwedge call with the SPECIFIC argument lists printed in Eqs. 97-98 (which wedge receives (R_S, R_M+R_R) vs (R_S+R_M, R_R) depends on which is primary — the caller passes the primary flag; plan 02-05's §5.21 logic decides primacy). TRANSCRIBE the argument lists from PDF page images (Assumption A6) and freeze them in doc comments — plan 02-04 consumes this signature.

    `p2edge` (Eqs. 100-104): same chaining, but the top segment T₁T₂ belongs to both wedges, the top face is FORCED hard (Ẑ=∞ — pass a large real impedance or a dedicated Hard marker), and the composed result carries the factor 0.5 (visible in the extraction — confirm on the page image). Wedge angles via the Max(arctan…, arctan…) forms (Eqs. 194-195).

    Robustness for the FORCE encodings: near-vertical 0.01 m segments make β = 2π−ε and stress the arctan slope math — Test 1 uses the literal case-71 coordinates from RESEARCH §14 (not idealized ones). Guard degenerate/zero-length wedge faces with typed errors.

    Oracle: `gen_wedge_fixtures.py` implements Hadden-Pierce independently in Python (imports common.py for the Q̂ chain — do NOT modify common.py; import only), generates `wedge_il.toml` with provenance header, commit. `oracle_wedge.rs` compares the engine against every fixture row.
  </action>
  <verify>
    <automated>cargo test -p envi-engine diffraction && cargo test -p envi-harness --test oracle_wedge</automated>
  </verify>
  <done>Thin/thick/double-screen kernels are complete with verified argument lists and the 0.5/hard-top rules; the literal FORCE case-71 geometry is finiteness-proven; oracle fixtures pin the whole kernel family.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine diffraction` and `cargo test -p envi-harness --test oracle_wedge` pass
    - Case-71 literal coordinates: 105/105 finite complex values
    - p2edge composition unit-test proves the 0.5 factor and hard top face
    - SUMMARY records the frozen p2wedge/p2edge argument lists (02-04's build-against contract)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Screen geometry from case files → WedgeGeometry construction | Untrusted near-degenerate segments (0.01 m steps) cross into angle math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | DoS (Inf/NaN at singular geometry) | θₙ=π boundary, sin|A|/|A|, near-vertical arctan | mitigate | ε=1e-8 guard (p. 41 verbatim), Taylor sinc below 1e-6, literal case-71 finiteness test across all 105 points, typed errors on degenerate faces |
| T-02-09 | Tampering (silently plausible wrongness) | Eq. 80 grazing angles, Eqs. 97-98/102-103 argument lists, 0.5 factor | mitigate | prescriptive transcription with page-image verification (A6), IL anchor table ±0.05 dB, shadow-boundary 0.500 analytic limit, independent Python oracle |
| T-02-10 | Tampering (phase corruption) | e^{+jωτ} outgoing phase | mitigate | phase-liveness test (non-zero Im, arg advances with f); no conj() anywhere in this module (quarantine rule) |
| T-02-SC | Tampering | installs | accept | zero new dependencies |
</threat_model>

<verification>
- `cargo test --workspace` green: IL anchor table, shadow-boundary both-sides limit, lit-zone free-field recovery, angle-modification admittance, case-71 finiteness sweep, p2edge/p2wedge composition tests, oracle fixture comparison
- `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`, `#![deny(unsafe_code)]`, engine dep quarantine
</verification>

<success_criteria>
- ENG-03 kernel family complete: single-edge (pwedge), two-wedge (p2wedge), thick-screen (p2edge), and non-reflecting (pwedge0) diffraction — all complex/phase-preserving per the user-locked contract, all anchored against research values and the committed oracle
- Plan 02-04 can build the screen⇄ground sub-models against the frozen kernel signatures
</success_criteria>

<output>
Create `.planning/phases/02-ground-effect-diffraction/02-03-SUMMARY.md` when done, recording: the verified Eqs. 97-98/102-103 argument lists (Assumption A6 resolution), the p. 42 lit-zone grazing-angle forms, and the final public kernel signatures for 02-04.
</output>
