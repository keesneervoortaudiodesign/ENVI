---
phase: 02-ground-effect-diffraction
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - crates/envi-engine/src/propagation/terrain_interpretation.rs
  - crates/envi-engine/src/propagation/terrain_effect/mod.rs
  - crates/envi-engine/src/propagation/mod.rs
  - crates/envi-engine/src/transfer.rs
  - crates/envi-harness/src/lib.rs
  - crates/envi-harness/src/capability.rs
  - crates/envi-harness/src/cases/mod.rs
  - crates/envi-harness/src/cases/toml.rs
  - cases/terrain_flat_sigma200.toml
  - cases/terrain_mixed_case21.toml
  - cases/terrain_screen_thin_case71.toml
  - cases/terrain_screen_thick_case81.toml
  - cases/terrain_screens_double_case91.toml
  - tools/nord2000_oracle/gen_case_fixtures.py
  - README.md
autonomous: true
requirements: [ENG-02, ENG-03, ENG-07]

must_haves:
  truths:
    - "Phase is LIVE in H_coh across ALL operators: after multiplying the terrain factor (ground + diffraction) into the Phase-1 TransferSpectrum, the ground-interference dip sits at the correct grid frequency AND the spectrum has non-zero imaginary parts at non-dip frequencies — for both a flat-ground case and a screen+ground case"
    - "P_incoh is a SEPARATE channel: with F→1 (zero turbulence, small Δτ) the p_incoh channel → 0 and band levels equal the pure coherent readout; p_incoh never modifies arg(H_coh) at any frequency (asserted)"
    - "Total level readout = 10·lg(|coherent Σ|² + P_incoh) — implemented as band_levels_db_two_channel, and for the dip-anchor geometry it reproduces Sub-model 1's delta_l_db through the FULL transfer path (case file → Scene → engine → readout) within 1e-9 dB"
    - "Exactly ONE conj() converts Nord2000-native ratios to ENVI's e^{+jωt} convention, and a two-path test proves the dip lands at the SAME frequency computed natively and through the transfer path (Pitfall 1 pin)"
    - "All five synthetic terrain cases (flat σ=200, mixed case-21, thin case-71, thick case-81, double case-91 geometries) report Pass against oracle-pinned 105-point references at 0.1 dB; every evaluated quantity is finite across all 62 FORCE straight-road geometries × 105 points (ROADMAP success criterion 3)"
    - "Capabilities GroundEffect and Diffraction are implemented; FORCE road cases now skip ONLY on the emission model (the requires-list shrank — asserted)"
  artifacts:
    - path: "crates/envi-engine/src/propagation/terrain_interpretation.rs"
      provides: "§5.21 Eqs. 300-328: edge/screen identification, r_scr1/r_scr2/r_scr12/r_flat transition params, equivalent flat terrain, screen-shape reduction"
      min_lines: 150
    - path: "crates/envi-engine/src/propagation/terrain_effect/mod.rs"
      provides: "terrain_effect() entry point: §5.21 dispatch + Eq. 332 composition → TerrainEffect two-channel spectrum"
      contains: "terrain_effect"
    - path: "crates/envi-engine/src/transfer.rs"
      provides: "nord_ratio_to_transfer (single conj boundary) + band_levels_db_two_channel readout"
      contains: "nord_ratio_to_transfer"
    - path: "cases/terrain_screen_thin_case71.toml"
      provides: "oracle-pinned end-to-end screen case through the harness"
      contains: "oracle"
  key_links:
    - from: "crates/envi-engine/src/propagation/terrain_effect/mod.rs"
      to: "crates/envi-engine/src/propagation/terrain_interpretation.rs"
      via: "dispatch on interpreted terrain (flat/screens) with transition parameters r_scr1/r_scr2/r_scr12/r_flat"
      pattern: "r_scr1"
    - from: "crates/envi-engine/src/transfer.rs"
      to: "crates/envi-engine/src/propagation/terrain_effect/mod.rs"
      via: "H_coh[f] = H_direct[f] · nord_ratio_to_transfer(h_coh_factor[f]); P_incoh carried alongside"
      pattern: "nord_ratio_to_transfer"
    - from: "crates/envi-harness/src/lib.rs"
      to: "crates/envi-engine/src/propagation/terrain_effect/mod.rs"
      via: "run_case Terrain arm drives Scene → terrain_effect → two-channel readout → comparison"
      pattern: "terrain_effect"
---

<objective>
Wire Phase 2 end to end: the §5.21 terrain-interpretation dispatcher (Eqs. 300-328), the §5.22 compound combination ΔL_t (Eq. 332), and — the phase's contractual heart — the two-channel transfer integration: H_coh stays a genuine Complex<f64> with live interference phase through ground AND diffraction, P_incoh rides alongside as a real energy channel added only at level readout, and exactly one conj() reconciles the Nord2000-native convention with ENVI's frozen e^{+jωt} contract. Flip the GroundEffect and Diffraction capabilities, run five oracle-pinned synthetic terrain cases through the harness, and prove finiteness across every FORCE geometry × the full 1/12-octave range.

Purpose: this closes ENG-07 exactly as the user locked it (PROJECT.md Key Decisions, 2026-07-07): direct + ground-reflected + diffracted combined as complex pressures with Δτ interference intact; the turbulence-decorrelated (1−F²) energy in a distinct P_incoh channel that never overwrites phase; total level = |coherent Σ|² + P_incoh; F→1 ⇒ pure coherent interference.

Scope note for the verifier: ROADMAP success criteria 1-2 say "FORCE cases match reference within tolerance" — per 02-RESEARCH ("FORCE Validation Reality") no FORCE road case can gate end-to-end before the Phase 4 emission model (reference spectra embed Jonasson pass-by integration; VAL-02 maps to Phase 4). This phase deliberately satisfies those criteria at PROPAGATION level via the acceptance ladder: exact anchors + committed scipy oracle (≤0.1 dB) + property tests, with FORCE road cases remaining Skipped on a now-shorter requires-list. This reframing is the researcher-recommended and orchestrator-approved reading, not a scope cut. Sub-model 3 (non-flat terrain, §5.12) is explicitly OUT of Phase 2: flat target profiles give r_flat = 1 so it is dead code here; it is stubbed with a typed error and scheduled for Phase 3 (see ROADMAP note + research Open Q4).

Output: `terrain_interpretation`, the `terrain_effect()` entry point, the conj boundary + two-channel readout in transfer.rs, capabilities flipped, five green terrain cases, the finiteness sweep, README updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ground-effect-diffraction/02-RESEARCH.md
@.planning/phases/02-ground-effect-diffraction/02-02-SUMMARY.md
@.planning/phases/02-ground-effect-diffraction/02-04-SUMMARY.md
@.planning/phases/01-force-harness-geometry-model-direct-path/01-01-SUMMARY.md
@.planning/phases/01-force-harness-geometry-model-direct-path/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Contracts this plan CREATES (the Phase 3/4 forward surface). -->

```rust
// propagation/terrain_effect/mod.rs
/// Per-frequency two-channel terrain effect relative to the free-field direct path.
pub struct TerrainEffect {
    pub h_coh_factor: Vec<Complex<f64>>, // len N_BANDS — Nord2000-native coherent complex factor
    pub p_incoh: Vec<f64>,               // len N_BANDS — incoherent power add-on (re free field)
    pub delta_l_db: Vec<f64>,            // len N_BANDS — Nord2000 band values (validation path)
}
pub fn terrain_effect(profile: &TerrainProfile, src: [f64; 3], rcv: [f64; 3],
                      atmos_c0: f64, coh: &CoherenceInputs, axis: &FreqAxis)
    -> Result<TerrainEffect, PropagationError>;

// transfer.rs
/// THE single convention boundary: Nord2000-native (e^{−jωt}) → ENVI transfer (e^{+jωt}).
pub fn nord_ratio_to_transfer(ratio: Complex<f64>) -> Complex<f64>; // = ratio.conj()
/// Total level readout: L(f) = L_W(f) + 10·lg(|H_coh(f)·G|²/G² … ) — precisely:
/// L = L_W + 10·lg(|H_coh|² + |H_ff|²·p_incoh), with H_coh = H_ff · conj(h_coh_factor).
pub fn band_levels_db_two_channel(h_coh: &TransferSpectrum, h_ff: &TransferSpectrum,
                                  p_incoh: &[f64], spectrum: &BandSpectrum) -> Vec<f64>;
```
</interfaces>

## Artifacts this plan produces

| Artifact | Kind | Public surface |
|----------|------|----------------|
| `envi-engine::propagation::terrain_interpretation` | module | §5.21: primary/secondary edge finding, screen-shape reduction (Convex Eq. 336), transition params r_scr1/r_scr2/r_scr12/r_flat, equivalent flat terrain LSQ |
| `terrain_effect()` entry point | fn | dispatch + Eq. 332 composition → `TerrainEffect` (the Phase 3 refraction and Phase 4 tensor forward contract) |
| `transfer::nord_ratio_to_transfer` | fn | the ONE conj boundary (RESEARCH Pattern 1) |
| `transfer::band_levels_db_two_channel` | fn | |coherent|² + P_incoh readout (the ENG-07 law) |
| harness | wiring | `Capability::{GroundEffect, Diffraction}` implemented; `CaseKind::Terrain` + run_case arm; skip-reason shrink assertion |
| 5 × `cases/terrain_*.toml` | fixtures | oracle-pinned end-to-end synthetic cases (flat/mixed/thin/thick/double) |
| README.md | docs | Phase 2 capabilities documented (project rule: README before phase done) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: terrain_interpretation.rs — §5.21 dispatcher + Sub-model 3 typed stub</name>
  <files>crates/envi-engine/src/propagation/terrain_interpretation.rs, crates/envi-engine/src/propagation/mod.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — §14 IN FULL (primary edge Eq. 300 with Heaviside sign; r_scr1 = r_Δℓ·r_h·r_Fz Eqs. 301-305 with the ramp breakpoints — transcribe 0.133λ / [0.1,0.3] / [0.026,0.082] exactly from the PDF; secondary screen/edge Eqs. 307-315 incl. r_w ramp; flatness Eqs. 316-319 + equivalent flat terrain LSQ Eqs. 320-327, evaluated 25 Hz-2 kHz frozen above; screen-shape reduction via Convex Eq. 336 threshold 0.0001 m; FORCE screen encodings), Open Question 4 (Sub-model 3 deferral — stub with typed error)
  </read_first>
  <behavior>
    - Test 1 (edge finding): on the literal FORCE case-71 profile (spike at x=15), the primary edge is the spike top; on case-91 (spike + trapezoid), primary + secondary screens are identified with the documented Δℓ₀ ordering; on a flat case-1-like profile, NO edge qualifies (all Δℓ₀ ≤ 0)
    - Test 2 (transition params): flat profile → r_scr1 == 0 and r_flat == 1 exactly; tall thin screen at mid path, mid frequencies → r_scr1 == 1; a marginal 0.15 m screen produces 0 < r_scr1 < 1 via the r_h ramp (frequency-dependent)
    - Test 3 (screen-shape reduction): the case-81 trapezoid (>3 points) reduces to a 3-point screen shape via the secondary-edge logic; the Convex test uses the 0.0001 m threshold
    - Test 4 (equivalent flat terrain): a flat profile returns itself (identity LSQ); the flatness parameter freezes above 2 kHz (values at 4 kHz == value at 2 kHz)
    - Test 5 (Sub-model 3 stub): constructing a dispatch where non-flat terrain would receive weight > 0 returns the typed error NonFlatTerrainNotImplemented (never a silent wrong answer) — Phase 2 target profiles never trigger it (asserted on all five case profiles)
  </behavior>
  <action>
    Implement §5.21 as the research lays it out: (1) primary edge of primary screen — every interior profile point above the terrain baseline is a candidate, pick max Δℓ₀ = H(zᵢ−z_m)·(|SPᵢ|+|PᵢR|−|SR|) per Eq. 300; efficiency r_scr1 = r_Δℓ·r_h·r_Fz per Eqs. 301-305 (r_Fz via fresnel::calc_fz_d at F_λ = 0.5λ per Eq. 304 — the 02-02 parametric F_λ pays off here); transcribe every ramp breakpoint from the PDF page images and cite. (2) Secondary screen (Eqs. 307-310) and secondary edge of primary screen (Eqs. 311-315) with the r_w edge-separation modifier. (3) Terrain flatness (Eqs. 316-319) + equivalent flat terrain least squares (Eqs. 320-327), evaluated 25 Hz-2 kHz and FROZEN above 2 kHz (Eq. 317 note). (4) Screen start/end = nearest non-convex points around the edge (Convex, Eq. 336, threshold 0.0001 m); shapes with > 3 points reduced via the secondary-edge logic. Output: a `TerrainInterpretation` struct carrying the identified screen shapes, per-region strip lists, and (r_scr1, r_scr2, r_scr12, r_flat) as functions of frequency (they are λ-dependent — return per-band vectors or closures over f).

    Sub-model 3 stub: an explicit typed error variant (research Open Q4 resolution) with a doc comment: "Non-flat terrain (Sub-model 3, §5.12) is scheduled with Phase 3 — flat Phase 2 target profiles give r_flat = 1, making this path unreachable for the current case families; reaching it with weight > 0 is a hard error by design, never a silent approximation." This is a documented deferral of an item the requirements assign to no phase (flagged to the user in planning), not a reduction of ENG-02/03 scope.
  </action>
  <verify>
    <automated>cargo test -p envi-engine terrain_interpretation</automated>
  </verify>
  <done>FORCE profile encodings interpret into the correct screen shapes and transition parameters; flat profiles give r_flat = 1/r_scr1 = 0 exactly; Sub-model 3 is a typed hard error, unreachable on Phase 2 targets.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine terrain_interpretation` passes behavior tests 1-5
    - Case-71/81/91 literal profiles produce the documented screen identifications
    - Ramp breakpoints carry "verified on p. NNN" citations
    - The Sub-model 3 error variant exists and is asserted unreachable on all five target profiles
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: terrain_effect() composition (Eq. 332) + the single conj boundary + two-channel readout</name>
  <files>crates/envi-engine/src/propagation/terrain_effect/mod.rs, crates/envi-engine/src/propagation/mod.rs, crates/envi-engine/src/transfer.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — §14 item 4 (Eq. 332 compound combination with the r-parameter interpolation tree), "Complex Combination & Conventions" IN FULL (the convention trap, Pattern 1 single conj, "What the engine returns", dip-position physics), Anti-Pattern "Treating ΔL values as complex multipliers everywhere", Pitfall 1 (dip-asymmetry through both code paths)
    - .planning/phases/01-force-harness-geometry-model-direct-path/01-03-SUMMARY.md — the frozen e^{+jωt} transfer contract and band_levels_db
  </read_first>
  <behavior>
    - Test 1 (Eq. 332 tree): with hand-set r parameters and stub sub-model results, ΔL_t interpolates exactly per the printed tree (r_scr1 between screen/no-screen branches; r_scr2/r_scr12 inside the screen branch; r_flat inside the no-screen branch); ΔL₇ terms enter the level/p_incoh side only
    - Test 2 (conj boundary): nord_ratio_to_transfer(z) == z.conj(); |z| invariant; there is exactly one call site converting terrain factors (grep-able single function, RESEARCH Pattern 1)
    - Test 3 (dip through both paths — Pitfall 1 pin): for the σ=200 anchor geometry, the dip frequency computed natively (argmin delta_l_db) equals the dip frequency computed through the transfer path (argmin of band levels from H_coh = H_ff·conj(h_coh_factor) with unit L_W) — same grid point
    - Test 4 (phase liveness across ALL operators): after applying the terrain factor for (a) the flat case and (b) the case-71 screen+ground case, the resulting H_coh spectrum has |Im| > 0 at ≥ 90 % of non-dip grid points, and arg(H_coh) ≠ arg(H_ff) somewhere (the terrain factor's phase is real and live, not stripped)
    - Test 5 (P_incoh separation): band_levels_db_two_channel with p_incoh all-zero equals band_levels_db(H_coh) exactly; with F forced → 1, terrain_effect returns p_incoh == 0 at every band; p_incoh values never alter arg(H_coh) (structural: readout only)
    - Test 6 (end-to-end level identity): for the flat anchor geometry, band_levels_db_two_channel through the full path (H_ff from direct_path × terrain factor + p_incoh readout) reproduces L_W + 20lg|H_ff| + delta_l_db within 1e-9 dB at every band
  </behavior>
  <action>
    `terrain_effect()` entry point in terrain_effect/mod.rs: run terrain_interpretation, dispatch — flat (r_scr1 == 0): Sub-model 1 or 2 (per strip count) with r_flat weighting (r_flat = 1 on Phase 2 targets; the r_flat < 1 branch calls the Sub-model 3 stub → typed error, per Task 1); screens present: SM4/SM5/SM6 per the interpretation (one screen one edge / one screen two edges / two screens) + ΔL₇ terms, composed per Eq. 332: ΔL_t = r_scr1·ΔL_scr + (1−r_scr1)·ΔL_noscr with the printed inner tree. Compose the TWO CHANNELS through the same tree: h_coh_factor interpolates complex-linearly with the SAME r weights on the coherent factors (10^(ΔL/20)-scaled where the tree mixes levels — document the exact rule: the r-interpolation is defined on dB levels in the document; apply it to the complex factors as |·|-consistent weighting factor^r₁·…-style is NOT defined, so use linear complex interpolation of the factors with the r weights and assert 10·lg|result|² stays within 0.2 dB of the document's dB-interpolated ΔL_t on the target cases where r values are 0 or 1 exactly — on Phase 2 target profiles every r is 0 or 1, so the two readings COINCIDE and no approximation is actually exercised; doc-comment this and add a debug assertion for r ∉ {0,1} inputs flagging Phase 3 revisit); p_incoh and the ΔL₇ energies combine on the level side. δ per-band: TerrainEffect { h_coh_factor, p_incoh, delta_l_db } over the 105-point axis, with delta_l_db always the document-exact Eq. 332 value (the validation channel).

    `transfer.rs`: add `nord_ratio_to_transfer` (Pattern 1 verbatim — doc: "|ratio| invariant; converts Nord2000-native e^{−jωt} to ENVI e^{+jωt}; the ONLY convention conversion in the codebase") and `band_levels_db_two_channel` implementing the user-locked readout law: L(f) = L_W(f) + 10·lg(|H_coh(f)|² + |H_ff(f)|²·p_incoh(f)) where H_coh = H_ff·nord_ratio_to_transfer(h_coh_factor). Keep band_levels_db (Phase 1) untouched — the two-channel variant supersedes it for terrain cases; Phase 4's tensor MAC builds on H_coh + a per-band P_incoh store (doc-comment the forward contract).

    `propagation/mod.rs`: re-export terrain_effect at the propagation root for the harness.
  </action>
  <verify>
    <automated>cargo test -p envi-engine terrain_effect && cargo test -p envi-engine transfer</automated>
  </verify>
  <done>Eq. 332 composes both channels; the single conj boundary is unit-tested with the dip landing at the same grid point through native and transfer paths; phase is provably live in H_coh after ground AND ground+screen; P_incoh is readout-only and vanishes as F→1.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine terrain_effect transfer` passes behavior tests 1-6
    - `grep -rh --include='*.rs' '\.conj()' crates/envi-engine/src/propagation/ | grep -v '^\s*//' | wc -l` outputs 0 (comment lines filtered — no code-level conjugation inside the Nord2000-native modules) AND `grep -c 'conj' crates/envi-engine/src/transfer.rs` ≥ 1 — the conversion lives ONLY at the transfer boundary (quarantine holds)
    - Dip at the same grid point through both code paths (Test 3)
    - Non-zero imaginary parts across the spectrum after ground+diffraction (Test 4 — the phase-live truth)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harness integration — capabilities, five oracle-pinned terrain cases, finiteness sweep, README</name>
  <files>crates/envi-harness/src/lib.rs, crates/envi-harness/src/capability.rs, crates/envi-harness/src/cases/mod.rs, crates/envi-harness/src/cases/toml.rs, cases/terrain_flat_sigma200.toml, cases/terrain_mixed_case21.toml, cases/terrain_screen_thin_case71.toml, cases/terrain_screen_thick_case81.toml, cases/terrain_screens_double_case91.toml, tools/nord2000_oracle/gen_case_fixtures.py, README.md</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — "FORCE Validation Reality" (the acceptance ladder + capability-gate honesty: cases 1-8 stay Skipped(requires: emission-model) with a SHRINKING requires-list), Pitfall 2 (never gate on "case 1 green"), Code Example "Finiteness sweep"
    - .planning/phases/01-force-harness-geometry-model-direct-path/01-01-SUMMARY.md — run_case/Outcome/capability API, CaseKind, SyntheticExpected, discover()
  </read_first>
  <behavior>
    - Test 1 (capabilities): implemented_capabilities() == {Geometry, FreeField, GroundEffect, Diffraction}; required−implemented for FORCE straight-road cases now yields exactly {EmissionModel} for homogeneous cases (and {EmissionModel, Refraction} for wind/gradient cases) — the requires-list SHRANK and an explicit test asserts the ground-effect and diffraction names no longer appear in any skip reason
    - Test 2 (e2e terrain cases): all five cases/terrain_*.toml report Pass — engine two-channel readout vs oracle-pinned 105-point expected bands at tolerance_db = 0.1
    - Test 3 (finiteness sweep — ROADMAP success criterion 3): for ALL 62 FORCE straight-road profiles (single placeholder sub-source) × all 105 points, terrain_effect returns finite delta_l_db, finite h_coh_factor, finite p_incoh — run with homogeneous weather + each case's own turbulence parameters; skip-and-report profiles that legitimately need Sub-model 3 (non-flat: cases 31-64, 101-124) via the typed error, asserting the error is the documented NonFlat variant, never NaN
    - Test 4 (two-channel e2e): the flat σ=200 case run with zero turbulence and with F forced to 1 yields identical band levels (p_incoh contributes 0); the case-71 case with turbulence yields strictly higher levels in deep shadow than with turbulence removed (scattering + decoherence floor the screen)
  </behavior>
  <action>
    Capability + case wiring: add `CaseKind::Terrain` (synthetic terrain-effect case) to cases/mod.rs with TOML support in toml.rs — fields: terrain profile rows (x, z, σ, r), source/receiver positions, turbulence (cv2, ct2), expected bands + tolerance_db (SyntheticExpected reuse). `required_capabilities` for Terrain: GroundEffect (+ Diffraction when the profile encodes a screen — reuse the existing screen-description detection or detect z-spikes). Flip `Capability::GroundEffect` and `Capability::Diffraction` into implemented_capabilities(). run_case Terrain arm: build TerrainProfile from rows → terrain_effect → band_levels_db_two_channel with unit spectrum → compare_pointwise at the case's tolerance. FORCE road cases keep gating on EmissionModel — do NOT attempt reference-spectrum comparison (Pitfall 2).

    Five fixtures via `gen_case_fixtures.py` (Python oracle composes the flat/screen chains already written in 02-01..02-04 oracle modules; import, don't modify): terrain_flat_sigma200 (hS=0.5/hR=1.5/d=97.5, σ=200, zero turbulence), terrain_mixed_case21 (FORCE case-21 strip layout, Cv²=0.12/CT²=0.008), terrain_screen_thin_case71, terrain_screen_thick_case81, terrain_screens_double_case91 (literal FORCE profile encodings from RESEARCH §14, forced turbulence, FΔν=1 both sides). Each TOML carries provenance (oracle git hash) + a tolerance_db = 0.1 rationale comment (cross-implementation gate, research acceptance ladder rung 2).

    Finiteness sweep: implement the research code-example test in the harness (iterate discover()'ed FORCE straight-road profiles; call terrain_effect directly; assert finite or the typed NonFlat error) — this is ROADMAP success criterion 3 verbatim and the T-02 numeric-DoS gate.

    README.md: add Phase 2 section — ground effect (Delany-Bazley + spherical-wave Q̂, segmented impedance), single/thick/double screen diffraction (Hadden-Pierce), two-channel partial-coherence output (H_coh/P_incoh), oracle-fixture validation approach, capability status table (project rule: README reflects new behavior before the phase is done).
  </action>
  <verify>
    <automated>cargo test --workspace && cargo run -p envi-harness -- report</automated>
  </verify>
  <done>Five terrain cases Pass end to end at 0.1 dB; capabilities flipped with the skip-reason shrink asserted; the finiteness sweep covers every FORCE geometry × band; README documents Phase 2. All quality gates green.</done>
  <acceptance_criteria>
    - `cargo test --workspace` exits 0; `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` clean
    - `cargo run -p envi-harness -- report` shows the 5 terrain cases Pass (9 total Pass rows incl. Phase 1's 4)
    - A test asserts no FORCE skip reason contains the ground-effect or diffraction capability names anymore
    - Finiteness sweep: 0 NaN/Inf across all evaluated profiles × 105 points; non-flat profiles fail ONLY with the typed NonFlat error
    - README.md contains a Phase 2 capabilities section
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Case TOML terrain rows / turbulence params → terrain_effect | Untrusted profile + weather data crosses into the full sub-model dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | DoS (NaN/Inf across the composed chain) | terrain_effect dispatch, r-parameter interpolation | mitigate | the 62-profile × 105-point finiteness sweep (ROADMAP criterion 3); typed NonFlat error instead of silent wrong answers; degenerate-profile guards inherited from scene::TerrainProfile::new |
| T-02-15 | Tampering (convention corruption) | conj boundary | mitigate | single-function conversion (grep gate: zero conj in propagation/, ≥1 in transfer.rs); dip-through-both-paths equality test; dip-asymmetry physics test |
| T-02-16 | Tampering (channel corruption) | two-channel readout | mitigate | p_incoh structurally readout-only (f64, never multiplied into H_coh); F→1 ⇒ p_incoh == 0 e2e test; level identity vs delta_l_db at 1e-9 dB |
| T-02-17 | Repudiation (silent tolerance drift) | terrain case fixtures | mitigate | tolerance_db = 0.1 committed per case with rationale + oracle provenance hash; comparator reports max deviation on Pass |
| T-02-SC | Tampering | installs | accept | zero new dependencies across the whole phase |
</threat_model>

<verification>
- `cargo test --workspace` green: §5.21 interpretation tests, Eq. 332 tree, conj-boundary + dip-equality + phase-liveness + P_incoh-separation tests, five e2e terrain cases, finiteness sweep, capability shrink assertion
- `cargo run -p envi-harness -- report`: 9 Pass rows; FORCE road cases Skipped with shrunken requires-lists
- Quality gates: clippy -D warnings, fmt --check, deny(unsafe_code), engine dep quarantine (`cargo tree -p envi-engine -e normal --depth 1`)
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 3 satisfied VERBATIM: direct + ground-reflected + diffracted combined as complex pressures retaining Δτ interference phase, finite and stable across the full 1/12-octave range on every evaluated geometry
- Success criteria 1-2 satisfied at propagation level per the acceptance ladder (documented reframing above; FORCE-green gate = Phase 4/VAL-02)
- ENG-02, ENG-03, ENG-07 complete: two-channel H_coh/P_incoh live end to end, capabilities flipped, oracle-pinned cases green
</success_criteria>

<output>
Create `.planning/phases/02-ground-effect-diffraction/02-05-SUMMARY.md` when done, recording: the r-interpolation two-channel rule (and confirmation that Phase 2 targets only exercise r ∈ {0,1}), the conj-boundary call-site inventory, the finiteness-sweep coverage numbers, and the Phase 3 handoff notes (Sub-model 3 owner, FΔν seam, shadow-zone attachment points, Δp_SCR phase decision log for Phase 4 multi-sub-source interference).
</output>
