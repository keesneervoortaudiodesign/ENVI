---
phase: 02-ground-effect-diffraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/envi-engine/src/propagation/mod.rs
  - crates/envi-engine/src/propagation/special.rs
  - crates/envi-engine/src/propagation/ground.rs
  - crates/envi-engine/src/propagation/rays.rs
  - crates/envi-engine/src/propagation/coherence.rs
  - crates/envi-engine/src/propagation/fresnel.rs
  - crates/envi-engine/src/propagation/diffraction.rs
  - crates/envi-engine/src/propagation/terrain_effect/mod.rs
  - crates/envi-engine/src/scene.rs
  - tools/nord2000_oracle/common.py
  - tools/nord2000_oracle/gen_ground_fixtures.py
  - crates/envi-harness/tests/fixtures/oracle/ground_w_qhat.toml
  - crates/envi-harness/tests/oracle_ground.rs
autonomous: true
requirements: [ENG-02, ENG-07]

must_haves:
  truths:
    - "faddeeva_w reproduces scipy.special.wofz anchors (0.5+0.5j, 1+1j, 3+2j, 7+1j) within 1e-6 relative, in all four quadrants, with no jump > 1e-4 across the |x|=3.9 / |y|=3 / x=6 / y=6 branch borders"
    - "spherical_q reproduces all six research Q̂ anchors (σ ∈ {12.5, 200, 20000}) within 1e-4 relative, and |Q̂| = 1.257 at σ=200/f=250 Hz is NOT clamped to ≤ 1"
    - "The cancellation-free ΔR identity 4·hS·hR/(R₁+R₂) matches the research geometry anchor (ΔR = 1.538259287e-2 at hS=0.5, hR=1.5, d=97.5) to 1e-9 relative and stays accurate at hS=0.01, hR=1.5, d=1000 where naive τ₂−τ₁ loses digits"
    - "coherence_f with Δτ=0 returns F ≤ 1 with Ff = 1, and takes FΔν as an injected factor (=1 in Phase 2) so Phase 3 can drop in refraction decoherence without touching call sites"
    - "A committed Python oracle (scipy-based) generated the w(ẑ)/Q̂ fixture grid, and the Rust engine matches every fixture point within 1e-6 (w) / 1e-4 (Q̂) relative"
  artifacts:
    - path: "crates/envi-engine/src/propagation/special.rs"
      provides: "Faddeeva w(ẑ) Eqs. 61-74 (three branches + Eq. 62 symmetries), Fresnel-integral fits f/g Eqs. 85-86 + Tables 4/5, clamped exp' Eq. 337"
      min_lines: 120
    - path: "crates/envi-engine/src/propagation/ground.rs"
      provides: "Delany-Bazley Ẑ_G (Eq. 57), Γ̂_p (59), Ê(ρ̂) (60), spherical-wave Q̂ (58), incoherent ρᵢ (75-76)"
      min_lines: 80
    - path: "crates/envi-engine/src/propagation/rays.rs"
      provides: "RayVars/RayPair straight-ray constructor with cancellation-free ΔR (the Phase 3 circular-ray seam)"
      min_lines: 40
    - path: "crates/envi-engine/src/propagation/coherence.rs"
      provides: "F = Ff·FΔν·Fc·Fr·Fs (Eqs. 110-114) with FΔν injected and Fs documented stub"
      min_lines: 50
    - path: "tools/nord2000_oracle/common.py"
      provides: "Independent scipy-based w(z)/Q̂ chain — the committed cross-implementation oracle"
      min_lines: 40
  key_links:
    - from: "crates/envi-engine/src/propagation/ground.rs"
      to: "crates/envi-engine/src/propagation/special.rs"
      via: "Ê(ρ̂) calls faddeeva_w(ρ̂)"
      pattern: "faddeeva_w"
    - from: "crates/envi-engine/src/propagation/coherence.rs"
      to: "crates/envi-engine/src/propagation/special.rs"
      via: "Fc/Fr use the clamped exponential exp' (Eq. 337)"
      pattern: "exp_clamped"
    - from: "crates/envi-harness/tests/oracle_ground.rs"
      to: "crates/envi-harness/tests/fixtures/oracle/ground_w_qhat.toml"
      via: "harness test loads oracle-generated fixtures and compares engine values"
      pattern: "ground_w_qhat"
---

<objective>
Build the Nord2000-native complex numerics core that everything else in Phase 2 stands on: the document's own Faddeeva approximation w(ẑ) (Eqs. 61-74), the Fresnel-integral polynomial fits f/g (Eqs. 85-86, Tables 4/5), the ground reflection-coefficient chain Ẑ_G → Γ̂_p → Ê(ρ̂) → Q̂ (Eqs. 57-60) plus the incoherent coefficient ρᵢ (Eqs. 75-76), the straight-ray RayVars with the cancellation-free ΔR identity, and the coherence coefficient F (Eqs. 110-114) with the FΔν injected-factor seam for Phase 3 — all validated against research anchors and a committed scipy oracle.

Purpose: ENG-02's ground effect and ENG-03's wedge faces both consume Q̂; ENG-07's partial-coherence combination consumes F and ρᵢ. Per the user-locked phase-coherence contract (PROJECT.md Key Decisions, 2026-07-07), every one of these operators is complex and phase-preserving — this plan lays them down in Nord2000's native e^{−jωt} convention inside a quarantined module family, so the single conj() boundary (plan 02-05) is the only place conventions ever meet.

Output: `propagation::{special, ground, rays, coherence}` fully implemented + module shells for `fresnel`, `diffraction`, `terrain_effect` (so wave-2 plans 02-02 and 02-03 can run in parallel without touching mod.rs), the committed Python oracle package, and oracle fixture tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ground-effect-diffraction/02-RESEARCH.md
@.planning/phases/01-force-harness-geometry-model-direct-path/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Contracts this plan CREATES. Plans 02-02..02-05 build against these signatures verbatim. -->

```rust
// special.rs — Nord2000-native module (e^{−jωt} convention, doc-commented)
pub fn faddeeva_w(z: Complex<f64>) -> Complex<f64>;      // Eqs. 61-74
pub fn fresnel_f(x: f64) -> f64;                          // Eq. 85 + Table 4 (x≥5: 1/(πx))
pub fn fresnel_g(x: f64) -> f64;                          // Eq. 86 + Table 5 (x≥5: 1/(π²x³))
pub fn exp_clamped(x: f64) -> f64;                        // Eq. 337 exp'

// ground.rs
pub fn ground_impedance(f_hz: f64, sigma_kpa: f64) -> Complex<f64>;                      // Eq. 57
pub fn gamma_p(psi_g: f64, z_g: Complex<f64>) -> Complex<f64>;                           // Eq. 59
pub fn spherical_q(f_hz: f64, tau2_s: f64, psi_g: f64, z_g: Complex<f64>) -> Complex<f64>; // Eqs. 58+60
pub fn incoherent_rho(f_hz: f64, z_g: Complex<f64>) -> f64;                              // Eqs. 75-76

// rays.rs
pub struct RayVars { pub tau: f64, pub r: f64, pub psi_g: f64, pub r1: f64, pub r2: f64 }
pub struct RayPair { pub direct: RayVars, pub reflected: Option<RayVars>, pub dtau: f64 }
pub fn straight_rays(d: f64, h_s: f64, h_r: f64, c0: f64) -> Result<RayPair, PropagationError>;
pub fn straight_rays_over_segment(s: [f64;2], r: [f64;2], seg_a: [f64;2], seg_b: [f64;2], c0: f64)
    -> Result<RayPair, PropagationError>;                 // sloped segment via image-source coords

// coherence.rs
pub struct CoherenceInputs { pub cv2: f64, pub ct2: f64, pub t_air_c: f64, pub c0: f64,
                             pub roughness_r: f64, pub f_delta_nu: f64 /* injected; = 1.0 in Phase 2 */ }
pub fn coherence_f(f_hz: f64, dtau: f64, rho_sep: f64, psi_g: f64, inputs: &CoherenceInputs) -> f64;
```

Existing API consumed (from 01-02/01-03, see SUMMARYs): `geometry::reflect_over_segment` (image-source reflection, grazing angle), `propagation::sound_speed_ms` (Eq. 335, c(15°C)=340.348), `PropagationError` (extend with new variants as needed), `scene::impedance_class`.
</interfaces>

## Artifacts this plan produces

| Artifact | Kind | Public surface |
|----------|------|----------------|
| `envi-engine::propagation::special` | module | `faddeeva_w`, `fresnel_f`, `fresnel_g`, `exp_clamped` — shared numerics for ground (w) and diffraction (f,g) |
| `envi-engine::propagation::ground` | module | `ground_impedance`, `gamma_p`, `spherical_q`, `incoherent_rho` — the reflection-coefficient chain |
| `envi-engine::propagation::rays` | module | `RayVars`, `RayPair`, `straight_rays`, `straight_rays_over_segment` — the Phase 3 circular-ray seam |
| `envi-engine::propagation::coherence` | module | `CoherenceInputs`, `coherence_f` — Ff·FΔν·Fc·Fr·Fs with FΔν injected |
| module shells | files | `fresnel.rs`, `diffraction.rs`, `terrain_effect/mod.rs` registered in `propagation/mod.rs` as doc-comment-only stubs so wave-2 plans never conflict on mod.rs |
| `tools/nord2000_oracle/` | Python pkg | `common.py` (scipy wofz + Q̂ chain, citation-only comments), `gen_ground_fixtures.py` (writes the fixture TOML; NOT a build dependency) |
| `crates/envi-harness/tests/oracle_ground.rs` | test | loads `tests/fixtures/oracle/ground_w_qhat.toml`, compares engine w/Q̂ against oracle grid |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: special.rs — Faddeeva w(ẑ), Fresnel fits f/g, clamped exp' + module shells</name>
  <files>crates/envi-engine/src/propagation/special.rs, crates/envi-engine/src/propagation/mod.rs, crates/envi-engine/src/propagation/fresnel.rs, crates/envi-engine/src/propagation/diffraction.rs, crates/envi-engine/src/propagation/terrain_effect/mod.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — "Verified Physics — Ground Effect" §4 (all three w branches with coefficients, anchor table, quadrant note), "Verified Physics — Diffraction" §10 (Tables 4/5 full-precision coefficients, f/g asymptotes), §6 (exp' Eq. 337 clamp structure), "Architecture Patterns" Pattern 1 (convention quarantine), "Common Pitfalls" 5 (branch borders/quadrants)
  </read_first>
  <behavior>
    - Test 1 (wofz anchors, tol 1e-6 relative): faddeeva_w at 0.5+0.5j == 0.533157+0.230488j, 1+1j == 0.304744+0.208219j, 3+2j == 0.092711+0.128317j, 7+1j == 0.019924+0.139158j
    - Test 2 (quadrants): for each anchor ẑ, the symmetry relations (Eq. 62) hold: w(conj(-ẑ)) == conj(w(ẑ)) and w(-ẑ) == 2·exp(-ẑ²) - w(ẑ), evaluated through the public function at negated/conjugated inputs (all four quadrants exercised — soft ground puts ρ̂ in x<0,y>0; hard ground in y<0)
    - Test 3 (branch-border continuity): |w(z⁺)-w(z⁻)| < 1e-4 straddling |x|=3.9 (x=3.89 vs 3.91 at y=1), |y|=3 (y=2.99 vs 3.01 at x=1), x=6 and y=6 borders
    - Test 4 (Fresnel fits): fresnel_f(0.0) == 0.49997531354311 (a₀, abs 1e-12); fresnel_g(0.0) == 0.50002414586702; continuity at the x=5 asymptote switch: |f(4.999)-f(5.001)| < 1e-3 and same for g; f(10.0) == 1/(10π) exactly (asymptote branch)
    - Test 5 (exp'): exp_clamped(0.0) == 1.0; exp_clamped(-0.5) == e^{-0.5}; exp_clamped(-3.0) == 0.0; continuous at x=-1 and x=-2 within 1e-9 (transcribe the x<-1 middle branch from AV 1106/07 §5.23.3 Eq. 337 during implementation)
  </behavior>
  <action>
    Create `propagation/special.rs` with module-level docs declaring the load-bearing rule: "Nord2000-native module family: time convention e^{−jωt}, outgoing phase e^{+jωτ}, impedance Im > 0 (AV 1106/07). Conversion to ENVI's e^{+jωt} transfer convention happens in exactly ONE function at the TransferSpectrum boundary — never here." (RESEARCH Pattern 1.)

    `faddeeva_w(z)`: reduce to the x≥0, y≥0 quadrant via the Eq. 62 symmetry relations (transcribe the exact case table from PDF p. 36 — pdfplumber page.to_image() renders it; the research formulas already reproduce scipy to ~1e-7, so treat the PDF read as confirmation), then dispatch: (|x|>3.9 ∨ |y|>3) ∧ (x>6 ∨ y>6) → two-pole rational Eq. 63; (|x|>3.9 ∨ |y|>3) otherwise → three-pole rational Eq. 64; else → Matta-Reichel series Eqs. 65-74 with h=0.8, n=1..=5, exactly the P₂/Q₂/H/K assembly printed in RESEARCH §4. Numeric guard: in the w(-ẑ) = 2·exp(-ẑ²) - w(ẑ) reflection, exp(-ẑ²) can overflow for large |Im ẑ| — compute via the real exponent and reject/saturate to a finite value with a doc-comment (the fixture grid in Task 2 covers this region).

    `fresnel_f(x)` / `fresnel_g(x)`: Eq. 85/86 — for x ≥ 5 return 1/(πx) and 1/(π²x³); otherwise evaluate the 12th-order polynomials with the FULL-precision Table 4/5 coefficients from RESEARCH §10 (a₀=0.49997531354311 … a₁₂ — copy every digit; a mistranscribed coefficient still produces smooth plausible curves, which is this phase's stated failure mode). Use Horner evaluation.

    `exp_clamped(x)`: Eq. 337 — e^x for x ≥ −1; the document's linear-tail form for −2 < x < −1 (transcribe from PDF §5.23.3); 0 for x ≤ −2.

    Also in this task: register `mod special; pub mod ground; pub mod rays; pub mod coherence; pub mod fresnel; pub mod diffraction; pub mod terrain_effect;` in `propagation/mod.rs` and create `fresnel.rs`, `diffraction.rs`, `terrain_effect/mod.rs` as doc-comment-only shells ("Implemented in plan 02-02 / 02-03 / 02-02+02-04."). This is deliberate interface-first sequencing: wave-2 plans 02-02 and 02-03 then own disjoint file sets and can execute in parallel. (`ground.rs`/`rays.rs`/`coherence.rs` are created by Tasks 2-3 of this plan; create them as shells here too so the crate compiles after this task.)
  </action>
  <verify>
    <automated>cargo test -p envi-engine special && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>All three w branches match the wofz anchors and are continuous across every branch border and correct in all four quadrants; f/g match Table 4/5 at full precision with continuous asymptote handoff; exp' clamps per Eq. 337; module shells registered so the workspace compiles.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine special` passes behavior tests 1-5
    - faddeeva_w(0.5+0.5j) == 0.533157+0.230488j within 1e-6 relative (research anchor table, all four rows)
    - `grep -n "e^{−jωt}\|e^{-jωt}" crates/envi-engine/src/propagation/special.rs` finds the convention doc
    - `grep -c "0.49997531354311" crates/envi-engine/src/propagation/special.rs` ≥ 1 (Table 4 full precision present)
    - crate compiles with all seven propagation submodules registered
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ground.rs — Ẑ_G, Γ̂_p, Ê, Q̂, ρᵢ + committed scipy oracle with fixture grid</name>
  <files>crates/envi-engine/src/propagation/ground.rs, crates/envi-engine/src/scene.rs, tools/nord2000_oracle/common.py, tools/nord2000_oracle/gen_ground_fixtures.py, crates/envi-harness/tests/fixtures/oracle/ground_w_qhat.toml, crates/envi-harness/tests/oracle_ground.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — "Verified Physics — Ground Effect" §1 (Eq. 57 + kPa unit trap), §2 (Table 2 impedance classes — B = 31.5, resolves Phase 1 A1), §3 (Eqs. 58-60 chain, τ₂ parameterization), §5 (ρᵢ Eqs. 75-76, Assumption A1: verify the √ radical on PDF p. 37 image), "Numeric Anchors" (Ẑ and Q̂ tables), "Anti-Patterns" (never clamp |Q̂| ≤ 1), Pattern 4 (oracle fixtures)
  </read_first>
  <behavior>
    - Test 1 (Ẑ_G anchors, tol 1e-4 relative): ground_impedance at (σ=200, f=1000) == 3.715553+3.675351j; (12.5, 1000) == 1.339444+0.485614j; (200, 100) == 16.270679+19.737805j; (20000, 1000) == 86.873338+105.998290j; (200000, 63) == 3841.191953+4283.317397j — Im positive (Nord2000 e^{−jωt} sign, do NOT flip)
    - Test 2 (Q̂ anchors, tol 1e-4 relative, geometry hS=0.5/hR=1.5/d=97.5, ψ_G=1.175133°, τ₂=R₂/c₀ with c₀=340.348): all six research Q̂ rows, including (20000, 4000) == -0.004250+0.608435j
    - Test 3 (surface-wave regime): |spherical_q| at σ=200, f=250 == 1.257 within 1e-3 — asserts the coefficient legitimately exceeds 1 and no clamp exists
    - Test 4 (ρᵢ): incoherent_rho ∈ (0, 1) for every impedance class A-H at 63/250/1000/4000 Hz; angle-independent (same value regardless of ψ_G — it takes only f and Ẑ_G)
    - Test 5 (impedance classes): scene::impedance_class('B') == Some(31.5) — corrected from Phase 1's assumed 31.6 (Table 2 verified)
    - Test 6 (oracle fixtures): every (z, w) and (σ, f, ψ_G, τ₂, Q̂) row in ground_w_qhat.toml matches the engine within 1e-6 / 1e-4 relative — run via the harness test
  </behavior>
  <action>
    `ground_impedance(f_hz, sigma_kpa)`: Eq. 57 with the kPa convention — argument X = f/σ (σ in kPa·s·m⁻², identical to the FORCE .xls column unit): Ẑ = 1 + 9.08·X^(−0.75) + j·11.9·X^(−0.73). Doc-comment the unit trap (Eq. 57 as printed uses SI Pa·s·m⁻² with 1000f/σ — same number). Reject σ ≤ 0 or non-finite with a typed error (untrusted case-file data, extends the T-01-05 posture).

    `gamma_p(psi_g, z_g)`: Eq. 59 = (sin ψ_G − 1/Ẑ_G)/(sin ψ_G + 1/Ẑ_G). `spherical_q(f, tau2, psi_g, z_g)`: Eq. 58/60 — ρ̂ = ((1+j)/2)·√(ω·τ₂)·(sin ψ_G + 1/Ẑ_G) with ω = 2πf (note the τ₂ travel-time parameterization is deliberate: Phase 3 refraction modifies Q̂ through τ — dovetails with the frozen τ-primitive contract from 01-03); Ê = 1 + j·√π·ρ̂·faddeeva_w(ρ̂); Q̂ = Γ̂_p + (1−Γ̂_p)·Ê. Do NOT clamp |Q̂| (RESEARCH anti-pattern; Test 3 pins it).

    `incoherent_rho(f, z_g)`: Eqs. 75-76 — transcribe the closed-form ᾱ_ri(X = Re Ẑ, Y = Im Ẑ) EXACTLY from PDF p. 37 (render the page image with pdfplumber; the pypdf text dump garbles the arctan/ln arguments), then ρᵢ = √(1−ᾱ_ri) — Assumption A1 says the radical is the physically consistent reading; CONFIRM it on the page image and record the confirmation in a code comment citing "AV 1106/07 Eq. (75), verified on p. 37". ρᵢ is angle-independent → document that callers may precompute per (class, f).

    `scene.rs`: change `impedance_class('B')` from 31.6 to 31.5 with a doc-comment citing Table 2 (verified this phase — resolves Phase 1 Assumption A1); confirm all eight class values against the RESEARCH Table 2 (A 12.5, B 31.5, C 80, D 200, E 500, F 2000, G 20000, H 200000).

    Oracle: create `tools/nord2000_oracle/common.py` — an INDEPENDENT Python implementation of the Q̂ chain using `scipy.special.wofz` (not the document's approximation) plus Eq. 57/59/60/58, with comments citing equation numbers only (no PDF text — licensing rule). `gen_ground_fixtures.py` writes `crates/envi-harness/tests/fixtures/oracle/ground_w_qhat.toml`: a `[[w]]` grid of ~30 points covering all four quadrants and straddling every branch border, and a `[[qhat]]` grid over σ ∈ {12.5, 80, 200, 2000, 20000, 200000} × f ∈ {63, 250, 1000, 4000} × the hS=0.5/hR=1.5/d=97.5 geometry. Header comment: "generated by gen_ground_fixtures.py — DO NOT EDIT" + oracle git-hash provenance field. Run it once and COMMIT the TOML (Python/scipy is not a build dependency; regeneration is operator-driven). Note w-fixture tolerance is 1e-6 relative EXCEPT where the document approximation's intrinsic error vs wofz dominates (~8e-7 relative per research) — use 2e-6 to be safe and comment why.

    `crates/envi-harness/tests/oracle_ground.rs`: integration test in the harness crate (toml already a dependency there — engine I/O quarantine holds) that loads the fixture TOML and asserts every row against `envi_engine::propagation::{special::faddeeva_w, ground::spherical_q}`.
  </action>
  <verify>
    <automated>cargo test -p envi-engine ground && cargo test -p envi-harness --test oracle_ground && cargo tree -p envi-engine -e normal --depth 1</automated>
  </verify>
  <done>The full reflection-coefficient chain matches every research anchor and the committed oracle fixture grid; |Q̂|>1 is preserved; ρᵢ transcribed from the page image with the radical confirmed; impedance class B corrected to 31.5; engine dependency quarantine intact (ndarray/num-complex/thiserror only).</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine ground` passes behavior tests 1-5
    - `cargo test -p envi-harness --test oracle_ground` passes (fixture grid ≥ 30 w-points + ≥ 20 Q̂-points, all four quadrants covered)
    - spherical_q(1000, τ₂, ψ_G, Ẑ(20000)) == +0.797560+0.416202j within 1e-4 (hard-ground anchor)
    - `cargo tree -p envi-engine -e normal --depth 1` lists only ndarray, num-complex, thiserror
    - tools/nord2000_oracle/common.py exists, imports scipy, contains equation-number citations
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: rays.rs + coherence.rs — straight-ray seam with cancellation-free Δτ, F coefficients</name>
  <files>crates/envi-engine/src/propagation/rays.rs, crates/envi-engine/src/propagation/coherence.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — §9 (homogeneous ray variables, the ΔR identity, sloped-segment guidance, RayVars seam), §6 (Eqs. 110-114: Ff glyph-verified x = 0.23·π·f·Δτ; Fc/Fr structure with Assumptions A3/A4 — constants must be transcribed from PDF pp. 52-53 images), Pattern 2 (RayVars struct shape)
    - .planning/phases/01-force-harness-geometry-model-direct-path/01-02-SUMMARY.md — geometry::reflect_over_segment API (image-source reflection, grazing angle, r1+r2)
  </read_first>
  <behavior>
    - Test 1 (geometry anchor, hS=0.5, hR=1.5, d=97.5, c₀=340.348): R₁ == 97.505128070, R₂ == 97.520510663, ΔR == 1.538259287e-2, Δτ == 4.519660952e-5 s, ψ_G == 1.175133° — each 1e-8 relative (research geometry anchor)
    - Test 2 (cancellation regression): at hS=0.01, hR=1.5, d=1000, the identity ΔR = 4·hS·hR/(R₁+R₂) agrees with an f128-free reference (compute R₂²−R₁² = 4·hS·hR exactly in expanded form) while naive R₂−R₁ in f64 deviates — assert the identity value to 1e-12 relative against 4·hS·hR/(R₁+R₂) evaluated symbolically, and document the naive-subtraction error magnitude in the test
    - Test 3 (sloped segment): straight_rays_over_segment on the 01-02 sloped anchor (reflection at x=5, path √104) produces R₂ == √104 and a Δτ consistent with the image-source expansion, no NaN
    - Test 4 (Ff): x = 0.23·π·f·Δτ; Ff == 1 at Δτ=0; == sin(x)/x for 0 < x ≤ π (spot-check f=646.7 Hz, Δτ=4.519660952e-5 → Ff ≈ 0.99993 per research); == 0 for x > π
    - Test 5 (F composition): coherence_f with cv2=ct2=0, roughness 0, f_delta_nu=1 equals Ff exactly (FORCE cases 1-8 regime); with cv2=0.12, ct2=0.008 (FORCE mixed/screen turbulence, forced even at u=0) Fc < 1 and F < Ff at high f; F is monotonically non-increasing in f for fixed geometry once x > 0
  </behavior>
  <action>
    `rays.rs`: implement `RayVars`/`RayPair` exactly as the Pattern 2 seam (fields tau, r, psi_g, r1, r2 — Phase 3 adds a `circular(...)` constructor behind identical fields; doc-comment this contract). `straight_rays(d, h_s, h_r, c0)`: R₁ = √(d²+(hR−hS)²), image-source R₂ = √(d²+(hR+hS)²), τᵢ = Rᵢ/c₀, ψ_G = atan((hS+hR)/d); dtau via the EXACT cancellation-free identity ΔR = (R₂²−R₁²)/(R₂+R₁) = 4·hS·hR/(R₁+R₂) (the document itself warns τ₂−τ₁ loses precision — RESEARCH §9; this is a numerics house rule, cite it). `straight_rays_over_segment`: reuse `geometry::reflect_over_segment` for the reflection point/ψ_G/r1/r2, and compute ΔR via image-source coordinates: R₂²−R₁² = |S′R|²−|SR|² expanded as a difference of dot products (no catastrophic subtraction) — per RESEARCH §9 sloped-segment guidance. Typed errors on degenerate inputs (d ≤ 0, non-finite, hS/hR < 0).

    `coherence.rs`: `coherence_f(f, dtau, rho_sep, psi_g, inputs)` = Ff(f,Δτ) · inputs.f_delta_nu · Fc(...) · Fr(...) · Fs, per Eq. 110. Ff per Eq. 111 (glyph-verified constant 0.23). Fc per Eq. 113 and Fr per Eq. 114: the research marks the exact constants/exponents as Assumptions A3/A4 — TRANSCRIBE them from AV 1106/07 PDF pp. 52-53 page images (pdfplumber to_image) during this task and record "verified on p. 52/53" comments; the structural forms (Fc = exp'(−x) with x ∝ (CT²/(273.15+t)² + (22/3)·Cv²/c²)·f²·ρ^{5/3}·d; Fr = exp'-form in (k₀·r·sinψ_G)² with the g(X) polynomial) are fixed. Both route through `special::exp_clamped`. FΔν: injected via `inputs.f_delta_nu` — Phase 2 callers pass 1.0; doc-comment the Phase 3 seam (needs Δτ⁺ under the A⁺ profile, per research Open Question 2 resolution). Fs: stub = 1.0 with a doc-comment citing the research decision (scattering-zone factor; not active for Phase 2 target cases) — this is a documented deferral, not silent scope reduction. rho_sep (transversal separation) is a CALLER responsibility (flat ground: ρ = 2·hS·hR/(hS+hR), Eq. 119; screen sub-models use Eqs. 178/180) — keep coherence.rs geometry-free.
  </action>
  <verify>
    <automated>cargo test -p envi-engine rays && cargo test -p envi-engine coherence</automated>
  </verify>
  <done>RayPair produces the research geometry anchor to 1e-8, survives the hS=0.01/d=1000 cancellation worst case, and handles sloped segments; F = Ff·FΔν·Fc·Fr·Fs with Ff anchored, Fc/Fr transcribed from page images, FΔν injected, Fs documented stub.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine rays coherence` passes behavior tests 1-5
    - ΔR == 1.538259287e-2 at the research geometry within 1e-8 relative
    - coherence_f(646.7, 4.519660952e-5, ρ, ψ, zero-turbulence inputs) ≈ 0.99993 (abs 1e-4)
    - `grep -n "f_delta_nu" crates/envi-engine/src/propagation/coherence.rs` finds the injected seam
    - `grep -n "4·hS·hR\|4.0 \* h_s \* h_r" crates/envi-engine/src/propagation/rays.rs` finds the cancellation-free identity
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Case-file σ/roughness/geometry → ground/rays constructors | Untrusted terrain data crosses into complex numerics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | DoS (NaN/Inf propagation) | faddeeva_w reflection branch, spherical_q, incoherent_rho | mitigate | overflow guard on 2·exp(−ẑ²); branch-border + four-quadrant fixture tests; typed errors on σ ≤ 0 / non-finite; no panic on data |
| T-02-02 | DoS (catastrophic cancellation) | rays::dtau | mitigate | cancellation-free ΔR identity is the only Δτ path; regression test at hS=0.01/d=1000 |
| T-02-03 | Tampering (silently plausible wrongness) | Table 4/5 coefficients, Fc/Fr constants | mitigate | full-precision coefficients pinned by tests; committed scipy oracle as independent cross-implementation; page-image transcription with "verified on p. NN" comments |
| T-02-04 | Info disclosure (copyright) | oracle scripts, code comments | mitigate | equations cited by report+number only; no PDF text/figures committed; refs/ stays gitignored |
| T-02-SC | Tampering | cargo/pip installs | accept | zero new dependencies (errorfunctions explicitly rejected as SUS in research audit); Python oracle uses the pre-existing local scipy, never a build input |
</threat_model>

<verification>
- `cargo test --workspace` green: w anchors + quadrants + borders, f/g precision, Q̂ chain anchors, |Q̂|>1 preserved, ρᵢ bounds, ΔR anchor + cancellation regression, Ff/Fc/Fr behavior, oracle fixture grid
- `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` clean; `#![deny(unsafe_code)]` holds
- `cargo tree -p envi-engine -e normal --depth 1` — only ndarray, num-complex, thiserror
</verification>

<success_criteria>
- The complete Nord2000-native reflection-coefficient chain (Eqs. 57-77), Faddeeva/Fresnel special functions (61-74, 85-86), straight-ray variables with guarded Δτ, and coherence F (110-114 with the FΔν seam) are implemented, anchored, and oracle-cross-checked — the numerics foundation ENG-02/ENG-03/ENG-07 build on in plans 02-02..02-05
- Wave-2 plans can start: fresnel.rs / diffraction.rs / terrain_effect shells registered, all contracts in this plan's interfaces block live
</success_criteria>

<output>
Create `.planning/phases/02-ground-effect-diffraction/02-01-SUMMARY.md` when done, recording: the confirmed ρᵢ radical reading (Assumption A1), the transcribed Fc/Fr constants (Assumptions A3/A4), any deviation of the document's Eq. 62 case table from the standard Faddeeva symmetries, and the oracle provenance hash.
</output>
