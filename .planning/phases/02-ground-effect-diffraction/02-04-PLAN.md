---
phase: 02-ground-effect-diffraction
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - crates/envi-engine/src/geometry.rs
  - crates/envi-engine/src/propagation/terrain_effect/mod.rs
  - crates/envi-engine/src/propagation/terrain_effect/screen.rs
  - crates/envi-engine/src/propagation/terrain_effect/submodel7.rs
  - tools/nord2000_oracle/gen_screen_fixtures.py
  - crates/envi-harness/tests/fixtures/oracle/screen_thin.toml
  - crates/envi-harness/tests/oracle_screen.rs
autonomous: true
requirements: [ENG-03, ENG-07]

must_haves:
  truths:
    - "Sub-model 4 computes the four-path image model p̂ = p̂₁ + Q̂₁p̂₂ + Q̂₂p̂₃ + Q̂₁Q̂₂p̂₄ (Eq. 157) with each p̂ᵢ a COMPLEX pwedge pressure — and the partially-coherent combination (Eq. 182) splits into the F-weighted coherent complex sum (h_coh channel) plus the (1−F²)·|ρᵢ·…|² residuals (p_incoh channel), never a scalar energy sum"
    - "The screen coherent factor handed upward is the COMPLEX ratio p̂/p̂₀ (its magnitude equals Nord2000's Δp_SCR·|Δp_G| level quantity, its phase carries the over-the-top path delay) — per the user-locked phase-coherence contract; Nord2000's Eq. 163 magnitude form is used ONLY inside the dB level, verified equal to |complex factor| at 1e-12"
    - "Sub-models 5 and 6 are the SAME generic screen engine with p2edge / p2wedge kernels and 2/3-region ray sets — SM6's eight-ray set (Eq. 222) is the four-ray set × middle-region reflection encoded as a region bitmask"
    - "Sub-model 7 turbulence scattering uses Cve² = 10·Cv² / CTe² = 10·CT² (Eq. 271 — the empirical boost is deliberate, cited in a comment) and only ever RAISES ΔL_t (adds energy) — never increases screen attenuation"
    - "One thin-screen geometry (FORCE case-71 shape, Cv²=0.12/CT²=0.008 forced) matches the committed scipy oracle ΔL₄+ΔL₇ curve within 0.1 dB at all 105 points"
  artifacts:
    - path: "crates/envi-engine/src/propagation/terrain_effect/screen.rs"
      provides: "generic screen engine (DiffractionKernel trait + ray-set/region combinatorics) realizing Sub-models 4/5/6 (Eqs. 157-~270)"
      min_lines: 200
    - path: "crates/envi-engine/src/propagation/terrain_effect/submodel7.rs"
      provides: "turbulence scattering behind screens (§5.16, Eq. 271-272, Tables 6/7 2-D interpolation)"
      min_lines: 80
    - path: "crates/envi-engine/src/geometry.rs"
      provides: "§5.23 helpers: SegmentVariables (Eq. 383), ImagePoint (370), NormLine (377), VertDist (390), WedgeCross (392)"
      contains: "wedge_cross"
  key_links:
    - from: "crates/envi-engine/src/propagation/terrain_effect/screen.rs"
      to: "crates/envi-engine/src/propagation/diffraction.rs"
      via: "kernel calls: pwedge (SM4) / p2edge (SM5) / p2wedge (SM6) per ray"
      pattern: "pwedge"
    - from: "crates/envi-engine/src/propagation/terrain_effect/screen.rs"
      to: "crates/envi-engine/src/propagation/ground.rs"
      via: "Q̂₁/Q̂₂(/Q̂₃) per reflecting region, endpoint at the screen top (τ₂ = side-local reflected travel time)"
      pattern: "spherical_q"
    - from: "crates/envi-engine/src/propagation/terrain_effect/screen.rs"
      to: "crates/envi-engine/src/propagation/coherence.rs"
      via: "F₂/F₃/F₄ per ray (Eqs. 177-181, side-local Δτ and ρ separations)"
      pattern: "coherence_f"
    - from: "crates/envi-engine/src/propagation/terrain_effect/submodel7.rs"
      to: "crates/envi-engine/src/propagation/terrain_effect/screen.rs"
      via: "C_SR ground-effect correction = ground part of Eq. 188 floored at 1"
      pattern: "c_sr"
---

<objective>
Implement the screen⇄ground interaction: one generic screen engine (RESEARCH Pattern 3) realizing Sub-model 4 (single screen, one edge — four-path image model, Eqs. 157-188), Sub-model 5 (thick screen — p2edge kernel, Eqs. 189-221), and Sub-model 6 (two screens — eight-ray set with middle-region reflection, Eqs. 222-~270), plus Sub-model 7 (turbulence scattering behind screens, §5.16) which floors screen attenuation in every FORCE screen case (they all carry Cv²=0.12, CT²=0.008 even at u=0).

Purpose: this is the heart of ENG-03 (screens interacting with segmented-impedance ground) and the four-ray generalization of ENG-07's two-channel combination. Per the user-locked phase-coherence contract (PROJECT.md Key Decisions, 2026-07-07), every ray p̂ᵢ stays a complex pwedge pressure and the coherent channel is the F-weighted COMPLEX sum — the (1−F²) residuals live exclusively in p_incoh. The research's Open Question 3 (whether the screen factor loses its phase in |Δp_SCR|) is resolved by that locked decision: the factor handed to the transfer chain stays complex; the magnitude form appears only inside the dB level and is asserted equal to the complex factor's magnitude.

Output: `terrain_effect::{screen, submodel7}` + the §5.23 geometry helpers, thin-screen oracle curve green, ScreenResult carrying the same two-channel shape as GroundResult.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ground-effect-diffraction/02-RESEARCH.md
@.planning/phases/02-ground-effect-diffraction/02-02-SUMMARY.md
@.planning/phases/02-ground-effect-diffraction/02-03-SUMMARY.md
@.planning/phases/01-force-harness-geometry-model-direct-path/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Contracts this plan CREATES for 02-05. -->

```rust
// terrain_effect/screen.rs
pub trait DiffractionKernel {   // pwedge (SM4) | p2edge (SM5) | p2wedge (SM6)
    fn diffract(&self, f_hz: f64, ray: &ScreenRayGeometry) -> Result<Complex<f64>, PropagationError>;
}
pub struct ScreenConfig<'a> { /* screen shape (≤3 pts), regions before/middle/after with
                                 per-region SurfaceStrip lists, wedge-face impedances
                                 (= σ of segments adjacent to the edge), coh: &'a CoherenceInputs */ }
/// Same two-channel shape as GroundResult; delta_l_db = 10·lg(|h_coh_factor|² + p_incoh)
pub fn submodel4(f_hz: f64, cfg: &ScreenConfig) -> Result<GroundResult, PropagationError>;
pub fn submodel5(f_hz: f64, cfg: &ScreenConfig) -> Result<GroundResult, PropagationError>;
pub fn submodel6(f_hz: f64, cfg: &ScreenConfig) -> Result<GroundResult, PropagationError>;

// terrain_effect/submodel7.rs — additive scattered ENERGY (real), combined by 02-05 per Eq. 332
pub fn submodel7_delta_l(f_hz: f64, geo: &ScreenScatterGeometry, cv2: f64, ct2: f64, c_sr: f64)
    -> Result<f64, PropagationError>;

// geometry.rs additions (pure 2-D helpers, doc'd with §5.23 equation numbers)
pub fn segment_variables(...) -> SegmentVariables;  // Eq. 383
pub fn image_point(...) -> [f64; 2];                // Eq. 370
pub fn norm_line(...) -> (f64, f64);                // Eq. 377 (projection + signed distance)
pub fn vert_dist(...) -> f64;                       // Eq. 390
pub fn wedge_cross(...) -> Option<[f64; 2]>;        // Eq. 392 (equivalent wedge top)
```

Consumes: `GroundResult` (02-02), `diffraction::{pwedge, p2wedge, p2edge, WedgeGeometry}` (02-03 — use the frozen argument lists from its SUMMARY), `ground::{spherical_q, incoherent_rho}`, `coherence::coherence_f`, `fresnel::*` with F_λ = λ/16 (Eq. 174), `geometry::reflect_over_segment` (01-02).
</interfaces>

## Artifacts this plan produces

| Artifact | Kind | Public surface |
|----------|------|----------------|
| `envi-engine::geometry` additions | fns | SegmentVariables/ImagePoint/NormLine/VertDist/WedgeCross (§5.23) — pure 2-D vector math |
| `envi-engine::propagation::terrain_effect::screen` | module | DiffractionKernel trait + generic engine; submodel4/5/6 with two-channel GroundResult |
| `envi-engine::propagation::terrain_effect::submodel7` | module | Tables 6/7 interpolation, Eq. 271 effective strengths, additive ΔL |
| oracle fixture | TOML | `screen_thin.toml`: ΔL₄+ΔL₇ 105-point curve for the case-71 thin-screen geometry with forced turbulence |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: §5.23 geometry helpers + generic screen engine + Sub-model 4</name>
  <files>crates/envi-engine/src/geometry.rs, crates/envi-engine/src/propagation/terrain_effect/screen.rs, crates/envi-engine/src/propagation/terrain_effect/mod.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — §12 IN FULL (Eq. 157 four-path model, Q̂ evaluation "as if the other endpoint were at the screen top", Eqs. 158-163 separation, Eq. 182 partial-coherent 4-ray combination, F₂/F₃/F₄ per Eqs. 177-181 with ρᵢ = 2h′h′/(h′+h′) separations, Fresnel weights Eq. 174 at F_λ=λ/16 + rS/rR edge-proximity modifiers Eqs. 175-176, normalization Eq. 187, final level Eq. 188 — Open Question 5: transcribe the exact double-sum normalization from PDF pp. 79-80), Pattern 3 (generic engine), Pitfall 4 (normalization branches differ from §5.8)
    - .planning/phases/02-ground-effect-diffraction/02-03-SUMMARY.md — frozen kernel signatures and the angle-modification behavior for image rays
  </read_first>
  <behavior>
    - Test 1 (geometry helpers): each §5.23 helper pinned by a hand-computed anchor (e.g. wedge_cross of two non-adjacent segments; image_point over a sloped segment matching 01-02's reflect_over_segment where applicable); degenerate inputs → typed errors, never NaN
    - Test 2 (four-path structure): with all F forced to 1 and a transparent kernel stub (diffract ≡ free-field ray), submodel4 reduces to |1 + Q̂₁·r₂ + Q̂₂·r₃ + Q̂₁Q̂₂·r₄|-form — verify against a hand-assembled Eq. 157 sum at three frequencies
    - Test 3 (two-channel split): 10·lg(|h_coh_factor|² + p_incoh) == delta_l_db to 1e-12; setting Cv²=CT²=0 and Δτ-small makes p_incoh ≪ |h_coh|² and forcing F≡1 gives p_incoh == 0 exactly
    - Test 4 (complex screen factor): |h_coh_factor| equals the Eq. 163/188-form level quantity (Δp_SCR·|coherent Δp_G sum|) at 1e-12 relative, AND arg(h_coh_factor) is non-trivial (non-zero, frequency-dependent) — the over-the-top phase is retained, not discarded
    - Test 5 (weight normalization, Pitfall 4): normalized weights ∈ [0,1]; per-region sums obey Eq. 187's excess-over-1 rule (property test on 1/2/4-strip regions per side)
    - Test 6 (screening physics): thin screen over hard ground: ΔL₄ < 0 in deep shadow at all f ≥ 250 Hz; removing the screen (edge below the S-R line) recovers the flat-ground Sub-model 1 result within 0.5 dB
  </behavior>
  <action>
    `geometry.rs`: add the five §5.23 pure helpers with the document's exact signatures in doc comments (Eqs. 370/377/383/390/392). Reuse existing reflect_over_segment internals where they overlap; keep everything f64, typed errors on degenerate segments (zero length guarded — the FORCE 0.01 m spikes are near-degenerate but valid).

    `screen.rs`: implement Pattern 3 — a generic engine parameterized by (DiffractionKernel, ray set, region count). Sub-model 4: ray set p̂₁ (S→T→R), p̂₂ (image-S→T→R), p̂₃ (S→T→image-R), p̂₄ (both images), each ray a kernel call whose WedgeGeometry uses the image coordinates (02-03's angle-modification scheme handles inside-the-wedge images). Q̂₁/Q̂₂ = spherical_q of the ground regions before/after the screen, evaluated with the other endpoint AT THE SCREEN TOP (τ₂ = side-local reflected travel time — research §12 prescription). F₂/F₃/F₄ per Eqs. 177-181 (side-local Δτ_S, Δτ_R, both for ray 4; ρ separations per Eqs. 178/180). Fresnel weights per Eq. 174 (F_λ = λ/16) with the rS/rR edge-proximity clamps of Eqs. 175-176 (transcribe the h_max clamp forms from the PDF); Q̂′ᵢ = w_Q·Q̂ᵢ; normalization per Eq. 187. Combination per Eq. 182: h_coh_factor(region pair) = 1 + F₂w″₁Q̂′₁(p̂₂/p̂₁) + F₃w″₂Q̂′₂(p̂₃/p̂₁) + F₄w″₁w″₂Q̂′₁Q̂′₂(p̂₄/p̂₁); p_incoh(region pair) = (1−F₂²)|w″₁ρ₁p̂₂/p̂₁|² + (1−F₃²)|w″₂ρ₂p̂₃/p̂₁|² + (1−F₄²)|w″₁w″₂ρ₁ρ₂p̂₄/p̂₁|². Iterate the general model (§5.13.2) over all combinations of reflecting strips before × after the screen (screen-shape segments excluded; wedge-face impedances = σ of the two segments adjacent to the edge), with the Eq. 188 double-sum normalization — TRANSCRIBE Eqs. 187-188 from PDF pp. 79-80 page images (research Open Question 5: the pypdf dump is ambiguous; the page image is authoritative; the oracle must implement the same reading).

    Two-channel output (user-locked contract): the full screen factor = (complex p̂₁/p̂₀) · (weighted coherent Δp̂_G sum) — COMPLEX end to end; p̂₀ = free-field direct pressure e^{+jωτ_SR}/R_SR (Nord-native), so |p̂₁/p̂₀| reproduces Eq. 163's Δp_SCR = |p̂₁|·R_SR exactly while the phase carries the over-the-top delay. delta_l_db = 10·lg(|h_coh_factor|² + p_incoh_total). Doc-comment that this is the phase-preserving reading of Eqs. 163+188 mandated by PROJECT.md (research Open Q3 resolved); the magnitude is IDENTICAL to Nord2000's level, so oracle/FORCE validation is unaffected.

    Shadow-zone branches (Eqs. 184-186) are ξ<0 refraction — Phase 3; document the attachment point (same pattern as Sub-model 1).
  </action>
  <verify>
    <automated>cargo test -p envi-engine screen && cargo test -p envi-engine geometry</automated>
  </verify>
  <done>Sub-model 4 produces the four-path partially-coherent combination with a complex screen factor whose magnitude matches the document's level form at 1e-12, weights normalize per Eq. 187/188 as transcribed from the page images, and screening physics sanity holds.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine screen geometry` passes behavior tests 1-6
    - |h_coh_factor| == level-form magnitude at 1e-12 AND arg(h_coh_factor) frequency-dependent (Test 4 — the contract pin)
    - Screen-removed limit recovers Sub-model 1 within 0.5 dB
    - Eq. 187/188 transcription cited with "verified on p. 79-80" comments
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sub-models 5 and 6 via kernel/region parameterization</name>
  <files>crates/envi-engine/src/propagation/terrain_effect/screen.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — §12 "Sub-model 5" and "Sub-model 6" (Eq. 222 eight-ray model: images of T₁/T₂ in the middle segment, three Fresnel regions each with weights and normalization; SM5 = SM4 with p2edge; the implementation strategy sentence: ONE generic engine, SM6's 8-ray set = 4-ray set × {with, without} middle-region reflection as a region bitmask), §14 (FORCE encodings: case 81 thick 15-30 m h=2; case 91 thin at 15 + trapezoid 75-85)
  </read_first>
  <behavior>
    - Test 1 (SM5 = SM4 shape): submodel5 on a thick screen equals submodel4's structure with the p2edge kernel — verify the thin-limit: a "thick" screen of 1 cm thickness matches submodel4 of the equivalent thin screen within 0.3 dB across 250-4000 Hz
    - Test 2 (SM6 eight rays): with all F ≡ 1, transparent kernels, and unit Q̂s, submodel6 assembles exactly the Eq. 222 eight-term sum (hand-check at one frequency with distinct marker values per ray via a stub kernel)
    - Test 3 (region bitmask): disabling the middle region (no reflecting middle segment) collapses SM6 to the four-term subset {p̂₁, Q̂₁p̂₂, Q̂₃p̂₃, Q̂₁Q̂₃p̂₄}
    - Test 4 (physics): FORCE case-81 (thick) and case-91 (double) literal geometries: finite two-channel results at all 105 points; double screens attenuate ≥ the single thin screen alone in deep shadow
  </behavior>
  <action>
    Extend the generic engine: SM5 swaps the kernel to p2edge (shared top segment, forced-hard top, factor 0.5 — all inside the 02-03 kernel) with wedge angles per Eqs. 194-195 and the SM5-specific ray/weight details of Eqs. 189-221 (transcribe deltas from the PDF where they diverge from SM4 — the research says "structurally identical"; treat every printed difference as intentional). SM6: eight-ray set per Eq. 222 with Q̂₂ = the middle-region coefficient and rays 5-8 the middle-reflection images of T₁/T₂; encode as the region bitmask over {before, middle, after}; three Fresnel regions each with weights + per-region normalization; kernel = p2wedge with primacy passed through (the most-important-wedge decision arrives from §5.21 in plan 02-05 — take it as a parameter, default = first wedge, document).

    Keep coherence per ray (F factors for source-side/receiver-side/middle reflections per the printed Δτ arguments) and the two-channel split exactly as SM4 — the engine code is shared, so Tests 1-3 pin the parameterization rather than re-testing the combination math.
  </action>
  <verify>
    <automated>cargo test -p envi-engine screen</automated>
  </verify>
  <done>SM5/SM6 are parameterizations of the one engine; thin-limit and eight-ray assembly verified; FORCE case-81/91 literal geometries finite end to end.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine screen` passes behavior tests 1-4
    - Thick 1 cm ≈ thin within 0.3 dB (Test 1)
    - Eq. 222 stub-kernel assembly matches term-for-term (Test 2)
    - No separate copy of the combination math exists for SM5/SM6 (one engine — verify by module structure, not duplicated Eq. 182 code)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sub-model 7 turbulence scattering + thin-screen oracle curve</name>
  <files>crates/envi-engine/src/propagation/terrain_effect/submodel7.rs, crates/envi-engine/src/propagation/terrain_effect/mod.rs, tools/nord2000_oracle/gen_screen_fixtures.py, crates/envi-harness/tests/fixtures/oracle/screen_thin.toml, crates/envi-harness/tests/oracle_screen.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — §13 IN FULL (Eq. 271 effective strengths ×10 deliberate; Tables 6/7 2-D interpolation in 40·R₂/R₁ and 40·h_e/R₁; h_e = edge height above the SR line, ignored if below; reciprocity fix = compute both orientations take max; C_SR = ground part of Eq. 188 floored at 1; f²/Coft frequency scaling per Eq. 272; enters Eq. 332 as additive energy), Pitfall 7 (the ×10 looks like a bug — cite Eq. 271), Assumption A5 (Tables 6/7 extraction from pp. 117-119; fall back to page-image transcription if pdfplumber tables are unreliable)
  </read_first>
  <behavior>
    - Test 1 (table interpolation): submodel7_delta_l is continuous in both table parameters (no jumps > 0.5 dB for 1 % parameter steps at interior points); values at exact table nodes reproduce the transcribed entries
    - Test 2 (energy direction): for the case-71 geometry with Cv²=0.12/CT²=0.008, ΔL₄+ΔL₇ ≥ ΔL₄ at every frequency (scattering only ADDS energy / floors the attenuation — research property test); with Cv²=CT²=0, ΔL₇ contributes nothing
    - Test 3 (reciprocity): swapping source and receiver orientations yields the same ΔL₇ (the max-of-both-orientations rule)
    - Test 4 (edge below SR line): h_e ≤ 0 → zero contribution, finite
    - Test 5 (oracle curve): engine ΔL₄+ΔL₇ for the case-71 thin-screen geometry (forced turbulence, FΔν=1) matches screen_thin.toml within 0.1 dB at all 105 points
  </behavior>
  <action>
    Transcribe Tables 6/7 from PDF pp. 117-119 (pdfplumber table extraction first; if any cell is ambiguous, render page images and read visually — Assumption A5 fallback; commit the tables as Rust const arrays with a "transcribed from AV 1106/07 Tables 6/7, pp. 117-119" citation). Implement: effective strengths Cve² = 10·Cv², CTe² = 10·CT² with the Pitfall 7 comment citing Eq. 271 ("the original model underestimated scattering; the ×10 effective strengths are deliberate — do not 'fix'"); bilinear interpolation in (40·R₂/R₁, 40·h_e/R₁) with edge clamping; the reciprocity max; the C_SR ground-effect correction parameter (computed by the caller from the SM4 ground part of Eq. 188, floored at 1 — expose a helper on the screen engine that returns it); frequency scaling via f² and Coft per Eq. 272 (transcribe from the PDF).

    ΔL₇ is a real, additive-ENERGY term — it never touches the coherent channel: `terrain_effect/mod.rs` gains a doc note that Eq. 332's ΔL_scr combinations (ΔL₄+ΔL₇,₁ etc., assembled in plan 02-05) add ΔL₇ energy into the p_incoh/level side only, keeping h_coh_factor untouched (user-locked contract: nothing overwrites phase).

    Oracle: `gen_screen_fixtures.py` implements SM4+SM7 in Python for the literal case-71 thin-screen geometry (screen spike at x=15 m per RESEARCH §14, FORCE-like S/R, Cv²=0.12, CT²=0.008, su=0.5-driven inputs, FΔν=1) using the SAME Eq. 187-188 reading as the Rust engine (cross-checking both against case-71's smooth ΔL(f) shape catches misreads — research Open Q5 protocol). Commit `screen_thin.toml` with provenance; `oracle_screen.rs` compares at ≤ 0.1 dB.
  </action>
  <verify>
    <automated>cargo test -p envi-engine submodel7 && cargo test -p envi-harness --test oracle_screen</automated>
  </verify>
  <done>Sub-model 7 floors screen attenuation with the deliberate ×10 strengths and table interpolation, contributes only additive energy, and the full ΔL₄+ΔL₇ thin-screen curve matches the oracle at 0.1 dB.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine submodel7` passes behavior tests 1-4
    - `cargo test -p envi-harness --test oracle_screen` passes: 105/105 points within 0.1 dB
    - ΔL₄+ΔL₇ ≥ ΔL₄ everywhere on the turbulent case (Test 2)
    - `grep -n "Eq. (271)\|Eq. 271" crates/envi-engine/src/propagation/terrain_effect/submodel7.rs` finds the ×10 citation comment
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Screen/terrain shapes from case files → screen engine ray-set construction | Untrusted near-degenerate screen encodings (0.01 m spikes, trapezoids) cross into image/wedge math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | DoS (NaN/Inf) | image rays inside wedges, degenerate regions, table-edge extrapolation | mitigate | 02-03 angle-modification scheme + typed errors on degenerate segments; Tables 6/7 edge clamping; case-71/81/91 literal-geometry finiteness tests |
| T-02-12 | Tampering (silently plausible wrongness) | Eq. 187-188 normalization, Tables 6/7 transcription, SM5/SM6 deltas | mitigate | page-image transcription with citations; stub-kernel term-for-term assembly tests; oracle implements the same Eq. 188 reading and must agree at 0.1 dB; table-node reproduction tests |
| T-02-13 | Tampering (phase/channel corruption) | complex screen factor, ΔL₇ additive energy | mitigate | |h_coh| == level-form magnitude at 1e-12 + frequency-dependent arg test; ΔL₇ never touches h_coh (structural: submodel7 returns f64 only) |
| T-02-SC | Tampering | installs | accept | zero new dependencies |
</threat_model>

<verification>
- `cargo test --workspace` green: §5.23 helper anchors, SM4 four-path assembly + two-channel identities + complex-factor pin, SM5 thin-limit, SM6 eight-ray assembly + bitmask, SM7 tables/direction/reciprocity, oracle thin-screen curve
- `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`, `#![deny(unsafe_code)]`, engine dep quarantine
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 2 (propagation-level reading per the acceptance ladder): single-edge, thick, and double screens over segmented-impedance ground compute as complex-pressure four/eight-path models with turbulence-scattering floors — the machinery FORCE cases 71-94 require, validated against the committed oracle
- ENG-03 complete at sub-model level; ENG-07's multi-ray two-channel combination proven on the hardest case (screens)
</success_criteria>

<output>
Create `.planning/phases/02-ground-effect-diffraction/02-04-SUMMARY.md` when done, recording: the transcribed Eq. 187-188 normalization reading (Open Q5 resolution), the SM5/SM6 printed deltas encountered, Tables 6/7 extraction method used, and the C_SR helper signature for 02-05.
</output>
