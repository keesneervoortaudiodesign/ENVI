---
phase: 02-ground-effect-diffraction
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - crates/envi-engine/src/propagation/fresnel.rs
  - crates/envi-engine/src/propagation/terrain_effect/mod.rs
  - crates/envi-engine/src/propagation/terrain_effect/submodel1.rs
  - crates/envi-engine/src/propagation/terrain_effect/submodel2.rs
  - tools/nord2000_oracle/gen_flat_fixtures.py
  - crates/envi-harness/tests/fixtures/oracle/flat_sigma200.toml
  - crates/envi-harness/tests/fixtures/oracle/flat_mixed_case21.toml
  - crates/envi-harness/tests/oracle_flat.rs
autonomous: true
requirements: [ENG-02, ENG-07]

must_haves:
  truths:
    - "Sub-model 1 at hS=0.5/hR=1.5/d=97.5/σ=200 with F=Ff reproduces the research ΔL_flat table (+5.2099 dB @100 Hz … −0.0726 @4 kHz) within ±0.05 dB, and the deepest dip lands on the 1/12-octave grid point 630.96 Hz or 667.42 Hz (the discrete neighbours of 646.7 Hz)"
    - "The dip sits BELOW the hard-ground 1/(2Δτ) frequency for σ=200 — arg Q̂ > 0 pulls dips down; this pins the e^{+j2πfΔτ}·Q̂ phase convention (Pitfall 1 dip-asymmetry test)"
    - "GroundResult carries TWO channels: h_coh_factor (Complex<f64>, the F-weighted coherent sum with live Δτ phase) and p_incoh (real, ONLY the (1−F²)·(R₁/R₂)²·ρᵢ² residual) — and 10·lg(|h_coh_factor|² + p_incoh) equals delta_l_db to 1e-12"
    - "With F→1 (zero turbulence, Δτ→0 band-averaging limit), p_incoh → 0 and the result is pure coherent interference — the incoherent channel NEVER overwrites phase"
    - "Sub-model 2 groups segments by surface TYPE (σ,r) — the four-segment/two-type FORCE case-21 profile produces exactly two ΔL_{ii,ir} evaluations per type pair, not four (Pitfall 3)"
    - "Engine ΔL_t curves for the case-1 and case-21 flat geometries match the committed scipy oracle within 0.1 dB at every 1/12-octave point"
  artifacts:
    - path: "crates/envi-engine/src/propagation/terrain_effect/submodel1.rs"
      provides: "Eqs. 115-123 homogeneous flat-terrain ground effect with two-channel GroundResult"
      min_lines: 80
    - path: "crates/envi-engine/src/propagation/terrain_effect/submodel2.rs"
      provides: "Eqs. 124-133 multi-surface Fresnel-weight blending + PhaseDiffFreq (Eqs. 378-381)"
      min_lines: 100
    - path: "crates/envi-engine/src/propagation/fresnel.rs"
      provides: "CalcFZd, FresnelZoneSize/W/Wm (§5.23.4-7, Eqs. 338-353)"
      min_lines: 60
    - path: "crates/envi-harness/tests/fixtures/oracle/flat_sigma200.toml"
      provides: "oracle-pinned 105-point ΔL_t curve, flat σ=200"
      contains: "oracle"
  key_links:
    - from: "crates/envi-engine/src/propagation/terrain_effect/submodel1.rs"
      to: "crates/envi-engine/src/propagation/ground.rs"
      via: "Q̂ and ρᵢ from the plan 02-01 chain"
      pattern: "spherical_q"
    - from: "crates/envi-engine/src/propagation/terrain_effect/submodel1.rs"
      to: "crates/envi-engine/src/propagation/coherence.rs"
      via: "F = coherence_f with flat-ground ρ = 2hS·hR/(hS+hR) (Eq. 119)"
      pattern: "coherence_f"
    - from: "crates/envi-engine/src/propagation/terrain_effect/submodel2.rs"
      to: "crates/envi-engine/src/propagation/fresnel.rs"
      via: "per-type weights via FresnelZoneW (F_λ = 0.25·λ) / FresnelZoneWm"
      pattern: "fresnel_zone_w"
---

<objective>
Implement the flat-terrain ground effect: Sub-model 1 (Eqs. 115-123, the partially-coherent two-ray sum) and Sub-model 2 (Eqs. 124-133, segmented impedance via frequency-interpolated Fresnel-zone weights), on top of the plan 02-01 numerics — this is ENG-02's "ground effect over a segmented-impedance profile (frequency-dependent, soft↔hard), preserving complex pressure".

Purpose: this plan lands the load-bearing two-channel shape of ENG-07 (per the user-locked phase-coherence contract, PROJECT.md Key Decisions 2026-07-07): `GroundResult { delta_l_db, h_coh_factor: Complex<f64>, p_incoh: f64 }`, where the coherent channel keeps the e^{+j2πfΔτ}·Q̂ interference phase LIVE and the incoherent channel carries ONLY the Nord2000 turbulence-decorrelated (1−F²) energy for final-level readout. Everything downstream (screens in 02-04, composition in 02-05) reuses this exact shape.

Output: `fresnel`, `terrain_effect::{submodel1, submodel2}` with dip anchors green, per-type segmented-impedance blending verified on the FORCE case-21 profile shape, and full-curve oracle fixtures matching ≤ 0.1 dB.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ground-effect-diffraction/02-RESEARCH.md
@.planning/phases/02-ground-effect-diffraction/02-01-SUMMARY.md
@.planning/phases/01-force-harness-geometry-model-direct-path/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Contracts this plan CREATES. Plans 02-04/02-05 build against these. -->

```rust
// terrain_effect/mod.rs
/// Two-channel result of ANY terrain-effect sub-model, normalized relative to the
/// free-field direct path p̂₀. Nord2000-native convention (e^{−jωt}) inside the engine;
/// conversion to the transfer convention happens once, in plan 02-05.
pub struct GroundResult {
    pub delta_l_db: f64,              // Nord2000 band value: 10·lg(|h_coh_factor|² + p_incoh)
    pub h_coh_factor: Complex<f64>,   // F-weighted coherent complex sum, phase LIVE
    pub p_incoh: f64,                 // (1−F²)·|ρᵢ·p̂ᵢ/p̂₀|² residual — real, added at readout only
}

// submodel1.rs — Eq. 123 signature shape kept (weather params pass through to Phase 3)
pub struct SubModel1Inputs<'a> { pub rays: &'a RayPair, pub sigma_kpa: f64, pub roughness_r: f64,
                                 pub coh: &'a CoherenceInputs }
pub fn submodel1(f_hz: f64, inp: &SubModel1Inputs) -> Result<GroundResult, PropagationError>;

// submodel2.rs
pub struct SurfaceStrip { pub x_start: f64, pub x_end: f64, pub sigma_kpa: f64, pub roughness_r: f64 }
pub fn submodel2(f_hz: f64, strips: &[SurfaceStrip], geom: &FlatGeometry, coh: &CoherenceInputs)
    -> Result<GroundResult, PropagationError>;

// fresnel.rs (§5.23.4-7)
pub fn calc_fz_d(...) -> f64;                 // exact arg lists per Eqs. 338-345 transcription
pub fn fresnel_zone_size(...) -> f64;
pub fn fresnel_zone_w(...) -> f64;            // low-frequency weights
pub fn fresnel_zone_wm(...) -> f64;           // high-frequency variant
```

Consumes from 02-01 (see that plan's interfaces block / SUMMARY): `ground::{ground_impedance, spherical_q, incoherent_rho}`, `rays::{RayPair, straight_rays, straight_rays_over_segment}`, `coherence::{CoherenceInputs, coherence_f}`, `special::exp_clamped`.
</interfaces>

## Artifacts this plan produces

| Artifact | Kind | Public surface |
|----------|------|----------------|
| `envi-engine::propagation::fresnel` | module | CalcFZd + FresnelZoneSize/W/Wm — the weight machinery Sub-models 2-6 share |
| `envi-engine::propagation::terrain_effect::submodel1` | module | Eq. 115-123 homogeneous flat ground with two-channel GroundResult |
| `envi-engine::propagation::terrain_effect::submodel2` | module | Eq. 124-133 segmented impedance: per-TYPE Sub-model 1 + Fresnel-weight blend + PhaseDiffFreq fL/fH interpolation |
| `terrain_effect::GroundResult` | type | THE two-channel shape (delta_l_db, h_coh_factor, p_incoh) — reused by 02-04 screens and 02-05 composition |
| oracle fixtures | TOML | 105-point ΔL_t curves for flat σ=200 (case-1-like) and mixed road/grass (case-21 profile), oracle-pinned at 0.1 dB |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fresnel.rs — Fresnel-zone machinery (§5.23.4-7)</name>
  <files>crates/envi-engine/src/propagation/fresnel.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — Architectural Responsibility Map row "Fresnel zones", §8 (which functions Sub-model 2 calls and with which F_λ), §14 item 1 (r_Fz uses CalcFZd at F_λ=0.5λ — plan 02-05 consumes this too)
    - AV 1106/07 PDF §5.23.4-5.23.7 (Eqs. 338-353) — transcribe the function definitions from page images; refs/AV1106-07-rev4.pdf is present and SHA-pinned (research Environment table); use pdfplumber page.to_image() for equation pages
  </read_first>
  <behavior>
    - Test 1 (degenerate guards): zero-length segments, coincident source/receiver projections, and F_λ = 0 return typed errors or well-defined limits — no NaN for any finite input
    - Test 2 (weight sanity): fresnel_zone_w ∈ [0, 1] before normalization for interior strips; a strip covering the whole ground plane gets weight → full; a strip far outside the zone gets weight → 0
    - Test 3 (frequency behavior): the Fresnel zone shrinks with frequency — for a fixed strip, w(4 kHz) ≤ w(250 Hz) when the strip sits at the zone edge; zone size at F_λ = λ/16 is smaller than at F_λ = λ/4 for identical geometry
    - Test 4 (symmetry): mirror-symmetric geometry (hS == hR) puts the zone centre at the midpoint; swapping S and R leaves the weights unchanged (reciprocity of the zone construction)
  </behavior>
  <action>
    Transcribe §5.23.4-5.23.7 (Eqs. 338-353) from the PDF page images: `calc_fz_d` (the Fresnel-zone distance construction), `fresnel_zone_size`, `fresnel_zone_w` (low-frequency strip weights), `fresnel_zone_wm` (high-frequency variant). Keep the document's exact argument lists and intermediate variable names in doc comments (cite "AV 1106/07 Eq. (NNN)") so plans 02-04/02-05 can call them with the printed parameterizations — these functions are consumed with F_λ = 0.25·λ (Sub-model 2, Eq. 125), λ/16 (screen sub-models, Eq. 174), and 0.5·λ (terrain interpretation r_Fz, Eq. 304): take F_λ as a parameter, never hard-code it.

    Behavior-first: since the equations are transcription work, write the property tests (Tests 1-4) BEFORE reading the PDF pages, then transcribe until they pass. The properties (bounds, shrink-with-frequency, reciprocity) hold for any correct Fresnel-zone construction and catch transposed arguments — the class of error transcription is prone to.

    All functions pure, f64, typed errors on degenerate geometry (extends the T-01-05/T-02-01 untrusted-profile posture).
  </action>
  <verify>
    <automated>cargo test -p envi-engine fresnel</automated>
  </verify>
  <done>Fresnel-zone machinery transcribed with parametric F_λ, property tests pin bounds/monotonicity/reciprocity, degenerate geometry returns typed errors.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine fresnel` passes behavior tests 1-4
    - `grep -n "F_λ\|f_lambda" crates/envi-engine/src/propagation/fresnel.rs` shows F_λ as a parameter (callers pass 0.25λ / λ/16 / 0.5λ)
    - `grep -c "Eq. (3" crates/envi-engine/src/propagation/fresnel.rs` ≥ 4 (equation citations present)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sub-model 1 — two-channel flat ground effect with the dip anchors</name>
  <files>crates/envi-engine/src/propagation/terrain_effect/mod.rs, crates/envi-engine/src/propagation/terrain_effect/submodel1.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — §7 (Eq. 120 + the partial-coherence identity + Eq. 123 signature note + shadow-zone deferral), §6 (flat-ground ρ Eq. 119), "Numeric Anchors" (coherent flat-ground ΔL table + dip 646.7 Hz/−19.16 dB + grid-point note), "Complex Combination & Conventions" (what the engine returns), Pitfall 1 (dip-asymmetry test), Code Example "Sub-model 1 homogeneous"
  </read_first>
  <behavior>
    - Test 1 (ΔL anchors, σ=200, hS=0.5, hR=1.5, d=97.5, F=Ff only, tol ±0.05 dB): delta_l_db == +5.2099 @100 Hz, +1.7311 @200, −12.6247 @400, −18.1225 @800, −15.4567 @1000, −6.7805 @2000, −0.0726 @4000 (research anchor table; evaluate at those exact frequencies)
    - Test 2 (discrete dip position): over the 105-point FreqAxis, argmin(delta_l_db) is the grid point 630.96 Hz or 667.42 Hz — the two neighbours of the continuous dip 646.7 Hz; min value ≈ −19.16 dB within 0.3 dB (grid discretization)
    - Test 3 (dip asymmetry / convention pin): the σ=200 dip frequency < c₀/(2·ΔR) (the hard-ground prediction) — arg Q̂ > 0 pulls dips DOWN; a sign error in the e^{+j2πfΔτ} phase or a stray conj would push it up (Pitfall 1)
    - Test 4 (two-channel identity): 10·lg(|h_coh_factor|² + p_incoh) == delta_l_db to 1e-12 at every point; with CoherenceInputs zero-turbulence AND Δτ small (Ff ≈ 1): p_incoh < 1e-3·|h_coh_factor|²; forcing F = 1 exactly (unit test hook) gives p_incoh == 0.0
    - Test 5 (soft vs hard ordering): mean delta_l_db over 200-2000 Hz for σ=12.5 (class A) < that for σ=20000 (class G) — soft ground attenuates more (research property test)
    - Test 6 (finiteness): delta_l_db, h_coh_factor, p_incoh finite at ALL 105 grid points for σ ∈ {12.5, 200, 20000, 200000} × the anchor geometry and an extreme geometry (hS=0.01, hR=1.5, d=1000)
  </behavior>
  <action>
    `terrain_effect/mod.rs`: define `GroundResult` exactly as the interfaces block — with module docs spelling out the user-locked two-channel contract (PROJECT.md Key Decisions 2026-07-07): the coherent channel is a genuine Complex<f64> with live Δτ interference phase (this is what multiplies into H_coh at the 02-05 boundary); p_incoh carries ONLY the (1−F²) turbulence-decorrelated energy and is added at final level readout; it never overwrites phase. Nothing in this module family collapses to magnitude/energy along the chain.

    `submodel1.rs`: Eq. 120 homogeneous specialization exactly as the research code example: Ẑ_G = ground_impedance(f, σ); Q̂ = spherical_q(f, τ₂, ψ_G, Ẑ_G); F = coherence_f(f, Δτ, ρ=2hS·hR/(hS+hR) per Eq. 119, ψ_G, coh) — FΔν arrives injected (=1); coherent = 1 + F·(R₁/R₂)·e^{+j2πf·Δτ}·Q̂ (NORD-NATIVE phase sign — doc-comment it); ρᵢ = incoherent_rho(f, Ẑ_G); p_incoh = (1−F²)·(R₁/R₂)²·ρᵢ²; delta_l_db = 10·lg(|coherent|² + p_incoh). Confirm the 10·lg prefix visually on the PDF during transcription (research Assumption A2) and note the confirmation in a comment.

    Keep the Eq. 123 signature shape (SubModel1Inputs carries CoherenceInputs so Phase 3 weather params pass through without an API break). Shadow-zone branch (Eqs. 121-122) needs ξ<0 refraction — in the homogeneous engine assert/document dSZ = ∞ and return the non-shadow branch always; doc-comment the Phase 3 attachment point. This is the researcher-scoped homogeneous specialization, not a scope cut.

    For Test 3, compute the hard-ground reference c₀/(2ΔR) inline from the RayPair — do not hard-code 646.7.
  </action>
  <verify>
    <automated>cargo test -p envi-engine submodel1</automated>
  </verify>
  <done>Sub-model 1 reproduces the full research ΔL anchor table, the dip lands on the predicted grid band with the correct downward asymmetry, the two-channel identity holds to 1e-12, F→1 zeroes p_incoh, and everything is finite across the sweep.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine submodel1` passes behavior tests 1-6
    - Deepest dip at grid point 630.96 Hz or 667.42 Hz, ≈ −19.16 dB (±0.3 dB)
    - `grep -n "p_incoh" crates/envi-engine/src/propagation/terrain_effect/mod.rs` finds the two-channel type with contract docs
    - The dip-asymmetry test compares against a computed c₀/(2ΔR), not a literal
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sub-model 2 — segmented impedance per surface TYPE + PhaseDiffFreq + oracle curves</name>
  <files>crates/envi-engine/src/propagation/terrain_effect/submodel2.rs, tools/nord2000_oracle/gen_flat_fixtures.py, crates/envi-harness/tests/fixtures/oracle/flat_sigma200.toml, crates/envi-harness/tests/fixtures/oracle/flat_mixed_case21.toml, crates/envi-harness/tests/oracle_flat.rs</files>
  <read_first>
    - .planning/phases/02-ground-effect-diffraction/02-RESEARCH.md — §8 (Eqs. 124-133 complete structure: per-type ΔL, w/r weights, Eq. 128 polynomial constants on PDF p. 57, fL/fH blend, PhaseDiffFreq Eqs. 378-381 + extrapolation rules + fL ≤ 0.8·fH guard, ≤2 weight-normalization rule), Pitfall 3 (per-TYPE not per-segment), Pitfall 8 (PhaseDiffFreq edges), Pattern 4 (fixture format), "FORCE Validation Reality" (case-21 parameters: Cv²=0.12, CT²=0.008 forced even at u=0)
    - .planning/phases/01-force-harness-geometry-model-direct-path/01-02-SUMMARY.md — GroundSegment/row-starts-segment rule and the real case-1 mixed profile (road σ=20000 then grass σ=12.5)
  </read_first>
  <behavior>
    - Test 1 (per-TYPE grouping, Pitfall 3): a profile with strips [G, D, G, D] (two types, four strips) invokes the per-type Sub-model 1 evaluation exactly once per (type_ii, type_ir) pair — verify via a counting hook or by asserting equality with a hand-grouped two-type computation
    - Test 2 (degenerate consistency): a "segmented" profile whose strips all share one (σ, r) reproduces submodel1 exactly (weights collapse to the single type) at every grid point, 1e-9 dB
    - Test 3 (PhaseDiffFreq): returns fL/fH with fL ≤ 0.8·fH enforced; extreme geometries (very hard ground → small Ψ; very soft + high frequencies → large Ψ) exercise the extrapolation rules (linear 10 kHz→100 kHz then constant; f = 25·Ψ/Ψ(25 Hz) below 25 Hz) without NaN (Pitfall 8)
    - Test 4 (weight normalization): after normalization, per-type weights ∈ [0, 1] and the total respects the §5.8 sum-to-2 rule (research §8 note)
    - Test 5 (oracle curves): engine submodel1/submodel2 ΔL_t at all 105 points matches flat_sigma200.toml and flat_mixed_case21.toml within 0.1 dB — the case-21 fixture includes Fc active (Cv²=0.12, CT²=0.008, FΔν=1 in BOTH implementations)
  </behavior>
  <action>
    `submodel2.rs`: Eq. 124 as printed — ΔL₂ = Σᵢᵢ Σᵢᵣ w′·ΔL_{ii,ir}, where ΔL_{ii,ir} is Sub-model 1 computed AS IF the whole ground were that (σ, r) combination (full path geometry, per-type — group strips by identical (σ, r) exactly per Eq. 127; Pitfall 3 makes this the plan's most-watched behavior). Low-frequency weights wᵢ via fresnel_zone_w with F_λ = 0.25·λ (Eq. 125 — Sub-model 2's own value); high-frequency rᵢ via fresnel_zone_wm (Eq. 126); high-frequency type weight w_H via the empirical log-polynomial in r̄ᵢᵢ and tan ψ_G (Eq. 128 — transcribe the 5th-order polynomial constants from PDF p. 57 page image, research flags them as approximately 8.78/21.95/21.76/10.69/1.36 — the page image is authoritative); blend low/high by log-frequency interpolation between fL and fH (Eq. 129). Combine the per-type GroundResults into the blended two-channel output: blend h_coh_factor complex-linearly with the same weights used for the level blend, and p_incoh with the squared-magnitude weights, so 10·lg(|h_coh|²+p_incoh) stays consistent with the document's ΔL₂ — document this as the two-channel extension of Eq. 124 (the document only defines the level; the complex blend is the phase-preserving reading mandated by the user contract).

    `PhaseDiffFreq` (Eqs. 378-381, §5.23.14) lives here (private): find f where Ψ(f) = 2πf·(R₂−R₁)/c₀ + arg Γ̂_p(f) hits the target; iterate over the 1/3-octave grid with log-interpolation (Eq. 380); implement BOTH extrapolation edges exactly (Eq. 381 + the 8-10 kHz linear extension to 100 kHz, constant above; f = 25·Ψ/Ψ(25 Hz) below 25 Hz) and the fL > 0.8·fH → fL = 0.8·fH clamp; h_min clamp ≥ 0.01 in the ΨL form (Eq. 132).

    Oracle: `gen_flat_fixtures.py` extends the 02-01 `common.py` chain with Sub-model 1/2 in Python (scipy wofz; same FΔν=1 stub; equation-number citations only) and writes two 105-point fixtures: `flat_sigma200.toml` (hS=0.5, hR=1.5, d=97.5, σ=200 uniform, zero turbulence — the case-1-like geometry) and `flat_mixed_case21.toml` (the FORCE case-21 flat mixed road/grass strip layout read from the research description — road G + grass D alternating; Cv²=0.12, CT²=0.008, su-driven Fc ACTIVE per the FORCE parameter audit). Commit both fixtures with provenance headers. `oracle_flat.rs` harness test compares engine vs fixtures at ≤ 0.1 dB per point.
  </action>
  <verify>
    <automated>cargo test -p envi-engine submodel2 && cargo test -p envi-harness --test oracle_flat</automated>
  </verify>
  <done>Segmented-impedance blending is per-TYPE with the Eq. 128/129 frequency interpolation and guarded PhaseDiffFreq; single-type profiles collapse to Sub-model 1; both oracle curves match ≤ 0.1 dB — ENG-02's segmented soft↔hard ground effect is functionally complete for flat profiles.</done>
  <acceptance_criteria>
    - `cargo test -p envi-engine submodel2` passes behavior tests 1-4
    - `cargo test -p envi-harness --test oracle_flat` passes: 2 × 105 points within 0.1 dB
    - Single-type degeneracy vs submodel1 at 1e-9 dB (Test 2) proves no double-counting
    - PhaseDiffFreq never returns NaN on the extreme-geometry sweep and enforces fL ≤ 0.8·fH
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terrain strips (σ, r, x-ranges) from case files → submodel2 | Untrusted segmented-impedance data crosses into the weight/blend machinery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | DoS (NaN/Inf) | PhaseDiffFreq extrapolation, Fresnel weights on degenerate strips | mitigate | Eq. 380/381 extrapolation implemented exactly + fL ≤ 0.8·fH clamp; typed errors on zero-length/overlapping strips; extreme-geometry sweep test |
| T-02-06 | Tampering (silently plausible wrongness) | Eq. 128 polynomial, per-type grouping | mitigate | page-image transcription with citation comments; per-TYPE counting test (Pitfall 3); single-type degeneracy identity vs submodel1; oracle full-curve cross-check at 0.1 dB |
| T-02-07 | Tampering (channel corruption) | GroundResult two-channel blend | mitigate | 10·lg(|h_coh|²+p_incoh) == delta_l_db identity test at 1e-12; F→1 ⇒ p_incoh == 0 test |
| T-02-SC | Tampering | installs | accept | zero new dependencies |
</threat_model>

<verification>
- `cargo test --workspace` green: fresnel properties, submodel1 anchor table + dip position/asymmetry + two-channel identity + soft/hard ordering + finiteness, submodel2 grouping/degeneracy/normalization, both oracle curve fixtures
- `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`, `#![deny(unsafe_code)]`, engine dep quarantine
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 1 (propagation-level reading, per the acceptance ladder — FORCE reference spectra need the Phase 4 emission model): flat soft, hard, and mixed segmented-impedance ground effects computed with interference dips at the correct grid frequencies, proving complex pressure is preserved, not band energy
- ENG-02 functionally complete for flat terrain; the GroundResult two-channel shape (ENG-07's structural core) is live and identity-tested
</success_criteria>

<output>
Create `.planning/phases/02-ground-effect-diffraction/02-02-SUMMARY.md` when done, recording: the confirmed Eq. 120 prefix (Assumption A2), the transcribed Eq. 128 constants, the PhaseDiffFreq edge-rule details, and the two-channel blend rule chosen for Sub-model 2 (for 02-04/02-05 reuse).
</output>
