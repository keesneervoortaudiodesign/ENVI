---
phase: 09-path-extraction-weather
plan: 06
type: execute
wave: 6
depends_on: [09-05]
files_modified:
  - web/tests/e2e/weather-import.spec.ts
  - web/tests/e2e/_mocks.ts
  - web/tests/e2e/fixtures/
autonomous: true
requirements: [METX-01]
must_haves:
  truths:
    - "An offline Playwright journey drives the REAL built bundle through the weather-import path with ALL network mocked (both Open-Meteo hosts + basemap + GIS) and no live-network escape (SC4)"
    - "SC4 zero-egress proof: after a cached import, a what-if edit issues ZERO Open-Meteo calls (network-log assertion), mirroring the Phase-8 DATA-04 replay"
    - "The call-cost weight is logged and asserted; the derived per-azimuth A/B/C appears in the panel"
  artifacts:
    - path: "web/tests/e2e/weather-import.spec.ts"
      provides: "offline weather-import journey + zero-egress what-if assertion"
      contains: "archive-api.open-meteo.com"
      min_lines: 70
  key_links:
    - from: "web/tests/e2e/weather-import.spec.ts"
      to: "web/src/panels/WeatherPanel.tsx"
      via: "drives data-testid controls on the real bundle"
      pattern: "data-testid"
    - from: "web/tests/e2e/weather-import.spec.ts"
      to: "web/tests/e2e/import-offline-replay.spec.ts"
      via: "reuses bootOffline unmocked-collector + record/route.abort zero-egress pattern"
      pattern: "bootOffline"
  prohibitions:
    - "MUST NOT let any live network request escape — the unmocked collector MUST end toEqual([]) (offline-first)"
    - "MUST NOT reimplement the app in the test — drive the real built bundle; mock only /api and third-party hosts via page.route"
    - "MUST NOT require credentials or a live Open-Meteo/CDS connection"
    - "MUST NOT add Playwright to the production bundle — it stays a devDependency"
---

<objective>
Prove SC4 automatically and offline: extend the Playwright suite with a weather-import journey that mocks both Open-Meteo hosts (forecast + archive-api), the basemap, and the GIS sources, drives the real built bundle to import weather and show per-azimuth A/B/C, and then asserts that a subsequent what-if edit issues ZERO Open-Meteo calls — the network-log zero-egress guarantee, mirroring the Phase-8 DATA-04 replay.

Purpose: SC4's "subsequent what-if edits issue zero API calls (verified by network log)" is a hard, user-visible acceptance property. This plan is its automated, credential-free, offline proof, and validates the call-cost logging.
Output: `web/tests/e2e/weather-import.spec.ts` + weather mock helpers/fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-path-extraction-weather/09-CONTEXT.md
@.planning/phases/09-path-extraction-weather/09-RESEARCH.md
@.planning/phases/09-path-extraction-weather/09-PATTERNS.md
@.planning/phases/09-path-extraction-weather/09-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offline weather-import journey + SC4 zero-egress what-if assertion</name>
  <read_first>
    - web/tests/e2e/import-offline-replay.spec.ts:29-103 (bootOffline unmocked-collector toEqual([]); record-then-route.abort inversion; the DATA-04 zero-egress assertion to mirror)
    - web/tests/e2e/_mocks.ts (existing page.route mock helpers for basemap + GIS + /api)
    - web/src/panels/WeatherPanel.tsx (the data-testid controls to drive)
    - web/src/import/weather.ts (the call-cost log line + endpoints to mock)
    - 09-PATTERNS.md "web/tests/e2e/weather-import.spec.ts" section; 09-RESEARCH.md Pattern 5 (call-cost weight) + Validation "METX-01 Playwright"
  </read_first>
  <action>
    Add weather mocks to `web/tests/e2e/_mocks.ts` (or the spec): `page.route` intercepts for both `api.open-meteo.com/**` and `archive-api.open-meteo.com/**` serving committed response fixtures (reuse or add under `web/tests/e2e/fixtures/`), plus the existing basemap + GIS mocks. Create `web/tests/e2e/weather-import.spec.ts` using `bootOffline(page)` so the `unmocked` collector MUST end `toEqual([])` (no live-network escape). The journey: pick a date+hour, click Import (driving `data-testid` controls on the real bundle), assert the per-azimuth A/B/C readout appears, and assert the call-cost weight line was logged. Then the SC4 proof: after the cached import, invert the network (record Open-Meteo egress + `route.abort()` on both hosts), perform a what-if edit (e.g. change a path azimuth / receiver), and assert the Open-Meteo egress collector stays `[]` — zero API calls on what-if. Cover both the date-switch branches (a historical date hits the archive-api mock; a recent date hits the forecast mock). Keep Playwright a devDependency; drive the real built bundle; no credentials.
  </action>
  <files>web/tests/e2e/weather-import.spec.ts, web/tests/e2e/_mocks.ts, web/tests/e2e/fixtures/</files>
  <verify>
    <automated>cd web && npx playwright test weather-import</automated>
  </verify>
  <done>Offline weather-import journey on the real bundle (unmocked collector []); per-azimuth A/B/C shown; call-cost logged; SC4 what-if issues zero Open-Meteo calls; both date-switch branches covered.</done>
  <acceptance_criteria>
    - The journey runs fully offline; the unmocked collector ends `toEqual([])`.
    - Per-azimuth A/B/C appears; call-cost weight logged and asserted.
    - After a cached import, a what-if edit issues ZERO Open-Meteo calls (egress collector `[]`).
    - Both date-switch branches (archive-api vs forecast) are exercised; suite green.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces (new symbols)
- `web/tests/e2e/weather-import.spec.ts` — offline weather-import journey + SC4 zero-egress what-if assertion + date-switch branch coverage.
- `web/tests/e2e/_mocks.ts` — Open-Meteo (forecast + archive-api) page.route mocks.
- `web/tests/e2e/fixtures/` — committed Open-Meteo response fixtures for the mocks.
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test runner → network | any live escape would break the offline/zero-egress guarantee |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Integrity | live-network escape masking a real fetch | mitigate | `bootOffline` unmocked collector asserted `toEqual([])`; both Open-Meteo hosts mocked |
| T-09-06-02 | Integrity | SC4 regression (what-if re-fetch) | mitigate | record + `route.abort()` egress collector asserted `[]` after cached import |
| T-09-06-03 | Access control | test needs credentials/live API | accept | mocked hosts + committed fixtures; no credentials, no live CDS/Open-Meteo (by design) |
</threat_model>

<verification>
- `cd web && npx playwright test weather-import` green, fully offline (no credentials, no live network).
- The full offline suite still passes: `cd web && npx playwright test`.
- Playwright remains a devDependency (not in the production bundle).
</verification>

<success_criteria>
- METX-01 / SC4: automated offline proof that weather imports once, caches in OPFS, and what-if edits issue zero API calls, with call-cost logged.
</success_criteria>

<output>
Create `.planning/phases/09-path-extraction-weather/09-06-SUMMARY.md` when done.
</output>
