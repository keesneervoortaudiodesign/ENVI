---
phase: 09-path-extraction-weather
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - crates/envi-gis/src/screening.rs
  - crates/envi-gis/src/grid.rs
  - crates/envi-gis/src/lib.rs
  - crates/envi-gis/Cargo.toml
autonomous: true
requirements: [GEOX-03, GRID-01]
must_haves:
  truths:
    - "Screening edges are derived from building/wall/barrier geometry along the path via an rstar corridor query and INJECTED as (x,z) hard vertices into the same TerrainProfile (SC2, GEOX-03)"
    - "Buildings (eaves_height_m), walls, and barriers (height_m) all screen (D-08); the cut-plane ∩ footprint-prism reduction keeps multi-edge (D-09) — a pass-through building yields TWO top vertices and the engine's terrain_interpretation classifies ≤2 diffracting edges"
    - "A building-aware constrained-Delaunay receiver grid generates a regular lattice at user-set spacing (default 10 m, D-06) inside calc_area, excludes building footprints (D-07), and includes discrete receiver points (SC3, GRID-01)"
    - "Guardrails: minimum spacing and a receiver-count cap return typed errors, never OOM"
  artifacts:
    - path: "crates/envi-gis/src/screening.rs"
      provides: "GEOX-03 rstar corridor + cut-plane∩prism → profile vertices"
      contains: "locate_in_envelope_intersecting"
      min_lines: 70
    - path: "crates/envi-gis/src/grid.rs"
      provides: "GRID-01 building-aware CDT receiver grid + discrete points"
      contains: "pub fn receiver_grid"
      min_lines: 70
  key_links:
    - from: "crates/envi-gis/src/screening.rs"
      to: "crates/envi-engine/src/scene.rs"
      via: "inject (x,z) tops into TerrainProfile with hard-σ GroundSegments (Building.eaves_height_m / Barrier height_m)"
      pattern: "eaves_height_m"
    - from: "crates/envi-gis/src/grid.rs"
      to: "crates/envi-dgm/src/tin.rs"
      via: "build_tin CDT with footprint rings as constraints + interpolate_z for receiver z"
      pattern: "build_tin"
  prohibitions:
    - "MUST NOT emit a separate Vec of screen edges to the solver — SolveJob has NO screens field; inject into TerrainProfile (the single biggest reshape vs ARCHITECTURE.md)"
    - "MUST NOT build a bespoke N-edge diffraction combiner — insert all crossing tops and let the engine cap at Nord2000's native ≤2 screens (documented limitation)"
    - "MUST NOT generate a receiver inside a building footprint (D-07)"
    - "MUST NOT add any async/network/browser crate to envi-gis — rstar 0.13 (pure-Rust) is the ONLY new dep; MUST NOT touch envi-engine deps"
    - "MUST NOT collapse the injected screen z to source/receiver height — z = ground_z(crossing) + object_height"
---

<objective>
Complete the geometry half of the pipeline: screening-edge derivation (GEOX-03) and the building-aware receiver grid (GRID-01), both as pure-Rust `envi-gis` modules.

Purpose: GEOX-03 is the phase's single biggest task reshape — the engine has NO `screens` field, so screen tops are injected as `(x,z)` vertices into the `TerrainProfile` and the engine's `terrain_interpretation` does the ≤2-edge diffraction classification. GRID-01 produces the receiver positions a calculation runs over, respecting building footprints as constrained-Delaunay holes.
Output: `crates/envi-gis/src/{screening,grid}.rs`, `rstar = "0.13"` added to `envi-gis`, `mod` declarations + guardrail `GisError` variants in `lib.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-path-extraction-weather/09-CONTEXT.md
@.planning/phases/09-path-extraction-weather/09-RESEARCH.md
@.planning/phases/09-path-extraction-weather/09-PATTERNS.md
@.planning/phases/09-path-extraction-weather/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Screening edges → profile vertices (GEOX-03, rstar corridor)</name>
  <read_first>
    - crates/envi-engine/src/solver.rs:53-121 (SolveJob fields — VERIFY there is NO screens/reflections field; only profile: &TerrainProfile)
    - crates/envi-engine/src/propagation/terrain_interpretation.rs (§5.21 derives ≤2 diffracting edges FROM profile interior points; Sub-model 4/5/6)
    - crates/envi-engine/src/scene.rs (Building.eaves_height_m, Barrier/wall height_m, GroundSegment hard-σ class H = 200000, TerrainProfile::new contract)
    - crates/envi-gis/src/landcover.rs (geo geometry handling) + crates/envi-gis/src/profile.rs (the ascending-x profile this splices into)
    - 09-RESEARCH.md Pattern 3 (rstar corridor + fresnel_half_width) + Pitfall 1/4; 09-PATTERNS.md "screening.rs" section
  </read_first>
  <behavior>
    - An rstar R-tree bulk-loaded with building/wall/barrier AABBs answers a corridor query around the S→R line; corridor half-width = max(20 m floor, first-Fresnel-zone radius at 250 Hz at path midpoint) as a named [ASSUMED] const.
    - For each candidate: plan-view intersection of the S→R line with a building exterior ring yields entry/exit crossings, each injected as a profile vertex at z = ground_z(crossing) + eaves_height_m; a wall/barrier segment yields one crossing at height_m.
    - A building the line passes through produces two top vertices (thick screen). Injected intervals spanning an object get a hard-σ GroundSegment (class H), roughness 0.
    - The resulting TerrainProfile is valid (strictly ascending x, N−1 segments) and, fed to terrain_interpretation, classifies the expected Sub-model 4/5/6.
  </behavior>
  <action>
    Add `rstar = "0.13"` to `crates/envi-gis/Cargo.toml` `[dependencies]` (pure-Rust, WASM-safe — the ONLY new dep). Create `crates/envi-gis/src/screening.rs` with the module skeleton. Per D-08 every height-bearing kind screens (`building` exterior rings at `eaves_height_m`, `wall`/`barrier` linestrings at `height_m`); per D-09 the reduction is cut-plane ∩ footprint prism → top edges with multi-edge kept (do NOT collapse to one dominant edge). Implement a named `pub const` corridor floor and `fn fresnel_half_width(d, f_ref)` returning `sqrt(lambda*d1*d2/(d1+d2))` at the midpoint, with `half_w = fresnel_half_width(d, 250.0).max(CORRIDOR_MIN_HALF_WIDTH_M)` — document both as [ASSUMED] engineering constants with rationale. Build an `RTree` of building/wall/barrier geometry keyed by AABB and query it with `locate_in_envelope_intersecting` over the corridor AABB (add a `pub const MAX_CORRIDOR_CANDIDATES` cap → typed `GisError`). For each candidate, intersect the S→R line (plan view) with the footprint exterior ring (building) or each linestring segment (wall/barrier) using `geo`, and for every crossing splice an `(x,z)` vertex at `ground_z + object_height` into the profile point list (keep x strictly ascending), tagging the spanned interval(s) as hard σ (class H via `impedance_class`, roughness 0). Return the augmented points + segments ready for `TerrainProfile::new`. Document the ≤2-screen ceiling in the module header and a test — do NOT emit a separate screen list and do NOT build an N-edge combiner. Add `pub mod screening;` to `lib.rs`. Unit + property tests: corridor selects the right candidates; a through-building yields two tops; the built profile is valid; heights ride on terrain.
  </action>
  <files>crates/envi-gis/src/screening.rs, crates/envi-gis/src/lib.rs, crates/envi-gis/Cargo.toml</files>
  <verify>
    <automated>cargo test -p envi-gis screening</automated>
  </verify>
  <done>rstar corridor selects candidates; cut-plane∩prism tops injected as (x,z) into a valid TerrainProfile (no separate screen Vec); through-building → 2 tops; hard-σ segments; caps/consts named; rstar the only new dep.</done>
  <acceptance_criteria>
    - rstar corridor query returns the correct candidates; a pass-through building injects exactly two top vertices at eaves height.
    - Output is injected into a valid `TerrainProfile` (no separate screen Vec); hard-σ segments over objects.
    - `MAX_CORRIDOR_CANDIDATES` cap returns a typed error; corridor constants are named and documented [ASSUMED].
    - `cargo tree -p envi-gis` shows `rstar` and still no HTTP/async/browser crate.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Building-aware receiver grid (GRID-01, spade CDT + lattice clip)</name>
  <read_first>
    - crates/envi-dgm/src/tin.rs:31,158-235 (build_tin: MAX_POINTS cap, can_add_constraint / IntersectingConstraint guard, interpolate_z)
    - crates/envi-store/src/geojson.rs (calc_area kind + building footprint rings as authored)
    - crates/envi-gis/src/lib.rs (GisError struct-variant style for guardrails)
    - 09-RESEARCH.md Pattern 4 + guardrail note (min spacing 1.0 m, count cap ~1_000_000, regular lattice clipped to the CDT-defined region — CONFIRM lattice over literal CDT vertices) + Open Question 3
    - 09-PATTERNS.md "grid.rs" section
  </read_first>
  <behavior>
    - Given calc_area, building footprints, a user spacing (default 10 m), discrete receiver points, and the DGM TIN, receiver_grid returns receiver positions [x,y,z].
    - Decision (confirmed): a REGULAR axis-aligned lattice at spacing_m covering the calc_area bbox, kept iff inside calc_area AND outside every footprint; footprint rings + calc_area boundary enter a spade CDT as constraint edges to define the valid region (no receiver straddles a footprint edge).
    - Discrete receiver points are appended. Each kept point's z is sampled via Tin::interpolate_z (receiver acoustic height is NOT baked in here — added at SolveJob assembly).
    - Guardrails: spacing_m < MIN_SPACING_M → typed error; total receiver count > MAX_RECEIVERS → typed error.
  </behavior>
  <action>
    Create `crates/envi-gis/src/grid.rs` with the module skeleton and named `pub const MIN_SPACING_M = 1.0` and `pub const MAX_RECEIVERS` (~1_000_000), both documented [ASSUMED] with rationale (client-side WASM compute scales with receiver count × sub-sources). Spacing is user-set with a default of 10 m and the min-spacing guardrail (D-06); building footprints are constrained-Delaunay holes with no interior receivers (D-07). Implement `pub fn receiver_grid(calc_area, footprints, spacing_m, discrete_points, tin) -> Result<Vec<[f64;3]>, GisError>`: reject `spacing_m < MIN_SPACING_M` with a typed `SpacingTooSmall { got, min }`; build the calc_area bbox lattice at `spacing_m`; build a spade CDT (via `envi_dgm::build_tin`, reusing its intersecting-constraint guard + MAX_POINTS posture) from the calc_area boundary + footprint rings as constraints to define the valid region; keep a lattice point iff `geo` point-in-polygon says it is inside calc_area and outside every footprint (D-07); append discrete receiver points; enforce `MAX_RECEIVERS` → `ReceiverCapExceeded { got, limit }` BEFORE allocating the full set; sample each kept point's z via `Tin::interpolate_z`, dropping hull-misses as typed errors (never silent 0.0). Add `pub mod grid;` + the `SpacingTooSmall`/`ReceiverCapExceeded` variants to `lib.rs`. Unit + property tests: no receiver inside a footprint; spacing honored; both guardrails return typed errors; discrete points included.
  </action>
  <files>crates/envi-gis/src/grid.rs, crates/envi-gis/src/lib.rs</files>
  <verify>
    <automated>cargo test -p envi-gis grid</automated>
  </verify>
  <done>Building-aware CDT receiver grid: regular lattice clipped to calc_area minus footprints (no receiver in a footprint), discrete points included, z from TIN; MIN_SPACING_M/MAX_RECEIVERS guardrails return typed errors.</done>
  <acceptance_criteria>
    - No generated receiver lies inside a footprint; spacing is honored; discrete points are included.
    - `spacing_m < 1.0` → `SpacingTooSmall`; count over cap → `ReceiverCapExceeded` (both typed, no OOM/panic).
    - Receiver z sampled from the TIN; hull-miss is typed, never 0.0.
    - `cargo test -p envi-gis` + clippy/fmt clean.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces (new symbols)
- `crates/envi-gis/src/screening.rs` — module; `pub fn` returning profile points+segments with injected screen tops; `pub const CORRIDOR_MIN_HALF_WIDTH_M`, `pub const MAX_CORRIDOR_CANDIDATES`; `fn fresnel_half_width`.
- `crates/envi-gis/src/grid.rs` — module; `pub fn receiver_grid(...) -> Result<Vec<[f64;3]>, GisError>`; `pub const MIN_SPACING_M`, `pub const MAX_RECEIVERS`.
- `crates/envi-gis/src/lib.rs` — `pub mod screening;` + `pub mod grid;`; new `GisError` variants `SpacingTooSmall { got, min }`, `ReceiverCapExceeded { got, limit }`, corridor-cap variant.
- `crates/envi-gis/Cargo.toml` — `rstar = "0.13"` dependency.
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| imported/drawn building+wall+barrier geometry → screening | untrusted geometry crosses here |
| calc_area + footprints + user spacing → grid | untrusted geometry + user-controlled spacing crosses here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Denial of Service | rstar corridor candidate set | mitigate | `MAX_CORRIDOR_CANDIDATES` cap → typed `GisError` before per-candidate work |
| T-09-02-02 | Denial of Service | receiver-grid explosion | mitigate | `MIN_SPACING_M` guardrail + `MAX_RECEIVERS` cap checked before allocation → typed errors |
| T-09-02-03 | Tampering | intersecting/degenerate footprint rings | mitigate | reuse `envi_dgm::build_tin` `can_add_constraint` guard → typed error, no panic |
| T-09-02-04 | Tampering | receiver under/inside a footprint or outside hull | mitigate | point-in-polygon exclusion (D-07); `interpolate_z` `None` → typed error, never 0.0 |
| T-09-02-SC | Tampering | dependency surface | mitigate | only `rstar` added; `cargo tree -p envi-gis` free of async/network; `envi-engine` unchanged |
</threat_model>

<verification>
- `cargo test -p envi-gis` (screening + grid unit/property) green.
- `cargo clippy -p envi-gis --all-targets -- -D warnings` + `cargo fmt --check` clean.
- `cargo tree -p envi-gis` shows `rstar` and no HTTP/async/browser edge; `cargo tree -p envi-engine` still 3 deps.
- (Integration seam) a screened profile fed to `terrain_interpretation` classifies the expected Sub-model 4/5/6 — covered by a test in this plan or the phase-close harness check.
</verification>

<success_criteria>
- GEOX-03: screening edges derived via rstar corridor and injected as profile vertices; ≤2-screen classification via the engine (SC2, screen half).
- GRID-01: building-aware CDT receiver grid + discrete points inside calc_area, footprints excluded, guardrails enforced (SC3).
</success_criteria>

<output>
Create `.planning/phases/09-path-extraction-weather/09-02-SUMMARY.md` when done.
</output>
