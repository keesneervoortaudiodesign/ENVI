---
phase: 09-path-extraction-weather
plan: 04
type: execute
wave: 4
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - crates/envi-gis-wasm/src/dto.rs
  - crates/envi-gis-wasm/src/lib.rs
  - crates/envi-service/src/api/era5.rs
  - crates/envi-service/src/api/mod.rs
  - crates/envi-service/Cargo.toml
  - crates/envi-service/tests/wire_no_drift.rs
  - crates/envi-service/tests/contract_era5.rs
  - web/src/generated/wire.ts
autonomous: true
requirements: [METX-02, GEOX-01, GEOX-02, GEOX-03, GRID-01, METX-01]
must_haves:
  truths:
    - "Every Phase-9 envi-gis core fn (cut_profile, segment_ground, screening, receiver_grid, components_from_levels/A-B-C, era5 derivation) is exposed across the wasm-bindgen boundary via thin logic-free shims"
    - "All new boundary DTOs are ts-rs-generated into the single committed web/src/generated/wire.ts; the no-drift test passes (a hand-written TS mirror is forbidden)"
    - "The ERA5/CDS retrieval endpoint exists on envi-service as an async job reusing the Phase-6 jobs.rs state machine, is disabled by default (#[cfg(feature=\"era5\")]), and leaks no CDS key / host / path in errors (SC5 groundwork, METX-02 transport)"
    - "The ERA5 endpoint is absent by default: a contract test asserts the route is not registered without the feature flag"
  artifacts:
    - path: "crates/envi-gis-wasm/src/dto.rs"
      provides: "Phase-9 boundary DTOs (ts-rs, deny_unknown_fields)"
      contains: "ts(export_to = \"wire.ts\")"
    - path: "crates/envi-service/src/api/era5.rs"
      provides: "flagged-off async ERA5/CDS retrieval endpoint (SSRF-allowlisted)"
      contains: "cfg(feature = \"era5\")"
      min_lines: 60
    - path: "crates/envi-service/tests/contract_era5.rs"
      provides: "endpoint-absent-by-default + SSRF/derivation contract tests"
  key_links:
    - from: "crates/envi-gis-wasm/src/lib.rs"
      to: "crates/envi-gis/src/*.rs"
      via: "logic-free shims delegating to envi_gis:: core fns; gis_err mapping; serialize_maps_as_objects(true)"
      pattern: "envi_gis::"
    - from: "crates/envi-service/src/api/era5.rs"
      to: "crates/envi-service/src/jobs.rs"
      via: "reuse JobStatus Queued/Running/Done/Failed/Cancelled state machine"
      pattern: "JobStatus"
    - from: "crates/envi-service/src/api/era5.rs"
      to: "crates/envi-service/src/api/proxy.rs"
      via: "copy the SOURCES allowlist + resolve_upstream SSRF chokepoint shape (CDS host pinned)"
      pattern: "resolve_upstream"
  prohibitions:
    - "MUST NOT hand-author any TS wire type — register every DTO in export_all_wire_types and regenerate web/src/generated/wire.ts (no-drift test fails in Rust on drift)"
    - "MUST NOT enable the ERA5 endpoint by default — #[cfg(feature=\"era5\")], default-off; reqwest only under that feature"
    - "MUST NOT leak the CDS key, upstream host, or path in any wire response, log line, or the client bundle (generic 500; key in server env only)"
    - "MUST NOT accept a user-supplied fetch URL — the CDS host is hardcoded/allowlisted exactly like the Phase-8 proxy (no SSRF)"
    - "MUST NOT put acoustic/A-B-C math in the shims — they marshal only; all math stays in envi_gis"
    - "MUST NOT add reqwest/tokio to envi-gis or envi-gis-wasm — reqwest lives only in envi-service behind feature=era5"
---

<objective>
Expose the Phase-9 pure-Rust core to the browser and stand up the flagged ERA5 groundwork endpoint: add thin wasm-bindgen shims + ts-rs DTOs for every new `envi-gis` function into the single committed `wire.ts`, and add a default-off async ERA5/CDS retrieval endpoint on `envi-service` that reuses the Phase-6 job state machine and the Phase-8 SSRF allowlist.

Purpose: The web frontend (next plan) can only call the geometry/weather core through generated, drift-checked DTOs. The ERA5 endpoint is the one acceptable server-compute surface (CDS is a queued multi-minute job) and must ship the security posture (allowlist, no key/host leak, disabled by default) even while the live retrieval is flagged off.
Output: updated `envi-gis-wasm/{dto,lib}.rs`, regenerated `web/src/generated/wire.ts`, `envi-service/src/api/era5.rs` + route wiring + `era5` feature, and contract/no-drift tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-path-extraction-weather/09-CONTEXT.md
@.planning/phases/09-path-extraction-weather/09-RESEARCH.md
@.planning/phases/09-path-extraction-weather/09-PATTERNS.md
@.planning/phases/09-path-extraction-weather/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WASM boundary DTOs + logic-free shims + wire no-drift regen</name>
  <read_first>
    - crates/envi-gis-wasm/src/dto.rs:1-100 (existing DTO derive pattern: Serialize/Deserialize + TS, deny_unknown_fields, ts(export_to="wire.ts"); GeoJSON as ts(type="unknown") serde_json::Value; bytes as &[u8] param)
    - crates/envi-gis-wasm/src/lib.rs:56-85,316-350 (logic-free shim: from_js → one envi_gis core fn → to_js; gis_err mapping; to_js MUST use serialize_maps_as_objects(true))
    - crates/envi-service/tests/wire_no_drift.rs:88-164,191 (export_all_wire_types registration + wire_ts_matches_committed_source + the --ignored regenerate command)
    - 09-PATTERNS.md S-7 + "envi-gis-wasm" section
    - the 09-01/09-02/09-03 SUMMARYs for the exact core fn signatures produced
  </read_first>
  <action>
    For each Phase-9 core function add a request/result DTO in `crates/envi-gis-wasm/src/dto.rs` following the existing derive pattern (`Serialize/Deserialize, TS`, `deny_unknown_fields` on requests, `ts(export_to = "wire.ts")`): cut-profile (S/R + step → points), impedance-segmentation (points + zones + default → segments), screening (S/R + objects → augmented profile points/segments), receiver-grid (calc_area + footprints + spacing + discrete → positions), weather-derivation (multi-level JSON + phi_wind + path azimuths → per-azimuth A/B/C), and ERA5-derivation (fields/hours → 1/L + class occurrence). GeoJSON payloads cross as `#[ts(type = "unknown")] serde_json::Value`; bulk tile/byte arrays as `&[u8]` params (never a serde field). In `crates/envi-gis-wasm/src/lib.rs` add one `#[wasm_bindgen]` shim per function that deserializes with `from_js`, calls exactly one `envi_gis::` core fn, and serializes with `to_js` using `.serialize_maps_as_objects(true)`, mapping `GisError` via the existing `gis_err`. The shims MUST contain no geometry/acoustic logic. Register every new DTO in `wire_no_drift.rs` `export_all_wire_types` and regenerate the committed `web/src/generated/wire.ts` via the `--ignored regenerate_committed_wire_ts` test.
  </action>
  <files>crates/envi-gis-wasm/src/dto.rs, crates/envi-gis-wasm/src/lib.rs, crates/envi-service/tests/wire_no_drift.rs, web/src/generated/wire.ts</files>
  <verify>
    <automated>cargo test -p envi-service --test wire_no_drift && cargo build -p envi-gis-wasm</automated>
  </verify>
  <done>Every Phase-9 core fn has a logic-free wasm-bindgen shim + ts-rs DTOs; wire.ts regenerated + committed; no-drift test green; no hand-authored TS.</done>
  <acceptance_criteria>
    - Every Phase-9 core fn has a shim + DTOs; shims are logic-free (delegate to `envi_gis::`).
    - `wire_no_drift` passes; `web/src/generated/wire.ts` regenerated and committed (no hand-authored TS).
    - `to_js` uses `serialize_maps_as_objects(true)`; `cargo tree -p envi-gis-wasm` free of reqwest/tokio.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Flagged-off ERA5/CDS retrieval endpoint (async job, SSRF-allowlisted)</name>
  <read_first>
    - crates/envi-service/src/api/proxy.rs:48-101 (SOURCES allowlist + resolve_upstream: reject unknown source/prefix-escape/.. BEFORE any I/O; hardcoded https + host; MAX_RELAY_BYTES; Policy::none; From<reqwest::Error> logs server-side but returns generic 500 — copy this shape, pin the CDS host)
    - crates/envi-service/src/jobs.rs:54-81 (JobStatus enum + is_terminal + watch::channel worker + SSE)
    - crates/envi-service/src/api/mod.rs:92-94 (route registration style, e.g. get(proxy::relay))
    - crates/envi-service/Cargo.toml:44-58 (existing reqwest block: default-features=false, rustls-tls — add [features] era5)
    - 09-CONTEXT.md D-04/D-05; 09-RESEARCH.md Pattern 6 + Security Domain (V10 SSRF, V6 key handling); 09-PATTERNS.md "envi-service/api/era5.rs" section
  </read_first>
  <action>
    Add an `era5` feature to `crates/envi-service/Cargo.toml` (`[features] era5 = ["dep:reqwest", ...]`, reqwest optional, `default-features = false, features = ["rustls-tls", "json"]`), default-off. Create `crates/envi-service/src/api/era5.rs` behind `#[cfg(feature = "era5")]`: a `POST /era5/import` handler that spawns an async job using the Phase-6 `jobs.rs` `JobStatus` machine (Queued→Running→Done/Failed/Cancelled) with SSE progress, and a retrieval stub that would call the CDS API through a `resolve_upstream`-style SSRF chokepoint copied from `proxy.rs` with the CDS host hardcoded/allowlisted (no user-supplied URL); on completion it runs the `envi_gis::era5` derivation. Read the CDS key from server env ONLY; never place it in a response, log, or the bundle; map upstream errors to a generic 500 (log full error server-side, leak no host/path). Register the route in `api/mod.rs` under the same `#[cfg(feature = "era5")]` gate. Add `crates/envi-service/tests/contract_era5.rs`: (a) `era5_route_absent_by_default` asserts the route returns 404/is not registered when the feature is off; (b) a derivation contract test that the endpoint's job produces the expected occurrence stats from a committed fixture (run under `--features era5`); (c) an SSRF unit test that `resolve_upstream` rejects unknown host / traversal before any I/O.
  </action>
  <files>crates/envi-service/src/api/era5.rs, crates/envi-service/src/api/mod.rs, crates/envi-service/Cargo.toml, crates/envi-service/tests/contract_era5.rs</files>
  <verify>
    <automated>cargo test -p envi-service era5 && cargo build -p envi-service</automated>
  </verify>
  <done>ERA5 endpoint compiles only under feature=era5 (absent by default, contract-tested); async job on the Phase-6 state machine; SSRF chokepoint pins the CDS host; no key/host/path leak (generic 500); reqwest only under the feature.</done>
  <acceptance_criteria>
    - Default build has NO ERA5 route (contract test green); endpoint compiles + runs only under `--features era5`.
    - Async job uses the Phase-6 `JobStatus` machine; SSRF chokepoint rejects non-allowlisted host / `..` before any request.
    - No CDS key / upstream host / path appears in any response or log; errors are generic 500.
    - `cargo tree -p envi-service` shows reqwest ONLY under the era5 feature; `cargo tree -p envi-gis` unchanged.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces (new symbols)
- `crates/envi-gis-wasm/src/dto.rs` — Phase-9 request/result DTOs: cut-profile, impedance-segmentation, screening, receiver-grid, weather-derivation (per-azimuth A/B/C), ERA5-derivation (all ts-rs `export_to = "wire.ts"`).
- `crates/envi-gis-wasm/src/lib.rs` — one `#[wasm_bindgen]` shim per Phase-9 core fn (logic-free).
- `web/src/generated/wire.ts` — regenerated with the new DTOs (committed, no-drift).
- `crates/envi-service/src/api/era5.rs` — `#[cfg(feature="era5")]` `POST /era5/import` async job + CDS `resolve_upstream` SSRF chokepoint.
- `crates/envi-service/src/api/mod.rs` — feature-gated route registration.
- `crates/envi-service/Cargo.toml` — `[features] era5` (optional reqwest, rustls-tls).
- `crates/envi-service/tests/{wire_no_drift.rs (updated), contract_era5.rs}` — no-drift + endpoint-absent-by-default + SSRF + derivation tests.
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser JS → wasm-bindgen shims | untrusted JSON/bytes cross the WASM boundary |
| envi-service → Copernicus CDS | new outbound network endpoint (flagged off) |
| CDS key (server env) → job/logs/wire | secret must never cross outward |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Spoofing/SSRF | ERA5 retrieval upstream | mitigate | `resolve_upstream` allowlist copied from proxy.rs: CDS host hardcoded, reject unknown host / `..` before any I/O; `Policy::none` redirects |
| T-09-04-02 | Information disclosure | CDS key / upstream host / path | mitigate | key in server env only; generic 500 (log server-side); never in response/log/bundle (V6/V10) |
| T-09-04-03 | Denial of Service | large/queued CDS job | mitigate | job runs on the Phase-6 state machine (Queued/Running/Cancelled); response byte cap; endpoint default-off |
| T-09-04-04 | Tampering | malformed JSON/bytes at the WASM boundary | mitigate | `deny_unknown_fields` DTOs + `gis_err` typed mapping; shims logic-free |
| T-09-04-05 | Integrity | hand-authored TS mirror drift | mitigate | ts-rs single source of truth + `wire_no_drift` fails in Rust on drift |
| T-09-04-SC | Access control | endpoint enabled unintentionally | mitigate | `#[cfg(feature="era5")]` default-off; `era5_route_absent_by_default` contract test |
</threat_model>

<verification>
- `cargo test -p envi-service --test wire_no_drift` green; `web/src/generated/wire.ts` committed and drift-free.
- `cargo test -p envi-service era5` (default) proves the route is absent; `cargo test -p envi-service --features era5` exercises the job + SSRF + derivation.
- `cargo build -p envi-gis-wasm` + `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` clean.
- `cargo tree -p envi-service` shows reqwest only under `era5`; `cargo tree -p envi-gis`/`-p envi-engine` unchanged.
</verification>

<success_criteria>
- METX-02 (transport/groundwork): async ERA5 job scaffold on envi-service, flagged off, SSRF-allowlisted, no key/host leak (SC5).
- Boundary: all Phase-9 geometry+weather core fns reachable from the browser via generated, drift-checked DTOs (enables SC1–SC4 in the web plan).
</success_criteria>

<output>
Create `.planning/phases/09-path-extraction-weather/09-04-SUMMARY.md` when done.
</output>
