---
phase: 09-path-extraction-weather
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - crates/envi-gis/src/weather.rs
  - crates/envi-gis/src/era5.rs
  - crates/envi-gis/src/lib.rs
  - crates/envi-gis/Cargo.toml
  - crates/envi-gis/tests/fixtures/openmeteo_archive.json
  - crates/envi-gis/tests/fixtures/openmeteo_forecast.json
  - crates/envi-gis/tests/fixtures/era5_synthetic.toml
  - crates/envi-harness/src/weather/mod.rs
  - crates/envi-harness/src/weather/route3.rs
  - crates/envi-harness/Cargo.toml
autonomous: true
requirements: [METX-01, METX-02]
must_haves:
  truths:
    - "Open-Meteo multi-level winds/temps derive per-azimuth A/B/C via the EXACT Phase-3 profile fit (lifted, not forked): downwind A > upwind A; inversion ⇒ B>0; round-trip fit identity holds (SC4 derivation, METX-01)"
    - "The A/B/C fit is the Route-2 profile fit (skips Monin–Obukhov reconstruction because Open-Meteo gives the profile directly); geopotential height is converted AMSL→AGL before fitting"
    - "ERA5 synthetic fields derive Obukhov 1/L (u*, θ*) and bin each hour into a wind×stability class occurrence table (METX-02 derivation)"
    - "The lifted fit lives in ONE place (envi-gis::weather); the Phase-3 harness round-trip oracle still covers it (no duplicate LSQ)"
  artifacts:
    - path: "crates/envi-gis/src/weather.rs"
      provides: "METX-01 Open-Meteo levels → per-azimuth WeatherComponents (A,B,C) via lifted fit_profile"
      contains: "pub fn profile_for_bearing"
      min_lines: 80
    - path: "crates/envi-gis/src/era5.rs"
      provides: "METX-02 Obukhov + wind×stability occurrence statistics (derivation only)"
      contains: "pub fn obukhov"
      min_lines: 70
  key_links:
    - from: "crates/envi-harness/src/weather/mod.rs"
      to: "crates/envi-gis/src/weather.rs"
      via: "harness re-exports/consumes the lifted WeatherComponents/profile_for_bearing/fit_profile (single source of truth)"
      pattern: "envi_gis::weather"
    - from: "crates/envi-gis/src/weather.rs"
      to: "crates/envi-engine/src/propagation/refraction/mod.rs"
      via: "produces SoundSpeedProfile { a,b,c,s_a,s_b,z0 } the solver consumes"
      pattern: "SoundSpeedProfile"
  prohibitions:
    - "MUST NOT promote the [ASSUMED] weather-route A/B/C constants to a false FORCE numeric pass — structural/property/round-trip tests ONLY (quarantine carried from Phase 3 03-03 Open-Q1)"
    - "MUST NOT fork/duplicate the 3×3 Cramer LSQ — lift it into envi-gis::weather and have envi-harness consume it (one source of truth)"
    - "MUST NOT sample geopotential height as height-above-ground — it is AMSL; subtract site elevation first"
    - "MUST NOT add reqwest/tokio/web-sys to envi-gis — all HTTP is browser-side (METX-01) or the flagged service endpoint (METX-02 transport, next plan)"
    - "MUST NOT map ERA5 classes to A/B/C or combine into L_den — occurrence statistics ONLY (deferred to GRID-03, D-05)"
    - "MUST NOT panic on non-finite weather/ERA5 fields — typed errors (mirror route3 finiteness checks)"
---

<objective>
Deliver the weather half's pure math: METX-01 turns an Open-Meteo multi-level profile into per-azimuth A/B/C by LIFTING the Phase-3 fit (`fit_profile`, `WeatherComponents`, `profile_for_bearing`) into a WASM-safe `envi-gis::weather`, and METX-02 derives the ERA5 Obukhov length + wind×stability class-occurrence statistics — both as `envi-gis` modules validated against committed fixtures.

Purpose: The engine already implements the A/B/C refraction; this plan feeds it real weather without re-deriving any acoustics and without forking the validated LSQ. The [ASSUMED] weather-route constants stay quarantined — no false FORCE pass.
Output: `crates/envi-gis/src/{weather,era5}.rs`, committed Open-Meteo + synthetic-ERA5 fixtures, and a harness re-wire so the lifted math has a single home.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-path-extraction-weather/09-CONTEXT.md
@.planning/phases/09-path-extraction-weather/09-RESEARCH.md
@.planning/phases/09-path-extraction-weather/09-PATTERNS.md
@.planning/phases/09-path-extraction-weather/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift Phase-3 fit into envi-gis::weather + Open-Meteo → per-azimuth A/B/C (METX-01)</name>
  <read_first>
    - crates/envi-harness/src/weather/mod.rs (WeatherComponents, profile_for_bearing, ReflectionProfiles + the [ASSUMED] banner + the "future Phase-4 collapse" note)
    - crates/envi-harness/src/weather/route3.rs (fit_profile(heights, c_eff, z0) -> Result<(f64,f64,f64), CaseLoadError>: 3×3 Cramer LSQ over [ln(z/z0+1), z, 1], Z0_MIN_M clamp, non-finite → typed error; reconstruct_profiles c_eff formula)
    - crates/envi-engine/src/propagation/refraction/mod.rs (SoundSpeedProfile { a,b,c,s_a,s_b,z0 }; sound_speed_ms)
    - crates/envi-gis/tests/cog_window.rs (committed-fixture test + provenance/SHA pattern)
    - 09-RESEARCH.md Pattern 5 + Code Examples ("components_from_levels") + Pitfall 5/7; 09-PATTERNS.md "weather.rs" section
  </read_first>
  <behavior>
    - components_from_levels(levels, phi_wind_deg, z0) fits A_temp,B,C from the TEMPERATURE-only sound speed and A_wind from the wind-speed profile via the SAME lifted fit_profile, returning WeatherComponents; heights are AGL (AMSL − site elevation).
    - profile_for_bearing(&comp, path_az_deg, phi_wind_deg) projects the wind part per azimuth: downwind A > upwind A; crosswind A == a_temp; B bearing-independent.
    - On the committed Open-Meteo archive and forecast fixtures the derivation runs, round-trips the fit identity, and satisfies the direction properties. Non-finite levels → typed error.
    - The 3×3 LSQ exists in exactly one place; envi-harness consumes it (its Phase-3 weather tests still pass unchanged).
  </behavior>
  <action>
    Relocate the pure math `fit_profile` (from `route3.rs`) and `WeatherComponents` + `profile_for_bearing` (from `weather/mod.rs`) into a new `crates/envi-gis/src/weather.rs` (they depend only on `envi_engine` — WASM-safe). Add `envi-gis` to `crates/envi-harness/Cargo.toml` `[dependencies]` and update `envi-harness` to `use envi_gis::weather::{...}` (re-export from `weather/mod.rs` for backward compatibility so existing harness call sites and tests are unchanged — do NOT keep a second copy of the LSQ). In `weather.rs`, add `pub fn components_from_levels(levels, phi_wind_deg, z0) -> Result<WeatherComponents, GisError>` implementing the Route-2 profile fit: build AGL heights (accept AMSL geopotential-height levels and subtract site elevation), compute the temperature-only `c_eff = sound_speed_ms(T)` series and fit `(a_temp, b, c)`, compute the wind-speed series and fit `a_wind`; wrap any `CaseLoadError`/non-finite as a typed `GisError` (message-wrapped to keep `PartialEq`). Add a `SoundSpeedProfile`-builder that emits `{a,b,c,s_a:0,s_b:0,z0}` for a given path azimuth via `profile_for_bearing`. Keep the [ASSUMED] banner verbatim. Add `pub mod weather;` to `lib.rs`. Commit `tests/fixtures/openmeteo_{archive,forecast}.json` (small real-shaped responses, provenance/SHA banner) and tests: downwind>upwind, inversion⇒B>0, crosswind isotropic, round-trip fit identity, AMSL→AGL conversion, non-finite→typed error. NO FORCE numeric assertion.
  </action>
  <files>crates/envi-gis/src/weather.rs, crates/envi-gis/src/lib.rs, crates/envi-gis/tests/fixtures/openmeteo_archive.json, crates/envi-gis/tests/fixtures/openmeteo_forecast.json, crates/envi-harness/src/weather/mod.rs, crates/envi-harness/src/weather/route3.rs, crates/envi-harness/Cargo.toml</files>
  <verify>
    <automated>cargo test -p envi-gis weather && cargo test -p envi-harness weather</automated>
  </verify>
  <done>fit_profile/WeatherComponents/profile_for_bearing lifted to envi-gis::weather (single source of truth, harness re-exports, Phase-3 tests unchanged); per-azimuth A/B/C from Open-Meteo levels (AMSL→AGL); direction + round-trip properties green; no FORCE numeric pass.</done>
  <acceptance_criteria>
    - The 3×3 LSQ lives only in `envi-gis::weather`; `envi-harness` re-exports/consumes it and its Phase-3 weather tests pass unchanged.
    - Direction properties + round-trip identity green on the committed Open-Meteo fixtures; geopotential AMSL→AGL applied.
    - No FORCE numeric pass; [ASSUMED] banner preserved; non-finite → typed error.
    - `cargo tree -p envi-gis` still free of HTTP/async/browser crates; `cargo tree -p envi-engine` unchanged (3 deps).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ERA5 Obukhov + wind×stability occurrence statistics (METX-02 derivation)</name>
  <read_first>
    - crates/envi-harness/src/weather/route3.rs:130-146 (finiteness-checked pure derivation + typed error posture to mirror)
    - crates/envi-gis/src/lib.rs (GisError struct-variant style — add an ERA5 field-parse/non-finite variant)
    - 09-RESEARCH.md Pattern 6 (Obukhov recipe: u* from iews/inss, θ* from ishf, 1/L = κ·g·θ*/(T_v·u*²), κ=0.4, g=9.81; sdfor<50 m quality gate; sign convention) + Pitfall 5; 09-PATTERNS.md "era5.rs" section
    - 09-CONTEXT.md D-04/D-05 (occurrence statistics ONLY; class→A/B/C + L_den deferred to GRID-03)
  </read_first>
  <behavior>
    - obukhov(fields) computes u* = ((iews²+inss²)/ρ)^(1/4), θ* = −ishf/(ρ·c_p·u*), 1/L = κ·g·θ*/(T_v·u*²) with T_v from 2t/2d/sp; downward-flux-positive sign convention.
    - Each hour bins into a wind-speed × stability(1/L sign+magnitude) class; occurrence_stats returns per-class counts/frequencies over the period.
    - On the committed synthetic ERA5 fixture the derived 1/L and class counts match expected values within tolerance; non-finite fields → typed error, never panic.
    - Bin edges are named [ASSUMED] consts; only the counting is validated — no class→A/B/C, no L_den.
  </behavior>
  <action>
    Create `crates/envi-gis/src/era5.rs` with the module skeleton. Implement `pub fn obukhov(fields) -> Result<f64, GisError>` (returns 1/L or L per a documented convention) from committed synthetic ERA5 single-level fields `(iews, inss, ishf, 2t, 2d, sp, sdfor)`, using named `pub const` physical constants (κ=0.4, g=9.81, c_p, air density) and finiteness checks that map non-finite/degenerate inputs to a typed `GisError` (mirror `route3` posture). Implement `pub fn occurrence_stats(hours) -> ClassOccurrence` binning each hour into a wind-speed × stability class with named `[ASSUMED]` `pub const` bin edges, returning counts + frequencies; expose a `sdfor`-based reliability flag. Do NOT map classes to A/B/C or combine into L_den (D-05, GRID-03). Add `pub mod era5;` + an ERA5 field-parse variant to `lib.rs`. Commit `tests/fixtures/era5_synthetic.toml` (fields + expected 1/L + expected class counts, provenance/SHA) and tests asserting 1/L within tolerance, class counts exact, non-finite → typed error, and the daytime-unstable/night-stable sign.
  </action>
  <files>crates/envi-gis/src/era5.rs, crates/envi-gis/src/lib.rs, crates/envi-gis/tests/fixtures/era5_synthetic.toml</files>
  <verify>
    <automated>cargo test -p envi-gis era5</automated>
  </verify>
  <done>obukhov 1/L matches the synthetic fixture (correct sign) + wind×stability occurrence counts exact; occurrence statistics ONLY (no A/B/C, no L_den); named [ASSUMED] consts; non-finite → typed error.</done>
  <acceptance_criteria>
    - `obukhov` matches the fixture 1/L within tolerance with the correct sign; `occurrence_stats` class counts exact.
    - Output is occurrence statistics only (no A/B/C mapping, no L_den); bin edges are named [ASSUMED] consts.
    - Non-finite ERA5 fields → typed `GisError`, no panic.
    - `cargo test -p envi-gis` + clippy/fmt clean.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces (new symbols)
- `crates/envi-gis/src/weather.rs` — module; lifted `pub struct WeatherComponents`, `pub fn profile_for_bearing(...)`, `pub fn fit_profile(...)`; new `pub fn components_from_levels(...) -> Result<WeatherComponents, GisError>` + a `SoundSpeedProfile` builder.
- `crates/envi-gis/src/era5.rs` — module; `pub fn obukhov(...) -> Result<f64, GisError>`, `pub fn occurrence_stats(...) -> ClassOccurrence`, `pub struct ClassOccurrence`, named [ASSUMED] bin-edge + physical consts.
- `crates/envi-gis/src/lib.rs` — `pub mod weather;` + `pub mod era5;`; new weather-fit + ERA5 field-parse `GisError` variants.
- `crates/envi-gis/Cargo.toml` — (no new runtime dep; serde/serde_json already present for fixture parsing in tests).
- `crates/envi-harness/{Cargo.toml, src/weather/mod.rs, src/weather/route3.rs}` — depend on + re-export the lifted math (single source of truth; no duplicate LSQ).
- Fixtures: `openmeteo_archive.json`, `openmeteo_forecast.json`, `era5_synthetic.toml`.
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Open-Meteo JSON → weather derivation | untrusted third-party met JSON crosses here |
| ERA5 fields → Obukhov derivation | untrusted reanalysis fields cross here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | NaN/inf in weather profile → poisoned A/B/C | mitigate | finiteness checks reject non-finite → typed `GisError` (mirror route3); z0 clamped ≥ 0.001 m |
| T-09-03-02 | Tampering | NaN/inf/degenerate ERA5 fields | mitigate | finiteness checks + `sdfor` reliability gate → typed error, no panic |
| T-09-03-03 | Integrity | false FORCE pass from [ASSUMED] constants | mitigate | structural/property/round-trip tests only; [ASSUMED] banner preserved; never a numeric FORCE assertion |
| T-09-03-SC | Tampering | dependency surface / async leak | mitigate | no reqwest/tokio/web-sys in `envi-gis` (`cargo tree`); `envi-engine` unchanged |
</threat_model>

<verification>
- `cargo test -p envi-gis weather era5` + `cargo test -p envi-harness` (Phase-3 weather tests unchanged) green.
- `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` clean across the touched crates.
- `cargo tree -p envi-gis` shows no HTTP/async/browser edge; `cargo tree -p envi-engine` still 3 deps.
- Grep/inspection confirms the 3×3 LSQ is defined once (envi-gis::weather) and re-exported by the harness.
</verification>

<success_criteria>
- METX-01 (derivation): per-azimuth A/B/C from the multi-level profile via the lifted fit; direction + round-trip properties hold; quarantine intact (SC4 derivation half).
- METX-02 (derivation): ERA5 Obukhov 1/L + wind×stability occurrence statistics from a committed fixture (SC5 derivation half).
</success_criteria>

<output>
Create `.planning/phases/09-path-extraction-weather/09-03-SUMMARY.md` when done.
</output>
