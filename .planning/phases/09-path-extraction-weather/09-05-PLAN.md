---
phase: 09-path-extraction-weather
plan: 05
type: execute
wave: 5
depends_on: [09-04]
files_modified:
  - web/src/import/weather.ts
  - web/src/store/weather.ts
  - web/src/panels/WeatherPanel.tsx
  - web/src/map/weatherOverlay.ts
  - web/src/import/opfs.ts
  - web/dist/
autonomous: true
requirements: [METX-01]
must_haves:
  truths:
    - "A weather-import panel lets the user pick a date+hour, imports from Open-Meteo, and displays the derived per-azimuth A/B/C (SC4, METX-01)"
    - "The Open-Meteo product is date-switched: historical → archive-api, recent/near-future → forecast (D-02)"
    - "Fetched weather is cached in OPFS keyed by (lat, lon, timestamp); the A/B/C derivation runs in envi-gis WASM over the cached JSON, never in TypeScript (D-01/D-03)"
    - "What-if edits read OPFS only — zero Open-Meteo calls; the weighted call cost is logged per fetch (SC4)"
    - "Debug overlays render the receiver grid, impedance segmentation, and injected screen vertices"
  artifacts:
    - path: "web/src/import/weather.ts"
      provides: "date-switched Open-Meteo fetch (direct CORS + proxy fallback) + OPFS cache + call-cost log"
      contains: "archive-api.open-meteo.com"
      min_lines: 60
    - path: "web/src/panels/WeatherPanel.tsx"
      provides: "date+hour picker → import → per-azimuth A/B/C readout"
      contains: "data-testid"
      min_lines: 60
    - path: "web/src/store/weather.ts"
      provides: "zustand weather store (import state machine + derived A/B/C)"
  key_links:
    - from: "web/src/import/weather.ts"
      to: "web/src/import/opfs.ts"
      via: "getWeather/putWeather cache keyed (lat,lon,timestamp) with safeSeg sanitization"
      pattern: "cache/weather"
    - from: "web/src/panels/WeatherPanel.tsx"
      to: "crates/envi-gis-wasm (via web/src/import/wasm.ts)"
      via: "call the weather-derivation WASM shim over cached JSON (no TS acoustic math)"
      pattern: "wasm"
  prohibitions:
    - "MUST NOT perform any acoustic / A-B-C arithmetic in TypeScript — the fit runs in the envi-gis WASM shim (wire contract)"
    - "MUST NOT issue an Open-Meteo request on a what-if edit — derivation reads OPFS only (SC4)"
    - "MUST NOT set innerHTML from any fetched/derived string — every string reaches the DOM as a React text child (T-08-07-05)"
    - "MUST NOT re-key the OPFS cache on view/UI state — key strictly (lat, lon, timestamp)"
    - "MUST NOT redeclare the ApiError type — reuse ../api/client; MUST NOT allow a path-traversal cache key (reuse safeSeg)"
---

<objective>
Build the browser-side weather-import journey: a `WeatherPanel` (date+hour → import → per-azimuth A/B/C), a date-switched Open-Meteo fetcher with OPFS caching, a zustand weather store, and debug overlays for the receiver grid, impedance segmentation, and injected screen vertices — all obeying the Phase-8 pivot (browser fetch → OPFS; WASM does the math; zero API calls on what-if).

Purpose: This is the user-facing half of METX-01 and the explicit frontend requirement. It closes the loop from the Phase-8 imported scene + this phase's geometry/weather core to a visible per-azimuth atmosphere, with SC4's zero-egress-on-what-if property built in (proven by the next plan's Playwright suite).
Output: `web/src/import/weather.ts`, `web/src/store/weather.ts`, `web/src/panels/WeatherPanel.tsx`, `web/src/map/weatherOverlay.ts`, an OPFS weather cache helper, and a rebuilt `web/dist`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-path-extraction-weather/09-CONTEXT.md
@.planning/phases/09-path-extraction-weather/09-RESEARCH.md
@.planning/phases/09-path-extraction-weather/09-PATTERNS.md
@.planning/phases/09-path-extraction-weather/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Date-switched Open-Meteo fetch + OPFS weather cache + call-cost log</name>
  <read_first>
    - web/src/import/fetchers.ts:32-60 (direct-vs-proxy fetch, whole-resource GET, non-2xx → ApiError from ../api/client, proxyUrl rewrite)
    - web/src/import/opfs.ts:36-40,54-85 (safeSeg path sanitization + getTile/putTile projects/<uuid>/cache/... layout)
    - web/src/import/wasm.ts (how the browser calls the envi-gis-wasm shims)
    - web/src/generated/wire.ts (the Phase-9 weather-derivation DTOs generated in 09-04)
    - 09-RESEARCH.md Pattern 5 (endpoints, variable list, AMSL→AGL, CORS + proxy fallback, call-cost weight) + Pitfall 6; 09-PATTERNS.md "web/src/import/weather.ts" section
  </read_first>
  <action>
    Create `web/src/import/weather.ts`: a `fetchWeather(lat, lon, timestamp)` that date-switches the endpoint (recent/near-future → `https://api.open-meteo.com/v1/forecast`; historical → `https://archive-api.open-meteo.com/v1/archive`) requesting the pressure-level + height-AGL variable set from RESEARCH Pattern 5, using the `fetchers.ts` direct-CORS-first + login-server-byte-proxy-fallback pattern and throwing the existing `ApiError` (from `../api/client`, never redeclared) on non-2xx. Add `getWeather`/`putWeather` to `web/src/import/opfs.ts` (or a sibling) reusing `safeSeg` and a fixed `projects/<uuid>/cache/weather/<key>` layout with `key = (lat,lon,timestamp)` (D-01). On a cache hit, do NOT fetch. Log an estimated call-cost weight (`n_variables × n_timesteps / 100`) to a metric/console line per network fetch. Expose a `deriveAbc(cachedJson, pathAzimuths)` that calls the 09-04 weather-derivation WASM shim (subtracting site elevation AMSL→AGL as the shim expects) — no acoustic arithmetic in TS.
  </action>
  <files>web/src/import/weather.ts, web/src/import/opfs.ts</files>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run weather 2>/dev/null || cd web && npx tsc --noEmit</automated>
  </verify>
  <done>Date-switched Open-Meteo fetch (direct CORS + proxy fallback) → OPFS cache keyed (lat,lon,timestamp); cache hit = zero network; call-cost logged; deriveAbc delegates to the WASM shim (no TS acoustic math).</done>
  <acceptance_criteria>
    - Date-switch selects archive-api vs forecast by requested date; direct CORS first, proxy fallback.
    - OPFS cache keyed strictly (lat,lon,timestamp); cache hit issues zero network calls; call-cost logged per fetch.
    - Derivation delegates to the WASM shim; no A/B/C math in TS; `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: WeatherPanel + weather store + debug overlays</name>
  <read_first>
    - web/src/panels/ImportPanel.tsx:40-172 (LayerRow status/progress/error/retry; every control carries data-testid; strings as React text children, never innerHTML; .chip.warn/.chip.crit)
    - web/src/store/import.ts:71-115 (zustand create<State> + GuardrailState shape)
    - web/src/map/impedanceOverlay.ts + web/src/map/DgmOverlay.tsx (overlay analogs)
    - 09-PATTERNS.md "web/src/store/weather.ts + WeatherPanel.tsx + overlay" section
  </read_first>
  <action>
    Create `web/src/store/weather.ts` (zustand, mirroring `import.ts` shape): import state machine (idle/fetching/cached/derived/error), the selected date+hour, cached-response reference, and the derived per-azimuth A/B/C. Create `web/src/panels/WeatherPanel.tsx`: a date+hour picker, an Import button, and a per-azimuth A/B/C readout, following `ImportPanel` conventions — every actionable control carries a `data-testid` (the E2E drives these), status chips reuse `.chip.warn`/`.chip.crit`, and every fetched/derived string reaches the DOM as a React text child (never `innerHTML`, T-08-07-05). Wire Import → `fetchWeather` → OPFS → `deriveAbc` → store. Create `web/src/map/weatherOverlay.ts` plus debug overlays for the receiver grid, impedance segmentation, and injected screen vertices (reuse the `impedanceOverlay`/`DgmOverlay` pattern), toggled from the panel. Rebuild the committed bundle (`web/dist`) so the app ships the new panel.
  </action>
  <files>web/src/store/weather.ts, web/src/panels/WeatherPanel.tsx, web/src/map/weatherOverlay.ts, web/dist/</files>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>WeatherPanel (date+hour → import → per-azimuth A/B/C, data-testid'd, no innerHTML) + zustand store + receiver-grid/impedance/screen-vertex debug overlays; tsc clean; web/dist rebuilt.</done>
  <acceptance_criteria>
    - WeatherPanel: date+hour → Import → per-azimuth A/B/C readout; every control has a `data-testid`; no `innerHTML`.
    - Debug overlays render receiver grid, impedance segmentation, and injected screen vertices.
    - `npx tsc --noEmit` clean; `npm run build` succeeds and `web/dist` is rebuilt/committed.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces (new symbols)
- `web/src/import/weather.ts` — `fetchWeather(lat,lon,timestamp)`, `deriveAbc(cachedJson, pathAzimuths)`, call-cost logging.
- `web/src/import/opfs.ts` — `getWeather`/`putWeather` (projects/<uuid>/cache/weather/<key>, safeSeg).
- `web/src/store/weather.ts` — zustand weather store (state machine + derived A/B/C).
- `web/src/panels/WeatherPanel.tsx` — date+hour picker + import + per-azimuth A/B/C readout (data-testid'd).
- `web/src/map/weatherOverlay.ts` — receiver-grid / impedance-segmentation / screen-vertex debug overlays.
- `web/dist/` — rebuilt committed bundle.
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → Open-Meteo (direct/proxy) | untrusted third-party weather JSON crosses here |
| fetched/derived strings → DOM | injection surface (XSS) |
| cache key → OPFS path | path-traversal surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Tampering (XSS) | WeatherPanel string rendering | mitigate | all strings as React text children; never `innerHTML` (T-08-07-05) |
| T-09-05-02 | Tampering | OPFS cache path traversal | mitigate | reuse `safeSeg`; fixed `projects/<uuid>/cache/weather/<key>` layout |
| T-09-05-03 | Spoofing/SSRF | Open-Meteo fetch host | mitigate | hosts hardcoded (api/archive-api.open-meteo.com); proxy fallback reuses the Phase-8 allowlist |
| T-09-05-04 | Integrity | client-side acoustic math drift | mitigate | A/B/C fit runs only in the WASM shim; TS does no acoustic arithmetic |
| T-09-05-05 | Denial of Service | redundant weather fetches | mitigate | OPFS cache-then-read; zero calls on what-if; call-cost logged |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit` clean; `npm run build` succeeds; `web/dist` rebuilt.
- Manual/automated: Import shows per-azimuth A/B/C; a cached re-open issues no network call (fully proven by 09-06 Playwright).
- No acoustic arithmetic in `web/src/**/*.ts(x)` (derivation delegates to WASM).
</verification>

<success_criteria>
- METX-01 (UI): weather-import panel imports date-switched Open-Meteo, caches in OPFS, derives per-azimuth A/B/C via WASM, logs call-cost; debug overlays render (SC4, UI half).
</success_criteria>

<output>
Create `.planning/phases/09-path-extraction-weather/09-05-SUMMARY.md` when done.
</output>
