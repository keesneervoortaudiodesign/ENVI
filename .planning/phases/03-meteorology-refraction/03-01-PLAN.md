---
phase: 03-meteorology-refraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/envi-engine/src/propagation/mod.rs
  - crates/envi-engine/src/propagation/refraction/mod.rs
  - crates/envi-engine/src/propagation/refraction/profile.rs
  - crates/envi-engine/src/propagation/refraction/eqssp.rs
  - crates/envi-engine/src/propagation/refraction/circular_ray.rs
  - crates/envi-engine/src/propagation/refraction/shadow_zone.rs
  - crates/envi-engine/src/propagation/rays.rs
  - crates/envi-engine/src/propagation/terrain_effect/mod.rs
  - crates/envi-engine/src/propagation/terrain_effect/submodel1.rs
  - tools/nord2000_oracle/gen_refraction_fixtures.py
  - crates/envi-harness/tests/oracle_refraction.rs
  - cases/refraction_downwind.toml
  - cases/refraction_upwind_shadow.toml
autonomous: true
requirements: [ENG-05, MET-01, MET-03]

must_haves:
  truths:
    - "c(z)=A·ln(z/z₀+1)+B·z+C evaluates with z₀ clamped ≥ 0.001 m (MET-01, success criterion 2)"
    - "CalcEqSSP collapses the log-lin profile to (ξ,c₀) with ∂c/∂z averaged between hS and hR via the Annex F closed form, no numerical integration (MET-03, success criterion 3)"
    - "circular_rays fills the same RayVars/RayPair fields as straight_rays; with |ξ|<1e-6 it reproduces straight_rays bit-for-bit (D-02 homogeneous anchor, success criterion 1)"
    - "Δτ is computed cancellation-safe in f64; no singularity blow-up across all 105 bands and up/down/shadow geometries (ENG-05, success criterion 1)"
    - "A refracted (downwind) case runs end-to-end through terrain_effect + the two-channel readout and a matching oracle fixture passes; upward-refraction shadow-zone attenuation is produced via the equivalent-wedge kernel (D-09)"
  artifacts:
    - path: "crates/envi-engine/src/propagation/refraction/profile.rs"
      provides: "c(z) Eq. 2 + C=Coft(t₀) Eq. 3, z₀ clamp ≥ 0.001 m"
      contains: "fn sound_speed_profile"
    - path: "crates/envi-engine/src/propagation/refraction/eqssp.rs"
      provides: "CalcEqSSP (ξ,c₀) collapse Eqs. 15–21 + Annex F c̄ Eq. 403"
      contains: "fn calc_eq_ssp"
    - path: "crates/envi-engine/src/propagation/refraction/circular_ray.rs"
      provides: "DirectRay/ReflectedRay/reflection cubic/Δτ/Δτ₀; HeightOfCircularRay"
      contains: "fn direct_ray"
    - path: "crates/envi-engine/src/propagation/refraction/shadow_zone.rs"
      provides: "ShadowZoneShielding via diffraction::pwedge0 (equivalent wedge)"
      contains: "fn shadow_zone_shielding"
    - path: "crates/envi-engine/src/propagation/rays.rs"
      provides: "circular_rays entry filling RayVars/RayPair with the |ξ|<1e-6 → straight_rays delegation"
      contains: "fn circular_rays"
    - path: "tools/nord2000_oracle/gen_refraction_fixtures.py"
      provides: "committed scipy oracle for ξ/Δτ/CalcEqSSP (regenerated offline; no Python at test time)"
  key_links:
    - from: "crates/envi-engine/src/propagation/rays.rs::circular_rays"
      to: "crates/envi-engine/src/propagation/rays.rs::straight_rays"
      via: "|ξ|<1e-6 homogeneous shortcut delegates so the D-02 anchor is structural, not a parallel reimplementation"
      pattern: "xi.abs\\(\\) < 1e-6"
    - from: "crates/envi-engine/src/propagation/terrain_effect/mod.rs"
      to: "crates/envi-engine/src/propagation/rays.rs::circular_rays"
      via: "dispatch swaps straight_rays→circular_rays when refraction is active (ξ from CalcEqSSP)"
      pattern: "circular_rays"
    - from: "crates/envi-engine/src/propagation/refraction/shadow_zone.rs"
      to: "crates/envi-engine/src/propagation/diffraction.rs"
      via: "equivalent-wedge attenuation reuses pwedge0/dwedge0 (D-09, no new diffraction primitive)"
      pattern: "pwedge0"
  prohibitions:
    - "No `.conj()` anywhere in propagation/ (grep gate = 0); write conjugation as explicit Complex::new(re, -im) (D-13 time-convention quarantine)"
    - "No numerical quadrature for c̄ (Eq. 19) — use the Annex F Eq. 403 closed form"
    - "No new envi-engine dependency — stays ndarray + num-complex + thiserror (cargo tree gate, D-15)"
    - "No separate homogeneous code path — the homogeneous limit routes through circular_rays' |ξ|<1e-6 shortcut"
    - "No comparison by nominal frequency — compare by BAND INDEX (D-14)"
    - "z₀ clamped ≥ 0.001 m; ξ/Δz/dSZ singularities clamped; Δτ via the cancellation-free ΔR identity extended to the circular case (D-16 numerics house rules)"
    - "Do not use 1e-10 as the homogeneous-shortcut threshold — that is the DISTINCT inner DirectRay division guard; the homogeneous shortcut is 1e-6"
    - "Numeric green comes ONLY from the same oracle+anchor ladder as Phase 2 — no direct FORCE up/downwind numeric comparison in Phase 3 (D-01)"
---

<objective>
Build the refraction core so a curved-ray (refracted) propagation path runs end-to-end through the existing `terrain_effect` two-channel readout, and prove it is a strict generalization of Phase 2 by reproducing the straight-ray results bit-for-bit in the homogeneous limit.

## Phase Goal (MVP user story)

**As an** acoustics-engine validation harness, **I want to** run a refracted (downwind and upward-refraction/shadow) propagation case through the existing sub-model composition and get a level that matches a committed oracle — while the homogeneous limit (|ξ| below the clamp) reproduces Phase 2 exactly, **so that** refraction is a proven, regression-free extension of the frozen ray machinery rather than a parallel reimplementation.

Purpose: this is the load-bearing slice. Everything in 03-02/03-03 (frequency-dependent ground, reflection split, weather routes, F_τ) feeds ξ/Δτ into the primitives built here. The D-02 bit-for-bit anchor is the safety net that guarantees no regression on the already-green Phase-2 ground/diffraction cases.
Output: `propagation/refraction/` submodule (profile, eqssp, circular_ray, shadow_zone), `circular_rays` in `rays.rs`, refraction dispatch + shadow branch in `terrain_effect`, the scipy oracle generator + Rust oracle test, and two refraction TOML cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-meteorology-refraction/03-CONTEXT.md
@.planning/phases/03-meteorology-refraction/03-RESEARCH.md
@.planning/phases/03-meteorology-refraction/03-PATTERNS.md
@refs/AV1106-07-rev4.pdf
</context>

<artifacts_this_phase_produces>
New symbols introduced by this plan (executor MUST export/name these exactly so 03-02/03-03 consume them):
- `propagation::refraction` submodule with `mod.rs`, `profile.rs`, `eqssp.rs`, `circular_ray.rs`, `shadow_zone.rs`
- `refraction::profile::sound_speed_profile(z, a, b, c, z0) -> f64` (Eq. 2, z₀ clamp ≥ 0.001 m)
- `refraction::eqssp::calc_eq_ssp(h_s, h_r, z0, a, b, c) -> Result<(f64, f64), PropagationError>` returning `(ξ, c₀)` (Eqs. 15–21 + Annex F Eq. 403); `|ξ|<1e-6 ⇒ (0.0, C)`
- `refraction::circular_ray::direct_ray(...)`, `reflected_ray(...)`, `reflection_point_cubic(...)`, `travel_time_diff(...)`, `height_of_circular_ray(...)`
- `refraction::shadow_zone::shadow_zone_shielding(...) -> Result<Vec<f64>, PropagationError>` (per-band L_SZ via `diffraction::pwedge0`)
- `rays::circular_rays(d, h_s, h_r, xi, c0) -> Result<RayPair, PropagationError>` (fills existing `RayVars`/`RayPair`)
- new `PropagationError` variants: `NoReflectionRoot`, `DegenerateProfile`, and any shadow-zone variant
- `tools/nord2000_oracle/gen_refraction_fixtures.py` + `crates/envi-harness/tests/oracle_refraction.rs`
- `cases/refraction_downwind.toml`, `cases/refraction_upwind_shadow.toml`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Log-lin profile + CalcEqSSP equivalent-linear collapse + error hub</name>
  <files>crates/envi-engine/src/propagation/mod.rs, crates/envi-engine/src/propagation/refraction/mod.rs, crates/envi-engine/src/propagation/refraction/profile.rs, crates/envi-engine/src/propagation/refraction/eqssp.rs, tools/nord2000_oracle/gen_refraction_fixtures.py, crates/envi-harness/tests/oracle_refraction.rs</files>
  <read_first>
    - crates/envi-engine/src/propagation/mod.rs (the `PropagationError` enum lines ~48–107 and the `pub mod` block ~28–37; and `sound_speed_ms` ~111–114 for `C=Coft(t₀)`, Eq. 335 coeff 20.05 — do NOT reintroduce 340.29)
    - crates/envi-engine/src/propagation/ground.rs (lines 1–49: the Nord2000-native convention doc-block and the typed-Result numeric-fn pattern to copy)
    - 03-RESEARCH.md §"CalcEqSSP" (Eqs. 15–21, the Annex F Eq. 403 c̄ closed form ready-to-paste, and the guards: hmin=5·z₀, hS=hR→±0.005 m fallback, |ξ|<1e-6⇒ξ=0,c̄=c₀=C)
    - 03-PATTERNS.md §"refraction/eqssp.rs" and §"refraction/profile.rs"
    - tools/nord2000_oracle/gen_ground_fixtures.py + crates/envi-harness/tests/oracle_ground.rs (the fixture generator + Rust oracle test pattern to clone — sha256 provenance header, committed TOML, `rel_err` per-row assert)
    - refs/AV1106-07-rev4.pdf §5.3.2 p.16 (Eqs. 2–3), §5.5.2 p.23–24 (Eqs. 15–21), Annex F p.177 (Eq. 403) — confirm every exponent/sign on the page image per D-04
  </read_first>
  <behavior>
    - `sound_speed_profile(z, a, b, c, z0)` returns `A·ln(z/z₀+1)+B·z+C`; passing `z0 < 0.001` clamps to 0.001 (assert the clamped result equals the value at z₀=0.001).
    - `calc_eq_ssp` returns `(0.0, C)` exactly whenever `|ξ|<1e-6` (homogeneous shortcut).
    - `calc_eq_ssp` matches the committed scipy oracle fixture for ξ and c₀ across a grid of (A,B,C,z₀,hS,hR) at Phase-2-style tolerance.
    - `hS == hR` (within 1e-9) uses the ±0.005 m modified-height gradient, never a divide-by-zero.
    - Non-finite or non-positive inputs return a typed `PropagationError`, never a panic or NaN.
  </behavior>
  <action>
    Add `pub mod refraction;` to `propagation/mod.rs` beside the existing `pub mod` block, and add the new `PropagationError` variants `NoReflectionRoot`, `DegenerateProfile` (and a shadow-zone variant) following the exact `#[error("…")]` + doc-comment style of `DegenerateRayGeometry`. Create `refraction/mod.rs` re-exporting the submodules with the Nord2000-native convention doc-block copied from `ground.rs`. Implement `refraction::profile::sound_speed_profile` (Eq. 2) clamping `z₀ ≥ 0.001 m`; reuse `sound_speed_ms` for `C=Coft(t₀)` (Eq. 3). Implement `refraction::eqssp::calc_eq_ssp(h_s, h_r, z0, a, b, c) -> Result<(f64,f64), PropagationError>`: average gradient `∂c/∂z=(c(hR)−c(hS))/(hR−hS)` (Eq. 18) with `hmin=5·z₀` height floor and the `hS=hR`→(hS−0.005, hR+0.005) fallback; `c̄` from the Annex F Eq. 403 closed form (antiderivative `L(h)=(h+z₀)·ln(h/z₀+1)−h`), `c₀=c̄−(∂c/∂z)·(hS+hR)/2` (Eq. 20), `ξ=(∂c/∂z)/c₀` (Eq. 17); return `(0.0, c)` when `|ξ|<1e-6`. Extend `tools/nord2000_oracle/gen_refraction_fixtures.py` (clone `gen_ground_fixtures.py`) to emit committed TOML pinning ξ and c₀ over a (A,B,C,z₀,hS,hR) grid with sha256 provenance; add `crates/envi-harness/tests/oracle_refraction.rs` (clone `oracle_ground.rs`) loading that fixture and asserting per-row `rel_err` ≤ tolerance. Reject non-finite / non-positive inputs with the new typed errors.
  </action>
  <verify>
    <automated>cargo test -p envi-engine profile eqssp && cargo test -p envi-harness --test oracle_refraction && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>profile + CalcEqSSP unit tests pass; the CalcEqSSP oracle fixture passes at tolerance; z₀ clamp and hS=hR fallback proven by tests; clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: circular_rays (DirectRay/ReflectedRay/cubic/Δτ) + shadow-zone shielding + homogeneous bit-for-bit anchor</name>
  <files>crates/envi-engine/src/propagation/refraction/circular_ray.rs, crates/envi-engine/src/propagation/refraction/shadow_zone.rs, crates/envi-engine/src/propagation/rays.rs, tools/nord2000_oracle/gen_refraction_fixtures.py, crates/envi-harness/tests/oracle_refraction.rs</files>
  <read_first>
    - crates/envi-engine/src/propagation/rays.rs (full: the `RayVars`/`RayPair` structs lines 29–56 to FILL not redefine; the input-guard block lines 73–88; the cancellation-free ΔR identity lines 90–95 and 174–184; the test layout lines 205–282)
    - 03-RESEARCH.md §"DirectRay" (Eqs. 29–44 incl. clamps Δz<0.01→0.01, |ξ|<1e-10→1e-10, dSZ + 0.95·dSZ shadow test), §"ReflectedRay + reflection-point cubic" (Eqs. 45–50, cubic Eq. 49 root-selection: closest to source if hS<hR else receiver; upward=single root), §"Travel-time difference Δτ" (Eqs. 51–53 incl. Δτ₀ shadow-edge cap Eq. 52), §"RayCurvature / ray height" (HeightOfCircularRay), §"ShadowZoneShielding" (Eqs. 384–388, ξSZ freeze above 2000 Hz Eq. 385)
    - 03-PATTERNS.md §"refraction/circular_ray.rs — THE frozen seam" (the delegation pattern + D-02 anchor test template) and §"refraction/shadow_zone.rs"
    - crates/envi-engine/src/propagation/diffraction.rs (WedgeGeometry lines 33–52, pwedge0 line 355, dwedge0 line 368, the validate reject-before-sqrt pattern line 96 — reuse for the equivalent wedge)
    - crates/envi-engine/src/geometry.rs (reflect_over_segment — the homogeneous reflection-point cross-check)
    - refs/AV1106-07-rev4.pdf pp.26–32 (Eqs. 29–56) and pp.158–159 (Eqs. 384–388) — the executor MUST transcribe every exponent/sign/radicand from the page images (Assumptions A3–A6): Eq. 32 tan αL, Eq. 43 dSZ radicand, Eq. 49 cubic coefficients, Eq. 52 Δτ₀, Eqs. 385/387/388
  </read_first>
  <behavior>
    - `circular_rays(d, h_s, h_r, 5e-7, c0)` returns a `RayPair` that is `assert_eq!`-equal (exact PartialEq, not approx) to `straight_rays(d, h_s, h_r, c0)` for the research geometry (d=97.5, hS=0.5, hR=1.5) — the D-02 anchor.
    - For a downward-refraction ξ>0 geometry, ξ/Δτ match the committed scipy oracle at tolerance.
    - Δτ stays finite and precise at hS=0.01, hR=1.5, d=1000 (cancellation regression) and for a high-source upward-refraction geometry near the shadow edge, where Δτ→0 at the edge (Eq. 52 Δτ₀ cap).
    - The homogeneous reflection point coincides with `reflect_over_segment` (cross-check).
    - `shadow_zone_shielding` returns a finite non-negative per-band attenuation vector (105 bands) using `pwedge0`; ξSZ is frozen above 2000 Hz.
    - Degenerate/non-finite inputs and a cubic with no real root return typed errors (`NoReflectionRoot`), never a panic.
  </behavior>
  <action>
    Implement `refraction::circular_ray` with `direct_ray` (Eqs. 29–44: tan αL Eq. 32, R/τ via the `f(0)/f(z)` log-ratio Eqs. 34–37, past-circle-top branch Eqs. 38–40, Δα Eq. 41, ξ<0 sign flip Eq. 42, dSZ Eq. 43; clamps Δz<0.01→0.01 and the inner |ξ|<1e-10→1e-10 division guard), `reflected_ray` (run `direct_ray` on the two half-paths, Eqs. 45–50), `reflection_point_cubic` (Eq. 49 — a robust Cardano/trigonometric solver returning all real roots then applying the source/receiver root-selection rule; `NoReflectionRoot` on empty), `travel_time_diff` (Eqs. 51–53 carrying the difference through the τ-domain log-ratio for cancellation safety, with the Δτ₀ shadow-edge cap Eq. 52 and Δτ=0 when d>0.95·dSZ), and `height_of_circular_ray` (Eqs. 54–56 geometry). Add `rays::circular_rays(d, h_s, h_r, xi, c0)` reusing the `straight_rays` input-guard block; delegate to `straight_rays` when `xi.abs() < 1e-6` (D-02), otherwise assemble the `RayPair` from `direct_ray`/`reflected_ray`/`travel_time_diff` filling the identical fields. Implement `refraction::shadow_zone::shadow_zone_shielding` as equivalent-wedge diffraction: `ξSZ(f)` frozen above 2000 Hz (Eq. 385), `hSZ` from `height_of_circular_ray` at dSZ, fed as a `WedgeGeometry` into `diffraction::pwedge0`/`dwedge0` (do NOT build a new diffraction primitive). Extend the oracle generator + `oracle_refraction.rs` to pin ξ and Δτ for the downward-refraction grid. Add the D-02 `assert_eq!` anchor test, the Δτ cancellation regression, and the homogeneous-reflection cross-check.
  </action>
  <verify>
    <automated>cargo test -p envi-engine circular_ray shadow_zone rays && cargo test -p envi-harness --test oracle_refraction && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>D-02 bit-for-bit anchor passes with exact PartialEq; ξ/Δτ oracle passes; cancellation regression + shadow-edge Δτ→0 proven; homogeneous reflection matches reflect_over_segment; shadow_zone returns finite per-band attenuation via pwedge0; clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire refraction into terrain_effect dispatch + Sub-model 1 shadow branch; direction property tests + finiteness sweep</name>
  <files>crates/envi-engine/src/propagation/terrain_effect/mod.rs, crates/envi-engine/src/propagation/terrain_effect/submodel1.rs, cases/refraction_downwind.toml, cases/refraction_upwind_shadow.toml</files>
  <read_first>
    - crates/envi-engine/src/propagation/terrain_effect/mod.rs (the `straight_rays` call site line 268, the `terrain_effect` dispatch ~137–165, the `NonFlatTerrainNotImplemented` typed-error branch ~161–163 as the model for gating not-yet-done paths)
    - crates/envi-engine/src/propagation/terrain_effect/submodel1.rs (the `eval` function lines 70–127: the Nord-native +j phase lines 114–117 with NO conj, the two-channel readout lines 119–126, `SubModel1Inputs`/`CoherenceInputs` lines 38–47)
    - 03-RESEARCH.md §"Sub-model 1 shadow-zone branch" (Eqs. 121–122: reflected term collapses, subtract L_SZ) and §"System Architecture Diagram"
    - 03-PATTERNS.md §"terrain_effect/submodel1.rs" and §"terrain_effect/mod.rs" (the swap-to-circular_rays instruction)
    - cases/terrain_flat_sigma200.toml (the TOML case shape to clone for the two refraction cases)
  </read_first>
  <behavior>
    - The refraction dispatch calls `circular_rays` (with ξ from `calc_eq_ssp`) in place of `straight_rays` when refraction is active; a homogeneous case (|ξ|<1e-6) yields a level bit-identical to the Phase-2 path (regression guard).
    - Downwind (ξ>0) refraction gives a level GAIN vs the homogeneous case at the same geometry; upward (ξ<0) refraction in the shadow zone gives a LOSS (direction property tests).
    - The refracted level is finite across all 105 bands for every up/down/shadow geometry in the two new cases (finiteness sweep).
    - The Sub-model 1 shadow branch (Eq. 121) collapses the reflected term and subtracts the `shadow_zone_shielding` L_SZ; F_τ still rides in unchanged via `CoherenceInputs::f_delta_nu` (untouched here, filled in 03-03).
  </behavior>
  <action>
    Extend the `terrain_effect` dispatch to compute `(ξ, c₀)` via `refraction::eqssp::calc_eq_ssp` and swap the `straight_rays(self.d, self.h_s, self.h_r, self.c0)` call at line 268 for `circular_rays(self.d, self.h_s, self.h_r, xi, c0)` when refraction is active; the `|ξ|<1e-6` shortcut keeps homogeneous cases bit-identical. In `submodel1::eval`, add the shadow-zone branch (Eq. 121) triggered when `ξ<0 && d>0.95·dSZ`: collapse the reflected-ray contribution and subtract the per-band `L_SZ` from `refraction::shadow_zone::shadow_zone_shielding`; preserve the Nord-native +j phase (no `.conj()`) and the two-channel `from_channels` readout. Create `cases/refraction_downwind.toml` (positive gradient / downwind) and `cases/refraction_upwind_shadow.toml` (upward refraction into shadow) cloning the terrain case shape. Add direction property tests (downwind→gain, upwind-shadow→loss vs the homogeneous baseline) comparing by BAND INDEX, and a finiteness sweep asserting all 105 bands finite (or a typed error, never NaN) across both cases.
  </action>
  <verify>
    <automated>cargo test -p envi-engine refraction_direction refraction_finiteness && cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>Refracted case runs end-to-end through terrain_effect + two-channel readout; direction property tests pass; finiteness sweep green across 105 bands; full `cargo test` green; clippy + fmt clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| case/oracle file → engine | Operator-controlled TOML case files and committed oracle fixtures cross into the pure-math engine; the only untrusted numeric input surface this phase. |
| refraction math → transfer boundary | The Nord2000-native e^{−jωt} refraction math must never conjugate; the single `.conj()` stays at `transfer::nord_ratio_to_transfer` (out of scope here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Denial of Service | circular_ray / eqssp singular ξ, Δz→0, dSZ radicand, cubic discriminant | mitigate | Δz<0.01→0.01 and |ξ|<1e-10→1e-10 inner clamps; |ξ|<1e-6 homogeneous shortcut; Δτ₀ shadow-edge cap; finiteness sweep across 105 bands × up/down/shadow asserts finite-or-typed-error |
| T-03-01-02 | Tampering | cubic solver with no real root / degenerate profile | mitigate | typed `PropagationError::NoReflectionRoot` / `DegenerateProfile`, never a panic; input-guard block rejects non-finite/non-positive before any sqrt/division |
| T-03-01-03 | Tampering | supply chain — new engine dependency | mitigate | zero new deps; `cargo tree -p envi-engine` stays ndarray + num-complex + thiserror |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan; RESEARCH Package Legitimacy Audit records zero packages added |
| T-03-01-04 | Information Disclosure | copyright leak of AV/FORCE material | mitigate | equations cited by AV report + number only; no PDF text/figures committed; oracle scripts carry formulas + citations, not document text; refs/ stays git-ignored |
| (n/a) | Spoofing/Repudiation/Elevation | — | accept | offline numeric library, no auth/session/network/privilege surface — no high-severity web/authz threat applies |
</threat_model>

<verification>
- `cargo test` (workspace, incl. the libtest-mimic FORCE runner) green.
- `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` clean.
- `cargo tree -p envi-engine` unchanged (ndarray + num-complex + thiserror only).
- `.conj()` grep gate over `crates/envi-engine/src/propagation/` returns 0.
- D-02 homogeneous anchor: `assert_eq!(straight_rays(...), circular_rays(..., 5e-7, ...))` exact.
- CalcEqSSP + ξ/Δτ oracle fixtures pass at Phase-2-style tolerance.
- Finiteness sweep across all 105 bands × up/down/shadow finite or typed-error.
</verification>

<success_criteria>
Refraction FORCE-shaped downwind/upward cases run end-to-end and match the committed oracle; the homogeneous limit reproduces Phase 2 bit-for-bit; no singularity blow-up; Δτ cancellation-safe in f64; shadow-zone attenuation produced via the equivalent-wedge kernel. Maps success criteria 1 (partial: homogeneous anchor + no blow-up + Δτ), 2 (c(z)+z₀ clamp), 3 (partial: CalcEqSSP collapse).
</success_criteria>

<output>
Create `.planning/phases/03-meteorology-refraction/03-01-SUMMARY.md` when done.
</output>
