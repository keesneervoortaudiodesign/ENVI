---
phase: 03-meteorology-refraction
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - crates/envi-engine/src/propagation/coherence.rs
  - crates/envi-engine/src/propagation/terrain_effect/submodel1.rs
  - crates/envi-engine/src/propagation/terrain_effect/mod.rs
  - crates/envi-harness/src/weather/mod.rs
  - crates/envi-harness/src/weather/route1.rs
  - crates/envi-harness/src/weather/route3.rs
  - crates/envi-harness/src/capability.rs
  - crates/envi-harness/src/cases/mod.rs
  - tools/nord2000_oracle/gen_refraction_fixtures.py
  - crates/envi-harness/tests/oracle_refraction.rs
autonomous: false
requirements: [MET-05, MET-06, ENG-08]

must_haves:
  truths:
    - "F_τ (fluctuating-refraction coherence, Eq. 112) is computed as sinc with x=2π·f·(Δτ⁺−Δτ), Δτ⁺ under A⁺=A+1.7sA/B⁺=B+1.7sB, and fed through CoherenceInputs::f_delta_nu with no call-site change (ENG-08, success criterion 5)"
    - "F_τ=1 bit-exact when sA=sB=0 (Δτ⁺=Δτ ⇒ F→1 ⇒ P_incoh→0), and F_τ<1 / monotonic under turbulence, changing results in the expected direction (D-11/D-12)"
    - "Route 1 loads a weather-class (A,B)+probability table and produces an energy-weighted L_den = 10·lg(Σ p·10^(L/10)) combination (MET-05, success criterion 4)"
    - "Route 3 reconstructs u(z),T(z) from surface met via Monin–Obukhov and least-squares fits (A,B,C) with a hand-rolled 3×3 normal-equations solve (MET-06, success criterion 4)"
    - "Capability::Refraction is implemented; the wind/gradient FORCE cases' Skipped(requires:…) set drops `refraction` but retains `emission-model` (stays Skipped, never a false Pass) (D-03)"
  artifacts:
    - path: "crates/envi-engine/src/propagation/coherence.rs"
      provides: "coherence_f_delta_nu(f_hz, dtau, dtau_plus) sinc (Eq. 112, x=2π·f·(Δτ⁺−Δτ))"
      contains: "fn coherence_f_delta_nu"
    - path: "crates/envi-harness/src/weather/route1.rs"
      provides: "weather-class table + energy-weighted L_den (TestYearlyAverage.xls Met. statistics)"
      contains: "fn route1"
    - path: "crates/envi-harness/src/weather/route3.rs"
      provides: "Monin–Obukhov reconstruction + hand-rolled 3×3 LSQ A/B/C fit"
      contains: "fn fit_profile"
    - path: "crates/envi-harness/src/capability.rs"
      provides: "Capability::Refraction in implemented_capabilities() + requires-shrink assertion"
      contains: "Refraction"
  key_links:
    - from: "crates/envi-engine/src/propagation/coherence.rs::coherence_f_delta_nu"
      to: "crates/envi-engine/src/propagation/coherence.rs::coherence_f"
      via: "result passed as CoherenceInputs::f_delta_nu; coherence_f already multiplies it into the F=Ff·FΔν·Fc·Fr·Fs chain (no call-site change)"
      pattern: "f_delta_nu"
    - from: "crates/envi-engine/src/propagation/terrain_effect/submodel1.rs"
      to: "crates/envi-engine/src/propagation/refraction/circular_ray.rs"
      via: "Δτ⁺ computed by running the ray chain on the A⁺/B⁺ profile, then fed to coherence_f_delta_nu"
      pattern: "coherence_f_delta_nu"
    - from: "crates/envi-harness/src/weather/route1.rs"
      to: "crates/envi-harness/src/cases/xls.rs"
      via: "reads TestYearlyAverage.xls Met. statistics sheet via the calamine label-anchored loader"
      pattern: "Met. statistics"
  prohibitions:
    - "No `.conj()` in propagation/ (grep gate = 0); coherence_f_delta_nu is real-valued; F_τ multiplies H_coh but NEVER overwrites its phase (D-12)"
    - "Do NOT copy Ff's 0.23π constant into F_τ — the argument is 2π·f·(Δτ⁺−Δτ), no 0.23 (Pitfall 5)"
    - "Do NOT pin F_τ to a fixed-value scipy oracle — property tests only (turbulence constants are AV Assumptions, D-11)"
    - "No `nalgebra`/`ndarray-linalg` for Route 3 — hand-rolled 3×3 normal equations (D-08); a checkpoint guards any linalg-crate proposal"
    - "No new envi-engine dependency (cargo tree gate); all route I/O in envi-harness (D-15)"
    - "Do NOT flip a wind/gradient FORCE case to a numeric Pass — it must stay Skipped on `emission-model` until Phase 4 (D-03); never invent a route A/B/C constant as verified"
---

<objective>
Wire the weather-input routes and the turbulence coherence factor so a full refraction case runs with meteorology end-to-end, flip `Capability::Refraction` to implemented, and land the acceptance ladder green — the slice that closes the phase.

## Phase Goal (MVP user story)

**As an** acoustics-engine validation harness, **I want to** turn weather data (class tables via Route 1, Monin–Obukhov surface met via Route 3) into the (A,B,C) the engine consumes and apply the fluctuating-refraction coherence F_τ, then flip the refraction capability, **so that** a wind/gradient FORCE case runs through the whole refraction chain with turbulence coherence and its Skipped-reason shrinks to `emission-model` only — honestly green with no false numeric Pass.

Purpose: ENG-08 activates the pre-built FΔν seam through `CoherenceInputs::f_delta_nu` with no call-site change (D-10); MET-05/06 build the two remaining weather routes — Route 1 weather classes → energy-weighted L_den (D-07) and Route 3 Monin–Obukhov + LSQ (all three routes are built now, no deferral, D-05); the capability flip + requires-shrink is the phase's closing gate. The weather-route A/B/C derivation constants are [ASSUMED] (AV does not specify them, origin research doc absent) — Task 1 is a blocking human checkpoint that quarantines them honestly.
Output: `coherence_f_delta_nu` + Δτ⁺ wiring, `weather::route1` (energy-weighted L_den) + `weather::route3` (MO + LSQ fit), the `Capability::Refraction` flip with the requires-shrink assertion, Route-3 synthetic oracle fixtures, and the full acceptance ladder green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-meteorology-refraction/03-CONTEXT.md
@.planning/phases/03-meteorology-refraction/03-RESEARCH.md
@.planning/phases/03-meteorology-refraction/03-PATTERNS.md
@.planning/phases/03-meteorology-refraction/03-01-SUMMARY.md
@.planning/phases/03-meteorology-refraction/03-02-SUMMARY.md
@refs/AV1106-07-rev4.pdf
@refs/TestYearlyAverage.xls
</context>

<artifacts_this_phase_produces>
New symbols introduced by this plan:
- `coherence::coherence_f_delta_nu(f_hz, dtau, dtau_plus) -> f64` (Eq. 112 sinc; x=2π·f·|Δτ⁺−Δτ|; 1.0 when x≤1e-15)
- Δτ⁺ computation in the refraction/submodel1 path (ray chain on the A⁺/B⁺ profile) feeding `CoherenceInputs::f_delta_nu`
- `weather::route1::route1(...)` (class table + probabilities → energy-weighted L_den) reading `TestYearlyAverage.xls` `Met. statistics`
- `weather::route3::fit_profile(heights, c_eff_samples, z0) -> (A,B,C)` (hand-rolled 3×3 normal equations) + a MO reconstruction `reconstruct_profiles(surface_met) -> (heights, c_eff)`
- `Capability::Refraction` added to `implemented_capabilities()`; the FORCE requires-shrink assertion test
- nonzero-turbulence default finalized on `PropagationParams`; Route-3 synthetic oracle fixtures in `gen_refraction_fixtures.py` + `oracle_refraction.rs`
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1 (checkpoint): Lock the source of truth for the [ASSUMED] weather-route A/B/C constants (Open Q1)</name>
  <action>Pause before Tasks 2–3 lock any weather-route derivation constant. Present the [ASSUMED]-constant situation to the developer and capture their decision on the source of truth (companion reference, `.xls` authoritative, or proceed-quarantined) plus confirmation of the honest-green D-03 contract, then continue with the chosen validation posture.</action>
  <what-built>Nothing yet — this is the gate that precedes locking the [ASSUMED] weather-route A/B/C derivation constants (RESEARCH Open Q1). AV 1106/07 does NOT specify the Route-1/2/3 conversions and the origin `docs/research.md` is absent from the repo. The route MACHINERY (energy-weighting, MO reconstruction, LSQ fit, per-azimuth projection) is built regardless; only the numeric derivation constants are quarantined behind this checkpoint.</what-built>
  <how-to-verify>
    1. Decide the source of truth for the weather-route constants. Options:
       (a) Supply the Nord2000 companion reference (AV 1851/00 Part 2, or the CNOSSOS-EU Annex meteo-class tables) so the Route-2/3 A/B/C scaling constants and the Route-1 class→(A,B)+probability table can be locked from an authoritative document.
       (b) Confirm that the `Met. statistics` sheet in `refs/TestYearlyAverage.xls` is authoritative for the Route-1 class table (values read straight from the .xls, cross-checked against reference "[4]").
       (c) Approve proceeding with the `[ASSUMED]` constants clearly quarantined — validated by structural + direction property tests and a same-transcription committed oracle only (the oracle cross-checks IMPLEMENTATION, not spec reading; caveat D-04), with NO false FORCE numeric Pass.
    2. Confirm the wind/gradient FORCE cases stay `Skipped(requires: emission-model)` (never a numeric Pass) until the Phase-4 road-emission model — the honest-green contract (D-03).
  </how-to-verify>
  <resume-signal>Reply with (a) + the companion reference path, (b) "Met. statistics authoritative", or (c) "proceed with [ASSUMED], property/oracle only". Then execution continues with the chosen validation posture.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F_τ fluctuating-refraction coherence (Eq. 112) via the f_delta_nu seam + Δτ⁺ wiring</name>
  <files>crates/envi-engine/src/propagation/coherence.rs, crates/envi-engine/src/propagation/terrain_effect/submodel1.rs, crates/envi-engine/src/propagation/terrain_effect/mod.rs, crates/envi-harness/src/cases/mod.rs</files>
  <read_first>
    - crates/envi-engine/src/propagation/coherence.rs (the `coherence_ff` sinc template lines 63–73 to mirror; `CoherenceInputs::f_delta_nu` line 53 — currently 1.0; the chain multiply `ff * inputs.f_delta_nu * fc * fr * fs` line 118; the property-test pattern `turbulence_reduces_f_monotonically` lines 169–193)
    - crates/envi-engine/src/propagation/terrain_effect/submodel1.rs (the `eval` two-channel readout lines 114–126 — F_τ multiplies H_coh, never overwrites the +j phase; `SubModel1Inputs`/`CoherenceInputs` carry the weather params so this attaches without an API break)
    - crates/envi-engine/src/propagation/refraction/circular_ray.rs (the ray chain — Δτ⁺ is computed by running it on the A⁺/B⁺ profile)
    - 03-RESEARCH.md §"Fluctuating-refraction coherence FΔν" (Eq. 112: x=2π·f·(Δτ⁺−Δτ), A⁺=A+1.7sA/B⁺=B+1.7sB Eq. 10; F→1⇒P_incoh→0 when sA=sB=0; the ready-to-paste helper) and §"Pitfall 5" (2π NOT 0.23π)
    - 03-PATTERNS.md §"coherence.rs (EXTEND)"
    - refs/AV1106-07-rev4.pdf p.52 (Eq. 112) + Eq. 10 p.20 — confirm the 2π and 1.7 constants on the page image
  </read_first>
  <behavior>
    - `coherence_f_delta_nu(f, dtau, dtau)` returns exactly 1.0 (Δτ⁺=Δτ ⇒ x=0); this drives `P_incoh→0` bit-exact on non-fluctuating cases.
    - For dtau_plus≠dtau it returns sin(x)/x for 0<x≤π and 0 for x>π, with x=2π·f·|dtau_plus−dtau| (NOT 0.23π).
    - Under turbulence (nonzero sA/sB), F_τ<1 and monotonically decreases with increasing f·(Δτ⁺−Δτ); the readout level changes in the expected direction vs the non-fluctuating baseline (property tests, no fixed-value oracle).
    - The homogeneous (sA=sB=0) case stays bit-identical to 03-01 (regression guard).
  </behavior>
  <action>
    Add `coherence::coherence_f_delta_nu(f_hz, dtau, dtau_plus) -> f64`: `x = 2π·f·|dtau_plus − dtau|`; return 1.0 when x≤1e-15, sin(x)/x when x≤π, else 0.0 — mirror `coherence_ff` exactly but with the 2π argument (NO 0.23 factor). In the refraction path (submodel1/terrain_effect dispatch), compute Δτ under the (A,B) profile and Δτ⁺ under the upper-refraction profile A⁺=A+1.7·sA, B⁺=B+1.7·sB (Eq. 10) by running `circular_rays` on the A⁺/B⁺ ξ⁺, evaluate `coherence_f_delta_nu`, and pass the result as `CoherenceInputs::f_delta_nu` — NO call-site change to `coherence_f`. When sA=sB=0, A⁺=A/B⁺=B ⇒ Δτ⁺=Δτ ⇒ f_delta_nu=1.0 (bit-exact). Finalize the nonzero-turbulence Nord2000 default on `PropagationParams` (Cv²/CT² from FORCE fields, documented default when omitted). Add property tests mirroring `turbulence_reduces_f_monotonically`: F_τ≤1, non-increasing, strict `<` at high f·(Δτ⁺−Δτ), and the sA=sB=0 → 1.0 bit-exact assert. Do NOT pin F_τ to a fixed oracle value.
  </action>
  <verify>
    <automated>cargo test -p envi-engine coherence_f_delta_nu coherence && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>coherence_f_delta_nu implemented with the 2π argument; F_τ=1 bit-exact when sA=sB=0; F_τ<1/monotonic under turbulence; Δτ⁺ wired through the f_delta_nu seam with no call-site change; property tests pass; clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Weather Route 1 (class table → L_den) + Route 3 (Monin–Obukhov + 3×3 LSQ) + Capability::Refraction flip; acceptance ladder green</name>
  <files>crates/envi-harness/src/weather/mod.rs, crates/envi-harness/src/weather/route1.rs, crates/envi-harness/src/weather/route3.rs, crates/envi-harness/src/capability.rs, tools/nord2000_oracle/gen_refraction_fixtures.py, crates/envi-harness/tests/oracle_refraction.rs</files>
  <read_first>
    - crates/envi-harness/src/cases/xls.rs (the calamine label-anchored read, row/sheet caps, NonFinite/MissingLabel errors — Route 1 clones this to read `TestYearlyAverage.xls` `Met. statistics`)
    - crates/envi-harness/src/capability.rs (the `Capability::Refraction` at line 30, `required_capabilities` flagging it off nonzero u/dt-dz lines 85–92, `implemented_capabilities` lines 108–115, the requires-shrink test template `force_skip_reason_no_longer_mentions_ground_effect_or_diffraction` lines 209–234, and the `assert!(!…Refraction)` at line 205 to flip to present)
    - crates/envi-harness/src/weather/mod.rs (`WeatherProfile` from 03-02 — the per-class (A,B) each route produces)
    - 03-RESEARCH.md §"Route 1" (energy-weighted L_den=10·lg(Σ p·10^(L/10)); harness-only; [ASSUMED] table values), §"Route 3" (MO u(z)/T(z) reconstruction, cloud→L stability proxy, c_eff(z)=20.05·√(T+273.15)+u·cos(az−φ), LSQ linear in A,B,C ⇒ 3×3 normal equations, the `fit_profile(heights, c_eff, z0)` clean v2 boundary), §"Don't Hand-Roll" (hand-rolled 3×3, no nalgebra)
    - 03-PATTERNS.md §"weather/… route1/route3", §"capability.rs (EXTEND)", §"No Analog Found" (route3 LSQ), and §"tools/… gen_refraction_fixtures.py"
    - refs/TestYearlyAverage.xls `Met. statistics` sheet (the Route-1 class table + probabilities — authoritative per the Task-1 checkpoint outcome)
  </read_first>
  <behavior>
    - Route 1 loads the class (A,B)+probability table from `Met. statistics`, runs the engine per class, and combines receiver levels energy-weighted: L_den=10·lg(Σ p·10^(L/10)); probabilities are normalized and rejected if non-finite or not summing to ~1.
    - Route 3 `fit_profile(heights, c_eff, z0)` recovers (A,B,C) exactly (to tolerance) for a synthetic profile generated FROM a known (A,B,C) — the round-trip identity; the MO reconstruction produces a monotonic u(z) log profile.
    - After the flip, `implemented_capabilities()` contains `Refraction`; a wind/gradient FORCE case's `missing` set equals `{EmissionModel}` — `refraction` dropped, `emission-model` retained (stays Skipped, never Pass).
    - Full `cargo test` (engine + harness + libtest-mimic FORCE runner) is green; `cargo tree -p envi-engine` unchanged; `.conj()` grep gate = 0.
  </behavior>
  <action>
    Implement `weather::route1::route1(...)`: read the `Met. statistics` sheet of `TestYearlyAverage.xls` via a calamine label-anchored loader cloned from `cases/xls.rs` (reuse the row/sheet caps + NonFinite/MissingLabel typed errors), build the per-class (A,B)+probability table, run the engine per class, and combine energy-weighted `L_den = 10·lg(Σ p·10^(L/10))` (per period, then day/eve/night penalties); normalize-and-check the probabilities. Implement `weather::route3`: `reconstruct_profiles(surface_met)` producing u(z)/T(z) via Monin–Obukhov similarity (cloud cover as the stability proxy → Obukhov length; Ψm/Ψh stability functions, all `[ASSUMED]` per the Task-1 checkpoint) and `c_eff(z)=20.05·√(T(z)+273.15)+u(z)·cos(az−φ)`, then `fit_profile(heights, c_eff_samples, z0) -> (A,B,C)` via a hand-rolled 3×3 normal-equations solve (XᵀXβ=Xᵀy, closed-form 3×3; NO nalgebra) — shaped so v2 METX supplies the samples without a rewrite. Flip `Capability::Refraction` into `implemented_capabilities()` and update the requires-shrink assertion (clone `force_skip_reason_no_longer_mentions_ground_effect_or_diffraction`) so a wind/gradient case's `missing` drops `refraction` but keeps `emission-model`; flip the `assert!(!…Refraction)` at line 205 to assert present. Add Route-3 synthetic oracle fixtures (round-trip A/B/C) to `gen_refraction_fixtures.py` + `oracle_refraction.rs`; Route 1 validated by unit test on the energy-weighting. Honor the Task-1 checkpoint outcome for the constant-source posture.
  </action>
  <verify>
    <automated>cargo test -p envi-harness weather_route1 weather_route3 capability && cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>Route 1 energy-weighted L_den + Route 3 MO+3×3 LSQ round-trip pass; Capability::Refraction implemented with the requires-shrink assertion (drops refraction, keeps emission-model); full `cargo test` green; cargo tree unchanged; conj gate 0; clippy + fmt clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TestYearlyAverage.xls → Route 1 | Operator-controlled .xls (git-ignored, SHA-pinned) crosses into the harness; malformed sheets/probabilities must fail typed, never panic. |
| surface met → Route 3 | Synthetic/derived surface met (no live API — v2 METX) crosses into the MO reconstruction + LSQ; non-finite inputs must fail typed. |
| weather (A,B,C) → engine | Real-valued profile coefficients cross harness→engine; treated as untrusted numeric input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering/DoS | Route 1 malformed `.xls`/probabilities (non-finite, not summing to 1) | mitigate | reuse the Phase-1 `confine` path-guard + typed `CaseLoadError`; row/sheet caps; probabilities normalized-and-checked, non-finite rejected |
| T-03-03-02 | Denial of Service | Route 3 LSQ singular normal matrix / NaN from bad met | mitigate | 3×3 discriminant/determinant guard → typed error; reject non-finite surface met before reconstruction; z0 clamp ≥0.001 m |
| T-03-03-03 | Spoofing (correctness) | [ASSUMED] route constants / a false FORCE numeric Pass | mitigate | blocking human checkpoint (Task 1) quarantines the constants; property/oracle-only validation; wind/gradient FORCE cases stay Skipped on `emission-model` (D-03) — asserted |
| T-03-03-04 | Tampering | supply chain — a linalg crate for Route 3 | mitigate | hand-rolled 3×3, no `nalgebra`/`ndarray-linalg`; `cargo tree -p envi-engine` unchanged; a checkpoint guards any linalg-crate proposal |
| T-03-03-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (RESEARCH Package Legitimacy Audit: zero added) |
| T-03-03-05 | Information Disclosure | copyright leak of the class-table / AV material | mitigate | class-table values are method-defined facts read from the git-ignored `.xls`; equations cited by report + number; no PDF text/figures committed |
| (n/a) | Repudiation/Elevation | — | accept | offline numeric library; no auth/session/privilege surface |
</threat_model>

<verification>
- `cargo test` (workspace, incl. the libtest-mimic FORCE runner) green.
- `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` clean.
- `cargo tree -p envi-engine` unchanged; no linalg crate added.
- `.conj()` grep gate over `propagation/` = 0.
- F_τ=1 bit-exact when sA=sB=0; F_τ<1/monotonic under turbulence (property, no fixed oracle).
- Route 3 fit_profile round-trip recovers (A,B,C) to tolerance; Route 1 energy-weighting unit test passes.
- `Capability::Refraction` implemented; wind/gradient FORCE `missing` = {EmissionModel} (refraction dropped, emission-model retained).
</verification>

<success_criteria>
F_τ blends coherent/partially-coherent contributions changing results in the expected direction on turbulent cases (F_τ=1 bit-exact off-turbulence); Route 1 produces energy-weighted L_den from a class table; Route 3 reconstructs u(z)/T(z) via Monin–Obukhov and LSQ-fits (A,B,C); the refraction capability is implemented and the FORCE requires-list shrinks honestly. Maps success criteria 4 (Route 1 L_den, Route 3 MO+LSQ), 5 (F_τ direction), and closes 1 (refraction FORCE cases run capability-gated, no false Pass).
</success_criteria>

<output>
Create `.planning/phases/03-meteorology-refraction/03-03-SUMMARY.md` when done.
</output>
