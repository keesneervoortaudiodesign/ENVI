---
phase: 03-meteorology-refraction
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - crates/envi-engine/src/propagation/refraction/eqssp.rs
  - crates/envi-harness/src/weather/mod.rs
  - crates/envi-harness/src/weather/route2.rs
  - crates/envi-harness/src/lib.rs
  - crates/envi-harness/src/cases/mod.rs
  - crates/envi-engine/src/geometry.rs
  - tools/nord2000_oracle/gen_refraction_fixtures.py
  - crates/envi-harness/tests/oracle_refraction.rs
autonomous: true
requirements: [MET-04, ENG-06, MET-02]

must_haves:
  truths:
    - "CalcEqSSPGround produces a frequency-dependent (ξ(f),c₀(f)) by log-interpolating the gradient between fL and fH, evaluated natively at all 105 1/12-octave points, compared by BAND INDEX (MET-04, success criterion 3)"
    - "Soft ground (σ<1e7 Pa·s·m⁻²) uses the frequency-dependent variant; hard ground uses plain frequency-independent CalcEqSSP (D-14, Pitfall 4)"
    - "A is derived per source→receiver azimuth: the isotropic temperature part is computed once and the projected wind part A ∝ u·cos(az−φ_u) is added per bearing; an inversion (dt/dz>0) yields B>0 (MET-02, success criterion 2)"
    - "Reflection paths compute with separate before/after profile coefficients A₁/B₁ (source→reflector) and A₂/B₂ (reflector→receiver), each via CalcEqSSP on its own sub-path (ENG-06, success criterion 4)"
    - "In the homogeneous limit CalcEqSSPGround collapses to CalcEqSSP and the before/after split reproduces the single-profile result (regression guard)"
  artifacts:
    - path: "crates/envi-engine/src/propagation/refraction/eqssp.rs"
      provides: "calc_eq_ssp_ground(...) frequency-dependent variant Eqs. 22–28"
      contains: "fn calc_eq_ssp_ground"
    - path: "crates/envi-harness/src/weather/mod.rs"
      provides: "WeatherProfile { a, b, c, s_a, s_b, z0 } struct + per-azimuth A projection"
      contains: "struct WeatherProfile"
    - path: "crates/envi-harness/src/weather/route2.rs"
      provides: "Route 2 (D-06) FORCE surface-met (u/φ/dt-dz) → (A,B,C) [ASSUMED constants quarantined]"
      contains: "fn route2"
  key_links:
    - from: "crates/envi-engine/src/propagation/refraction/eqssp.rs::calc_eq_ssp_ground"
      to: "crates/envi-engine/src/propagation/terrain_effect/submodel2.rs::phase_diff_freq"
      via: "fL/fH from the existing PhaseDiffFreq aux (Eqs. 24–27), only the target phase differs"
      pattern: "phase_diff_freq"
    - from: "crates/envi-harness/src/weather/mod.rs"
      to: "crates/envi-engine/src/geometry.rs::azimuth_deg"
      via: "per-bearing wind projection A ∝ u·cos(az−φ_u), clockwise-from-north"
      pattern: "azimuth_deg"
    - from: "crates/envi-harness/src/weather/route2.rs"
      to: "crates/envi-harness/src/cases/mod.rs::PropagationParams"
      via: "reads parsed FORCE met fields t0/u/phi/su/dtdz/sdtdz/cv2/ct2/roughness"
      pattern: "PropagationParams"
  prohibitions:
    - "No `.conj()` in propagation/ (grep gate = 0); CalcEqSSPGround is real-valued gradient math"
    - "No new envi-engine dependency (cargo tree gate); all weather-route I/O lives in envi-harness (D-15)"
    - "No `nalgebra`/`ndarray-linalg` added (reflection split needs no linear algebra)"
    - "No comparison by nominal frequency — CalcEqSSPGround evaluated by BAND INDEX at the 105 points (D-14)"
    - "Do NOT present the Route-2 A/B scaling constants as verified — they are [ASSUMED] (AV does not specify them); validate by direction/structure property tests only, not a false FORCE numeric pass"
    - "Do NOT run the fL/fH machinery for hard ground (σ≥1e7) — branch to plain CalcEqSSP"
---

<objective>
Extend the refraction core with the frequency-dependent soft-ground variant, the reflection-path before/after coefficient split, and the per-azimuth weather-coefficient derivation — the slice that makes soft ground and directional wind work on the 1/12-octave grid.

## Phase Goal (MVP user story)

**As an** acoustics-engine validation harness, **I want to** feed frequency-dependent soft-ground refraction, directional (per-bearing) wind coefficients, and split reflection-path profiles into the ray machinery, **so that** a soft-ground refracted case yields a correct band-indexed ξ(f) and a wind case gets the right A per azimuth — each collapsing to the 03-01 single-profile result in the homogeneous/hard limit.

Purpose: MET-04 makes ξ frequency-dependent over soft ground (the FORCE targets are grassland); MET-02 turns wind/temperature into the (A,B) the engine consumes per bearing; ENG-06 builds the reflector before/after split so Phase-4 obstacle-reflection cases plug in. All three are strict generalizations that reduce to 03-01 in their trivial limits.
Output: `calc_eq_ssp_ground` in `eqssp.rs`, the `envi-harness::weather` module (profile struct + per-azimuth projection + Route 2), the reflection-path split interface, and extended oracle fixtures (CalcEqSSPGround by band index).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-meteorology-refraction/03-CONTEXT.md
@.planning/phases/03-meteorology-refraction/03-RESEARCH.md
@.planning/phases/03-meteorology-refraction/03-PATTERNS.md
@.planning/phases/03-meteorology-refraction/03-01-SUMMARY.md
@refs/AV1106-07-rev4.pdf
</context>

<artifacts_this_phase_produces>
New symbols introduced by this plan:
- `refraction::eqssp::calc_eq_ssp_ground(f_hz, d, h_s, h_r, z_g_impedance, z0, a, b, c) -> Result<(f64,f64), PropagationError>` returning frequency-dependent `(ξ(f), c₀(f))` (Eqs. 22–28); soft-ground branch on σ<1e7, hard ground delegates to `calc_eq_ssp`
- `envi-harness::weather` module (`mod.rs`, `route2.rs`) with `WeatherProfile { a, b, c, s_a, s_b, z0 }` and a per-azimuth A projection function (`project_wind`/`profile_for_bearing`)
- `weather::route2::route2(params, bearing_deg) -> WeatherProfile` (FORCE u/φ/dt-dz → A/B, [ASSUMED] constants)
- reflection-path before/after profile pair (a `ReflectionProfiles { before: WeatherProfile, after: WeatherProfile }`-style interface producing two `calc_eq_ssp` results — A₁/B₁, A₂/B₂)
- CalcEqSSPGround oracle rows added to `gen_refraction_fixtures.py` + `oracle_refraction.rs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CalcEqSSPGround frequency-dependent soft-ground variant (fL/fH log-interpolation at the 105 points)</name>
  <files>crates/envi-engine/src/propagation/refraction/eqssp.rs, tools/nord2000_oracle/gen_refraction_fixtures.py, crates/envi-harness/tests/oracle_refraction.rs</files>
  <read_first>
    - crates/envi-engine/src/propagation/refraction/eqssp.rs (the `calc_eq_ssp` built in 03-01 — the frequency-independent base this reuses above fH and below fL)
    - crates/envi-engine/src/propagation/terrain_effect/submodel2.rs (the `phase_diff_freq` aux at line 134, Eqs. 378–381 — the exact PhaseDiffFreq CalcEqSSPGround reuses at phases Ψ and 2Ψ; reuse verbatim, only the target phase differs)
    - crates/envi-engine/src/propagation/ground.rs (`ground_impedance` for Ẑ_G(f), the soft/hard σ context)
    - 03-RESEARCH.md §"CalcEqSSPGround" (Eqs. 22–28: Eq. 23 log-interpolated gradient, Eqs. 24–27 fL/fH via PhaseDiffFreq with d≤400 m and hS,hR≥0.5 m clamps, Eq. 27 fH=max(f2Ψ,1.25·fL), soft-ground trigger σ<1e7) and §"Pitfall 4"
    - 03-PATTERNS.md §"refraction/eqssp.rs" (Analog B)
    - refs/AV1106-07-rev4.pdf pp.24–26 (Eqs. 22–28) — confirm the Eq. 26 fL piecewise constants (~343, 40.51) on the p.26 page image (Assumption A7)
  </read_first>
  <behavior>
    - Above fH `calc_eq_ssp_ground` equals the frequency-independent `calc_eq_ssp` gradient; below fL ξ(f)=0; between fL and fH the gradient is log-interpolated (Eq. 23) so ξ(f) varies monotonically with band index.
    - Hard ground (σ≥1e7) returns the same value at every band (frequency-independent) — asserts ξ(f) is flat over asphalt.
    - CalcEqSSPGround matches the committed scipy oracle for ξ(f) and c₀(f) at representative band indices at tolerance, compared by BAND INDEX.
    - The homogeneous limit (|ξ|<1e-6) collapses to (0, C) at every band.
  </behavior>
  <action>
    Add `refraction::eqssp::calc_eq_ssp_ground(f_hz, d, h_s, h_r, z_g, z0, a, b, c) -> Result<(f64,f64), PropagationError>`. Branch on the ground: for hard ground (σ≥1e7 Pa·s·m⁻²) delegate to `calc_eq_ssp` (frequency-independent). For soft ground compute fL (Eq. 26, piecewise in Δc₁₀=c(10)−c(0)) and fH=max(f2Ψ,1.25·fL) (Eq. 27) using the existing `submodel2::phase_diff_freq` at phases Ψ and 2Ψ with d clamped ≤400 m and hS,hR clamped ≥0.5 m for Eqs. 24/25; above fH use the plain gradient, below fL set ∂c/∂z=0, between fL and fH log-interpolate the gradient (Eq. 23) then feed it through Eqs. 15–20 to get (ξ(f),c₀(f)). Evaluate natively at the 105 1/12-octave grid points; return the homogeneous (0,C) below the |ξ|<1e-6 clamp. Extend `gen_refraction_fixtures.py` + `oracle_refraction.rs` to pin ξ(f)/c₀(f) at representative band indices for a soft-ground grid, comparing by band index.
  </action>
  <verify>
    <automated>cargo test -p envi-engine calc_eq_ssp_ground && cargo test -p envi-harness --test oracle_refraction && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>CalcEqSSPGround soft/hard branch works; ξ(f) varies by band index between fL/fH and is flat over hard ground; oracle passes by band index; clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: envi-harness weather module — WeatherProfile + per-azimuth A projection + Route 2 (FORCE u/dt-dz → A/B)</name>
  <files>crates/envi-harness/src/weather/mod.rs, crates/envi-harness/src/weather/route2.rs, crates/envi-harness/src/lib.rs, crates/envi-harness/src/cases/mod.rs, crates/envi-engine/src/geometry.rs</files>
  <read_first>
    - crates/envi-harness/src/cases/mod.rs (the `CaseLoadError` typed-error template lines 41–120, and `PropagationParams` line 196: t0_c, u_ms, phi_deg, su_ms, dtdz, sdtdz, cv2, ct2, roughness — the input Route 2 reads)
    - crates/envi-engine/src/geometry.rs (`azimuth_deg` clockwise-from-north — the documented `A ∝ u·cos(az−φ_u)` wind-projection consumer)
    - crates/envi-harness/src/lib.rs (module declaration site for the new `weather` module)
    - 03-RESEARCH.md §"Route 2 — FORCE surface-met → A/B" ([ASSUMED] — c_eff(z)=c(T(z))+u(z)·cos(az−φ); A_wind ∝ (u*/κ)·cos(az−φ); inversion dt/dz>0 ⇒ B>0; the SIGN logic is physically certain, the scaling constants are [ASSUMED]) and the Weather-Routes warning banner
    - 03-PATTERNS.md §"weather/mod.rs + route2.rs" and §"Injected seam"
  </read_first>
  <behavior>
    - `WeatherProfile` carries (a, b, c, s_a, s_b, z0); z0 is clamped ≥ 0.001 m on construction.
    - The isotropic temperature part of A is computed once; the projected wind part is `u·cos(az−φ_u)` per bearing — the same met at two bearings 180° apart gives A values that differ in the wind sign (downwind A larger than upwind).
    - An inversion (dt/dz>0) produces B>0; a lapse (dt/dz<0) produces B<0 (direction property tests).
    - Route 2 returns a finite `WeatherProfile`; malformed/non-finite met fields return a typed `CaseLoadError`, never a panic.
  </behavior>
  <action>
    Create the `envi-harness::weather` module (declare it in `lib.rs`). Define `WeatherProfile { a, b, c, s_a, s_b, z0 }` with a constructor clamping z0≥0.001 m, and a per-azimuth projection `profile_for_bearing(base, bearing_deg, phi_u_deg)` that computes the isotropic temperature part of A once and adds the projected wind part `∝ u·cos(bearing−φ_u)` using the `geometry::azimuth_deg` convention (add/document a small helper in `geometry.rs` only if a shared projection primitive is warranted; keep the projection itself in the harness). Implement `weather::route2::route2(params, bearing_deg) -> Result<WeatherProfile, CaseLoadError>` mapping FORCE surface met (u@zu, φ, dt/dz, t0, z0) to (A,B,C): A from the logarithmic wind/temperature term (wind projected per bearing), B from the temperature gradient with the sign contract inversion→B>0, C=Coft(t0). Mark the A/B scaling constants explicitly as `[ASSUMED]` in doc-comments (AV does not specify them — locked pending the Open-Q1 checkpoint in 03-03) and validate by direction/structure property tests only. Add a nonzero-turbulence Nord2000 default on `PropagationParams` where a case omits Cv²/CT² (documented in the loader). Reuse the `CaseLoadError` typed-error posture.
  </action>
  <verify>
    <automated>cargo test -p envi-harness weather_route2 weather_profile && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>WeatherProfile + per-azimuth projection built; downwind A>upwind A and inversion→B>0 proven by property tests; Route 2 returns typed errors on bad input; [ASSUMED] constants documented; clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reflection-path before/after profile split (A₁/B₁, A₂/B₂ via CalcEqSSP per sub-path)</name>
  <files>crates/envi-harness/src/weather/mod.rs, crates/envi-harness/src/weather/route2.rs</files>
  <read_first>
    - crates/envi-engine/src/propagation/refraction/eqssp.rs (`calc_eq_ssp` — applied per sub-path)
    - crates/envi-engine/src/geometry.rs (`reflect_over_segment` — the homogeneous reflection-point cross-check; `azimuth_deg` for the two sub-path bearings)
    - 03-RESEARCH.md §"Separate before/after profiles" (§5.3.4, Eq. 1: A₁,B₁ before / A₂,B₂ after the reflection point; each sub-path gets its own CalcEqSSP; per-azimuth A differs per sub-path bearing; B bearing-independent) and Open Q2 (no FORCE numeric target this phase — property tests + clean interface, defer the numeric obstacle case to Phase 4)
    - 03-PATTERNS.md §"weather/mod.rs" (the profile-pair interface)
  </read_first>
  <behavior>
    - The reflection split produces two `WeatherProfile`s: `before` (source→reflector bearing) and `after` (reflector→receiver bearing), each yielding its own CalcEqSSP (ξ,c₀).
    - The two sub-paths at different bearings get different wind-projected A (A₁≠A₂ when the bearings differ); B is the same on both (bearing-independent).
    - In the homogeneous limit both sub-path profiles collapse to (0,C) and the combined reflection reduces to the single-profile 03-01 result; the homogeneous reflection point matches `reflect_over_segment`.
    - Degenerate reflector geometry returns a typed error, never a panic.
  </behavior>
  <action>
    Add a reflection-path split to `weather`: a `ReflectionProfiles { before: WeatherProfile, after: WeatherProfile }`-style constructor that derives the before/after coefficients from the two sub-path bearings (reusing `profile_for_bearing` — A projected per sub-path bearing, B shared), and a helper that returns the two `calc_eq_ssp((A₁,B₁,C))` and `calc_eq_ssp((A₂,B₂,C))` results for the source→reflector and reflector→receiver legs. Keep this a clean before/after interface (no FORCE numeric target this phase per Open Q2 — Phase-4 obstacle reflection consumes it). Add property tests: A₁≠A₂ under differing bearings, B₁=B₂, the homogeneous collapse to the single-profile result, and the homogeneous reflection-point cross-check against `reflect_over_segment`.
  </action>
  <verify>
    <automated>cargo test -p envi-harness reflection_path_profiles && cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>Before/after A₁/B₁/A₂/B₂ split built with per-bearing A projection; property tests + homogeneous collapse + reflection-point cross-check pass; full `cargo test` green; clippy + fmt clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FORCE met fields → weather routes | Operator-controlled `PropagationParams` (u, φ, dt/dz, z0) cross into the harness weather derivation; must be validated, never panic. |
| weather (A,B,C) → engine | Real-valued profile coefficients cross the harness→engine boundary; the engine treats them as untrusted numeric input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Denial of Service | CalcEqSSPGround fL/fH with degenerate d/hS/hR; NaN/Inf from bad met | mitigate | d≤400 m and hS,hR≥0.5 m clamps for Eqs. 24/25; reject non-finite (A,B,C,z0,u,dt/dz) with typed errors; z0 clamp ≥0.001 m |
| T-03-02-02 | Tampering | Route 2 with malformed FORCE met fields | mitigate | typed `CaseLoadError`, never a panic; finite checks before projection/division |
| T-03-02-03 | Spoofing (correctness) | [ASSUMED] A/B scaling constants presented as verified | mitigate | constants explicitly marked `[ASSUMED]` in doc-comments, validated by direction/structure property tests only — no false FORCE numeric pass; locked pending the 03-03 Open-Q1 checkpoint |
| T-03-02-04 | Tampering | supply chain — new dependency (esp. a linalg crate) | mitigate | zero new deps; no `nalgebra`; `cargo tree -p envi-engine` unchanged; harness dep count flat |
| T-03-02-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (RESEARCH Package Legitimacy Audit: zero added) |
| (n/a) | Repudiation/Info-Disclosure/Elevation | — | accept | offline numeric library; no auth/session/network/privilege surface |
</threat_model>

<verification>
- `cargo test` (workspace) green; `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` clean.
- `cargo tree -p envi-engine` unchanged; harness gains no new crate.
- `.conj()` grep gate over `propagation/` = 0.
- CalcEqSSPGround oracle passes by BAND INDEX; hard-ground ξ(f) flat.
- Direction property tests: downwind A > upwind A; inversion→B>0; A₁≠A₂ across bearings, B₁=B₂.
- Homogeneous collapse: CalcEqSSPGround→(0,C) and the before/after split→single-profile 03-01 result.
</verification>

<success_criteria>
Soft-ground refraction yields a band-indexed frequency-dependent ξ(f); hard ground stays frequency-independent; A is derived per azimuth (temperature once + projected wind per bearing) with inversion→B>0; reflection paths compute separate before/after A₁/B₁/A₂/B₂ profiles. Maps success criteria 2 (per-azimuth A, inversion→B>0), 3 (CalcEqSSPGround fL/fH at 1/12-oct), 4 (partial: reflection before/after split).
</success_criteria>

<output>
Create `.planning/phases/03-meteorology-refraction/03-02-SUMMARY.md` when done.
</output>
