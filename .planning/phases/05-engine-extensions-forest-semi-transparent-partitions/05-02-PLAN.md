---
phase: 05-engine-extensions-forest-semi-transparent-partitions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/envi-engine/src/propagation/transmission.rs
  - crates/envi-engine/src/propagation/mod.rs
  - tools/nord2000_oracle/gen_minphase_fixtures.py
  - crates/envi-harness/tests/fixtures/oracle/minphase.toml
  - crates/envi-harness/tests/oracle_minphase.rs
autonomous: true
requirements: [ENG-10]

must_haves:
  truths:
    - "IsolationSpectrum validates a 105-point R(f) >= 0 dB at construction (finite, typed error on NaN/Inf/negative) so non-finite log-magnitude can never reach the cepstrum (D-06/D-10 guard)"
    - "A flat R(f) yields phi identically 0 (< 1e-12): a spectrally flat partition is a pure real attenuation (T1)"
    - "phi is exactly linear in R — phi(2R) = 2*phi(R) to 1e-12 (Hilbert linearity, T2)"
    - "A rising (mass-law) R(f) yields a non-increasing ENVI-side phi trend at mid-band (causal, positive group delay after the conj boundary) — the D-08 first-principles sign check (T3)"
    - "The fold's real part reproduces ln|T| to 1e-12 (internal consistency, T4)"
    - "Engine phi matches the committed numpy-fft oracle to 1e-9 rad on all 105 bands for flat/ramp/bumpy/notch spectra (T5)"
  artifacts:
    - path: "crates/envi-engine/src/propagation/transmission.rs"
      provides: "IsolationSpectrum (validated R(f)), min-phase cepstral kernel (hand-rolled 208-pt DFT), TransmissionFilter (native e^{-j omega t} complex T(f))"
      min_lines: 150
      contains: "TransmissionFilter"
    - path: "crates/envi-engine/src/propagation/mod.rs"
      provides: "pub mod transmission registration + InvalidIsolationSpectrum error variant"
      contains: "transmission"
    - path: "tools/nord2000_oracle/gen_minphase_fixtures.py"
      provides: "numpy.fft real-cepstrum min-phase oracle generator (exact engine sample layout)"
    - path: "crates/envi-harness/tests/fixtures/oracle/minphase.toml"
      provides: "Committed min-phase oracle fixture (flat/ramp/bumpy/notch R sets, phi_envi per band)"
    - path: "crates/envi-harness/tests/oracle_minphase.rs"
      provides: "Oracle comparison test (T5) with coverage assertions"
  key_links:
    - from: "crates/envi-engine/src/propagation/transmission.rs"
      to: "crates/envi-engine/src/propagation/mod.rs"
      via: "module registration + PropagationError::InvalidIsolationSpectrum"
      pattern: "pub mod transmission"
    - from: "crates/envi-harness/tests/oracle_minphase.rs"
      to: "crates/envi-harness/tests/fixtures/oracle/minphase.toml"
      via: "TOML fixture load, phi compared per band index"
      pattern: "fixtures/oracle/minphase.toml"
  prohibitions:
    - statement: "MUST NOT introduce the conj method anywhere in crates/envi-engine/src/propagation/ — the native T(f) phase sign is written as an explicit negative sine, never a conjugation call (D-08; single-conj quarantine)"
      status: pending
      verification: "grep -rn '\\.conj(' crates/envi-engine/src/propagation/ | grep -v '//' | wc -l  -> outputs 0"
    - statement: "MUST NOT add an FFT/DSP crate — the 208-point DFT is hand-rolled with Complex<f64>; envi-engine deps stay ndarray + num-complex + thiserror only"
      status: pending
      verification: "cargo tree -p envi-engine -e normal --depth 1 | tail -n +2 | grep -vcE 'ndarray|num-complex|thiserror' outputs 0; Cargo.toml diff for envi-engine is empty"
    - statement: "ln|T| = -infinity MUST be unrepresentable at the kernel input: opaque is None (structural, D-10), and the IsolationSpectrum constructor rejects non-finite R — no magic large-R opaque encoding anywhere"
      status: pending
      verification: "T10 constructor-rejection tests pass; no f64::INFINITY / f64::MAX sentinel appears in transmission.rs (source check)"
    - statement: "The kernel MUST NOT write phase into any incoherent quantity — its output types are a per-band phase array and a complex coherent filter only (integration-side enforcement follows in plan 05-03)"
      status: pending
      verification: "transmission.rs exposes no P_incoh-typed output; API surface review in code-review gate"
---

<objective>
Build the ENG-10+ minimum-phase transmission kernel (D-06): the isolation spectrum R(f) becomes a
complex minimum-phase filter T(f) = 10^(−R(f)/20)·e^{jφ_min(f)} with φ_min reconstructed from the
log-magnitude via the discrete Hilbert transform (real-cepstrum fold) over the 105-point band
axis. This is a documented ENVI extension beyond stock Nord2000 (D-07 — same spirit as the Phase-4
directional complex phase; REQUIREMENTS/README wording follows in plan 05-03). The kernel lives in
`propagation/transmission.rs` on the Nord2000-native e^{−jωt} side (D-05's placement), with the
D-08 sign pinned per 05-RESEARCH Finding 3: the cepstral recipe (numpy-FFT conventions) yields the
ENVI e^{+jωt} lagging phase φ_cep, so the native filter is |T|·e^{−jφ_cep}, written as an explicit
negative-sine rotation — the conj-in-propagation gate stays at zero.

This plan is kernel-only (new files; zero overlap with plan 05-01 — same wave). Threading into
`SolveJob`/`terrain_effect`/`screen_channel` and the opaque-limit regression are plan 05-03.

Purpose: the numerically hardest, most novel piece of the phase (no codebase analog exists per
05-PATTERNS "No Analog Found") gets built and oracle-pinned in isolation before any composition.
Output: `transmission.rs` (IsolationSpectrum + kernel + TransmissionFilter), property tests T1–T4
+ T10, committed numpy oracle + comparison test (T5), per D-09/D-12.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-engine-extensions-forest-semi-transparent-partitions/05-CONTEXT.md
@.planning/phases/05-engine-extensions-forest-semi-transparent-partitions/05-RESEARCH.md
@.planning/phases/05-engine-extensions-forest-semi-transparent-partitions/05-PATTERNS.md
@./.claude/CLAUDE.md
</context>

## Artifacts this plan produces (new symbols — exclude from source-grounding checks)

- Module `envi_engine::propagation::transmission`:
  - `pub struct IsolationSpectrum` (validated newtype over `[f64; N_BANDS]` R(f) dB) + `IsolationSpectrum::new([f64; N_BANDS]) -> Result<Self, PropagationError>` + an accessor (`as_bands()` or similar)
  - `pub fn min_phase_phi_envi(r_db: &[f64; N_BANDS]) -> [f64; N_BANDS]` (φ_cep, ENVI e^{+jωt} lagging phase)
  - `pub struct TransmissionFilter { pub bands: [Complex<f64>; N_BANDS] }` + `TransmissionFilter::from_isolation(&IsolationSpectrum) -> Self` (native e^{−jωt} T(f))
  - internal naive-DFT helpers (`dft_forward` / `idft`, M = 208, numpy sign conventions)
- `PropagationError::InvalidIsolationSpectrum { band: usize, value: f64 }` (new variant in `propagation/mod.rs`)
- New oracle generator `tools/nord2000_oracle/gen_minphase_fixtures.py`
- New fixture `crates/envi-harness/tests/fixtures/oracle/minphase.toml`
- New test `crates/envi-harness/tests/oracle_minphase.rs`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: propagation/transmission.rs — IsolationSpectrum + min-phase cepstral kernel + native TransmissionFilter</name>
  <files>crates/envi-engine/src/propagation/transmission.rs, crates/envi-engine/src/propagation/mod.rs</files>
  <read_first>
    - .planning/phases/05-engine-extensions-forest-semi-transparent-partitions/05-RESEARCH.md Finding 3 (the full algorithm spec 3a, the sign derivation 3b, band-axis expectations 3c, numerical guards 3d) and Pitfalls 2/3
    - crates/envi-engine/src/propagation/special.rs lines 6–17 (native-convention module header discipline) and 36–45 (explicit-conjugation comment style — the quarantine wording to mirror)
    - crates/envi-engine/src/directivity.rs lines 262–288 and 415–425 (optional-phase construction via explicit (cos, sin); validation pattern)
    - crates/envi-engine/src/freq.rs (N_BANDS = 105; the grid is uniform in log-f, so band index IS the uniform Hilbert axis)
    - crates/envi-engine/src/propagation/mod.rs lines 28–96 (module registration list + PropagationError struct-variant pattern)
  </read_first>
  <behavior>
    - T1: flat R (e.g. 30.0 dB everywhere) gives phi[n] with |phi[n]| < 1e-12 for all n — pure real attenuation
    - T2: linearity — for a non-trivial R set, |phi_2R[n] − 2*phi_R[n]| < 1e-12 for all n
    - T3 (the D-08 verify item, first-principles — independent of the oracle): a monotonically rising mass-law-like R (e.g. R[n] = 20 + 0.25·n, always >= 0) gives an ENVI-side phi with a non-increasing trend at mid-band (check phi[n+1] <= phi[n] + 1e-9 for n in ~30..75) — lagging phase, positive group delay after the conj; and the NATIVE filter's phase is the negation (assert arg(T_native[n]) == −phi[n] up to 2π wrap, or directly that T_native[n].im has the opposite sign of sin(phi[n]) at bands with live phase)
    - T4: internal consistency — Re(Z[n]) reproduces ln_mag[n] to 1e-12 for all n (expose the fold's real part for the test, e.g. a doc-hidden helper returning both parts)
    - T10: IsolationSpectrum::new rejects NaN, +Inf, −Inf, and negative R at any band with PropagationError::InvalidIsolationSpectrum carrying the band index and offending value; accepts R = 0.0 (fully transparent) and large finite R
  </behavior>
  <action>
    Create `crates/envi-engine/src/propagation/transmission.rs`; register `pub mod transmission;`
    in `propagation/mod.rs` (alphabetical position in the list) and add the
    `InvalidIsolationSpectrum { band, value }` variant to `PropagationError` following the
    existing struct-variant style.

    **Module header (load-bearing, mirror special.rs discipline):** state (1) Nord2000-native
    e^{−jωt} convention, conversion to ENVI's e^{+jωt} happens ONLY at the transfer boundary in
    transfer.rs — never here (mirror the quarantine wording of refraction/mod.rs lines 19–21);
    (2) this is an ENVI extension beyond stock Nord2000 (D-06/D-07: stock treats R as real energy
    loss; ENVI derives the physical minimum phase — a passive partition's transmitted phase
    follows its amplitude); (3) the DFT sign convention IS load-bearing and pinned: forward DFT
    uses exp(−j2πnm/M), inverse uses (1/M)·exp(+j2πnm/M) — the numpy.fft convention — and with it
    the cepstral fold yields the ENVI (lagging) phase, so the NATIVE filter negates it
    (05-RESEARCH Finding 3b, verified to 6.7e-16 against the known minimum-phase system
    H = 1 + 0.5·e^{−jω}); (4) the hand-rolled naive O(M²) DFT is the deliberate dep-quarantine
    hand-roll (no FFT crate; ~43k complex multiplies at M = 208 — the directivity.rs Rotation3
    precedent); (5) the honesty note from Finding 3c: this is the minimum phase of ln|T| as a
    periodic function of BAND INDEX (log-f axis), not the analog Bode minimum phase — flat R gives
    exactly zero phase; edge bands carry the largest end effects; tight-tolerance analytic phase
    anchors are wrong by construction (properties + oracle are the pin).

    **IsolationSpectrum:** validated newtype over `[f64; N_BANDS]` (R(f) in dB, indexed by band —
    never nominal Hz). Constructor rejects non-finite and negative values per band with the typed
    error (explicit is_finite check — Pitfall: a bare comparison lets +Inf through). Rustdoc: the
    105-point grid is the engine contract; 1/1→1/3→1/12 interpolation is upstream (Phase 7
    SCN-03); ONE spectrum per job = the total crossed partition stack (multi-partition composition
    is a Phase-9 path concern, D-11/Q4); opaque is `Option::None` at the seam, NOT a large R —
    R = ∞ is structurally unrepresentable here (D-10) and R >= 0 keeps ln|T| finite always.

    **Kernel `min_phase_phi_envi`** — implement EXACTLY the 5-step recipe of 05-RESEARCH
    Finding 3a (the numpy oracle mirrors it sample-for-sample, tolerance 1e-9 rad depends on
    exactness):
    step 1: ln_mag[n] = −R[n]·ln(10)/20 for n = 0..104;
    step 2: even-mirror to M = 208: y[n] = ln_mag[n] for n <= 104, y[n] = ln_mag[208−n] for
    n = 105..207 (even symmetry about BOTH endpoints; no zero-padding);
    step 3: real cepstrum c = IDFT_208(y) (complex arithmetic throughout — do NOT shortcut with
    real-only transforms; the oracle uses complex ifft/fft and rounding must match);
    step 4: causal fold ĉ[0] = c[0], ĉ[m] = 2c[m] for 1 <= m <= 103, ĉ[104] = c[104], ĉ[m] = 0
    for m > 104 — the M/2 element stays ×1 (Pitfall 3: doubling or zeroing it biases every band);
    step 5: Z = DFT_208(ĉ); φ_cep[n] = Im(Z[n]) for n = 0..104, and Re(Z[n]) ≈ ln_mag[n] is the
    free consistency check (expose it for T4).
    Naive DFT/IDFT helpers with `Complex<f64>` and per-element sin_cos twiddles; document numpy
    equivalence.

    **TransmissionFilter::from_isolation:** mag[n] = 10^(−R[n]/20);
    `let (s, c) = phi[n].sin_cos(); bands[n] = Complex::new(mag * c, mag * (-s));` — the native
    e^{−jωt} filter |T|·e^{−jφ_cep} with the sign flip written as an explicit negative sine
    (D-08). Do NOT call the conj method; the quarantine comment mirrors special.rs:36–45. Rustdoc
    the round-trip: the single conjugation at the transfer boundary maps this to
    |T|·e^{+jφ_cep} = the lagging causal ENVI filter.

    Write the `<behavior>` tests (T1–T4, T10) in `#[cfg(test)]` FIRST against a stub, then
    implement to green (task-level TDD). Property tests probe mid-band indices (edge bands carry
    end effects, Finding 3c).
  </action>
  <verify>
    <automated>cargo test -p envi-engine transmission</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p envi-engine transmission` passes with T1, T2, T3, T4, T10 present
    - `grep -c 'pub mod transmission;' crates/envi-engine/src/propagation/mod.rs` == 1 and `grep -c 'InvalidIsolationSpectrum' crates/envi-engine/src/propagation/mod.rs` >= 1
    - The native sign is an explicit negative sine: `grep -cE '\-s|\-\s*sin' crates/envi-engine/src/propagation/transmission.rs` >= 1, and `grep -rn '\.conj(' crates/envi-engine/src/propagation/ | grep -v '//' | wc -l` outputs 0
    - No FFT crate: `git diff --name-only` contains no Cargo.toml, and `cargo tree -p envi-engine -e normal --depth 1 | tail -n +2 | grep -vcE 'ndarray|num-complex|thiserror'` outputs 0
    - No infinity sentinel: `grep -c 'INFINITY' crates/envi-engine/src/propagation/transmission.rs` outputs 0
    - Module header states the forward-DFT sign convention and the ENVI-extension status: `grep -ci 'numpy' crates/envi-engine/src/propagation/transmission.rs` >= 1 and `grep -ci 'extension' crates/envi-engine/src/propagation/transmission.rs` >= 1
    - `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` pass
  </acceptance_criteria>
  <done>The min-phase kernel exists native-side with the D-08 sign pinned by a first-principles causality property test (independent of the oracle), validated inputs, and the dep quarantine intact.</done>
</task>

<task type="auto">
  <name>Task 2: Committed numpy min-phase oracle (gen script + fixture + comparison test, T5)</name>
  <files>tools/nord2000_oracle/gen_minphase_fixtures.py, crates/envi-harness/tests/fixtures/oracle/minphase.toml, crates/envi-harness/tests/oracle_minphase.rs</files>
  <read_first>
    - tools/nord2000_oracle/gen_submodel11_fixtures.py (whole structure: docstring discipline lines 1–18, formula reimplementation, named configs, provenance + tolerance emission lines 99–134)
    - tools/nord2000_oracle/common.py (shared constants/axis helpers)
    - crates/envi-harness/tests/oracle_submodel11.rs (serde structs, load(), assertion-by-band-index, coverage assertions lines 105–112)
    - .planning/phases/05-engine-extensions-forest-semi-transparent-partitions/05-RESEARCH.md Finding 3e (the exact numpy recipe — use it verbatim; scipy.signal.hilbert is NOT equivalent and must not be used)
  </read_first>
  <action>
    **Generator** `gen_minphase_fixtures.py` following the gen_submodel11 structure: docstring
    with operator-driven regeneration, "Python/numpy are NOT build or test dependencies", and the
    MANDATORY oracle-independence caveat (D-09): this oracle implements the SAME mirror/fold
    recipe as the engine — it cross-checks the implementation (DFT conventions, fold indices,
    sign), not the choice of recipe; the recipe itself is pinned independently by the analytic
    verification in 05-RESEARCH 3a/3b and by the engine's T3 causality property test. Implement
    `min_phase_envi(r_db)` exactly as Finding 3e: ln_mag = −r·ln(10)/20; even mirror via
    `np.concatenate([ln_mag, ln_mag[-2:0:-1]])` (M = 208); `c = np.fft.ifft(y)`; fold weights
    {1, 2×(1..103), 1 at 104, 0 above}; `z = np.fft.fft(c * fold)`; return `z.imag[:105]` — the
    ENVI lagging phase (native = negation, engine-side). Do NOT use scipy.signal.hilbert
    (different sample layout).

    Four named R sets over ALL 105 bands (min-phase is a whole-grid transform — store every band,
    not a stride): (a) `flat_30` — R ≡ 30.0 (φ ≡ 0 row); (b) `mass_law_ramp` — a rising
    20·log10-shaped ramp clipped >= 0; (c) `bumpy` — a plausible measured-looking spectrum
    (deterministic formula, e.g. a sum of two sinusoids in band index, offset positive); (d)
    `notch` — a narrow dip (stress case). Emit per case: `r_db = [...]` (105 values, full
    precision) and `phi_envi_rad = [...]` (105 values, `%.15e` formatting), plus `[meta]` with
    sha256 provenance of the script and `tol_abs_rad = 1e-9`. Run once; commit
    `crates/envi-harness/tests/fixtures/oracle/minphase.toml`.

    **Comparison test** `oracle_minphase.rs`: serde-load the fixture; for each case, build the
    `[f64; N_BANDS]` from `r_db`, call `envi_engine::propagation::transmission::min_phase_phi_envi`,
    assert |engine − oracle| <= tol per band (band index in the panic message). ALSO cross-check
    the full filter path: `TransmissionFilter::from_isolation(&IsolationSpectrum::new(r)?)` must
    equal `10^(−R/20)·(cos φ, −sin φ)` with the oracle's φ to 1e-9 per band (pins the native
    negation end-to-end against the fixture). Coverage assertions: exactly 4 cases; the flat case
    has max|φ| < 1e-12 (saw the zero regime); at least one other case has max|φ| > 0.01 rad (saw
    the live-phase regime) — a truncated fixture cannot pass silently.
  </action>
  <verify>
    <automated>cargo test -p envi-harness --test oracle_minphase</automated>
  </verify>
  <acceptance_criteria>
    - `crates/envi-harness/tests/fixtures/oracle/minphase.toml` committed with 4 cases × 105-value arrays and a sha256 provenance line (`grep -c 'sha256' ...minphase.toml` >= 1)
    - `cargo test -p envi-harness --test oracle_minphase` passes at 1e-9 rad on every band of every case, including the native-filter cross-check
    - Coverage assertions present: flat-zero regime AND live-phase regime both asserted in the test source
    - The generator does not import scipy.signal: `grep -c 'signal' tools/nord2000_oracle/gen_minphase_fixtures.py` outputs 0
    - `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`, `cargo test` all green
  </acceptance_criteria>
  <done>The kernel is numerically pinned to a committed independent-runtime reference on all 105 bands, with the oracle-independence caveat stated where D-09 requires it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → IsolationSpectrum | Unvalidated per-band R(f) (future Phase-7 UI / Phase-10 service data) enters the pure-math engine here |
| recipe → conventions | A silently flipped DFT/phase sign convention produces plausible magnitudes with wrong interference (survives casual review — Pitfall 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering (data integrity) | IsolationSpectrum input | mitigate | Validating constructor: explicit is_finite + R >= 0 per band, typed InvalidIsolationSpectrum with band+value (T10); ln|T| therefore always finite; R = ∞ structurally unrepresentable (opaque = None, D-10) |
| T-05-02-02 | Tampering (correctness — sign flip) | min-phase φ sign, native negation | mitigate | T3 first-principles causality property (independent of oracle); pinned DFT convention in module docs; explicit negative-sine construction; oracle T5 cross-checks both φ and the assembled native filter |
| T-05-02-03 | Tampering (correctness — fold bug) | cepstral fold M/2 element | mitigate | Exact fold weights documented (Pitfall 3); T1 flat⇒0 and T2 linearity catch fold bugs; T4 Re(Z) reconstruction to 1e-12; oracle full-grid comparison |
| T-05-02-04 | Tampering (contract) | conj quarantine / two-channel | mitigate | Zero conj-method calls in propagation/ (grep gate); kernel exposes only a phase array + coherent filter — no incoherent-energy output type exists |
| T-05-02-05 | DoS (panic on degenerate input) | DFT on adversarial R | mitigate | Constructor rejects all non-finite input; kernel is total on finite input (no division, no log of user data beyond ln|T| with finite R) |
| T-05-SC | Tampering (supply chain) | package installs | accept | No new packages (hand-rolled DFT precisely to avoid an FFT crate; Python tools pre-existing) |
</threat_model>

<verification>
- `cargo test` (workspace) green including transmission unit tests (T1–T4, T10) and `oracle_minphase` (T5)
- `cargo clippy --all-targets -- -D warnings` clean; `cargo fmt --check` clean
- `#![deny(unsafe_code)]` intact on envi-engine
- Dep quarantine unchanged: `cargo tree -p envi-engine -e normal --depth 1 | tail -n +2 | grep -vcE 'ndarray|num-complex|thiserror'` outputs 0 (no FFT crate)
- Conj gate: `grep -rn '\.conj(' crates/envi-engine/src/propagation/ | grep -v '//' | wc -l` outputs 0
</verification>

<success_criteria>
- The ENG-10+ min-phase kernel (D-06) exists on the native side with the D-08 sign verified two independent ways (first-principles causality property + committed oracle), inputs validated (no NaN/Inf can reach the tensor), and zero new dependencies
- D-09's oracle discipline honored: committed fixture, sha256 provenance, no Python at test time, oracle-independence caveat stated
- Ready for plan 05-03 to thread into screen_channel without touching the kernel
</success_criteria>

<output>
Create `.planning/phases/05-engine-extensions-forest-semi-transparent-partitions/05-02-SUMMARY.md` when done.
State in the SUMMARY: the pinned DFT convention, the native-negation sign result (D-08), and that the kernel is not yet reachable from any solve path (plan 05-03 wires it).
</output>
