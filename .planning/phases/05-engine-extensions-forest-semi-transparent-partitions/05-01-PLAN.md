---
phase: 05-engine-extensions-forest-semi-transparent-partitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/envi-harness/tests/solve_baseline.rs
  - crates/envi-harness/tests/fixtures/regression/solve_baseline.toml
  - crates/envi-engine/src/forest.rs
  - crates/envi-engine/src/lib.rs
  - crates/envi-engine/src/solver.rs
  - crates/envi-harness/tests/mac_identity.rs
  - crates/envi-harness/tests/tensor_budget.rs
  - tools/nord2000_oracle/gen_forest_fixtures.py
  - crates/envi-harness/tests/fixtures/oracle/forest.toml
  - crates/envi-harness/tests/oracle_forest.rs
autonomous: true
requirements: [ENG-09]

must_haves:
  truths:
    - "A SolveJob with forest: Some(ForestCrossing) attenuates H_coh by 10^(ΔL_s/20) and P_incoh_abs by 10^(ΔL_s/10) per band, with arg(H_coh) unchanged (D-03)"
    - "forest_delta_l is exactly 0.0 for every band with ka <= 0.7 (analytic anchor F1) and reproduces the on-node hand anchor to 1e-12 (F2)"
    - "forest_delta_l is floored at exactly -15.0 dB for large crossings (F3) and stays within [-15, 0.01] over a randomized parameter sweep (F4)"
    - "Engine forest values match the committed scipy PCHIP oracle to 1e-9 dB on sampled bands (F5)"
    - "A SolveJob with forest: None reproduces the pre-Phase-5 solver output bit-for-bit (pinned baseline fixture)"
  artifacts:
    - path: "crates/envi-engine/src/forest.rs"
      provides: "SM10 scattering-zone excess attenuation: ForestCrossing, ForestError, forest_delta_l, Tables 8/9 consts, scipy-equivalent PCHIP"
      min_lines: 200
      contains: "Eq. 291"
    - path: "crates/envi-engine/src/solver.rs"
      provides: "SolveJob.forest seam + post-conj two-channel application"
      contains: "forest"
    - path: "crates/envi-harness/tests/fixtures/regression/solve_baseline.toml"
      provides: "Bit-exact pre-extension solver baseline (hex f64 bit patterns per band)"
    - path: "tools/nord2000_oracle/gen_forest_fixtures.py"
      provides: "scipy PchipInterpolator forest oracle generator"
    - path: "crates/envi-harness/tests/fixtures/oracle/forest.toml"
      provides: "Committed forest oracle fixture (no Python at test time)"
    - path: "crates/envi-harness/tests/oracle_forest.rs"
      provides: "Oracle comparison test (F5) with coverage assertions"
  key_links:
    - from: "crates/envi-engine/src/solver.rs"
      to: "crates/envi-engine/src/forest.rs"
      via: "solve_pair calls forest_delta_l(job.axis.centres[i], fc, job.coh.c0) inside the band loop"
      pattern: "forest_delta_l"
    - from: "crates/envi-harness/tests/oracle_forest.rs"
      to: "crates/envi-harness/tests/fixtures/oracle/forest.toml"
      via: "TOML fixture load keyed by band_index"
      pattern: "fixtures/oracle/forest.toml"
    - from: "crates/envi-harness/tests/solve_baseline.rs"
      to: "crates/envi-harness/tests/fixtures/regression/solve_baseline.toml"
      via: "to_bits equality against committed hex bit patterns"
      pattern: "solve_baseline.toml"
  prohibitions:
    - statement: "MUST NOT introduce the conj method anywhere in crates/envi-engine/src/propagation/ (single-conj quarantine; this plan touches only crate-root modules, gate stays 0)"
      status: pending
      verification: "grep -rn '\\.conj(' crates/envi-engine/src/propagation/ | grep -v '//' | wc -l  -> outputs 0 (comment lines filtered; all pre-existing hits are comments)"
    - statement: "Forest MUST NOT touch arg(H_coh) or route energy into P_incoh as decorrelation — it is a real magnitude factor on both channels (D-03), never an operator inside propagation/ (D-04)"
      status: pending
      verification: "solver test asserts arg unchanged to <1e-12 with forest Some; forest_delta_l returns f64 (type-level: cannot carry phase); no forest symbol appears under crates/envi-engine/src/propagation/"
    - statement: "F->1 => P_incoh->0 MUST stay bit-exact with forest enabled"
      status: pending
      verification: "solver test F7: zero-turbulence job + forest Some asserts p_incoh_abs.to_bits() == 0.0f64.to_bits() per band"
    - statement: "forest: None MUST NOT perturb any output bit (structural identity)"
      status: pending
      verification: "cargo test -p envi-harness --test solve_baseline (pinned pre-extension bit fixture stays green after the seam lands)"
    - statement: "MUST NOT implement a literal per-metre a(f) or import the ISO 9613-2 distance-clamp regimes (10/20/200 m table) — SM10 Eqs. 288-291 is the law (Pitfall 1, deferred-ideas exclusion)"
      status: pending
      verification: "forest.rs cites AV 1106/07 Eqs. 289/290/291 in the module header; no function returns dB-per-metre; source contains no 10/20/200 m regime branches (code-review gate check)"
---

<objective>
Implement ENG-09 forest attenuation as the REAL Nord2000 law — AV 1106/07 §5.19 Sub-Model 10
"Scattering Zones" (Eqs. 288–291, Tables 8/9, verified against page images in 05-RESEARCH.md
Finding 1) — NOT the TI 386 "A = d·a(f)" paraphrase (roadmap SC1 wording; the SM10 law is what
that paraphrase describes and satisfies SC1). The engine owns the formula (D-02); the crossing
arrives via a `SolveJob` seam (D-01); the resulting ΔL_s ≤ 0 dB applies solver-side, post-conj, as
a real phase-preserving factor on both channels (D-03/D-04), exactly like `directivity_gain_db`.

**Interface deviation from CONTEXT D-01 (research-mandated, flag in SUMMARY):** the locked field
list `{d_m, density, stem_radius, kp, absorption}` is amended per 05-RESEARCH.md Finding 1:
`kp` is dropped as an input (TI 386's "kp" ≡ the tabulated frequency weighting k_f of Table 8, a
function of ka the engine computes — precisely D-02's "engine owns the formula"), and `height_m`
is added (h' = nQ·h is un-evaluable without average tree height; Pitfall 5). Phase-7 SCN-04
already carries height, so this is downstream-consistent.

**Fs (Eq. 288) is deferred per the accepted Q1 disposition:** only the ΔL_s excess attenuation
lands now (D-03's locked scope); the `Fs = 1 − k_f·T` coherence-reduction channel is recorded as a
documented seam (forest.rs module header here; deferred-items.md in plan 05-03), mirroring the
Phase-4 directional-phase-seam discipline. It is NOT silently dropped.

Purpose: drawn forests must actually attenuate (FEATURES.md: "do not silently draw forests that do
nothing") before Phase 7 forest objects and Phase 10 calc wiring exist.
Output: `forest.rs` (SM10 math + tables), `SolveJob.forest` seam, pinned pre-extension solver
baseline, committed scipy oracle, full F1–F7 test ladder (D-12, adapted — the "linear in d" anchor
is replaced by SM10's exact zero/floor/on-node anchors, since linear-in-d is physically wrong for
Nord2000).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-engine-extensions-forest-semi-transparent-partitions/05-CONTEXT.md
@.planning/phases/05-engine-extensions-forest-semi-transparent-partitions/05-RESEARCH.md
@.planning/phases/05-engine-extensions-forest-semi-transparent-partitions/05-PATTERNS.md
@./.claude/CLAUDE.md
</context>

## Artifacts this plan produces (new symbols — exclude from source-grounding checks)

- Module `envi_engine::forest` (crate root, registered in `lib.rs`):
  - `pub struct ForestCrossing { d_m, density_per_m2, stem_radius_m, absorption, height_m }` (Copy) + `ForestCrossing::new(...) -> Result<Self, ForestError>`
  - `pub enum ForestError` (thiserror, self-contained like `DirectivityError`)
  - `pub fn forest_delta_l(f_hz: f64, fc: &ForestCrossing, c0: f64) -> f64` (ΔL_s ∈ [−15, ~0] dB, infallible on validated inputs)
  - `fn table8_kf(ka: f64) -> f64` (Table 8, linear interp, edge-clamped)
  - `fn table9_delta_l(h_norm: f64, alpha: f64, r_norm: f64) -> f64` (Table 9, tensor-product PCHIP)
  - `fn pchip_derivatives` / `fn pchip_eval` (scipy-equivalent Fritsch–Carlson–Butland monotone cubic, hand-rolled)
  - Table consts `KA_AXIS`, `KF_VALS`, `R_NORM_AXIS`, `H_NORM_AXIS`, `ALPHA_AXIS`, `TABLE9` (transcribed data, cited by report+page — SM7 Tables 6/7 precedent)
- `SolveJob.forest: Option<ForestCrossing>` (new field, `solver.rs`)
- New test files: `crates/envi-harness/tests/solve_baseline.rs`, `crates/envi-harness/tests/oracle_forest.rs`
- New fixtures: `crates/envi-harness/tests/fixtures/regression/solve_baseline.toml`, `crates/envi-harness/tests/fixtures/oracle/forest.toml`
- New oracle generator: `tools/nord2000_oracle/gen_forest_fixtures.py`

<tasks>

<task type="auto">
  <name>Task 1: Pin the pre-extension solver baseline (bit-exact regression fixture)</name>
  <files>crates/envi-harness/tests/solve_baseline.rs, crates/envi-harness/tests/fixtures/regression/solve_baseline.toml</files>
  <read_first>
    - crates/envi-harness/tests/mac_identity.rs (SolveJob construction at line 53, sink usage, job/profile setup — mirror this)
    - crates/envi-harness/tests/oracle_submodel11.rs (serde TOML fixture loading pattern, CARGO_MANIFEST_DIR path style)
    - crates/envi-engine/src/solver.rs lines 98–157 (public `solve` signature — jobs + n_sub + chunk_receivers + TensorSink)
    - crates/envi-engine/src/tensor.rs (the sink type mac_identity uses to capture H_coh / P_incoh_abs)
  </read_first>
  <action>
    Create a permanent bit-exact regression that pins today's (pre-Phase-5) solver output, so the
    seam additions of Task 3 (and plan 05-03's isolation seam) are PROVEN to leave the
    `forest: None` / `isolation: None` path untouched (D-10 discipline applied to the solver; D-12).

    1. New harness test `solve_baseline.rs`: build ONE canonical `SolveJob` mirroring
       `mac_identity.rs:53` (same profile/atmosphere/coherence construction — reuse its helper
       shapes; a single sub-source, single receiver is enough), run `envi_engine::solver::solve`
       into the same sink type mac_identity uses, and extract `H_coh[0,0,band]` (re, im) and
       `P_incoh_abs[0,0,band]` for all 105 bands.
    2. Fixture format `fixtures/regression/solve_baseline.toml` (create the `regression/` dir):
       a `[meta]` block (description + the sentence "captured from the pre-Phase-5 tree — DO NOT
       REGENERATE; regenerating after the Phase-5 seams land destroys the regression's meaning"),
       then `h_re_bits`, `h_im_bits`, `p_bits` — three arrays of 105 hex strings, each the
       `format!("{:#018x}", value.to_bits())` of the f64. Hex strings because TOML integers are
       i64 and u64 bit patterns overflow them.
    3. Two tests in the file:
       - `#[test] #[ignore = "baseline generator - run ONLY on the pre-extension tree"]`
         `regenerate_baseline()` — computes the values and writes the TOML (std::fs, path via
         `concat!(env!("CARGO_MANIFEST_DIR"), "/tests/fixtures/regression/solve_baseline.toml")`).
       - `#[test] solver_output_matches_pinned_baseline()` — loads the fixture, re-runs the
         identical job, asserts `u64::from_str_radix(&s[2..], 16)` equality against
         `.to_bits()` per band for all three channels, with the band index in the panic message.
    4. Run the generator ONCE now (this is the pre-extension tree — Task 3 has not run yet):
       `cargo test -p envi-harness --test solve_baseline -- --ignored`, then confirm the normal
       test passes. Commit the fixture as part of this task.
  </action>
  <verify>
    <automated>cargo test -p envi-harness --test solve_baseline</automated>
  </verify>
  <acceptance_criteria>
    - `crates/envi-harness/tests/fixtures/regression/solve_baseline.toml` exists with exactly 105 entries in each of `h_re_bits`, `h_im_bits`, `p_bits`, and a DO-NOT-REGENERATE meta note
    - `cargo test -p envi-harness --test solve_baseline` passes (1 test run, generator ignored)
    - `grep -c '#\[ignore' crates/envi-harness/tests/solve_baseline.rs` outputs 1 (generator is opt-in only)
    - Comparison is `to_bits()` u64 equality, not float tolerance: `grep -c 'to_bits' crates/envi-harness/tests/solve_baseline.rs` outputs >= 1
  </acceptance_criteria>
  <done>Pinned bit-exact solver baseline committed; the fixture makes any later perturbation of the None-path a hard test failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: forest.rs — Sub-Model 10 (Eqs. 288–291, Tables 8/9) as engine-owned pure math</name>
  <files>crates/envi-engine/src/forest.rs, crates/envi-engine/src/lib.rs</files>
  <read_first>
    - .planning/phases/05-engine-extensions-forest-semi-transparent-partitions/05-RESEARCH.md Finding 1 (verified equations + full Table 8/9 transcriptions + assumptions A1–A5) and the "SM10 core" code example
    - crates/envi-engine/src/propagation/air_absorption.rs (validating-constructor + infallible per-band-fn pattern: Atmosphere::new lines 46–84, alpha_db_per_m 149–163, anchor tests 228–245)
    - crates/envi-engine/src/directivity.rs (top-level module precedent: lib.rs registration, self-contained thiserror error enum lines 77–149, hand-roll-over-crate precedent lines 493–497)
    - crates/envi-engine/src/propagation/terrain_effect/submodel7.rs (table-transcription-as-consts precedent with provenance rustdoc and edge-clamped interpolation)
    - crates/envi-engine/src/freq.rs (grid law, N_BANDS, band-index discipline)
  </read_first>
  <behavior>
    - F1: for every band with ka = 2π·f·a/c0 <= 0.7, forest_delta_l returns exactly 0.0 (assert == 0.0, no tolerance)
    - F2 on-node hand anchor (NO interpolation anywhere): at f = axis.centres[64] (exactly 1000 Hz), choose a = 3·c0/(2π·1000) so ka = 3 (Table-8 node, k_f = 0.70); choose density so nQ·d_m = 1 (Table-9 node R' = 1, and T = (1/1.75)²); height so h' = nQ·h = 0.1 (node); α = 0 (node). Then ΔL_s must equal `1.25 * 0.7 * (1.0/1.75f64).powi(2) * (-20.5 + 20.0 * 8.0f64.log10())` to 1e-12. COMPUTE the expected value from this expression in the test — do NOT hardcode a decimal string (05-RESEARCH F2's printed digits "−0.696776" are a typo; the formula evaluates to ≈ −0.6966286 dB)
    - F3: for a very large d_m (k_f·T saturated), ΔL_s == -15.0 exactly (the Max floor); and ΔL_s is monotone non-increasing in d_m until the floor engages (sample a grid of d values at a fixed high band)
    - F4: randomized/parametric sweep over (density, a, α, h, d) within valid ranges: every ΔL_s is finite, <= 0.01 (tiny positive interpolation corner tolerated per Pitfall 4 analysis) and >= -15.0 exactly
    - Constructor: ForestCrossing::new rejects NaN/Inf anywhere, d_m < 0, density < 0, stem_radius <= 0, height <= 0, absorption outside [0,1] — each a typed ForestError, never a panic
  </behavior>
  <action>
    Create `crates/envi-engine/src/forest.rs` and register `pub mod forest;` in `lib.rs` beside
    `pub mod directivity;` — crate ROOT, not propagation/ (D-04: post-conj-side real-dB math;
    directivity.rs placement precedent). Everything below implements D-02 (engine owns the
    formula) with the verified SM10 law.

    **Module header (I/O doc contract):** AV 1106/07 §5.19 Sub-Model 10, Eqs. 288/289/290/291 and
    Tables 8/9 cited by report + page number ONLY (licensing rule — transcribed table *data* is
    the SM7 precedent); state that ΔL_s is a real per-band dB effect applied solver-side to both
    channels (D-03/D-04, phase-untouchable by type: f64); state the Q1 deferral: the Eq. 288
    coherence factor `Fs = 1 − k_f·T` (entering F = Ff·FΔν·Fc·Fr·Fs, Eq. 110) is deliberately NOT
    implemented in Phase 5 — a documented seam exists (`CoherenceInputs` already carries a
    caller-multiplied `f_delta_nu`-style factor; the screen engine's F₂/F₃/F₄ would need the same
    multiply) and it is revisited when real forest crossings land (Phase 9); see
    deferred-items.md. State assumptions A1–A3 (interpolation scheme, ka>20 clamp, R' clamp).

    **Types:** `ForestCrossing { d_m, density_per_m2, stem_radius_m, absorption, height_m }`
    (`#[derive(Debug, Clone, Copy, PartialEq)]`), validating `new()` mirroring `Atmosphere::new`
    (explicit non-finite rejection — a bare comparison lets +Inf through). Self-contained
    `ForestError` thiserror enum (DirectivityError precedent). Document the D-01 amendment in the
    struct rustdoc: TI 386's "kp" is Table 8's k_f (computed, not an input, A5); `height_m` is
    required for h' = nQ·h (Pitfall 5). `absorption` doc: Table-9 axis, "normally 0.1–0.4"
    (AV 1106/07 p. 126); accepted range [0,1], edge-clamped to the table domain [0, 0.4] at lookup.

    **`forest_delta_l(f_hz, fc, c0) -> f64`** exactly per the research SM10-core example:
    nQ = 2·a·n″ (Eq. 290); T = min((d·nQ/1.75)², 1) (Eq. 289); ka = 2π·f·a/c0;
    k_f = table8_kf(ka); early-return exactly 0.0 when k_f == 0.0 or T == 0.0 (F1's exact zero —
    no table math constructed); h' = nQ·h; R' = (nQ·d).clamp(0.0625, 10.0) — the clamp applies to
    BOTH the table lookup AND the 20·log10(8·R') term (A3/Pitfall 4: clamping only one side
    manufactures spurious attenuation); A_e = table9_delta_l(h', α, R') + 20·log10(8·R')
    (log base 10, A4); return (1.25·k_f·T·A_e).max(-15.0) (Eq. 291 floor).

    **Table 8** consts from 05-RESEARCH Finding 1 (`KA_AXIS`/`KF_VALS`, 8 nodes 0..20), linear
    interpolation, edge-clamped: ka <= 0.7 gives exactly 0.0, ka >= 20 gives 1.0 (A2).

    **Table 9** const: the full 12×3×3 transcription from 05-RESEARCH Finding 1 (rows = R' axis
    {0.0625, 0.125, 0.25, 0.5, 0.75, 1, 1.5, 2, 3, 4, 6, 10}; h' ∈ {0.01, 0.1, 1};
    α ∈ {0, 0.2, 0.4}), with the provenance comment "AV 1106/07 pp. 126–127 Table 9, verified
    against page images 2026-07-09". If `refs/AV1106-07-rev4.pdf` is present locally, re-verify a
    spot sample of rows against the rendered pages (project transcription rule; pdftoppm is
    available via the winget poppler path — fail-soft: if refs are absent, the research page-image
    verification stands, note it in the commit message).

    **Interpolation (A1, must match the Task-3 scipy oracle EXACTLY):** tensor-product PCHIP,
    nesting order fixed as: (1) PCHIP along R' (12 nodes) for each of the 9 (h', α) columns,
    (2) PCHIP along α (3 nodes) for each h', (3) PCHIP along log10(h') (3 nodes — they are exact
    decades). h' edge-clamped to [0.01, 1], α edge-clamped to [0, 0.4]. Hand-roll PCHIP with the
    scipy-equivalent derivative rule (scipy.interpolate.PchipInterpolator, Fritsch–Carlson–Butland):
    with h_k = x_{k+1}−x_k and secants δ_k, an interior derivative d_k = 0 when δ_{k−1}·δ_k <= 0,
    else the weighted harmonic mean d_k = (w1+w2)/(w1/δ_{k−1} + w2/δ_k) with w1 = 2h_k + h_{k−1},
    w2 = h_k + 2h_{k−1}; endpoints use the one-sided three-point estimate
    d = ((2h_0+h_1)δ_0 − h_0·δ_1)/(h_0+h_1) with the two fixups (sign(d) ≠ sign(δ_0) ⇒ 0;
    sign(δ_0) ≠ sign(δ_1) and |d| > 3|δ_0| ⇒ 3δ_0). Evaluate as a cubic Hermite segment. Document
    in the rustdoc that this matches scipy so the oracle tolerance can be 1e-9 dB. No linalg/FFT
    crate — hand-roll precedent per directivity.rs Rotation3 (dep quarantine).

    Write the `<behavior>` tests (F1–F4 + constructor rejections) in `#[cfg(test)]` FIRST, watch
    them fail against a stub, then implement until green (task-level TDD). Anchor tests must read
    frequencies as `axis.centres[i]` / exact grid values, never nominal Hz.
  </action>
  <verify>
    <automated>cargo test -p envi-engine forest</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p envi-engine forest` passes with F1, F2, F3, F4 and constructor-rejection tests present
    - F2's expected value is computed from the formula expression in-test: `grep -c 'log10' crates/envi-engine/src/forest.rs` outputs >= 1 and no test contains a hardcoded `0.6966` or `0.696776` decimal literal
    - forest.rs module header cites §5.19 and Eqs. 288–291 and states the Fs (Eq. 288) deferral + seam: `grep -c 'Eq. 291' crates/envi-engine/src/forest.rs` >= 1 and `grep -ci 'deferred' crates/envi-engine/src/forest.rs` >= 1
    - `forest_delta_l` returns `f64` (grep the signature: `grep -c 'pub fn forest_delta_l' crates/envi-engine/src/forest.rs` == 1) — type-level phase safety
    - `lib.rs` registers the module: `grep -c 'pub mod forest;' crates/envi-engine/src/lib.rs` == 1
    - `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` pass
  </acceptance_criteria>
  <done>SM10 forest math exists as engine-owned, document-exact, anchor-tested pure f64 code at crate root, with the Fs deferral documented in the module header.</done>
</task>

<task type="auto">
  <name>Task 3: SolveJob.forest seam + two-channel application + scipy oracle (F5–F7)</name>
  <files>crates/envi-engine/src/solver.rs, crates/envi-harness/tests/mac_identity.rs, crates/envi-harness/tests/tensor_budget.rs, crates/envi-harness/tests/solve_baseline.rs, tools/nord2000_oracle/gen_forest_fixtures.py, crates/envi-harness/tests/fixtures/oracle/forest.toml, crates/envi-harness/tests/oracle_forest.rs</files>
  <read_first>
    - crates/envi-engine/src/solver.rs lines 55–80 (SolveJob fields), 159–209 (solve_pair application order: directivity gain, then directional phase), 341–500 (test patterns: struct-update syntax, arg-unchanged assert, .to_bits() channel isolation)
    - crates/envi-harness/tests/mac_identity.rs line 53 and crates/envi-harness/tests/tensor_budget.rs line 56 (SolveJob literal sites)
    - tools/nord2000_oracle/gen_submodel11_fixtures.py and tools/nord2000_oracle/common.py (oracle generator structure: docstring discipline, sha256 provenance, band stride, TOML emission, freq_axis()/C0_M_S reuse)
    - crates/envi-harness/tests/oracle_submodel11.rs (serde structs, load(), per-band-index assertion, coverage assertions)
  </read_first>
  <action>
    **Seam (D-01):** add `pub forest: Option<ForestCrossing>` to `SolveJob` (Copy struct by value,
    like `src: [f64; 3]`), with rustdoc mirroring the `directivity_gain_db` doc style: a
    pre-computed scalar through-path length + physical params; real magnitude factor on both
    channels; geometry extraction (rubber-band R_sc over obstacles, Figure 29) is a Phase-9
    upstream concern; multiple/heterogeneous crossings deferred (single scalar d_m per D-01).
    This lands on the PROMOTED `envi_engine::solver` (one solve path, two callers — ROADMAP
    coordination flag), never harness-private.

    **Application (D-03/D-04):** in `solve_pair`'s band loop, AFTER the directivity-phase block
    (order: directivity gain → directional phase → forest), add: if `job.forest` is Some, compute
    `let dls = forest_delta_l(job.axis.centres[i], fc, job.coh.c0);` then
    `hc *= 10f64.powf(dls / 20.0);` and `pa *= 10f64.powf(dls / 10.0);` — a real factor,
    arg(H_coh) untouched, structurally identical to the directivity magnitude block at
    solver.rs:194–197. ΔL_s <= 0, so this only attenuates. NEVER route forest into
    terrain_effect or anything under propagation/ (D-04). Update the solver.rs module
    header/chain doc (line ~10) to list the forest step.

    **Mechanical fallout:** add `forest: None` to every existing `SolveJob { ... }` literal — 8
    sites in solver.rs tests (lines 256, 308, 348, 365, 399, 417, 454, 480), mac_identity.rs:53,
    tensor_budget.rs:56, and Task 1's solve_baseline.rs job literal. The compiler enforces
    completeness (no `..Default::default()` shortcuts — the struct has no Default).

    **Solver tests (F6/F7),** mirroring `directivity_gain_scales_magnitude_only_leaving_phase_unchanged`
    and the .to_bits() pattern at solver.rs:439–443:
    - F6: `base` job vs `SolveJob { forest: Some(fc), ..base }` — per band assert
      |H| ratio == 10^(dls/20) to 1e-12, arg difference < 1e-12, P ratio == 10^(dls/10) to 1e-12
      (compute dls in-test via forest_delta_l with the same inputs).
    - F7: a zero-turbulence job (mirror the existing zero-turbulence/exact-zero P_incoh contract
      test — locate it by grepping solver.rs/terrain tests for the to_bits zero assertion) with
      forest Some: `p_incoh_abs.to_bits() == 0.0f64.to_bits()` per band (a real scale of an exact
      0.0 stays exact 0.0).
    - The Task-1 pinned baseline test must still pass untouched (forest: None identity).

    **Oracle (D-09/D-12, F5):** `tools/nord2000_oracle/gen_forest_fixtures.py` copying
    gen_submodel11_fixtures.py structure end-to-end (docstring: operator-driven regeneration, no
    Python at test time, oracle-independence caveat — the oracle shares the equation transcription
    and interpolation scheme, so it cross-checks implementation, not spec reading; equations cited
    by report+number only). Reimplement SM10 in plain Python: np.interp for Table 8,
    `scipy.interpolate.PchipInterpolator` for Table 9 with the IDENTICAL nesting order as Task 2
    (R' per column → α per h' → log10(h')), same clamps. Reuse `flat_models.freq_axis()` and
    `common.C0_M_S`. At least 4 named parameter sets: a zero-effect set (large stems + low bands
    only would be partial — instead use d_m = 0 for an all-zero row), a light crossing, a dense
    crossing that hits the −15 floor at high bands, and a mid-range set; bands
    `BAND_INDICES = list(range(0, 105, 8))`; `tol_abs_db = 1e-9`; sha256 provenance line. Write
    `crates/envi-harness/tests/fixtures/oracle/forest.toml`, run the script once, commit the TOML.

    **Oracle test** `oracle_forest.rs` mirroring oracle_submodel11.rs: serde structs, per-row
    assertion by band index (`let f = axis.centres[bi];` — band-index discipline), tolerance from
    meta, plus coverage assertions: >= 4 cases, saw an all-zero row AND an attenuating row AND at
    least one −15.0-floor value (a truncated fixture cannot pass silently).
  </action>
  <verify>
    <automated>cargo test -p envi-engine && cargo test -p envi-harness --test oracle_forest && cargo test -p envi-harness --test solve_baseline</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub forest: Option<ForestCrossing>' crates/envi-engine/src/solver.rs` == 1
    - Every SolveJob literal compiles (cargo build --workspace) — 11 sites updated with `forest: None`
    - F6 passes: magnitude scaled by 10^(ΔL_s/20), arg unchanged < 1e-12, P scaled by 10^(ΔL_s/10)
    - F7 passes: zero-turbulence + forest Some gives p_incoh_abs bits == 0.0 bits per band
    - `cargo test -p envi-harness --test solve_baseline` still passes (pre-extension bits unmoved)
    - `cargo test -p envi-harness --test oracle_forest` passes at tol 1e-9 dB with coverage assertions (>= 4 cases, zero + attenuating + floor regimes all seen)
    - `crates/envi-harness/tests/fixtures/oracle/forest.toml` is committed and contains a sha256 provenance line: `grep -c 'sha256' crates/envi-harness/tests/fixtures/oracle/forest.toml` >= 1
    - `grep -rn '\.conj(' crates/envi-engine/src/propagation/ | grep -v '//' | wc -l` outputs 0
    - `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`, `cargo test` all green
  </acceptance_criteria>
  <done>ENG-09 lands end-to-end: seam + application + oracle + full F-ladder green; None-path proven bit-identical by the pinned baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness/service → SolveJob.forest | Unvalidated physical parameters (future service callers) enter the pure-math engine here |
| document → transcribed Tables 8/9 | Transcription errors become silent wrong physics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering (data integrity) | ForestCrossing params | mitigate | Validating constructor: explicit non-finite rejection (NaN/±Inf), d_m/density >= 0, stem_radius/height > 0, α ∈ [0,1] — typed ForestError, never a panic; F4 finiteness sweep |
| T-05-01-02 | Tampering (correctness) | Table 8/9 consts, Eq. 291 | mitigate | Page-image-verified transcription (05-RESEARCH Finding 1) + on-node anchors F1/F2 (interpolation-free) + scipy oracle F5 with sha256 provenance |
| T-05-01-03 | Tampering (contract) | H_coh phase / P_incoh channel | mitigate | forest_delta_l returns f64 (cannot carry phase); F6 arg-unchanged assert; F7 exact-zero bits; solve_baseline pins the None path bit-for-bit |
| T-05-01-04 | DoS (panic on degenerate input) | Table 9 lookup at extreme R'/h'/α | mitigate | Consistent edge clamps (A2/A3): R' ∈ [0.0625,10] in BOTH the table and log term; h'/α edge-clamped; early exact-zero return for k_f==0 or T==0 avoids all table math |
| T-05-01-05 | Info disclosure / licensing | Copyrighted AV 1106/07 content | mitigate | Transcribed table DATA only, cited by report+page (SM7 precedent); refs/ stays git-ignored |
| T-05-SC | Tampering (supply chain) | package installs | accept | No new packages installed this phase (engine dep quarantine; Python tools pre-existing) — RESEARCH Package Legitimacy Audit: no installs |
</threat_model>

<verification>
- `cargo test` (workspace) green, including: engine forest unit tests (F1–F4 + constructor), solver F6/F7, `oracle_forest` (F5), `solve_baseline` (None-path bit pin)
- `cargo clippy --all-targets -- -D warnings` clean; `cargo fmt --check` clean
- `#![deny(unsafe_code)]` intact on envi-engine: `grep -c 'deny(unsafe_code)' crates/envi-engine/src/lib.rs` >= 1
- Dep quarantine unchanged: `cargo tree -p envi-engine -e normal --depth 1 | tail -n +2 | grep -vcE 'ndarray|num-complex|thiserror'` outputs 0
- Conj gate: `grep -rn '\.conj(' crates/envi-engine/src/propagation/ | grep -v '//' | wc -l` outputs 0
- FORCE cases unaffected: no capability flips, `cargo run -p envi-harness -- report` shows the same Pass/Skip inventory as before the plan
</verification>

<success_criteria>
- ENG-09 delivered per roadmap SC1 (the faithful SM10 law is what "A = d·a(f)" paraphrases): a forest crossing of length d attenuates the path per band at the 1/12-octave points, matching the analytic anchors exactly and the committed oracle to 1e-9 dB
- D-01 (amended: k_f computed, height_m added), D-02, D-03, D-04, D-12 all implemented and traceable in code/rustdoc
- Q1/Fs deferral documented in the forest.rs header (deferred-items.md follows in plan 05-03)
- The forest: None path is bit-identical to the pre-phase engine, proven by a committed fixture
</success_criteria>

<output>
Create `.planning/phases/05-engine-extensions-forest-semi-transparent-partitions/05-01-SUMMARY.md` when done.
Flag in the SUMMARY: the D-01 ForestCrossing interface amendment (kp dropped — computed as Table-8 k_f; height_m added) and the Q1 Fs deferral.
</output>
