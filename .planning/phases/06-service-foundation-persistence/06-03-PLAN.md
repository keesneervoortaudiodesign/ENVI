---
phase: 06-service-foundation-persistence
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - crates/envi-service/Cargo.toml
  - crates/envi-service/src/main.rs
  - crates/envi-service/src/selfcheck.rs
  - crates/envi-service/src/state.rs
  - crates/envi-service/src/error.rs
  - crates/envi-service/src/api/mod.rs
  - crates/envi-service/src/api/meta.rs
  - crates/envi-service/src/api/projects.rs
  - crates/envi-service/src/api/scene.rs
  - crates/envi-service/tests/contract_meta_static.rs
  - crates/envi-service/tests/contract_projects.rs
  - web/dist/index.html
  - .gitignore
autonomous: true
requirements: [SVC-03, SVC-04, SVC-05, SVC-07]

must_haves:
  truths:
    - "One deployable axum binary serves /api/v1 AND the web/dist bundle, binds 127.0.0.1:8080 by default (ENVI_BIND override logged with a loud warning when non-localhost), and refuses to start unless the startup self-check passes (SC2 — ADJUSTED per D-08: the self-check is the pure-Rust CRS landmark round-trip <= 1 m with CRS/zone logged, NOT a GDAL/PROJ check; GDAL provisioning is Phase 8)"
    - "GET /api/v1/meta/freq-axis serves the 105-point 1/12-octave axis built at runtime from envi_engine::freq::FREQ_AXIS — served once, no client-side acoustic math (SVC-07); centres_hz[64] == 1000.0 bit-exact"
    - "Project CRUD works over HTTP: create/list/get(open)/update/delete/duplicate + GET /api/v1/projects/last (reopen-last) — thin handlers delegating to envi-store (D-06, SVC-05)"
    - "A project survives service restart: create + PUT scene under one AppState, drop it, build a fresh AppState over the same root, GET scene returns the identical FeatureCollection (SC1)"
    - "Scene GET/PUT round-trips WGS84 GeoJSON; invalid kind vocabulary, bad uuids, or out-of-range coordinates are rejected with structured JSON errors, never persisted"
    - "GET / serves the placeholder index.html and unknown non-API paths fall back to it (SPA contract, SVC-03)"
  artifacts:
    - path: "crates/envi-service/Cargo.toml"
      provides: "Service binary manifest: axum 0.8, tokio, tower-http fs, tracing; dev-deps tower util + http-body-util"
      contains: "axum"
    - path: "crates/envi-service/src/main.rs"
      provides: "tracing init -> D-08 self-check (refuse-to-start) -> router -> localhost bind -> serve"
      contains: "ENVI_BIND"
    - path: "crates/envi-service/src/selfcheck.rs"
      provides: "Pure-Rust CRS landmark round-trip self-check (Dam Square, <= 1 m, zone logged)"
      contains: "crs_self_check"
    - path: "crates/envi-service/src/error.rs"
      provides: "ApiError -> IntoResponse (status + structured JSON body)"
      contains: "IntoResponse"
    - path: "crates/envi-service/src/api/mod.rs"
      provides: "/api/v1 router assembly (axum 0.8 brace-syntax paths) + ServeDir fallback wiring"
      contains: "nest"
    - path: "crates/envi-service/src/api/meta.rs"
      provides: "GET /meta/freq-axis DTO from envi_engine::freq (runtime-built, never hardcoded)"
      contains: "FREQ_AXIS"
    - path: "crates/envi-service/src/api/projects.rs"
      provides: "Project CRUD + duplicate + reopen-last handlers (Path<Uuid> extractors)"
      contains: "Path<Uuid>"
    - path: "crates/envi-service/src/api/scene.rs"
      provides: "Scene GET/PUT handlers (WGS84 GeoJSON wire, validation before persist)"
    - path: "web/dist/index.html"
      provides: "Dependency-free placeholder that live-fetches /api/v1/meta/freq-axis as a smoke check"
      contains: "freq-axis"
  key_links:
    - from: "crates/envi-service/src/api/meta.rs"
      to: "crates/envi-engine/src/freq.rs"
      via: "DTO built from FREQ_AXIS.centres / N_BANDS / NOMINAL_THIRD_OCT at runtime (serde stays out of the engine — serialization happens service-side)"
      pattern: "FREQ_AXIS"
    - from: "crates/envi-service/src/main.rs"
      to: "crates/envi-service/src/selfcheck.rs"
      via: "main returns Err (non-zero exit) when crs_self_check fails — refuse-to-start (D-08)"
      pattern: "crs_self_check"
    - from: "crates/envi-service/src/api/projects.rs"
      to: "crates/envi-store/src/project_dir.rs"
      via: "handlers delegate to ProjectStore (no storage logic in handlers)"
      pattern: "ProjectStore"
    - from: "crates/envi-service/src/api/mod.rs"
      to: "web/dist/index.html"
      via: "fallback_service(ServeDir::new(web_dist).fallback(ServeFile index.html)) — SPA deep links"
      pattern: "ServeDir"
  prohibitions:
    - statement: "The service MUST NOT bind a non-loopback address by default; only an explicit ENVI_BIND override may widen it, and doing so logs a prominent warning (SVC-04, accepted light/no-auth posture)"
      status: pending
      verification: "main.rs default bind literal is 127.0.0.1:8080; test/inspection confirms the warning branch on non-127.0.0.1 values"
    - statement: "No handler may take a raw String path parameter for project ids — Path<Uuid> extractors only (Pitfall 7, path traversal)"
      status: pending
      verification: "grep -n 'Path<String>' crates/envi-service/src/api/*.rs returns no matches on id-bearing routes (code-review gate)"
    - statement: "Route definitions MUST use the axum 0.8 brace path-parameter syntax; the 0.7 colon syntax panics at router construction (Pitfall 2)"
      status: pending
      verification: "the contract tests construct the FULL router — a syntax straggler panics the suite; all routes visibly use braces in api/mod.rs"
    - statement: "No acoustic computation may exist in the service or reach the client — the freq axis and all spectra originate server-side from engine constants (SVC-07)"
      status: pending
      verification: "meta.rs imports envi_engine::freq and contains no hardcoded frequency table (test pins centres_hz[64] == 1000.0 bit-exact against the engine value)"
    - statement: "MUST NOT create anything under .github/workflows/ (no CI scaffolding — CLAUDE.md)"
      status: pending
      verification: "git status shows no .github/ additions"
---

<objective>
Create the **`envi-service`** axum binary — the single self-hosted deployable (SVC-03/SVC-04): tracing
init, the **D-08 startup self-check** (pure-Rust CRS landmark round-trip <= 1 m; the binary refuses to
start on failure; CRS/zone logged — this REPLACES the roadmap's literal "GDAL/PROJ self-check" per the
D-08 adjustment; the GDAL/PROJ check moves to Phase 8 with the C dependency), the `/api/v1` router,
localhost-by-default bind, the placeholder `web/dist` bundle, the `GET /api/v1/meta/freq-axis` band-index
wire anchor (SVC-07), and project CRUD + scene GET/PUT over the envi-store layer (SVC-05, SC1).

Handlers are thin delegates: HTTP + validation + status mapping only; storage lives in envi-store
(plan 06-02), reprojection in envi-geo (plan 06-01). Jobs/SSE and the recondition/recompute contracts
land in plan 06-04 on top of this router.

Purpose: this is the deployable skeleton every later phase binds to — Phase 7's frontend consumes these
exact endpoints, and SC2's single-binary + refuse-to-start contract is proven here.
Output: the `envi-service` crate (main, selfcheck, state, error, api/{mod,meta,projects,scene}),
`web/dist/index.html`, gitignored `projects/` root, and the oneshot contract-test suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-service-foundation-persistence/06-CONTEXT.md
@.planning/phases/06-service-foundation-persistence/06-RESEARCH.md
@.planning/phases/06-service-foundation-persistence/06-PATTERNS.md
@.planning/phases/06-service-foundation-persistence/06-01-SUMMARY.md
@.planning/phases/06-service-foundation-persistence/06-02-SUMMARY.md
@./.claude/CLAUDE.md
</context>

## Artifacts this plan produces (new symbols — exclude from source-grounding checks)

- New crate **`envi-service`** (binary, auto-joins workspace):
  - `main.rs`: `#[tokio::main] async fn main() -> Result<(), Box<dyn Error>>` — refuse-to-start on self-check failure
  - `selfcheck.rs`: `pub fn crs_self_check() -> Result<SelfCheckReport, SelfCheckError>` (report carries zone + round-trip error meters)
  - `state.rs`: `pub struct AppState { pub store: envi_store::project_dir::ProjectStore, .. }` behind `Arc` (jobs/calc registries added by plan 06-04)
  - `error.rs`: `pub enum ApiError` + `impl IntoResponse` (404 NotFound, 400 BadRequest with typed detail, 409 Conflict reserved for 06-04, 500 Internal)
  - `api/mod.rs`: `pub fn api_router() -> Router<Arc<AppState>>` + `pub fn app(state, web_dist) -> Router` (nest + fallback_service)
  - `api/meta.rs`: `FreqAxisDto { n_bands, centres_hz, third_octave_indices, nominal_third_octave_hz }`
  - `api/projects.rs`, `api/scene.rs`: handlers for the endpoint set below
- **Frozen endpoint set (this plan):** `GET /api/v1/meta/freq-axis`; `GET|POST /api/v1/projects`; `GET /api/v1/projects/last`; `GET|PUT|DELETE /api/v1/projects/{id}`; `POST /api/v1/projects/{id}/duplicate`; `GET|PUT /api/v1/projects/{id}/scene`
- **Env vars:** `ENVI_BIND` (default `127.0.0.1:8080`), `ENVI_PROJECTS_DIR` (default `./projects`), `ENVI_WEB_DIST` (default `web/dist`)
- `web/dist/index.html` placeholder; `.gitignore` gains `projects/`
- Run command: `cargo run -p envi-service`

<tasks>

<task type="auto">
  <name>Task 1: Binary scaffold — main, self-check, state, error, placeholder bundle</name>
  <files>crates/envi-service/Cargo.toml, crates/envi-service/src/main.rs, crates/envi-service/src/selfcheck.rs, crates/envi-service/src/state.rs, crates/envi-service/src/error.rs, web/dist/index.html, .gitignore</files>
  <read_first>
    - .planning/phases/06-service-foundation-persistence/06-RESEARCH.md "Pattern 6" (self-check reference impl), "Code Examples" router/bind block, Installation block (exact dep versions), "Open Questions" 2 and 3 (projects root + placeholder content — recommendations adopted)
    - crates/envi-harness/src/main.rs (binary-entry analog: usage doc header, workspace_root() CARGO_MANIFEST_DIR idiom, explicit non-zero exit)
    - crates/envi-geo/src/transform.rs + src/crs.rs (06-01 output — the ProjectCrs API the self-check drives)
    - .planning/phases/06-service-foundation-persistence/06-PATTERNS.md sections 1, 3, 5 (Archetype C note: ApiError has no analog — build per RESEARCH error.rs sketch)
  </read_first>
  <action>
    Cargo.toml per the 06-RESEARCH Installation block: path deps envi-engine, envi-geo, envi-store;
    axum = "0.8", tokio 1 (rt-multi-thread, macros, net, signal, sync, time), tokio-util = "0.7",
    tokio-stream = { "0.1", features ["sync"] } (used by 06-04 — declare now so the manifest is
    stable), tower-http = { "0.7", features ["fs"] }, serde + serde_json, uuid (v4, serde), tracing,
    tracing-subscriber, thiserror. Dev-deps: tower = { "0.5", features ["util"] }, http-body-util =
    "0.1". Boundary comment: "thin HTTP layer — routing, jobs, contracts; storage in envi-store,
    CRS in envi-geo, math NEVER here (SVC-07)".

    main.rs (`//! Usage: cargo run -p envi-service` doc header): init tracing-subscriber (env filter,
    default info); run `selfcheck::crs_self_check()` FIRST — on Err, log the error and return Err from
    main (non-zero exit; refuse-to-start per D-08); on Ok, tracing::info! the zone label and round-trip
    error meters. Resolve config from env: ENVI_BIND (default "127.0.0.1:8080" — when the parsed bind
    is not loopback, emit a tracing::warn! stating the service has no auth and is exposed beyond
    localhost), ENVI_PROJECTS_DIR (default "./projects"; create if missing; log the canonicalized
    absolute path), ENVI_WEB_DIST (default "web/dist"; log resolved path; warn-but-start if missing).
    Build `AppState` -> `api::app(state, web_dist)` (Task 2 provides it; for THIS task have main call
    a minimal placeholder router so the binary compiles standalone, replaced in Task 2) -> bind
    tokio::net::TcpListener -> axum::serve.

    selfcheck.rs per 06-RESEARCH Pattern 6: Dam Square (4.8936 E, 52.3731 N) -> ProjectCrs::for_location
    -> to_utm -> to_wgs84; error meters via the lat/lon-to-meters formula; fail if > 1.0 m. Return a
    `SelfCheckReport { utm_zone, south, err_m }` so main can log it. `SelfCheckError` (thiserror) wraps
    GeoError + the RoundTrip { err_m } variant. Module header cites D-08 and states explicitly: "this
    ADJUSTS roadmap SC2 — the GDAL/PROJ version/proj.db/GDAL_DATA self-check is deferred to Phase 8
    with the C dependency (D-02); Phase 6's check proves the pure-Rust CRS seam at every startup".
    Unit test `self_check_failure_refuses_start`: crs_self_check() returns Ok on the healthy path
    (assert report.utm_zone == 31), and main's refusal path is exercised by asserting the error type
    propagates (call a testable `run(config) -> Result` split if needed — keep main a thin shell).

    state.rs: `AppState { store: ProjectStore }` (constructed from the projects root) — designed for
    Arc<AppState> sharing; rustdoc notes plan 06-04 extends it with the job and calc registries.

    error.rs: `ApiError` enum (NotFound, BadRequest { detail: String }, Conflict { body: serde_json::Value }
    — reserved for 06-04's 409, Internal { detail }) with From<StoreError> mapping (NotFound ->
    NotFound, validation errors -> BadRequest, io -> Internal) and IntoResponse producing
    `{ "error": "<code>", "detail": ... }` JSON bodies with the right status codes.

    web/dist/index.html (06-RESEARCH Open Q3 adopted): a single dependency-free page — heading
    "ENVI service running — frontend arrives in Phase 7", an inline script that fetches
    /api/v1/meta/freq-axis and renders n_bands + the first/last centre frequencies as text (a live
    smoke check of the bundle-serving + API contracts). No npm, no external assets.

    .gitignore: add a `projects/` line (user data, never repo content — 06-RESEARCH Open Q2).
  </action>
  <verify>
    <automated>cargo build -p envi-service && cargo test -p envi-service && cargo clippy -p envi-service --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p envi-service` produces the binary; `cargo test -p envi-service` exits 0
    - selfcheck unit test asserts zone 31 and Ok on the healthy path; the module header contains the SC2-adjustment note naming D-08 and Phase 8
    - main.rs contains the literal default 127.0.0.1:8080 and a warn branch for non-loopback ENVI_BIND values
    - web/dist/index.html exists, references /api/v1/meta/freq-axis, and loads zero external resources (no http(s):// asset URLs in the file)
    - `grep -c 'projects/' .gitignore` outputs >= 1
    - `grep -c 'deny(unsafe_code)' crates/envi-service/src/main.rs` outputs 1
  </acceptance_criteria>
  <done>The deployable binary exists: refuses to start on self-check failure, binds localhost, logs zone/paths, ships a placeholder bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Router + freq-axis meta endpoint + static bundle serving, contract-tested</name>
  <files>crates/envi-service/src/api/mod.rs, crates/envi-service/src/api/meta.rs, crates/envi-service/src/main.rs, crates/envi-service/tests/contract_meta_static.rs</files>
  <read_first>
    - .planning/phases/06-service-foundation-persistence/06-RESEARCH.md "Code Examples" (freq-axis DTO + router assembly reference impls — follow the brace path syntax exactly; Pitfall 2) and "Pitfall 9" (never hardcode the axis)
    - crates/envi-engine/src/freq.rs (FREQ_AXIS, N_BANDS, N_THIRD_OCT, NOMINAL_THIRD_OCT — the runtime source of the DTO; note the module-header band-index rule)
    - crates/envi-service/src/state.rs + src/error.rs (Task 1 outputs)
    - crates/envi-service/src/main.rs (Task 1 output — modified by this task to swap the placeholder router for app(); read current state before editing)
    - .planning/phases/06-service-foundation-persistence/06-RESEARCH.md "Validation Architecture" (oneshot test technique: tower::ServiceExt::oneshot, http-body-util collect — no socket binding)
  </read_first>
  <action>
    api/mod.rs: `api_router()` with this task's route — `.route("/meta/freq-axis", get(meta::freq_axis))`
    — using axum 0.8 brace syntax for ALL parameterized routes added later; `app(state: Arc<AppState>,
    web_dist: &Path) -> Router` = Router::new().nest("/api/v1", api_router()).fallback_service(
    ServeDir::new(web_dist).fallback(ServeFile::new(web_dist.join("index.html")))) per the RESEARCH
    router example (SPA deep links). Wire main.rs to use app() (replacing Task 1's placeholder router).
    Leave the axum default request-body limit in place and document it in the api/mod.rs header
    (~2 MB default is ample for authored scenes — 06-RESEARCH Security Domain V5, accepted).

    api/meta.rs per the RESEARCH freq-axis example: `FreqAxisDto` Serialize struct built at runtime
    from envi_engine::freq — n_bands = N_BANDS, centres_hz = FREQ_AXIS.centres.to_vec(),
    third_octave_indices = (0..N_THIRD_OCT).map(|i| i * 4), nominal_third_octave_hz =
    NOMINAL_THIRD_OCT.to_vec(). Rustdoc: "served ONCE; the axis is the only place Hz values appear on
    the wire — all spectra are keyed by band index (SVC-07); serialization happens HERE because serde
    never enters envi-engine (D-05)".

    tests/contract_meta_static.rs (oneshot, no sockets):
    - `freq_axis_meta_matches_engine`: GET /api/v1/meta/freq-axis -> 200 JSON; centres_hz.len() == 105;
      centres_hz[64].to_bits() == 1000.0f64.to_bits() (bit-exact — house rule, freq.rs test l. 139
      precedent); third_octave_indices == [0, 4, ..., 104] (27 entries); nominal list has 27 entries;
      every centres_hz value strictly ascending.
    - `static_bundle_served_with_spa_fallback`: GET / -> 200 with the placeholder HTML (assert body
      contains the Phase-7 placeholder heading text); GET /some/unknown/spa/path -> 200 index.html
      fallback; GET /api/v1/nonexistent -> 404 (API namespace does NOT fall back to HTML).
    Point the test's ServeDir at the repo's web/dist via the CARGO_MANIFEST_DIR ancestor idiom
    (harness main.rs precedent). Use tempfile::TempDir for the AppState projects root.
  </action>
  <verify>
    <automated>cargo test -p envi-service --test contract_meta_static && cargo clippy -p envi-service --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/v1/meta/freq-axis returns 200 with a 105-element centres_hz array; centres_hz[64] equals 1000.0 bit-exact against the engine constant (test-asserted via to_bits)
    - third_octave_indices is exactly the 27-element arithmetic sequence 0,4,...,104
    - GET / and an unknown non-API deep link both return the placeholder HTML; an unknown /api/v1 path returns 404 JSON, not HTML
    - meta.rs contains no hardcoded frequency array literal (the only numeric axis data flows from envi_engine::freq exports)
    - `cargo test -p envi-service` exits 0
  </acceptance_criteria>
  <done>The band-index wire anchor and the single-binary-serves-frontend contract are live and contract-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Project CRUD + scene GET/PUT endpoints with restart-survival proof</name>
  <files>crates/envi-service/src/api/projects.rs, crates/envi-service/src/api/scene.rs, crates/envi-service/src/api/mod.rs, crates/envi-service/tests/contract_projects.rs</files>
  <read_first>
    - crates/envi-store/src/project_dir.rs + src/geojson.rs + src/dto.rs (06-02 outputs — the store API these handlers delegate to; handlers add NO storage logic)
    - crates/envi-service/src/error.rs (the ApiError mapping — StoreError::NotFound -> 404 etc.)
    - .planning/phases/06-service-foundation-persistence/06-RESEARCH.md "Architecture Patterns" reopen-last + duplicate notes, "Pitfall 7" (Path<Uuid> extractors)
    - .planning/phases/06-service-foundation-persistence/06-RESEARCH.md "Phase Requirements -> Test Map" rows SVC-01/05 (the two named integration tests this task implements)
  </read_first>
  <action>
    api/projects.rs — handlers (all Path parameters typed `Path<Uuid>`, Pitfall 7):
    - `GET /projects` -> 200 [ProjectMetaDto]; `POST /projects` body
      `CreateProjectRequest { name: String, description: Option<String>, origin: { lon_deg, lat_deg } }`
      (deny_unknown_fields) -> 201 ProjectMetaDto (store.create pins the UTM CRS from origin — D-03;
      response carries the CrsDto so the client sees the pinned zone).
    - `GET /projects/last` -> 200 ProjectMetaDto of the last-opened project, 404 when none/stale
      (reopen-last, D-06). Registered BEFORE the `{id}` route in the router so "last" is not captured
      as a uuid path param.
    - `GET /projects/{id}` -> 200 metadata + records reopen-last (documented open semantics);
      `PUT /projects/{id}` (metadata/settings update; bumps modified_at) -> 200;
      `DELETE /projects/{id}` -> 204; `POST /projects/{id}/duplicate` -> 201 the new ProjectMetaDto.
    api/scene.rs:
    - `GET /projects/{id}/scene` -> 200 the persisted WGS84 FeatureCollection verbatim;
      `PUT /projects/{id}/scene` -> validates via envi_store::geojson::validate_feature_collection
      (vocabulary + uuids + coordinate ranges) BEFORE persisting (invalid scenes never reach disk),
      then store.save_scene (atomic; autosave semantics per D-06) -> 204. Handlers stay thin: parse,
      delegate, map errors.
    Extend api/mod.rs api_router() with all routes above (brace syntax).

    tests/contract_projects.rs (oneshot against the full app router, TempDir roots):
    - `crud_lifecycle_and_reopen_last`: create (Dam Square origin -> response CrsDto has utm_zone 31,
      south false) -> list shows 1 -> GET {id} 200 -> GET /projects/last returns the same id ->
      PUT metadata rename -> 200 + name changed on re-GET -> duplicate -> 201 new id, list shows 2,
      duplicated folder has no calc/ -> DELETE original -> 204, list shows 1 -> GET deleted id -> 404
      JSON error body (not HTML).
    - `project_round_trips_across_restart` (SC1's restart-survival proof): build AppState A over a
      TempDir -> create project -> PUT a scene containing at least one feature of every one of the 9
      kinds -> drop A -> build AppState B over the SAME directory -> GET scene -> the parsed
      FeatureCollection is equal (feature count, kind multiset, ids, coordinates) to what was PUT;
      GET /projects/last still resolves.
    - `scene_put_rejects_invalid`: PUT with an out-of-vocabulary kind -> 400 with a structured JSON
      error naming the kind; a subsequent GET returns the PREVIOUS scene unchanged (bad input never
      persisted).
  </action>
  <verify>
    <automated>cargo test -p envi-service && cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --check && cargo tree -p envi-engine -e normal --depth 1</automated>
  </verify>
  <acceptance_criteria>
    - `crud_lifecycle_and_reopen_last` and `project_round_trips_across_restart` both green — SC1's create/open/save/duplicate/delete + reopen-last + restart-survival + scene GET/PUT round-trip demonstrated over HTTP
    - POST /projects response carries the pinned CRS with utm_zone 31 for a Dam Square origin
    - PUT scene with an invalid kind returns 400 and does not alter the persisted scene (test-asserted)
    - Every id-bearing handler uses a Path<Uuid> extractor (no Path<String> on id routes)
    - Full workspace test + clippy + fmt green; engine dep list unchanged; `git diff --stat HEAD -- crates/envi-engine/` empty
  </acceptance_criteria>
  <done>The project lifecycle and scene round-trip are live over HTTP against the flat-file store, with restart survival proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/client -> HTTP API | All request bodies, path params, and query strings are untrusted |
| Network interface -> service | Bind address decides who can reach the (unauthenticated) API |
| Filesystem -> ServeDir | Static-path resolution over web/dist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Spoofing / Info disclosure | accidental LAN exposure | mitigate | Default bind 127.0.0.1:8080; non-loopback ENVI_BIND logged as a prominent warning at startup |
| T-06-03-02 | Elevation / broken access control | no authn/authz on the API | accept | PROJECT.md locked posture: self-hosted single-user internal tool, localhost bind IS the access control (ASVS V2/V3 out of scope this milestone) — explicitly accepted, not ignored |
| T-06-03-03 | Tampering / Info disclosure | path traversal via ids in URLs | mitigate | Path<Uuid> extractors on every id route (parse = gate); store-side uuid+containment guards (T-06-02-01/02) as defense in depth |
| T-06-03-04 | DoS | oversized / malformed request bodies | mitigate (partial accept) | axum default body limit (~2 MB) retained + serde strict DTOs with deny_unknown_fields; no streaming uploads exist; documented as sufficient for authored scenes |
| T-06-03-05 | Tampering | invalid scenes corrupting projects | mitigate | validate_feature_collection before persist; atomic writes below; bad input never reaches disk (test-asserted) |
| T-06-03-06 | Info disclosure | static-path traversal via ServeDir | mitigate | tower-http ServeDir performs traversal-safe path resolution (Don't-Hand-Roll table); no hand-rolled file reads in handlers |
| T-06-03-07 | DoS | startup on a broken CRS stack producing silent garbage | mitigate | D-08 refuse-to-start self-check: no service without a proven <= 1 m CRS round-trip |
| T-06-SC | Tampering (supply chain) | cargo installs (axum, tokio, tower-http, tracing) | mitigate | All packages OK in the 06-RESEARCH Package Legitimacy Audit; versions pinned per audit |
</threat_model>

<verification>
- `cargo test -p envi-service` — contract suites green (freq-axis, static/SPA, CRUD, restart round-trip)
- `cargo test` — full workspace green
- `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` — clean
- `cargo tree -p envi-engine -e normal --depth 1` — only ndarray, num-complex, thiserror
- Single-conj quarantine (trivially green): `grep -rn '\.conj(' crates/envi-engine/src/propagation/ | grep -v '//' | wc -l` outputs 0
- Manual smoke (operator, optional pre-gate): `cargo run -p envi-service` — observe the self-check log line (zone 31-style label + err_m), open http://127.0.0.1:8080/ and see the placeholder render live freq-axis data
</verification>

<success_criteria>
- SC2 (ADJUSTED per D-08): one deployable axum binary serves API + bundle, binds localhost by default, refuses to start unless the pure-Rust CRS round-trip self-check passes (zone logged) — the GDAL/PROJ wording is explicitly replaced; Phase 8 owns that check
- SC1 (HTTP half): create/open/save(autosave)/duplicate/delete + reopen-last over HTTP; project survives service restart; scene GET/PUT round-trips
- SVC-07 (axis half): the 105-point axis served once at /api/v1/meta/freq-axis, runtime-built from the engine
- SVC-03/SVC-04/SVC-05 delivered
</success_criteria>

<output>
Create `.planning/phases/06-service-foundation-persistence/06-03-SUMMARY.md` when done, listing the
frozen endpoint table (method, path, request/response DTO names, status codes) for plan 06-04 and
Phase 7 consumers, and noting the SC2 adjustment wording used.
</output>
