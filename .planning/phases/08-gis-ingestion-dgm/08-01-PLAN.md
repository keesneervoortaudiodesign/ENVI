---
phase: 08-gis-ingestion-dgm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/envi-geo/src/crs.rs
  - crates/envi-geo/tests/oracle_rd.rs
  - crates/envi-geo/tests/fixtures/oracle/rd_landmarks.toml
  - tools/crs_oracle/gen_rd.py
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "envi-geo can reproject an EPSG:28992 (RD New) coordinate to WGS84 and back within 1.0 m"
    - "A committed pyproj oracle fixture pins the RD New round-trip and cargo test fails if envi-geo drifts from it"
    - "RD New reprojection speaks degrees/meters at the public boundary — the radians quarantine in transform.rs is not widened"
  artifacts:
    - path: "crates/envi-geo/src/crs.rs"
      provides: "RD_NEW proj const + named-CRS constructor for EPSG:28992"
      contains: "RD_NEW"
    - path: "crates/envi-geo/tests/fixtures/oracle/rd_landmarks.toml"
      provides: "committed pyproj RD-New landmark fixture with sha256 provenance header + tol_m"
      contains: "provenance"
    - path: "tools/crs_oracle/gen_rd.py"
      provides: "dev-time pyproj generator for the RD-New oracle (Python NOT a test dependency)"
      contains: "sha256"
  key_links:
    - from: "crates/envi-geo/tests/oracle_rd.rs"
      to: "crates/envi-geo/tests/fixtures/oracle/rd_landmarks.toml"
      via: "fixture loaded via concat!(env!(\"CARGO_MANIFEST_DIR\"), ...); tol_m read from [meta]"
      pattern: "CARGO_MANIFEST_DIR"
  prohibitions:
    - "MUST NOT add a new crate dependency to envi-geo (proj4rs already supports sterea + towgs84 — zero new deps)"
    - "MUST NOT touch or reference radians outside transform.rs (crate doc lib.rs:15-17 hard rule)"
    - "MUST NOT hardcode the oracle tolerance in the test body — it is read from the fixture [meta] tol_m"
---

<objective>
Add EPSG:28992 (Dutch RD New) as a named reprojection to `envi-geo`, the single CRS boundary, so
AHN4 terrain coordinates (RD New) can be reprojected to WGS84 / project UTM during import. Validate
against a committed pyproj oracle fixture at ≤ 1.0 m round-trip.

Purpose: AHN4 DTM tiles are published in RD New; the terrain-import path (08-04) must reproject their
samples, and the house rule is that every reprojection goes through `envi-geo` (GEOX-04), never inline
proj strings elsewhere.
Output: `RD_NEW` proj const + constructor in `crs.rs`; `gen_rd.py` dev-time generator; committed
`rd_landmarks.toml` fixture; `oracle_rd.rs` test pinning the round-trip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-gis-ingestion-dgm/08-RESEARCH.md
@.planning/phases/08-gis-ingestion-dgm/08-PATTERNS.md
@crates/envi-geo/src/crs.rs
@crates/envi-geo/src/lib.rs
@crates/envi-geo/tests/oracle_utm.rs
@tools/crs_oracle/gen_utm.py
</context>

<artifacts_produced>
New symbols/files this plan creates (exclude from drift verification):
- `envi_geo::crs::RD_NEW` (private const proj string, EPSG:28992)
- `ProjectCrs::rd_new()` (or `from_named_crs`) constructor exposing the RD-New projection
- `tools/crs_oracle/gen_rd.py`
- `crates/envi-geo/tests/fixtures/oracle/rd_landmarks.toml`
- `crates/envi-geo/tests/oracle_rd.rs`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RD New (EPSG:28992) named CRS to envi-geo::crs</name>
  <files>crates/envi-geo/src/crs.rs</files>
  <read_first>
    - crates/envi-geo/src/crs.rs:21-23 (WGS84_PROJ named-const pattern) + :89-103 (from_zone: Proj::from_proj_string + proj_err wrap) + :143-147 (proj_err helper)
    - crates/envi-geo/src/lib.rs:7-17 (radians-quarantine crate doc: public API is degrees/meters only)
    - 08-RESEARCH.md §Code Examples "RD New in envi-geo" (the exact sterea + bessel + 7-param towgs84 proj string) and §Don't Hand-Roll (RD New row: proj4rs supports sterea + towgs84)
    - 08-PATTERNS.md "crates/envi-geo/src/crs.rs MODIFIED" (named-CRS const pattern, projection construction + error wrap)
  </read_first>
  <behavior>
    - A WGS84 point at the Amersfoort RD origin (≈ lon 5.3876, lat 52.1562) reprojects to RD New ≈ (155000, 463000) within 1.0 m
    - Round-trip WGS84 → RD New → WGS84 returns the input within the fixture tolerance
    - Constructing the RD projection with a malformed proj string surfaces GeoError::Proj { message }, never a panic
  </behavior>
  <action>
    Add a private `RD_NEW` const holding the EPSG:28992 proj string exactly as given in 08-RESEARCH
    §Code Examples (proj=sterea, lat_0/lon_0 Amersfoort, k=0.9999079, x_0=155000, y_0=463000,
    ellps=bessel, the 7-parameter towgs84 Helmert, units=m, no_defs). Add a public constructor on
    `ProjectCrs` (or a small sibling type) that builds the RD-New `Proj` and the WGS84 `Proj` via
    `Proj::from_proj_string(...).map_err(proj_err)?`, mirroring `from_zone`. Route all coordinate
    conversion through the existing `transform` module so the radians handling stays quarantined there
    — do NOT add radians logic in crs.rs. Document the const with a provenance doc comment citing
    epsg.io/28992 and the ~0.5 m towgs84 vs RDNAPTRANS accuracy note. Keep the public API in
    degrees/meters. Add no new crate dependency.
  </action>
  <acceptance_criteria>
    - `crates/envi-geo/src/crs.rs` contains a `RD_NEW` const whose value includes `+proj=sterea`, `+ellps=bessel`, and a `+towgs84=` 7-parameter list
    - The new constructor returns `Result<_, GeoError>` and wraps proj failures via `proj_err` (no `unwrap`/`expect`/`panic!` on the construction path)
    - `grep -rn "radians" crates/envi-geo/src/crs.rs` returns zero matches (quarantine intact)
    - `cargo tree -p envi-geo` shows no dependency added versus the committed baseline
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p envi-geo && cargo clippy -p envi-geo --all-targets -- -D warnings</automated>
  </verify>
  <done>envi-geo compiles with the RD_NEW named CRS and a fallible constructor; clippy clean; no new deps; radians untouched outside transform.rs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit pyproj RD-New oracle fixture + round-trip test</name>
  <files>tools/crs_oracle/gen_rd.py, crates/envi-geo/tests/fixtures/oracle/rd_landmarks.toml, crates/envi-geo/tests/oracle_rd.rs</files>
  <read_first>
    - tools/crs_oracle/gen_utm.py:1-16 (docstring contract: operator-driven regeneration, Python NOT a build/test dep, sha256 provenance header)
    - crates/envi-geo/tests/oracle_utm.rs:39-56 (fixture load via CARGO_MANIFEST_DIR, truncation guard, tol_m from [meta]) + :102-105 (coverage assertions at end)
    - crates/envi-geo/tests/fixtures/oracle/utm_landmarks.toml:1-8 (header: "generated by ... DO NOT EDIT" + [meta] provenance = sha256)
    - 08-RESEARCH.md §Validation Architecture (pyproj fixtures for EPSG:28992, Amersfoort landmark, ≤ 1 m round-trip)
  </read_first>
  <behavior>
    - Test loads rd_landmarks.toml, reprojects each landmark WGS84 → RD New and back through envi-geo, asserts both directions within [meta] tol_m
    - Test fails loudly (expect with message) if the fixture is missing or truncated (< expected case count)
    - Coverage assertion pins that the fixture includes the Amersfoort RD-origin landmark
  </behavior>
  <action>
    Write `gen_rd.py` as a sibling of `gen_utm.py`: a pyproj Transformer (EPSG:4326 ↔ EPSG:28992) over
    a small LANDMARKS list (must include Onze Lieve Vrouwetoren Amersfoort ≈ RD (155000, 463000), plus a
    few spread-out NL points), writing `rd_landmarks.toml` with the DO-NOT-EDIT banner, a `[meta]`
    section carrying `tol_m` (set 1.0 per research) and a `provenance = "gen_rd.py sha256:..."` line, and
    one `[[case]]` per landmark with lon/lat and expected RD x/y. Then write `oracle_rd.rs` copying the
    `oracle_utm.rs` load-and-compare shape: load from CARGO_MANIFEST_DIR-relative path with a loud
    `expect`, a truncation guard on case count, read `tol_m` from `[meta]` (never hardcode), assert both
    forward and round-trip directions within tolerance, and a closing coverage assertion that the
    Amersfoort landmark is present. Regenerate the committed fixture by running the generator once at dev
    time; commit the output.
  </action>
  <acceptance_criteria>
    - `rd_landmarks.toml` begins with the DO-NOT-EDIT banner and a `[meta]` block containing `tol_m = 1.0` and a `provenance = "gen_rd.py sha256:..."` line
    - `oracle_rd.rs` reads `tol_m` from `[meta]` (no numeric tolerance literal in an assertion) and loads the fixture via `concat!(env!("CARGO_MANIFEST_DIR"), ...)`
    - `cargo test -p envi-geo --test oracle_rd` passes with the Amersfoort round-trip within 1.0 m
    - Running `cargo test -p envi-geo` requires no Python interpreter
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-geo --test oracle_rd</automated>
  </verify>
  <done>The RD-New reprojection is pinned to a committed pyproj oracle at ≤ 1.0 m; test green; no runtime Python dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported GIS coords → engine scene | RD New source coordinates cross into project UTM/WGS84; a wrong transform silently misplaces all terrain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering (silent numeric drift) | RD New reprojection | mitigate | Committed pyproj oracle fixture + no-drift `cargo test` gate at ≤ 1.0 m round-trip |
| T-08-01-02 | Denial of Service (panic on bad CRS string) | ProjectCrs constructor | mitigate | Fallible constructor wraps proj errors via `proj_err` into `GeoError::Proj`; no panic path |
</threat_model>

<verification>
- `cargo test -p envi-geo` green (both existing UTM oracle and the new RD oracle)
- `cargo clippy -p envi-geo --all-targets -- -D warnings` clean; `cargo fmt --check`
- `cargo tree -p envi-geo` shows no added dependency
</verification>

<success_criteria>
RD New (EPSG:28992) is a named reprojection inside the single `envi-geo` boundary, pinned to a
committed pyproj oracle at ≤ 1.0 m, with zero new dependencies and the radians quarantine intact.
</success_criteria>

<output>
Create `.planning/phases/08-gis-ingestion-dgm/08-01-SUMMARY.md` when done.
</output>
