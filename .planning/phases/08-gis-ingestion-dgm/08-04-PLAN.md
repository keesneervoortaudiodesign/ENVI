---
phase: 08-gis-ingestion-dgm
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - crates/envi-gis/Cargo.toml
  - crates/envi-gis/src/lib.rs
  - crates/envi-gis/src/registry.rs
  - crates/envi-gis/src/registry/ahn_index.toml
  - crates/envi-gis/src/terrain.rs
  - crates/envi-gis/src/impedance_table.rs
  - crates/envi-gis/src/provenance.rs
  - crates/envi-gis/src/buildings.rs
  - crates/envi-gis/src/merge.rs
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]

must_haves:
  truths:
    - "A source registry selects AHN4 DTM inside NL coverage and GLO-30 globally as pure data (bbox -> SourceDescriptor), so new national DTMs are pure-data additions later"
    - "A decoded terrain window decimates to a bounded elevation-sample set (well under envi-dgm's 500k cap), reprojected to WGS84 lon/lat via envi-geo and constructed as editable elevation_point GeoJSON features (WGS84 on the wire per the Phase-6 contract; the server converts to SceneXY), with nodata dropped"
    - "Building base elevations are sampled from footprint-boundary ground (median), never DSM-under-building"
    - "The WorldCover->Nordtest impedance table maps every one of the 11 classes to a valid engine class letter, resolving sigma only through envi_engine::scene::impedance_class (never restated)"
    - "Overpass JSON becomes building features whose height comes from the locked fallback chain (measured -> height tag -> levels*3+1.5 -> user default) with height_provenance + per-feature provenance"
    - "Re-import merges by (source, source_ref): user_modified features are preserved, untouched imports refreshed, genuinely-new added"
  artifacts:
    - path: "crates/envi-gis/src/impedance_table.rs"
      provides: "reviewed WorldCover code -> Nord2000 class table (11 rows) + per-row test"
      contains: "impedance_class"
    - path: "crates/envi-gis/src/buildings.rs"
      provides: "Overpass JSON -> footprints + eaves_height_m via fallback chain + base elevation"
      contains: "eaves_height_m"
    - path: "crates/envi-gis/src/merge.rs"
      provides: "re-import merge by (source, source_ref) with user_modified guard (D-09)"
      contains: "user_modified"
    - path: "crates/envi-gis/src/registry.rs"
      provides: "SourceDescriptor registry-as-data + committed AHN kaartblad index"
      contains: "SourceDescriptor"
  key_links:
    - from: "crates/envi-gis/src/impedance_table.rs"
      to: "crates/envi-engine/src/scene.rs"
      via: "per-row test resolves sigma via envi_engine::scene::impedance_class(letter), never restating sigma"
      pattern: "impedance_class"
    - from: "crates/envi-gis/src/terrain.rs"
      to: "crates/envi-geo/src/crs.rs"
      via: "RD New / source-CRS samples reproject to WGS84 lon/lat through envi_geo (single boundary); elevation_point features carry WGS84 on the wire"
      pattern: "envi_geo"
  prohibitions:
    - "MUST NOT restate the sigma flow-resistivity numbers in envi-gis — resolve them through envi_engine::scene::impedance_class in the test"
    - "MUST NOT invent a parallel building height property — imported buildings emit eaves_height_m (the exact key building_from_feature reads)"
    - "MUST NOT reproject with inline proj strings — envi-gis calls envi_geo types only (no second proj4rs edge)"
    - "MUST NOT sample building base elevation from under the footprint — use footprint-boundary ground (median)"
    - "MUST NOT generate UUIDs in Rust (getrandom/wasm hazard) — feature ids are assigned in TS via crypto.randomUUID()"
---

<objective>
Build the deterministic `envi-gis` feature layer that turns decoded rasters and Overpass JSON into
editable scene features: the source registry (D-04), terrain decimation + footprint-boundary base
elevation, the SC3 reviewed WorldCover→σ impedance table, the building height fallback chain +
provenance, and the re-import merge (D-09).

Purpose: this is the core of DATA-01/02/03 — everything imported becomes an ordinary editable GeoJSON
feature carrying provenance, mergeable on re-import without clobbering user edits.
Output: `registry.rs` (+committed AHN index), `terrain.rs`, `impedance_table.rs`, `provenance.rs`,
`buildings.rs`, `merge.rs`, all with unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-gis-ingestion-dgm/08-RESEARCH.md
@.planning/phases/08-gis-ingestion-dgm/08-PATTERNS.md
@crates/envi-store/src/geojson.rs
@crates/envi-engine/src/scene.rs
@crates/envi-dgm/src/tin.rs
</context>

<artifacts_produced>
New symbols/files this plan creates (exclude from drift verification):
- `envi_gis::registry` (`SourceDescriptor`, `SourceKind`, `plan`/coverage lookup) + `ahn_index.toml`
- `envi_gis::terrain` (`decimate_window` → bounded samples; `terrain_features` → WGS84 `elevation_point` GeoJSON features; `sample_base_elevation`)
- `envi_gis::impedance_table` (`WORLDCOVER_TABLE` const, `worldcover_to_class`)
- `envi_gis::provenance` (property-stamping helper, provenance key set)
- `envi_gis::buildings` (Overpass parse, `height_from_tags` fallback chain, `eaves_height_m` emit)
- `envi_gis::merge` (re-import merge by `(source, source_ref)` with `user_modified` guard)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Source registry (D-04) + committed AHN kaartblad index</name>
  <files>crates/envi-gis/Cargo.toml, crates/envi-gis/src/lib.rs, crates/envi-gis/src/registry.rs, crates/envi-gis/src/registry/ahn_index.toml</files>
  <read_first>
    - crates/envi-engine/src/scene.rs:317-341 (table-as-code const pattern) + :410-422 (its test)
    - crates/envi-geo/tests/fixtures/oracle/utm_landmarks.toml:1-8 (committed-artifact provenance header: "generated by ... DO NOT EDIT" + sha256)
    - 08-RESEARCH.md §Architecture Patterns "Pattern 3: Source registry as data" (SourceDescriptor fields: id, kind Dtm|Dsm|Landcover|Buildings, coverage MultiPolygon, crs, tile_scheme, endpoint_template, cors Direct|Proxy, license, attribution; AHN coverage = NL polygon; GLO-30 = global; AHN kaartblad index generated at dev time from the PDOK ATOM feed and committed) + §Per-Source CORS Capability Map
    - 08-PATTERNS.md "crates/envi-gis/src/registry.rs"
  </read_first>
  <action>
    Add `envi-geo` (+ `envi-engine` for impedance letters) to `envi-gis/Cargo.toml` if not already present.
    Create `registry.rs` with `SourceDescriptor` (fields per research Pattern 3) and a `SourceKind`
    enum (Dtm/Dsm/Landcover/Buildings), plus a static registry table and a `plan(bbox) -> Vec<SourceId>`
    (or coverage-lookup) that selects AHN4 within the NL coverage polygon and GLO-30 globally for terrain,
    WorldCover for landcover, Overpass for buildings. Encode each source's `cors: Direct|Proxy` from the
    verified capability map (AHN Direct, GLO-30/WorldCover Proxy, Overpass Direct) and its license +
    attribution string. Commit the AHN kaartblad index (tile name ↔ RD bbox) as `registry/ahn_index.toml`
    with the DO-NOT-EDIT + sha256 provenance banner (generated at dev time from the ATOM feed — no runtime
    feed dependency). Add `pub mod registry;` to lib.rs. Unit-test: a NL bbox selects AHN4 as DTM; a bbox
    outside NL selects GLO-30 (flagged Dsm); the table lists the expected source ids with correct cors mode.
  </action>
  <acceptance_criteria>
    - `SourceDescriptor` carries `kind`, `coverage`, `cors: Direct|Proxy`, `license`, `attribution`
    - `ahn_index.toml` begins with the DO-NOT-EDIT banner + a sha256 provenance line
    - A unit test asserts a NL-interior bbox → AHN4 (Dtm) and a non-NL bbox → GLO-30 (Dsm)
    - GLO-30 and WorldCover descriptors carry `cors: Proxy`; AHN and Overpass carry `cors: Direct`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-gis registry</automated>
  </verify>
  <done>The pluggable source registry selects AHN4/GLO-30/WorldCover/Overpass as pure data with the correct CORS mode, backed by a committed AHN index.</done>
</task>

<task type="auto">
  <name>Task 2: Terrain decimation + base-elevation sampling; impedance table (SC3); provenance stamping</name>
  <files>crates/envi-gis/src/terrain.rs, crates/envi-gis/src/impedance_table.rs, crates/envi-gis/src/provenance.rs, crates/envi-gis/src/lib.rs</files>
  <read_first>
    - crates/envi-dgm/src/tin.rs:1-21 (Module I/O header) + :158-211 (guard ordering) + :70-83 (Option/no-silent-0.0); MAX_POINTS = 500_000 cap (tin.rs:31)
    - crates/envi-engine/src/scene.rs:317-341 (impedance_class letters → σ) + :410-422 (test)
    - crates/envi-store/src/geojson.rs:50-57 (unknown additional properties preserved — provenance rides through with zero schema change) + :207-227 (feature_uuid gate: id must be a UUID string)
    - 08-RESEARCH.md §WorldCover → Nordtest σ mapping (the 11-row table + rationale + row confidence; Roughness defaults to N; NEVER restate σ — resolve via engine) + §Buildings "Base elevation" (densify exterior ring ≤ N m, sample terrain per boundary point, take median) + §Open Questions 4 (target 2–10k points/import) + Pattern 4 (provenance key set)
    - 08-PATTERNS.md "crates/envi-gis/src/impedance_table.rs" + "merge.rs + provenance.rs"
  </read_first>
  <action>
    `terrain.rs`: Module I/O header; a `decimate_window(raster, geotransform, target_points, source_crs)`
    that grid-strides the decoded window to a bounded elevation-sample set (auto-picked spacing from
    viewport size, target 2–10k, hard-capped well under envi-dgm's 500k) and DROPS nodata holes; plus a
    `terrain_features(raster, geotransform, target_points, source_crs) -> Vec<Feature>` that wraps
    `decimate_window`, reprojects each kept sample to WGS84 lon/lat through `envi_geo` (no inline proj),
    and constructs editable `elevation_point` GeoJSON features — WGS84 on the wire per the Phase-6
    contract (the server converts WGS84→SceneXY; do NOT emit project/UTM coordinates in the feature).
    Also a
    `sample_base_elevation(footprint_ring, terrain)` that densifies the exterior ring to ≤ N m spacing,
    samples the terrain at each boundary point, and returns the MEDIAN (robust vs DSM spikes) — the
    "footprint-boundary, never DSM-under-building" rule; returns `Option`/typed error when no sample is
    available (never a silent 0.0). `impedance_table.rs`: a `const` array of `(u8 WC code, char Nord2000
    class, &'static str rationale)` rows keyed to the research table (all 11 classes), a
    `worldcover_to_class(code) -> Option<char>`, provenance doc comments per row (report + confidence), and
    the mandated per-row test that (a) asserts `TABLE.len() == 11`, (b) for every row resolves σ via
    `envi_engine::scene::impedance_class(letter)` asserting `is_some()` and the exact engine value — σ is
    never restated here. `provenance.rs`: a helper building the provenance property map (source, source_ref,
    license, retrieved_at, imported, user_modified, height_provenance?, vertical_datum?) per research
    Pattern 4, as plain GeoJSON properties (unknown-props-preserved contract — no store schema change).
    Add the three `pub mod`s to lib.rs.
  </action>
  <acceptance_criteria>
    - `impedance_table.rs` has exactly 11 rows; the per-row test resolves σ through `envi_engine::scene::impedance_class` and asserts the engine value (no σ literal in envi-gis outside a doc comment)
    - `sample_base_elevation` returns the median of footprint-boundary samples and never reads a single under-footprint sample; a test proves a DSM spike under the building does not change the base
    - `decimate_window` output count is bounded (test asserts ≤ target and ≤ the hard cap) and drops nodata
    - `terrain_features` emits `elevation_point` GeoJSON features whose coordinates are WGS84 lon/lat (not project/UTM) — a test asserts coordinate magnitude is degree-range, so features are wire-valid for the Phase-6 PUT /scene contract
    - Terrain reprojection goes through `envi_geo` — `grep -rn "proj=" crates/envi-gis/src` returns zero matches
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-gis terrain impedance provenance</automated>
  </verify>
  <done>Terrain decimates to a bounded, nodata-clean sample set and `terrain_features` builds WGS84 elevation_point features (wire-valid); base elevation is footprint-boundary median; the SC3 table maps all 11 classes to engine letters with σ resolved (not restated); provenance stamps as plain properties.</done>
</task>

<task type="auto">
  <name>Task 3: Overpass buildings (height chain + provenance) + re-import merge (D-09)</name>
  <files>crates/envi-gis/src/buildings.rs, crates/envi-gis/src/merge.rs, crates/envi-gis/src/lib.rs</files>
  <read_first>
    - crates/envi-store/src/geojson.rs:229-244 (opt_f64/req_f64 helpers — copy as house pattern) + :302-331 (building_from_feature: exterior-ring extraction, RFC-7946 closing-vertex drop, req_f64(feature,"building","eaves_height_m") at :316) + :111 (skip-non-mappable, never mutate input) + :50-57 (unknown-props preserved) + :207-227 (feature_uuid: id must be UUID string)
    - 08-RESEARCH.md §Buildings (Overpass query shape; multipolygon outer/inner rings; height fallback chain: measured [empty branch reserved] → height tag [parse "12"/"12 m"/"12.5m", reject non-finite/negative; building:height synonym] → building:levels×3+1.5 → user default; every building gets height_m/height_provenance + D-11 provenance; MUST emit the property building_from_feature consumes) + §Pitfall 12 (validate rings: close, ≥4 positions, finite; skip-and-report invalid) + Pattern 4 (merge match on (source, source_ref), user_modified guard)
    - 08-PATTERNS.md "crates/envi-gis/src/buildings.rs" + "merge.rs + provenance.rs"
  </read_first>
  <action>
    `buildings.rs`: Module I/O header; parse Overpass JSON (`out geom` inlined coords; handle way +
    multipolygon relation outer/inner rings), using `opt_f64`/`req_f64`-style helpers. Implement the LOCKED
    height fallback chain in order: (1) measured — an empty reserved branch (record `height_provenance`
    slot); (2) `height`/`building:height` tag — tolerant meter parse of forms like plain number, trailing
    unit; reject non-finite/negative; (3) `building:levels` × 3 + 1.5 (parse decimal levels; ignore
    roof:levels); (4) user default (a caller-supplied value). Emit the resulting height on the exact
    property key the existing schema consumes — `eaves_height_m` (geojson.rs:316) — NOT a parallel
    `height_m`. Attach `height_provenance` + the D-11 provenance props (via provenance.rs). Validate rings
    (close, ≥4 positions, finite) and SKIP-and-report invalid features (return a per-feature report list),
    never fail the whole layer (D-07). Do NOT assign feature ids in Rust — leave id assignment to TS.
    `merge.rs`: implement the D-09 re-import merge over two feature sets keyed on `(source, source_ref)`: if
    an existing feature has `user_modified == true` keep the user's version; if it exists untouched, refresh
    geometry/props from the new import; if absent, add. Property tests over {untouched, moved/user-modified,
    deleted, user-created} sets asserting user edits always survive. Add both `pub mod`s to lib.rs.
  </action>
  <acceptance_criteria>
    - Buildings emit `eaves_height_m` (not `height_m`); a test asserts the property key matches what `building_from_feature` reads
    - One height-chain unit test per branch, including tag-parse edge cases (e.g. "12 m", levels "2.5") and non-finite/negative rejection
    - Invalid Overpass geometry is skipped and reported (a returned report entry), never panics or fails the layer
    - Merge property tests prove `user_modified` features survive re-import; new features are added; untouched imports refresh
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-gis buildings merge && cargo clippy -p envi-gis --all-targets -- -D warnings</automated>
  </verify>
  <done>Overpass buildings carry chain-derived eaves_height_m + provenance with skip-and-report robustness, and re-import merges without clobbering user edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Overpass JSON → scene features | untrusted third-party geometry/tags become scene objects |
| decoded raster → decimated samples | large windows could produce unbounded feature counts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Denial of Service | terrain::decimate_window (unbounded points) | mitigate | Bounded sample count auto-picked + hard cap well under envi-dgm's 500k |
| T-08-04-02 | Tampering (poisoned buildings) | buildings.rs (Overpass geometry quirks) | mitigate | Ring validation + skip-and-report; non-finite/negative height rejected; typed no-panic |
| T-08-04-03 | Tampering (wrong ground effect) | impedance_table.rs | mitigate | Per-row test resolves σ via the engine; TABLE.len()==11 pinned; user reviews the table (SC3 mechanism) |
| T-08-04-04 | Tampering (inflated base heights) | terrain::sample_base_elevation | mitigate | Footprint-boundary median, never DSM-under-building |
| T-08-04-SC | Tampering | crate deps (tiff/geo/geojson via envi-gis) | mitigate | Legitimacy-audited in 08-RESEARCH (all Approved); no new install in this plan |
</threat_model>

<verification>
- `cargo test -p envi-gis` green (registry, terrain, impedance, provenance, buildings, merge)
- `cargo clippy -p envi-gis --all-targets -- -D warnings` clean; `cargo fmt --check`
- `grep -rn "proj=" crates/envi-gis/src` → zero (single reprojection boundary preserved)
- No σ literal restated in envi-gis production code
</verification>

<success_criteria>
The `envi-gis` feature layer deterministically produces editable, provenance-stamped scene features:
registry-selected terrain decimated to a bounded reprojected sample set, buildings with chain-derived
`eaves_height_m` and footprint-boundary base elevation, the SC3 impedance table (σ resolved through the
engine), and D-09 re-import merge that never clobbers user edits.
</success_criteria>

<output>
Create `.planning/phases/08-gis-ingestion-dgm/08-04-SUMMARY.md` when done.
</output>
