---
phase: 08-gis-ingestion-dgm
plan: 05
type: execute
wave: 3
depends_on: ["08-04"]
files_modified:
  - crates/envi-gis/Cargo.toml
  - crates/envi-gis/src/lib.rs
  - crates/envi-gis/src/landcover.rs
autonomous: false
requirements: [DATA-02]

must_haves:
  truths:
    - "A WorldCover class raster window is vectorized into editable ground_zone features, one class per contiguous region, carrying the mapped Nord2000 impedance class letter + provenance"
    - "Vectorized zones do not violate Phase-7's crossing rejection — marching-squares isobands over a class partition produce only adjacency/containment, never crossings"
    - "The vectorization dependency (contour crate, flagged SUS) is only installed after an explicit human-verify checkpoint; otherwise the hand-rolled marching-squares fallback is used"
  artifacts:
    - path: "crates/envi-gis/src/landcover.rs"
      provides: "WorldCover class raster -> per-class simplified polygons -> ground_zone features"
      contains: "ground_zone"
  key_links:
    - from: "crates/envi-gis/src/landcover.rs"
      to: "crates/envi-gis/src/impedance_table.rs"
      via: "each vectorized class region resolves its Nord2000 letter via worldcover_to_class"
      pattern: "worldcover_to_class"
  prohibitions:
    - "MUST NOT install the contour crate before the human-verify checkpoint approves it"
    - "MUST NOT emit ground_zone polygons that partially cross each other (Phase-7 draw-time rule) — a partition vectorization yields adjacency/containment only"
    - "MUST NOT restate σ — the class letter comes from impedance_table, σ stays in the engine"
---

<objective>
Vectorize the WorldCover class raster into editable `ground_zone` scene features so imported ground
cover is ordinary check-and-complete geometry, each region carrying its Nord2000 impedance class and
provenance. Gate the SUS `contour` dependency behind a human-verify checkpoint, with a hand-rolled
marching-squares fallback if declined.

Purpose: SC3 — the impedance debug overlay (08-07) styles these zones; DATA-02's landcover becomes
editable objects, not an opaque raster.
Output: `landcover.rs` (per-class mask → isobands → simplified `ground_zone` polygons) + the gated
dependency decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-gis-ingestion-dgm/08-RESEARCH.md
@.planning/phases/08-gis-ingestion-dgm/08-PATTERNS.md
@crates/envi-store/src/geojson.rs
@crates/envi-gis/src/impedance_table.rs
</context>

<artifacts_produced>
New symbols/files this plan creates (exclude from drift verification):
- `envi_gis::landcover` module; `vectorize_landcover(class_raster, geotransform, project_crs) -> Vec<GroundZoneFeature>`
- (conditionally) a `contour` dependency edge on `envi-gis/Cargo.toml`, OR a hand-rolled `marching_squares` submodule
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Approve or decline the SUS `contour` crate before install</name>
  <action>
    Do NOT install `contour` until the human approves. Present the legitimacy evidence below, then wait
    for the resume signal. On "approve contour" the executor may add `contour = "0.13"`; on
    "decline — hand-roll" the executor uses the documented hand-rolled marching-squares fallback and
    adds no new dependency. This is a package-legitimacy gate and is never auto-approvable.
  </action>
  <what-built>
    Nothing yet — this checkpoint gates a third-party crate install. Research flagged the `contour`
    crate (contour-rs, github.com/mthh/contour-rs) as SUS on download count only (~800/wk); the repo is
    real and it is already ARCHITECTURE.md's Phase-11 isoband pick. Per the package-legitimacy gate, a
    SUS crate cannot be auto-approved.
  </what-built>
  <how-to-verify>
    1. Open https://crates.io/crates/contour and https://github.com/mthh/contour-rs
    2. Confirm the crate is real, maintained since ~2019, pure-Rust (wasm32-clean — Assumption A8), and license-compatible
    3. Decide: APPROVE installing `contour` (0.13+) as the isoband vectorizer, or DECLINE — in which case the executor uses the documented hand-rolled marching-squares fallback (~200 lines, tested) with no new dependency
  </how-to-verify>
  <resume-signal>Reply "approve contour" to install the crate, or "decline — hand-roll" to use the marching-squares fallback</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Vectorize WorldCover classes into ground_zone features</name>
  <files>crates/envi-gis/Cargo.toml, crates/envi-gis/src/lib.rs, crates/envi-gis/src/landcover.rs</files>
  <read_first>
    - crates/envi-store/src/geojson.rs (ground_zone feature construction: kind vocabulary, impedance/roughness property keys, unknown-props-preserved) + :50-57
    - crates/envi-gis/src/impedance_table.rs (worldcover_to_class — from 08-04) + crates/envi-gis/src/provenance.rs (stamping)
    - 08-RESEARCH.md §WorldCover mapping "Vectorization for editability" (per-class binary mask → marching-squares isobands (contour crate, gated, or hand-rolled) → geo simplification (Douglas-Peucker ~1–2 px) → drop polygons under a min-area threshold → ground_zone features with class + provenance; adjacency/containment are legal, crossings are not produced) + §Don't Hand-Roll (geo::Simplify; marching squares only as documented fallback) + §Open Questions 5 (min-area, water-as-zone-or-skip)
    - 08-PATTERNS.md "crates/envi-gis/src/cog/... landcover.rs" (parser/transform role, tin.rs guard style)
  </read_first>
  <action>
    Per the checkpoint outcome, EITHER add `contour = "0.13"` to `envi-gis/Cargo.toml` and use it for
    isobands, OR implement a hand-rolled marching-squares submodule (tested) if declined. Implement
    `landcover.rs` (Module I/O header): for each WorldCover class present in the window, build a binary
    mask → marching-squares isobands → `geo::Simplify` (Douglas-Peucker ~1–2 px tolerance) → drop polygons
    under a min-area threshold (discretion; default documented) → construct a `ground_zone` feature per
    region with the Nord2000 class letter from `worldcover_to_class`, roughness defaulting to class N, and
    D-11 provenance via `provenance.rs`. Reproject region coords through `envi_geo` (no inline proj).
    Decide water (WC 80 → H) as zones vs skip per research Open-Q5 and document the choice. Add
    `pub mod landcover;` to lib.rs. Unit-test: a synthetic multi-class raster vectorizes to the expected
    class letters; assert produced polygons are non-crossing (adjacency/containment only) so Phase-7
    draw-time validation would accept them.
  </action>
  <acceptance_criteria>
    - `landcover.rs` produces `ground_zone` features whose impedance class letter comes from `worldcover_to_class` (no σ restated)
    - A test asserts vectorized polygons contain no partial crossings (adjacency/containment only)
    - If `contour` was declined, `grep -rn "contour" crates/envi-gis/Cargo.toml` returns zero and a tested `marching_squares` submodule exists
    - Region coords reproject via `envi_geo` — `grep -rn "proj=" crates/envi-gis/src/landcover.rs` returns zero
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-gis landcover && cargo clippy -p envi-gis --all-targets -- -D warnings</automated>
  </verify>
  <done>WorldCover classes vectorize into non-crossing, provenance-stamped ground_zone features carrying the mapped Nord2000 class, using the approved (or hand-rolled) vectorizer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorldCover raster → ground_zone features | class raster becomes editable scene geometry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-SC | Tampering (supply chain) | contour crate (SUS) | mitigate | Blocking human-verify checkpoint before install; hand-rolled marching-squares fallback; never auto-approved |
| T-08-05-01 | Tampering (invalid scene geometry) | landcover vectorization | mitigate | Partition vectorization yields adjacency/containment only; test asserts no crossings (Phase-7 rule) + min-area drop |
</threat_model>

<verification>
- `cargo test -p envi-gis` green including landcover
- `cargo clippy -p envi-gis --all-targets -- -D warnings` clean; `cargo fmt --check`
- Dependency decision recorded in SUMMARY (contour approved vs hand-rolled)
</verification>

<success_criteria>
Imported WorldCover ground cover becomes editable, non-crossing `ground_zone` features carrying the
reviewed Nord2000 impedance class + provenance, using a legitimacy-gated (or hand-rolled) vectorizer.
</success_criteria>

<output>
Create `.planning/phases/08-gis-ingestion-dgm/08-05-SUMMARY.md` when done.
</output>
