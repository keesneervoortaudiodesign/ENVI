---
phase: 10-calculation-service
plan: 06
type: execute
wave: 4
depends_on: ["10-04"]
files_modified:
  - crates/envi-compute/src/scene_dto.rs
  - crates/envi-compute/src/lib.rs
  - crates/envi-store/src/dto.rs
  - crates/envi-store/src/interpolate.rs
  - crates/envi-gis-wasm/src/dto.rs
  - crates/envi-compute-wasm/src/dto.rs
  - crates/envi-compute-wasm/src/scene.rs
  - crates/envi-compute-wasm/src/lib.rs
  - crates/envi-service/tests/wire_no_drift.rs
  - web/src/generated/wire.ts
  - web/src/compute/opfs.ts
  - web/src/compute/worker.ts
  - web/src/compute/worker.test.ts
autonomous: true
requirements: [GRID-02, SVC-02]

must_haves:
  truths:
    - "solve_chunk_range returns a real RangeProgress (not ComputeError::Pending) for a valid prepared scene"
    - "The marshalled-solve tensor is bit-equal (f64::to_bits) to a direct envi_engine::solver::solve over the same assembled jobs"
    - "A scene with a forest (ENG-09) + isolation spectrum (ENG-10) shows those effects in the results; a phase-carrying directivity balloon changes arg(H_coh) (directional phase visible, SC4); a phase-free baseline stays bit-identical"
    - "prepare_solve builds an owned scene stored keyed by tensor_hash; solve_chunk_range verifies the hash before solving"
    - "An OPFS FileSystemSyncAccessHandle open path exists per chunk file keyed by tensor_hash (D-08/D-09)"
    - "Engine byte-identical: cargo tree -p envi-engine = 3 deps AND git diff --stat crates/envi-engine empty"
    - "ts-rs wire no-drift green AND cd web && npx tsc --noEmit clean"
    - "Threaded npm run build:wasm:compute exits 0"
  prohibitions:
    - "No Pending / stub / placeholder / TODO return on the real solve path (grep asserts none)"
    - "No second solve path — Pattern 1: reuse envi_engine::solver::solve UNCHANGED"
    - "No hand-authored TS DTO mirror — every wire type is ts-rs-generated; reuse the existing SoundSpeedProfileDto + scene DTOs, never a duplicate export"
    - "No engine modification — no edit under crates/envi-engine"
    - "No .conj() added anywhere in propagation/ (the conj grep gate stays 0)"
  artifacts:
    - path: "crates/envi-compute-wasm/src/scene.rs"
      provides: "PreparedScene owned-scene builder + solve_prepared_range (rayon shard-assemble over the unchanged engine solve)"
      contains: "fn solve_prepared_range"
    - path: "crates/envi-compute-wasm/src/dto.rs"
      provides: "PrepareSolveReq + engine-type marshalling DTOs (atmosphere, coherence, directivity, receiver/sub-source placements)"
      contains: "PrepareSolveReq"
    - path: "crates/envi-compute-wasm/src/lib.rs"
      provides: "prepare_solve export + the real (non-Pending) solve_chunk_range body + the static prepared-scene registry"
      contains: "pub fn prepare_solve"
    - path: "crates/envi-compute/src/scene_dto.rs"
      provides: "The WASM-safe scene DTOs factored out of envi-store (re-exported at their existing paths, no-drift unchanged)"
      contains: "pub struct TerrainProfileDto"
    - path: "web/src/compute/opfs.ts"
      provides: "Real per-chunk createSyncAccessHandle open keyed by tensor_hash, hoisted ahead of the synchronous solve"
      contains: "createSyncAccessHandle"
    - path: "web/src/compute/worker.ts"
      provides: "prepare_solve wired onto ComputeWasm + CalcJobSpec carrying the scene, called once per submit"
      contains: "prepare_solve"
  key_links:
    - from: "web/src/compute/worker.ts"
      to: "crates/envi-compute-wasm/src/lib.rs"
      via: "ComputeWasm.prepare_solve(spec.scene) called once after the capability gate, before the tier loop"
      pattern: "prepare_solve"
    - from: "crates/envi-compute-wasm/src/lib.rs"
      to: "crates/envi-engine/src/solver.rs"
      via: "solve_chunk_range → solve_prepared_range → pool::solve_tier → envi_engine::solver::solve (unchanged)"
      pattern: "solver::solve"
    - from: "crates/envi-compute-wasm/src/scene.rs"
      to: "crates/envi-compute/src/job_assembly.rs"
      via: "PreparedScene lends a SolveCtx to assemble_jobs for the chunk's receiver range"
      pattern: "assemble_jobs"
    - from: "crates/envi-compute-wasm/src/lib.rs"
      to: "web/src/compute/opfs.ts"
      via: "OpfsChunkSink::open_opfs → the envi-compute-opfs extern openChunk registry (async handle pre-opened by the worker)"
      pattern: "open_opfs|openChunk"
---

<objective>
Close the last Phase-10 seam: turn the typed `solve_chunk_range` `Pending` stub into a
REAL Nord2000 range-solve so the phase delivers its goal — "run a real calculation
end-to-end" — and GRID-02's real receiver-grid compute is met.

Waves 1–3 built everything AROUND the solve: the pure `envi-compute` core (cost model,
hierarchical tiers, `SolveCtx`/`assemble_jobs`, tensor identity), the `envi-compute-wasm`
cdylib, the OPFS `TensorSink`, the `pool::solve_tier` rayon driver, the Web Worker job
machine, and the calc store. But `solve_chunk_range` (lib.rs ~line 232) still returns
`ComputeError::Pending` because no scene geometry crosses the WASM boundary — the request
DTO carries only `{tensor_hash, chunk_index, r_offset, len}`, and `pool::solve_tier` needs
an `assemble` closure that produces real `SolveJob`s from terrain / atmosphere / sources /
weather / forest / isolation.

Plan 10-06 marshals the scene across the boundary ONCE per submit (`prepare_solve`), builds
an OWNED scene that lends a `SolveCtx<'a>`, and runs the UNCHANGED engine `solve()` per
chunk range into the OPFS sink. The key correctness gate is a native equivalence test:
the marshalled range-solve tensor is bit-equal to a direct `envi_engine::solver::solve` of
the same jobs — including a scene with a forest (ENG-09), an isolation spectrum (ENG-10),
and a phase-carrying directivity balloon (directional phase visible, SC4).

Purpose: deliver the actual calculation (GRID-02 real compute, SVC-02 compute-job model).
Output: `prepare_solve` + a real `solve_chunk_range`, the scene DTOs, the OPFS runtime open,
and the worker wiring — engine byte-identical, wire types generated, no second solve path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-calculation-service/10-CONTEXT.md
@.planning/phases/10-calculation-service/10-03-SUMMARY.md
@.planning/phases/10-calculation-service/10-04-SUMMARY.md

# The seam being closed (read before implementing)
@crates/envi-compute/src/job_assembly.rs
@crates/envi-compute-wasm/src/pool.rs
@crates/envi-compute-wasm/src/lib.rs
@crates/envi-compute-wasm/src/dto.rs
@crates/envi-compute-wasm/src/opfs_sink.rs
@crates/envi-engine/src/solver.rs
@crates/envi-store/src/dto.rs
@web/src/compute/worker.ts
@web/src/compute/opfs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scene marshalling contract — factor the WASM-safe scene DTOs + define PrepareSolveReq</name>
  <files>crates/envi-compute/src/scene_dto.rs, crates/envi-compute/src/lib.rs, crates/envi-store/src/dto.rs, crates/envi-store/src/interpolate.rs, crates/envi-gis-wasm/src/dto.rs, crates/envi-compute-wasm/src/dto.rs, crates/envi-service/tests/wire_no_drift.rs, web/src/generated/wire.ts</files>
  <read_first>crates/envi-store/src/dto.rs (TerrainProfileDto:230, GroundSegmentDto:206, IsolationSpectrumDto:278, AuthoredSpectrumDto:258, ForestParamsDto:308, and the `crate::interpolate::{Resolution, interpolate}` uses), crates/envi-gis-wasm/src/dto.rs:549 (the existing SoundSpeedProfileDto — fields a,b,c,s_a,s_b,z0, identical to envi_engine SoundSpeedProfile), crates/envi-compute/src/identity.rs (the 10-01 factor-and-re-export precedent: types moved to envi-compute, re-exported from envi-store at their original paths so the public API + wire.ts are byte-unchanged), crates/envi-compute-wasm/src/dto.rs (the existing boundary-DTO discipline: deny_unknown_fields, ts_rs export_to="wire.ts", no getrandom)</read_first>
  <action>
Establish a SINGLE marshalling contract for a solve scene, reusing existing DTOs (never hand-mirroring) and keeping the engine 3-dep quarantine and the wire no-drift green.

FIRST, factor the scene DTOs the solve needs into the WASM-safe `envi-compute` crate, exactly as 10-01 factored the identity closure — MOVE these types (with their `TryFrom`/`From` engine conversions and tests) from `envi-store` into a new `crates/envi-compute/src/scene_dto.rs`, then re-export them from `envi_store::dto` at their existing paths so `envi-store`'s public API is source-compatible and `wire.ts` stays byte-identical (the no-drift test keeps importing `envi_store::dto::{...}`): `GroundSegmentDto`, `TerrainProfileDto`, `AuthoredSpectrumDto`, `IsolationSpectrumDto`, `ForestParamsDto`. The isolation conversion needs the interpolation helper, so also move `Resolution` + `interpolate` into `crates/envi-compute/src/interpolate.rs` (pure math, WASM-safe) and re-export from `envi_store::interpolate`. Register the moved module in `envi_compute::lib`. Rationale for factoring rather than adding `envi-store` as a wasm dep: `envi-store` pulls `tempfile` + `envi-geo` (fs / reprojection), which must never enter the browser cdylib; `envi-compute` is already the compute crate's WASM-safe dep. Do NOT change any field, serde attribute, or `ts(export_to)` — moving must be a pure relocation so `wire.ts` does not drift.

Likewise reuse the EXISTING `SoundSpeedProfileDto` (currently `envi_gis_wasm::dto`, fields a,b,c,s_a,s_b,z0 — a 1:1 mirror of `envi_engine::propagation::refraction::SoundSpeedProfile`): a second `SoundSpeedProfileDto` would emit a duplicate `export type SoundSpeedProfileDto` and break `tsc` + no-drift. Move it too into `envi-compute::scene_dto` and re-export from `envi_gis_wasm::dto` (pure relocation, no field change) so both wasm crates share the one wire type.

THEN define the boundary request DTO in `crates/envi-compute-wasm/src/dto.rs`, all `#[serde(deny_unknown_fields)]`, all `#[derive(Deserialize, TS)]` with `#[ts(export_to = "wire.ts")]`, no id-minting:
- `PrepareSolveReq` carrying: `tensor_hash: String` (keying, must match the later solve_chunk_range req), `n_sub: u32`, `terrain: TerrainProfileDto` (points + ground segments), `atmosphere: AtmosphereDto`, `coherence: CoherenceInputsDto`, `weather: Option<SoundSpeedProfileDto>`, `sub_sources: Vec<SubSourcePlacementDto>`, `receivers: Vec<ReceiverPlacementDto>`, `forest: Option<ForestParamsDto>`, `forest_path_length_m: Option<f64>` (the Phase-9 through-forest `d_m` geometry `ForestParamsDto` leaves at its `0.0` placeholder — supplied here so the solve sees a real crossing length), `isolation: Option<IsolationSpectrumDto>`.
- `AtmosphereDto { temperature_c, humidity_pct, pressure_kpa }` (built via the validating `Atmosphere::new`).
- `CoherenceInputsDto { cv2, ct2, t_air_c, c0, roughness_r, f_delta_nu, d_m }` (all f64; finiteness validated at build).
- `ReceiverPlacementDto { global_index: u32, position: [f64; 3] }`.
- `SubSourcePlacementDto { position: [f64; 3], directivity: Option<DirectionalDto> }`.
- `DirectionalDto { balloon: DirectivityBalloonDto, orientation: RotationDto }`.
- `DirectivityBalloonDto { azimuths_deg: Vec<f64>, polars_deg: Vec<f64>, grid_db: Vec<f64>, phase_grid_rad: Option<Vec<f64>> }` — `grid_db`/`phase_grid_rad` are flattened row-major `(az, pol, band=105)`; the builder reshapes into `Array3` and routes through the engine's validating `DirectivityBalloon::new` / `new_with_phase` (which reject non-ascending axes, wrong band count, non-finite). Note the solve marshals the TRANSFER scene only — source sound-power spectra (`BandSpectrumDto`) are readout inputs (Phase 11) and are NOT carried here.
- `RotationDto { matrix: [[f64; 3]; 3] }` (built via `Rotation3::from_matrix`).

Register every NEW compute-wasm DTO (`PrepareSolveReq`, `AtmosphereDto`, `CoherenceInputsDto`, `ReceiverPlacementDto`, `SubSourcePlacementDto`, `DirectionalDto`, `DirectivityBalloonDto`, `RotationDto`) in `crates/envi-service/tests/wire_no_drift.rs`'s explicit `export_all` list, then regenerate the committed `web/src/generated/wire.ts` via the documented `--ignored regenerate_committed_wire_ts` path so the committed artifact matches. Do not hand-edit `wire.ts`.

Add serde round-trip + `deny_unknown_fields`-rejection unit tests for `PrepareSolveReq` and each new DTO. Do NOT reference `T-10-06-03` as a literal string inside any DTO doc-comment (it is negative-grepped in Task 2's gates).
  </action>
  <acceptance_criteria>
- The five scene DTOs + `Resolution`/`interpolate` live in `envi-compute`, re-exported from `envi-store` at their original paths; `SoundSpeedProfileDto` re-exported from `envi-gis-wasm`.
- `cargo test -p envi-service --test wire_no_drift` passes (wire.ts byte-stable; no duplicate `SoundSpeedProfileDto`).
- `cargo tree -p envi-engine` is unchanged (ndarray + num-complex + thiserror).
- New DTO round-trip + unknown-field tests pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-compute-wasm dto:: && cargo test -p envi-service --test wire_no_drift && cargo tree -p envi-engine | grep -cE '^(ndarray|num-complex|thiserror)' | grep -q '^3$'</automated>
  </verify>
  <done>PrepareSolveReq and its engine-type DTOs exist, reuse the factored scene + SoundSpeedProfile DTOs, generate cleanly into the committed wire.ts with no drift, and the engine dep tree is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: prepare_solve + the REAL solve_chunk_range body + the native equivalence gate</name>
  <files>crates/envi-compute-wasm/src/scene.rs, crates/envi-compute-wasm/src/lib.rs</files>
  <behavior>
- Equivalence: `solve_prepared_range` over the FULL receiver set into an in-memory RangeSink produces a `TensorPair` bit-equal (f64::to_bits on every H_coh re/im and every P_incoh cell) to a direct `envi_engine::solver::solve` of `assemble_jobs(0..N, ctx)`.
- ENG-09/10 present: a scene WITH a forest crossing + an isolation spectrum yields H_coh differing from the same scene without them (the effects are not silently dropped).
- Directional phase visible (SC4): a phase-carrying balloon rotated on one co-located sub-source changes the coherent-sum argument; a phase-free balloon leaves arg(H_coh) bit-identical to the magnitude-only path.
- Hash gate: solve_chunk_range against a tensor_hash that does not match the prepared scene returns a typed error, never a solve.
- No Pending on the real path: solve_chunk_range's body contains no ComputeError::Pending / stub / TODO.
  </behavior>
  <read_first>crates/envi-compute-wasm/src/pool.rs (solve_tier signature + the local-indexed one-range-one-file pattern, the AtomicBool CANCEL check, the SC3 residency test), crates/envi-compute/src/job_assembly.rs (SolveCtx fields + assemble_jobs receiver-major, the phased/magnitude balloon test helpers to mirror), crates/envi-compute-wasm/src/opfs_sink.rs (OpfsChunkSink::open_opfs + the RangeSink::finish impl), crates/envi-compute-wasm/src/lib.rs (CANCEL static, from_js/to_js/compute_err helpers, the current Pending stub at ~232)</read_first>
  <action>
Create `crates/envi-compute-wasm/src/scene.rs` with the owned scene + the natively-testable solve, and replace the `Pending` boundary in `lib.rs`.

<!-- planner-discipline-allow: Pending -->
Note: this action names `Pending` to describe REMOVING it; the negative grep gate is scoped to `lib.rs`/`scene.rs` file bodies, not this plan.

OWNED SCENE (`PreparedScene`): built from a `PrepareSolveReq` via the validating engine constructors (`TerrainProfile::try_from`/`::new`, `Atmosphere::new`, `IsolationSpectrum::try_from`, `ForestCrossing::new` with the real `forest_path_length_m`, `DirectivityBalloon::new`/`new_with_phase`, `Rotation3::from_matrix`, `SoundSpeedProfile` from its DTO, `CoherenceInputs` from its DTO). It OWNS: `tensor_hash`, `profile`, `atmosphere`, `coh`, `axis: FreqAxis`, `weather: Option<SoundSpeedProfile>`, per-sub-source `Option<(DirectivityBalloon, Rotation3)>` + positions, `receivers: Vec<ReceiverPlacement>` (global indices), `forest: Option<ForestCrossing>`, `isolation: Option<IsolationSpectrum>`, `n_sub`. Avoid a self-referential struct: provide `with_local_ctx(&self, range, f)` that (a) selects the receivers whose global index is in `[range.r_offset, range.r_offset+range.len)`, REINDEXED to local `0..len` (mirroring pool.rs `local_receivers`), (b) builds the `SubSourcePlacement` slice borrowing `self`'s owned balloons INSIDE the method, (c) constructs a `SolveCtx` and calls `f(&ctx)` — so all borrows live only within the call and no `'self` leaks.

NATIVELY-TESTABLE SOLVE (`solve_prepared_range(scene, range: ChunkRange, sink: &mut impl RangeSink)`): shard the chunk's `len` LOCAL receivers into pool-sized disjoint sub-ranges and run them on the rayon pool via `pool::solve_tier` (reusing the proven cancel-check + one-`solve()`-per-range + SC3 path), each sub-range solving the UNCHANGED `envi_engine::solver::solve` into its own in-memory sub-sink; then ASSEMBLE the sub-range `TensorPair`s in receiver order into one `[n_sub, len, 105]` paired buffer and write it to the passed `sink` with a single `put_chunk(0, assembled_h.view(), assembled_p.view())` + `sink.finish()`. JUSTIFY (record in the SUMMARY): the frozen OPFS layout is ONE file per `chunk_index` (worker.ts spans + opfs.ts `chunkRelativePath`) and the byte format is sub-source-major `[s][r_local][f]`, so a chunk cannot be split into sub-FILES; sharding into in-memory sub-ranges then assembling one s-major chunk is the design that (i) genuinely uses the wasm-bindgen-rayon pool per call so GRID-02 "parallelized (rayon)" is met on the real path and D-03 max-parallelism intent holds, (ii) writes exactly one file per chunk_index (worker.ts/opfs.ts unchanged), (iii) keeps the engine byte-identical (Pattern 1 — many `solve()` calls, no new solve path), and (iv) keeps the resident working set O(one chunk): at most n_workers in-memory sub-chunks (summing to one chunk) plus the one assembled chunk. The engine solve is byte-identical regardless of shard boundaries because receivers are independent.

PREPARED-SCENE REGISTRY (lib.rs): a module-level `static PREPARED: RwLock<Option<PreparedScene>>` (via `std::sync::RwLock` / `OnceLock`) — NOT `thread_local`, because wasm-bindgen-rayon pool threads share linear memory but have SEPARATE thread-locals; a `static` in linear memory IS shared. `prepare_solve(req: JsValue)` deserializes `PrepareSolveReq`, builds the `PreparedScene` (surfacing any validating-constructor error as a typed JsValue), takes the WRITE lock and stores it (replacing any prior), returns `Ok`. Called ONCE per submit before the chunk loop.

REAL `solve_chunk_range(req: JsValue)`: deserialize `SolveChunkRangeReq`; take the READ lock; error if empty or if its `tensor_hash` != the prepared scene's (never solve a stale/mismatched scene); build `ChunkRange { chunk_index, r_offset, len }`; open the OPFS sink `OpfsChunkSink::open_opfs` for this chunk's `tensor/` + `pincoh/` files (see Task 3 for the pre-opened-handle contract); run `solve_prepared_range` into it; return `RangeProgress { chunk_index, receivers: len }` via `to_js`. Keep `ComputeError::Cancelled` honest (a set CANCEL flag short-circuits before opening the file). Remove the `Pending` return and the `ComputeError::Pending` variant is no longer used on this path (leave the variant only if still referenced elsewhere; otherwise delete it).

TESTS (native, in scene.rs `#[cfg(test)]`, mirroring the job_assembly + pool test helpers): (1) equivalence — full receiver set through `solve_prepared_range` into an in-memory RangeSink is bit-equal to a direct `solve()` of `assemble_jobs(0..N, ctx)`; (2) a forest + isolation + phased-balloon scene differs from the bare scene AND the phased balloon changes the coherent-sum argument while a phase-free balloon is bit-identical (ENG-09/10 + SC4); (3) a two-sub-range shard assembles to the same bits as a single-range solve; (4) the hash-mismatch guard returns an error. Use an in-memory `RangeSink` (an InMemorySink-backed adapter) — the OPFS path is wasm-only and covered by Task 3's build gates.
  </action>
  <acceptance_criteria>
- `solve_prepared_range` over the full receiver set is bit-equal (f64::to_bits) to a direct `envi_engine::solver::solve` of the same jobs.
- A forest + isolation + phased-balloon scene shows ENG-09/10 + directional-phase effects; a phase-free baseline is bit-identical.
- A two-sub-range shard assembles to the same bits as a single-range solve.
- solve_chunk_range with a mismatched tensor_hash errors (never solves); the real path has no `Pending`/stub/TODO.
- `git diff --stat crates/envi-engine` empty; stable wasm32 build compiles.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p envi-compute-wasm scene:: && cargo build -p envi-compute-wasm --target wasm32-unknown-unknown && grep -RInE 'Pending|todo!|unimplemented!|placeholder' crates/envi-compute-wasm/src/lib.rs crates/envi-compute-wasm/src/scene.rs | grep -vE '^\s*//' | grep -c . | grep -q '^0$' && git diff --stat crates/envi-engine | grep -q '^$'</automated>
  </verify>
  <done>solve_chunk_range runs a real range-solve (no Pending) whose tensor is bit-equal to a direct engine solve; ENG-09/10 + directional phase are observable; the engine is byte-identical and the stable wasm32 build compiles.</done>
</task>

<task type="auto">
  <name>Task 3: OPFS runtime open + worker.ts prepare_solve wiring + build/no-drift gates</name>
  <files>web/src/compute/opfs.ts, web/src/compute/worker.ts, web/src/compute/worker.test.ts</files>
  <read_first>web/src/compute/opfs.ts (openChunkHandle async createSyncAccessHandle + the envi-compute-opfs extern openChunk/writeChunk/flushChunk/closeChunk + the V12 safeSeg/assertHex/chunkFileName path guards + the async-open-vs-sync-solve note at the bottom), web/src/compute/worker.ts (createJobMachine — the tier loop is COMPLETE; the ComputeWasm interface line 60; the CalcJobSpec line 37; bootstrapWorker's glue adapter lines 249-254), web/src/compute/worker.test.ts (the mock ComputeWasm shape)</read_first>
  <action>
Wire the scene preparation + make the OPFS handle real, WITHOUT rewriting the tier loop.

OPFS RUNTIME (opfs.ts): implement the real per-chunk `FileSystemSyncAccessHandle` lifecycle keyed by `tensor_hash` (D-08/D-09). Because `createSyncAccessHandle()` is async and the Rust `openChunk` extern is synchronous, HOIST the open: add `setActiveProject(projectId)` + a module-level handle registry keyed by the relative chunk path; add async `preopenChunk(tensorHash, channel, chunkIndex)` that calls the existing `openChunkHandle(activeProjectId, tensorHash, channel, chunkIndex)` (which already walks the fixed `projects/<id>/calc/<hash>/{tensor,pincoh}/chunk_<idx>.bin` layout through the V12 `safeSeg`/`assertHex`/`chunkFileName` guards) and stashes the handle under its path; make the synchronous extern `openChunk(path)` RETURN the pre-opened handle from the registry (throw if absent), and `closeChunk` close it AND evict it from the registry. Keep `assertHex(tensorHash)` on the hash segment (hex-only, reject traversal) — the path key is never a raw user string.

WORKER WIRING (worker.ts): extend the `ComputeWasm` interface with `prepare_solve(req: PrepareSolveReq): unknown`; add `scene: PrepareSolveReq` to `CalcJobSpec`. In `createJobMachine.submit`, AFTER the capability gate and `reset_cancel()`, BEFORE the tier loop, call `deps.wasm.prepare_solve(spec.scene)` once (surface a thrown shape/validation error as `failed`). Set the active project for OPFS via the prepare step (call `setActiveProject(spec.projectId)`; the projectId flows through prepare, the "scene-preparation call" the phase adds). In `bootstrapWorker`'s glue adapter (NOT the tier loop): map `prepare_solve: (req) => glue.prepare_solve(req)`; and wrap `solve_chunk_range` so it `await preopenChunk(req.tensor_hash, "tensor"/"pincoh", req.chunk_index)` for both channels, then calls `glue.solve_chunk_range(req)`, then `closeChunk` for both — hoisting the async OPFS open ahead of the synchronous engine solve. The `createJobMachine` tier loop stays byte-unchanged except for the single added `prepare_solve` call.

TEST MOCK (worker.test.ts): add `prepare_solve` (a spy) to the mock `ComputeWasm`; add a `scene` to the test `CalcJobSpec` fixtures; assert `prepare_solve` is called exactly once per submit, before the first `solve_chunk_range`, and that a `prepare_solve` throw lands the job `failed`. Keep the existing tier-loop / cancel / capability assertions green.
  </action>
  <acceptance_criteria>
- opfs.ts opens a real per-chunk `FileSystemSyncAccessHandle` keyed by `tensor_hash` (hex-only), hoisted ahead of the synchronous solve; one file per chunk_index.
- worker.ts calls `prepare_solve(spec.scene)` exactly once per submit, before the first `solve_chunk_range`; a prepare_solve throw lands the job `failed`.
- The `createJobMachine` tier loop is unchanged except for the single added `prepare_solve` call.
- `cd web && npx tsc --noEmit` clean; `npm run test:unit` green; `npm run build:wasm:compute` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm run test:unit && npm run build:wasm:compute</automated>
  </verify>
  <done>The worker prepares the scene once per submit, opens real OPFS sync handles per chunk keyed by tensor_hash, tsc + unit tests pass, and the threaded build:wasm:compute exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main-thread client → compute worker | `CalcJobSpec` (incl. the scene DTO) crosses via `postMessage` |
| worker JS → wasm boundary | untrusted `PrepareSolveReq` / `SolveChunkRangeReq` deserialized in `#[wasm_bindgen]` fns |
| wasm → OPFS | chunk path (from `tensor_hash` + integer chunk index) + chunk bytes crosses to `FileSystemSyncAccessHandle` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering/Info | OPFS chunk path key (opfs.ts) | mitigate | Path segments are hex-only `tensor_hash` (`assertHex`), fixed literal channel dirs, and an integer chunk index (`chunkFileName`); `safeSeg` strips `/ \ .. NUL`; reject anything else. No raw scene string reaches `getDirectoryHandle` (the V12 layout, reused). |
| T-10-06-02 | DoS | prepare_solve / plan_tiers scene + receiver size | mitigate | The pre-run cost guardrail (`envi_compute::cost::guardrail`) `Block`s over the OPFS byte budget BEFORE compute; cross-check the calc store gates Run on it. Bound the receiver count and the directivity grid size at marshalling; reject before allocating the tensor. |
| T-10-06-03 | Tampering | scene-DTO deserialization (dto.rs / scene.rs) | mitigate | All request DTOs `#[serde(deny_unknown_fields)]`; every value finiteness-checked; degenerate geometry rejected by the engine's validating constructors (`TerrainProfile::new`, `DirectivityBalloon::new`, `IsolationSpectrum::new`, `ForestCrossing::new`, `Atmosphere::new`) — never a panic on data. |
| T-10-06-04 | DoS/Tampering | prepared-scene static + cancel (lib.rs) | mitigate | `RwLock<Option<PreparedScene>>`: write-locked only by `prepare_solve` (once per submit, no concurrent solve), read-locked by `solve_chunk_range`; a `tensor_hash` mismatch is a typed error (never solve a stale scene). The `CANCEL` flag is independent; a cancelled run leaves the prepared scene intact + reusable and closes OPFS handles cleanly (no partial-file poisoning). |
| T-10-06-05 | Spoofing | worker message origin (worker.ts) | accept | The dedicated worker only handles `postMessage` from its spawning client; no cross-origin message handling exists. Documented; real auth deferred (D-12). |
| T-10-06-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan — it reuses `envi-compute` / `envi-engine` / `rayon` / `wasm-bindgen(-rayon)`, all OK-verdict in 10-RESEARCH's Package Legitimacy Audit. No new supply-chain surface. |
</threat_model>

<verification>
- `cargo clippy --all-targets -- -D warnings` — zero warnings.
- `cargo fmt --check` — clean.
- `cargo test` — workspace green, incl. `envi-compute-wasm scene::` (equivalence + ENG-09/10 + directional phase + hash guard) and `envi-service --test wire_no_drift`.
- `cargo build -p envi-compute-wasm --target wasm32-unknown-unknown` — stable wasm32 compiles.
- `cargo tree -p envi-engine` = 3 deps (ndarray + num-complex + thiserror); `git diff --stat crates/envi-engine` empty.
- `grep -Rn '\.conj()' crates/envi-engine/src/propagation` count == 0 (conj gate unbroken).
- No `Pending`/`todo!`/`unimplemented!`/`placeholder` on the real solve path (Task 2 grep gate).
- `cd web && npx tsc --noEmit` clean; `npm run test:unit` green (incl. the prepare_solve mock).
- `npm run build:wasm:compute` exits 0 (threaded module builds with the real solve_chunk_range).
</verification>

<success_criteria>
- `solve_chunk_range` returns a real `RangeProgress` (no `Pending`) for a valid prepared scene.
- The marshalled range-solve tensor is bit-equal to a direct `envi_engine::solver::solve` of the same jobs (the key correctness gate).
- A forest + isolation + phase-carrying-balloon scene shows ENG-09/10 + directional-phase effects; a phase-free baseline stays bit-identical.
- One OPFS file per `chunk_index`, keyed by `tensor_hash`, opened via a real `createSyncAccessHandle`.
- Engine byte-identical; wire types generated (no drift, no duplicate `SoundSpeedProfileDto`); no second solve path; no `.conj()` added in `propagation/`.
- GRID-02 (real receiver-grid compute, rayon-parallel per call) and SVC-02 (compute-job model) delivered.
</success_criteria>

<output>
Create `.planning/phases/10-calculation-service/10-06-SUMMARY.md` when done.
</output>
