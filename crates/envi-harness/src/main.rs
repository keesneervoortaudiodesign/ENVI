//! `envi-harness` CLI — human-readable per-case outcome report.
//!
//! Usage: `cargo run -p envi-harness -- report`

fn main() {
    // Task 3 implements the real report; stub keeps the bin target alive.
    println!("envi-harness report: no cases");
}
