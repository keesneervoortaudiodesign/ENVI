// ExportMenu.tsx — STUB owned by plan 11-09 (results export).
// Render-nothing placeholder so the `ResultsPanel` shell mounts all five slots in
// wave 3; 11-09 fills this file without re-editing the shell.
import type { ReactElement } from "react";

export function ExportMenu(): ReactElement | null {
  return null;
}
