// ColorScaleEditor.tsx — STUB owned by plan 11-06 (isophone map + colour scale).
// Created render-nothing here so `ResultsPanel` can mount all five result slots
// from wave 3 without being re-edited when 11-06 fills this file (avoids a
// same-wave file conflict on the shell). 11-06 replaces this body.
import type { ReactElement } from "react";

export function ColorScaleEditor(): ReactElement | null {
  return null;
}
