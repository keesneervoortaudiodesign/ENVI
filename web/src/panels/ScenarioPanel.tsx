// ScenarioPanel.tsx — STUB owned by plan 11-08 (weather / scenario compare).
// Render-nothing placeholder so the `ResultsPanel` shell mounts all five slots in
// wave 3; 11-08 fills this file without re-editing the shell.
import type { ReactElement } from "react";

export function ScenarioPanel(): ReactElement | null {
  return null;
}
