/// <reference types="vite/client" />
// Vite ambient client types (import.meta.env.DEV / .PROD). Standard Vite scaffold file — enables the
// DEV-only test-bridge guard in main.tsx to be statically eliminated from the production bundle.
